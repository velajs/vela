---
'@velajs/agent': minor
---

**Behavior change:** the optional `@velajs/mail` peer range moves to `^1.32.0`, the mail release published with this version. The agent's behavior is unchanged, but a release that raises a peer floor ships as a minor, never as a patch an existing range would install. With mail 1.31, keep `@velajs/agent` 1.28.2.
