---
"@velajs/vela": minor
---

Await typed endpoint input and output schemas, retaining distinct wire input, parsed handler input, domain result and serialized output types. Generate directional OpenAPI contracts and preserve validator implementation errors as internal failures instead of mapping every exception to 400.
