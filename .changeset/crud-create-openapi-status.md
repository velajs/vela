---
'@velajs/crud': patch
---

Document generated `create` operations with only the 201 and 400 responses they return.
With the matching `@velajs/vela` release, OpenAPI documents and generated clients no longer
list a default 200 response beside the 201.
