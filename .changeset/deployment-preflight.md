---
'@velajs/cli': minor
---

Add read-only `vela deploy check` for explicit Wrangler environments and saved entrypoint snapshots. Validate configuration, cron and queue alignment, and WebSocket Durable Object bindings; report redacted target information and git/input provenance without constructing applications, running custom builds or uploading code.
