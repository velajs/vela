---
'@velajs/vela': minor
---

`ErrorReportContext.edge` accepts `'workflow'`, `'email'` and `'tail'`: the edges on which Cloudflare Workflow runs, Email Workers messages and Tail Workers events report failures. The minimal `VelaFactory.create()` Worker is unchanged.
