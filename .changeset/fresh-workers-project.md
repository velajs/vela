---
"@velajs/cli": minor
---

Add `vela new <name>` to create a minimal Cloudflare Workers application with a module, controller, and constructor-injected service. Include published npm dependencies, TypeScript and SWC decorator configuration, Wrangler source rebuilds, pnpm scripts, and a short runnable README. Reject invalid names and nonempty destinations without overwriting files. Report the actual package version from `vela --version`.
