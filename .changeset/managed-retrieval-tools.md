---
'@velajs/ai': minor
---

Add the optional `@velajs/ai/ai-search` helper for request-bound Cloudflare AI Search retrieval and AI SDK tools. Bind instance authority on the server, validate and bound results and citations, and require current source authorization before exposing indexed content. Document immutable item publication, deletion and re-sync, and the consistency requirements that prevent a direct Vectorize adapter.
