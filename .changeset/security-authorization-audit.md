---
'@velajs/authz': minor
---

Add an opt-in mounted HTTP authorization wiring audit. Inspect effective permission/role metadata, built-in guard registration, and module-visible authorization engines without constructing providers. Report opaque factories and custom guards as unverified; support startup errors or warning diagnostics.
