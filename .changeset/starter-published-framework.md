---
'@velajs/cli': patch
---

Update generated Worker projects to core 1.27.0 and Cloudflare 1.24.0 so new applications use the current framework APIs and execution lifetimes. Keep exact dependency pins and install generated projects from the public npm registry.
