---
'@velajs/vela': minor
---

Extend the HTTP client with injectable fetch transports, composed cancellation and timeouts, streaming response byte limits, schema-inferred response validation, and optional instrumentation hooks. Preserve URL prefix and generic-call compatibility while fixing query/fragment handling, case-insensitive headers, multipart boundaries, and Web request-body forwarding.
