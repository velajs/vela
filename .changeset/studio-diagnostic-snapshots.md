---
'@velajs/studio': patch
---

Bound Studio entrypoint metadata snapshots, make bigint and cycles JSON-safe, and avoid traversing getters or live class instances. Copy route descriptions at the inspection boundary and avoid duplicate GET rows for attributed HEAD handlers.
