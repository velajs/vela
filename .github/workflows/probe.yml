name: Probe
on: workflow_dispatch
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check NPM_TOKEN visibility
        run: |
          if [ -n "${{ secrets.NPM_TOKEN }}" ]; then echo "NPM_TOKEN=PRESENT"; else echo "NPM_TOKEN=MISSING"; fi
