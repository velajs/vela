{
  "name": "event-sourcing-inventory",
  "version": "0.0.0",
  "private": true,
  "description": "Rebuild inventory projections from domain history and resume checkpoints without a live server.",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "node src/index.ts",
    "start": "node src/index.ts"
  },
  "dependencies": {
    "@velajs/event-source": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
