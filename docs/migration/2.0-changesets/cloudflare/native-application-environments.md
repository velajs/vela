---
"@velajs/cloudflare": major
---

Use typed native Workers environments registered before DI factories and lifecycle. Add a worker facade caching isolated applications by environment identity with retryable cold startup. Remove mutable BindingRefs, per-binding wrappers, and deferred email/workflow integration. Require per-app live factories and native typed namespaces; move native Durable Object exports to /durable-objects. Preserve cold queue/cron/live behavior, storage signing checks, and trusted WebSocket identity boundaries. Cache/object flag reads now expose unknown persisted data for caller validation.
