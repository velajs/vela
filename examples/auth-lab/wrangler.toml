name = "auth-lab"
main = "dist/worker.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat_v2"]

# Drop nodejs_compat_v2 once you've confirmed your DB adapter is fully edge-safe.
# The memoryAdapter used in this lab does NOT need Node compat — it's pure JS.
