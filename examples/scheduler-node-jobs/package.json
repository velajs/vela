{
  "name": "scheduler-node-jobs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fictional maintenance scheduler used as a real @velajs/vela/schedule-node example project.",
  "scripts": {
    "build:vela": "pnpm --dir ../.. build",
    "sync:deps": "pnpm run build:vela && pnpm install",
    "build": "pnpm run sync:deps && rm -rf dist && swc src -d dist --strip-leading-paths && tsc --emitDeclarationOnly",
    "smoke": "pnpm run build && node dist/smoke.js",
    "test": "pnpm run sync:deps && vitest run",
    "typecheck": "pnpm run sync:deps && tsc --noEmit && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@velajs/vela": "file:../..",
    "hono": "^4"
  },
  "devDependencies": {
    "@swc/cli": "^0.8.0",
    "@swc/core": "^1.15.11",
    "@types/node": "^22",
    "typescript": "^5",
    "unplugin-swc": "^1.5.9",
    "vitest": "^4.0.18"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@swc/core", "esbuild"]
  }
}
