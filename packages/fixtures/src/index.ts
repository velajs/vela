/**
 * @velajs/studio-fixtures — shared test fixtures for Vela Studio (private).
 *
 * The FakeAdminTransport and canned scenarios land in later milestones; this
 * version marker is the stable placeholder export.
 */
export const STUDIO_FIXTURES_VERSION = '0';
