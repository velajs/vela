/**
 * @velajs/studio-host — the Node-only loopback dev host.
 *
 * Serves the Vela Studio SPA on localhost and proxies the admin API to a
 * running Vela app. The runtime lands in later milestones; this version
 * marker is the stable placeholder export.
 */
export const STUDIO_HOST_VERSION = '0';
