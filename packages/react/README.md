# @velajs/react

React bindings for Vela live queries, with subscription lifecycle management and
isolated optimistic state.

```sh
npm install @velajs/react @velajs/client react
```

See the [client and React guide](https://github.com/velajs/vela/blob/main/docs/client/README.md).
