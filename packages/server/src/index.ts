/**
 * @velajs/studio — the edge-safe Vela Studio admin module.
 *
 * Mounts the reserved admin surface, hosts `@AdminRpc` operations, and wires
 * the time-travel port. Feature code lands in later milestones; the reserved
 * base path is the stable placeholder export.
 */
export const STUDIO_ADMIN_BASE_PATH = '/_vela/admin';
