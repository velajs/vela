/**
 * @velajs/studio-ui — the Vela Studio admin panel UI (React).
 *
 * Components and hooks land in later milestones. The `./client` subpath is the
 * embeddable widget surface and `./standalone` the full-page app shell; this
 * version marker is the stable placeholder export.
 */
export const STUDIO_UI_VERSION = '0';
