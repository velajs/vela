# `@velajs/vela` public API

Generated by `pnpm api:update` from the built declaration graph. Verified by
`pnpm api:check`; do not edit by hand. Documentation comments, content-hashed
chunk names, and line endings are normalized. Private/protected declarations remain
because they can affect TypeScript assignability.

## Ordered package export map

Object condition order is resolution-significant, so this tree is kept verbatim.

```json
{
  ".": {
    "types": "./dist/index.d.ts",
    "import": "./dist/index.js"
  },
  "./module-kit": {
    "types": "./dist/module-kit.d.ts",
    "import": "./dist/module-kit.js"
  },
  "./internal": {
    "types": "./dist/internal.d.ts",
    "import": "./dist/internal.js"
  },
  "./cache": {
    "types": "./dist/cache/index.d.ts",
    "import": "./dist/cache/index.js"
  },
  "./contract": {
    "types": "./dist/contract/index.d.ts",
    "import": "./dist/contract/index.js"
  },
  "./dispatch": {
    "types": "./dist/dispatch/index.d.ts",
    "import": "./dist/dispatch/index.js"
  },
  "./events": {
    "types": "./dist/event-emitter/index.d.ts",
    "import": "./dist/event-emitter/index.js"
  },
  "./health": {
    "types": "./dist/health/index.d.ts",
    "import": "./dist/health/index.js"
  },
  "./http-client": {
    "types": "./dist/fetch/index.d.ts",
    "import": "./dist/fetch/index.js"
  },
  "./i18n": {
    "types": "./dist/i18n/index.d.ts",
    "import": "./dist/i18n/index.js"
  },
  "./live": {
    "types": "./dist/live/index.d.ts",
    "import": "./dist/live/index.js"
  },
  "./logging": {
    "types": "./dist/logging/index.d.ts",
    "import": "./dist/logging/index.js"
  },
  "./observability": {
    "types": "./dist/observability/index.d.ts",
    "import": "./dist/observability/index.js"
  },
  "./openapi": {
    "types": "./dist/openapi/index.d.ts",
    "import": "./dist/openapi/index.js"
  },
  "./queue": {
    "types": "./dist/queue/index.d.ts",
    "import": "./dist/queue/index.js"
  },
  "./schedule": {
    "types": "./dist/schedule/index.d.ts",
    "import": "./dist/schedule/index.js"
  },
  "./schedule-node": {
    "types": "./dist/schedule-node/index.d.ts",
    "import": "./dist/schedule-node/index.js"
  },
  "./security": {
    "types": "./dist/security/index.d.ts",
    "import": "./dist/security/index.js"
  },
  "./seeder": {
    "types": "./dist/seeder/index.d.ts",
    "import": "./dist/seeder/index.js"
  },
  "./streaming": {
    "types": "./dist/streaming/index.d.ts",
    "import": "./dist/streaming/index.js"
  },
  "./throttler": {
    "types": "./dist/throttler/index.d.ts",
    "import": "./dist/throttler/index.js"
  },
  "./validation": {
    "types": "./dist/validation/index.d.ts",
    "import": "./dist/validation/index.js"
  },
  "./websocket": {
    "types": "./dist/websocket/index.d.ts",
    "import": "./dist/websocket/index.js"
  },
  "./websocket-node": {
    "types": "./dist/websocket-node/index.d.ts",
    "import": "./dist/websocket-node/index.js"
  }
}
```

## `.`

Type conditions: `types`

Declaration entry: `./dist/index.d.ts`

```ts
import { Et as defineProvider, Ft as VelaHonoEnv, G as ENV, It as VelaMiddlewareHandler, K as InjectEnv, Mt as Scope, Nt as VelaContext, Ot as forwardRef, Pt as VelaHono, Q as DependencyToken, a as ExecutionContext, at as InjectableOptions, bt as TypedProviderLiteral, c as HttpExecutionContext, d as PipeTransform, ht as ProviderLiteral, i as ExceptionFilter, l as NestInterceptor, mt as ProviderDefinition, n as CallHandler, o as HandlerFunction, ot as InjectionToken, pt as Provider, q as VelaEnv, r as CanActivate, s as HttpArgumentsHost, t as ArgumentMetadata, tt as ForwardRef, u as NestMiddleware, vt as Token, xt as TypedToken, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { $ as ModuleRef, F as ApplicationContextLookupOptions, I as VelaApplicationContext, M as ErrorReportContext, N as ExceptionHandler, P as matchesAny, et as ModuleRefContext, h as VelaApplication, i as RequestContextKeyOptions, j as ErrorMatcher, n as RequestContext, r as RequestContextKey, t as REQUEST_CONTEXT, tt as ModuleRefLookupOptions } from "<internal:request-context.d.ts>";
import { A as NestModule, D as VersioningOptions, E as RoutePathOptions, H as ModuleImport, I as DynamicModule, M as CorsOptions, N as AsyncModuleOptions, O as MiddlewareConsumer, Q as VersionValue, T as GlobalPrefixOptions, W as ModuleOptions, X as RouteVersion, Z as VERSION_NEUTRAL, b as VelaQuerySecurityOptions, c as RouteParams, j as RouteInfo, l as VelaRouteMap, s as RouteName, v as VelaBodyLimitOverride, x as VelaSecurityOptions, y as VelaBodySecurityOptions } from "<internal:types-http-version.d.ts>";
import { a as RouteHandlerResult, c as RouteResponseFormat, i as RouteFormBody, l as RouteResponseOptions, n as RouteBodyOptions, o as RouteJsonBody, s as RouteMultipartBody, t as RouteBinaryBody, u as RouteSchemaResult } from "<internal:route-contract-http-route-contract.d.ts>";
import { $ as ParseFloatPipe, $t as ConfigService, A as ForbiddenException, B as NotImplementedException, D as BadRequestException, Dn as SseResult, E as BadGatewayException, En as Sse, F as HttpExceptionOptions, Fn as VelaApplicationContextOptions, G as UnauthorizedException, Gt as DefineModuleSpec, H as RequestTimeoutException, I as InternalServerErrorException, In as VelaCreateOptions, J as DefaultValuePipe, Jt as ModuleSetupContext, K as UnprocessableEntityException, Kt as GlobalComponentSlot, L as MethodNotAllowedException, Ln as VelaFactory, M as GoneException, Mn as Inject, N as HttpErrorResponse, Nn as Injectable, Nt as EXECUTION_LIFETIME, O as ConflictException, On as GuardPhase, P as HttpException, Pn as Optional, Pt as ExecutionLifetime, Q as ParseEnumPipe, R as NotAcceptableException, St as UsePipes, T as ErrorsModuleOptions, Tn as MessageEvent, U as ServiceUnavailableException, V as PayloadTooLargeException, W as TooManyRequestsException, Wt as ConfigurableModuleBuilder, X as ParseArrayPipeOptions, Y as ParseArrayPipe, Yt as defineModule, Z as ParseBoolPipe, _ as OnModuleInit, _t as Catch, a as VelaError, an as ConfigurableModuleExtrasTransform, at as APP_FILTER, b as getErrorStatus, bn as SignedUrl, bt as UseInterceptors, c as defineErrorCatalog, cn as ModuleFactoryOptions, ct as APP_MIDDLEWARE, d as SerializerDefinition, dn as LogLevel, dt as CreateDecoratorOptions, en as ConfigModule, et as ParseIntPipe, f as defineSerializer, fn as Logger, ft as ReflectableDecorator, g as OnModuleDestroy, gt as SetMetadata, h as OnApplicationShutdown, hn as Writer, ht as ReflectorTarget, i as ErrorCatalogEntry, in as ConfigurableModuleExtras, it as APP_EXCEPTION_HANDLER, j as GatewayTimeoutException, jn as mixin, k as ExceptionResponse, l as isVelaError, ln as ModuleRegistrationOptions, lt as APP_PIPE, m as OnApplicationBootstrap, mn as LoggerService, mt as ReflectorContext, n as Catalog, nn as ConfigurableModuleBuilderOptions, nt as ParseUUIDPipeOptions, o as VelaErrorOptions, on as ConfigurableModuleHost, ot as APP_GUARD, p as BeforeApplicationShutdown, pn as LoggerLevelName, pt as Reflector, q as UnsupportedMediaTypeException, qt as ModuleContributions, r as ErrorBodyResult, rn as ConfigurableModuleClassType, rt as RequiredPipe, s as composeCatalogs, sn as ConfigurableModuleOptionsFactory, st as APP_INTERCEPTOR, t as CORE_CATALOG, tn as ConfigurableModuleAsyncOptions, tt as ParseUUIDPipe, u as toErrorBody, un as ContextProvider, ut as ERROR_CATALOG, v as RenderHttpErrorOptions, vt as UseFilters, w as ErrorsModule, x as renderHttpError, xn as SignedUrlGuard, xt as UseMiddleware, y as RenderedHttpError, yn as URL_SIGNING_SECRET, yt as UseGuards, z as NotFoundException } from "<internal:index-factory.d.ts>";
import { A as SchemaParamDecorator, C as Query, D as Res, E as Req, M as applyDecorators, N as createParamDecorator, O as RouteMethodDecorator, S as Put, T as Redirect, _ as Ip, a as Body, b as Patch, c as Cookies, d as Get, f as Head, g as HttpMethodDecorator, h as HttpCode, i as All, j as Version, k as RouteOptions, l as Ctx, m as Headers, n as UrlForOptions, o as Controller, p as Header, r as UrlGeneratorService, s as Cookie, t as SignedUrlGenerateOptions, u as Delete, v as Options, w as RawBody, x as Post, y as Param } from "<internal:url-generator.service.d.ts>";
import { c as ConfigModuleOptions, d as ConfigSchema, f as AnyConfigNamespace, g as registerAs, h as ConfigType, i as ModuleDecoratorOptions, l as ConfigPath, m as ConfigShape, n as Global, p as ConfigNamespace, r as Module, u as ConfigPathValue } from "<internal:component.manager.d.ts>";
export { APP_EXCEPTION_HANDLER, APP_FILTER, APP_GUARD, APP_INTERCEPTOR, APP_MIDDLEWARE, APP_PIPE, All, type AnyConfigNamespace, type ApplicationContextLookupOptions, type ArgumentMetadata, type AsyncModuleOptions, BadGatewayException, BadRequestException, type BeforeApplicationShutdown, Body, CORE_CATALOG, type CallHandler, type CanActivate, type Catalog, Catch, ConfigModule, type ConfigModuleOptions, type ConfigNamespace, type ConfigPath, type ConfigPathValue, type ConfigSchema, ConfigService, type ConfigShape, type ConfigType, type ConfigurableModuleAsyncOptions, ConfigurableModuleBuilder, type ConfigurableModuleBuilderOptions, type ConfigurableModuleClassType, type ConfigurableModuleExtras, type ConfigurableModuleExtrasTransform, type ConfigurableModuleHost, type ConfigurableModuleOptionsFactory, ConflictException, type ContextProvider, Controller, Cookie, Cookies, type CorsOptions, type CreateDecoratorOptions, Ctx, DefaultValuePipe, type DefineModuleSpec, Delete, type DependencyToken, type DynamicModule, ENV, ERROR_CATALOG, EXECUTION_LIFETIME, type ErrorBodyResult, type ErrorCatalogEntry, type ErrorMatcher, type ErrorReportContext, ErrorsModule, type ErrorsModuleOptions, type ExceptionFilter, type ExceptionHandler, type ExceptionResponse, type ExecutionContext, type ExecutionLifetime, ForbiddenException, ForwardRef, GatewayTimeoutException, Get, Global, type GlobalComponentSlot, type GlobalPrefixOptions, GoneException, type GuardPhase, type HandlerFunction, Head, Header, Headers, type HttpArgumentsHost, HttpCode, type HttpErrorResponse, HttpException, type HttpExceptionOptions, type HttpExecutionContext, type HttpMethodDecorator, Inject, InjectEnv, Injectable, type InjectableOptions, InjectionToken, InternalServerErrorException, Ip, LogLevel, Logger, type LoggerLevelName, type LoggerService, type MessageEvent, MethodNotAllowedException, type MiddlewareConsumer, Module, type ModuleContributions, type ModuleDecoratorOptions, type ModuleFactoryOptions, type ModuleImport, type ModuleOptions, ModuleRef, type ModuleRefContext, type ModuleRefLookupOptions, type ModuleRegistrationOptions, type ModuleSetupContext, type NestInterceptor, type NestMiddleware, type NestModule, NotAcceptableException, NotFoundException, NotImplementedException, type OnApplicationBootstrap, type OnApplicationShutdown, type OnModuleDestroy, type OnModuleInit, Optional, Options, Param, ParseArrayPipe, type ParseArrayPipeOptions, ParseBoolPipe, ParseEnumPipe, ParseFloatPipe, ParseIntPipe, ParseUUIDPipe, type ParseUUIDPipeOptions, Patch, PayloadTooLargeException, type PipeTransform, Post, type Provider, type ProviderDefinition, type ProviderLiteral, Put, Query, REQUEST_CONTEXT, RawBody, Redirect, type ReflectableDecorator, Reflector, type ReflectorContext, type ReflectorTarget, type RenderHttpErrorOptions, type RenderedHttpError, Req, type RequestContext, RequestContextKey, type RequestContextKeyOptions, RequestTimeoutException, RequiredPipe, Res, type RouteBinaryBody, type RouteBodyOptions, type RouteFormBody, type RouteHandlerResult, type RouteInfo, type RouteJsonBody, type RouteMethodDecorator, type RouteMultipartBody, type RouteName, type RouteOptions, type RouteParams, type RoutePathOptions, type RouteResponseFormat, type RouteResponseOptions, type RouteSchemaResult, type RouteVersion, type SchemaParamDecorator, Scope, type SerializerDefinition, ServiceUnavailableException, SetMetadata, SignedUrl, type SignedUrlGenerateOptions, SignedUrlGuard, Sse, type SseResult, type Token, TooManyRequestsException, type Type, type TypedProviderLiteral, type TypedToken, URL_SIGNING_SECRET, UnauthorizedException, UnprocessableEntityException, UnsupportedMediaTypeException, type UrlForOptions, UrlGeneratorService, UseFilters, UseGuards, UseInterceptors, UseMiddleware, UsePipes, VERSION_NEUTRAL, VelaApplication, VelaApplicationContext, type VelaApplicationContextOptions, type VelaBodyLimitOverride, type VelaBodySecurityOptions, type VelaContext, type VelaCreateOptions, type VelaEnv, VelaError, type VelaErrorOptions, VelaFactory, type VelaHono, type VelaHonoEnv, type VelaMiddlewareHandler, type VelaQuerySecurityOptions, type VelaRouteMap, type VelaSecurityOptions, Version, type VersionValue, type VersioningOptions, type Writer, applyDecorators, composeCatalogs, createParamDecorator, defineErrorCatalog, defineModule, defineProvider, defineSerializer, forwardRef, getErrorStatus, isVelaError, matchesAny, mixin, registerAs, renderHttpError, toErrorBody };
```

## `./cache`

Type conditions: `types`

Declaration entry: `./dist/cache/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { J as Container, a as ExecutionContext, l as NestInterceptor, n as CallHandler, ot as InjectionToken, q as VelaEnv } from "<internal:types-http-hono.types.d.ts>";
import { pt as Reflector, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { i as EnvFactory } from "<internal:binding.d.ts>";

type Awaitable<T> = T | Promise<T>;

interface CacheStore {
  get(key: string): Awaitable<unknown>;
  set(key: string, value: unknown, ttl?: number): Awaitable<void>;
  del(key: string): Awaitable<void>;
  clear(): Awaitable<void>;
}
interface CacheEntry<T = unknown> {
  value: T;
  expiresAt: number;
}

interface CacheEntryReader {
  getEntry(key: string): Awaitable<{
    value: unknown;
    expiresAt?: number;
  } | undefined>;
}

interface CacheEntryWriter {
  setEntry(key: string, entry: CacheEntry): Awaitable<void>;
}

type CacheScope = {
  visibility: 'public';
  partition: string;
} | {
  visibility: 'private';
  partition: string;
};

interface CacheInvalidationStore {
  getVersion(key: string): Awaitable<string>;
  invalidate(key: string): Awaitable<void>;
}
interface CacheEntryOptions {

  ttl?: number;

  tags?: readonly string[];
}
interface CacheResponseOptions extends CacheEntryOptions {

  key?: string;
}
interface CacheModuleOptions {

  namespace: string;

  scope: (context: ExecutionContext) => Awaitable<CacheScope | undefined>;

  store?: CacheStore | EnvFactory<CacheStore>;

  invalidation?: CacheInvalidationStore | EnvFactory<CacheInvalidationStore>;

  ttl?: number;

  max?: number;

  maxBytes?: number;

  shouldCache?: (value: unknown) => boolean;

  onError?: (operation: 'read' | 'write' | 'invalidate' | 'scope', error: unknown) => void;
}

interface ResolvedCacheOptions extends Omit<CacheModuleOptions, 'store' | 'invalidation'> {
  store: CacheStore;
  invalidation?: CacheInvalidationStore;
}

type CacheInvalidationResult = {
  ok: true;
} | {
  ok: false;
  reason: 'unsupported' | 'invalid-input' | 'store-error';
};
interface ScopedCache {
  get(key: string): Promise<unknown>;
  getParsed<T>(key: string, parse: (value: unknown) => T): Promise<T | undefined>;
  set(key: string, value: unknown, options?: CacheEntryOptions): Promise<boolean>;

  remember<T>(key: string, load: () => Promise<T>, options?: CacheEntryOptions): Promise<unknown | T>;
  invalidateKey(key: string): Promise<CacheInvalidationResult>;
  invalidateTags(tags: readonly string[]): Promise<CacheInvalidationResult>;
  invalidateAll(): Promise<CacheInvalidationResult>;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<CacheModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;

export declare class CacheModule extends ConfigurableModuleClass {}

interface CachedResponse {
  readonly status: number;

  readonly type: string;
  readonly body: string;
}

type CachedResponseLookup = {
  readonly hit: CachedResponse;
} | {
  readonly store: (response: CachedResponse) => Promise<void>;
};

export declare class CacheService {
  #private;
  private readonly container?;
  readonly options: Readonly<ResolvedCacheOptions>;
  constructor(options: CacheModuleOptions, env?: VelaEnv, container?: Container | undefined);

  scope(scope: CacheScope): ScopedCache;
  /** @internal */
  scoped(scope: CacheScope, domain: 'service' | 'http'): ScopedCache;
  /** @internal */
  lookupResponse(scope: CacheScope, key: string, options: CacheEntryOptions): Promise<CachedResponseLookup | undefined>;
  /** @internal */
  report(operation: 'read' | 'write' | 'invalidate' | 'scope', error: unknown): void;
  private acceptResponse;
  private accept;
}

export declare function CacheResponse(options?: CacheResponseOptions): (target: object, propertyKey?: string | symbol, descriptor?: PropertyDescriptor) => void;

export declare class CacheInterceptor implements NestInterceptor {
  private readonly cache;
  private readonly reflector;
  constructor(cache: CacheService, reflector: Reflector);
  intercept(context: ExecutionContext, next: CallHandler): Promise<unknown>;
}

export declare const CACHE_MODULE_OPTIONS: InjectionToken<CacheModuleOptions>;

export declare class MemoryCacheStore implements CacheStore {
  private store;
  private defaultTtl;
  private max;
  constructor(ttl?: number, max?: number);
  get(key: string): unknown;
  getEntry(key: string): CacheEntry | undefined;
  set(key: string, value: unknown, ttl?: number): void;
  setEntry(key: string, entry: CacheEntry): void;
  del(key: string): void;
  clear(): void;
  private evict;
}

export declare class TieredCacheStore implements CacheStore, CacheEntryReader, CacheEntryWriter {
  private readonly tiers;
  private revision;
  private pending;
  constructor(tiers: CacheStore[]);
  get(key: string): Promise<unknown>;
  getEntry(key: string): Promise<{
    value: unknown;
    expiresAt?: number;
  } | undefined>;
  set(key: string, value: unknown, ttl?: number): Promise<void>;
  setEntry(key: string, entry: CacheEntry): Promise<void>;
  del(key: string): Promise<void>;
  clear(): Promise<void>;
  private mutate;
  private enqueue;
}

export declare class MemoryCacheInvalidationStore implements CacheInvalidationStore {
  private readonly max;
  private readonly versions;
  constructor(max?: number);
  getVersion(key: string): string;
  invalidate(key: string): void;
}

export type { Awaitable, CacheEntry, CacheEntryOptions, CacheEntryReader, CacheEntryWriter, CacheInvalidationResult, CacheInvalidationStore, CacheModuleOptions, CacheResponseOptions, CacheScope, CacheStore, ResolvedCacheOptions, ScopedCache };
```

## `./contract`

Type conditions: `types`

Declaration entry: `./dist/contract/index.d.ts`

```ts
import { n as SchemaOutput, r as ValidationSchema, t as SchemaInput } from "<internal:parse-schema.d.ts>";
import { a as defineRoute, i as contractFormEncodings, n as RouteContract, r as RouteContractMethod, t as ContractFormEncoding } from "<internal:route-contract-contract-route-contract.d.ts>";
import { Hono } from "hono";
import { BlankEnv } from "hono/types";
import { JSONParsed } from "hono/utils/types";

type Simplify<T> = { [K in keyof T]: T[K]; } & {};
type Wire<T> = [T] extends [string] ? T : string;
type FormField<T> = [T] extends [Blob] ? File | Blob : Wire<T>;
type ParamName<Segment extends string> = Segment extends `${infer Name}{${string}` ? Name : Segment extends `${infer Name}?` ? Name : Segment;
type PathParams<Path extends string> = Path extends `${string}:${infer Segment}/${infer Rest}` ? ParamName<Segment> | PathParams<`/${Rest}`> : Path extends `${string}:${infer Segment}` ? ParamName<Segment> : never;
type QueryWire<T> = { [K in keyof T]: NonNullable<T[K]> extends readonly unknown[] ? Array<Wire<NonNullable<T[K]>[number]>> : Wire<NonNullable<T[K]>>; };
type FormWire<T> = { [K in keyof T]: NonNullable<T[K]> extends readonly unknown[] ? Array<FormField<NonNullable<T[K]>[number]>> : FormField<NonNullable<T[K]>>; };
type Group<Key extends string, T, Required extends boolean> = Required extends true ? { [P in Key]: T; } : { [P in Key]?: T; };
type ContractPath<C> = C extends {
  readonly path: infer Path extends string;
} ? Path : never;
type ParamInput<C> = C extends {
  readonly params: infer S extends ValidationSchema;
} ? {
  param: { [K in keyof SchemaInput<S>]-?: Wire<NonNullable<SchemaInput<S>[K]>>; };
} : [PathParams<ContractPath<C>>] extends [never] ? {} : {
  param: { [K in PathParams<ContractPath<C>>]: string; };
};
type QueryInput<C> = C extends {
  readonly query: infer S extends ValidationSchema;
} ? Group<'query', QueryWire<SchemaInput<S>>, {} extends SchemaInput<S> ? false : true> : {};
type BodyInput<C> = C extends {
  readonly body: infer S extends ValidationSchema;
} ? C extends {
  readonly form: object;
} | {
  readonly multipart: object;
} ? Group<'form', FormWire<NonNullable<SchemaInput<S>>>, undefined extends SchemaInput<S> ? false : true> : Group<'json', SchemaInput<S>, undefined extends SchemaInput<S> ? false : true> : {};

type ContractInput<C extends RouteContract> = Simplify<ParamInput<C> & QueryInput<C> & BodyInput<C>>;

type ContractOutput<C extends RouteContract> = C extends {
  readonly format: 'binary';
} ? Blob : C extends {
  readonly format: 'stream';
} ? ReadableStream<Uint8Array> | null : C extends {
  readonly format: 'response';
} ? unknown : C extends {
  readonly response: null;
} ? never : C extends {
  readonly response: infer S extends ValidationSchema;
} ? C extends {
  readonly format: 'text';
} ? Extract<SchemaOutput<S>, string> extends never ? string : Extract<SchemaOutput<S>, string> : JSONParsed<SchemaOutput<S>> : C extends {
  readonly format: 'text';
} ? string : unknown;

type ContractStatus<C extends RouteContract> = C extends {
  readonly status: infer Status extends number;
} ? Status : C extends {
  readonly response: null;
} ? 204 : C extends {
  readonly method: 'POST';
} ? 201 : 200;

type ContractEndpoint<C extends RouteContract> = {
  input: ContractInput<C>;
  output: ContractOutput<C>;
  outputFormat: C extends {
    readonly format: infer Format extends string;
  } ? Format : 'json';
  status: ContractStatus<C>;
};

type ContractSchema<Routes extends RouteContract> = { [Path in ContractPath<Routes>]: { [Route in Routes as Route extends {
  readonly path: Path;
} ? `$${Lowercase<Route['method']>}` : never]: ContractEndpoint<Route>; }; };
type RoutesOf<Routes> = Routes extends readonly (infer Route)[] ? Route : Routes[keyof Routes];

type ContractApp<Routes extends readonly RouteContract[] | Readonly<Record<string, RouteContract>>> = Hono<BlankEnv, ContractSchema<Extract<RoutesOf<Routes>, RouteContract>>, '/'>;

type ContractBody<C extends RouteContract> = C extends {
  readonly body: infer S extends ValidationSchema;
} ? SchemaOutput<S> : undefined;

type ContractQuery<C extends RouteContract> = C extends {
  readonly query: infer S extends ValidationSchema;
} ? SchemaOutput<S> : Record<string, string | string[]>;

type ContractParams<C extends RouteContract> = C extends {
  readonly params: infer S extends ValidationSchema;
} ? SchemaOutput<S> : { [K in PathParams<ContractPath<C>>]: string; };

type ContractResponse<C extends RouteContract> = C extends {
  readonly response: infer S extends ValidationSchema;
} ? SchemaOutput<S> : unknown;

export { type ContractApp, type ContractBody, type ContractEndpoint, type ContractFormEncoding, type ContractInput, type ContractOutput, type ContractParams, type ContractQuery, type ContractResponse, type ContractSchema, type ContractStatus, type RouteContract, type RouteContractMethod, contractFormEncodings, defineRoute };
```

## `./dispatch`

Type conditions: `types`

Declaration entry: `./dist/dispatch/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { a as ExecutionContext, q as VelaEnv, r as CanActivate } from "<internal:types-http-hono.types.d.ts>";
import { a as NonceStore, i as InvocationTransport, n as InvocationRouteTarget, o as RunInit, r as InvocationTarget, t as InvocationPathTarget } from "<internal:types-http-version.d.ts>";
import { M as applyDecorators } from "<internal:url-generator.service.d.ts>";
import { t as InternalDispatcher } from "<internal:internal-dispatcher.d.ts>";
import { t as INVOCATION_SIGNING_SECRET } from "<internal:tokens.d.ts>";

export declare class SignedInvocationGuard implements CanActivate {
  #private;
  private readonly nonceStore?;
  constructor(invocationSecret?: string, urlSecret?: string, nonceStore?: NonceStore | undefined, env?: VelaEnv);
  canActivate(context: ExecutionContext): Promise<boolean>;
}

export declare function SignedInvocation(): ReturnType<typeof applyDecorators>;

export declare const INVOCATION_AUDIENCE: 'vela:invoke';

export declare const INVOCATION_DEFAULT_TTL_SECONDS = 60;

interface InvocationClaim {

  aud: typeof INVOCATION_AUDIENCE;

  method: string;

  path: string;

  bodyHash: string;

  exp: number;

  nonce: string;

  iss?: string;
}
interface VerifyInvocationOptions {

  now?: number;
}

export declare function signInvocation(claim: InvocationClaim, secret: string): Promise<string>;

export declare function verifyInvocation(token: string, secret: string, opts?: VerifyInvocationOptions): Promise<InvocationClaim | null>;

export { INVOCATION_SIGNING_SECRET, InternalDispatcher, type InvocationClaim, type InvocationPathTarget, type InvocationRouteTarget, type InvocationTarget, type InvocationTransport, type RunInit, type VerifyInvocationOptions };
```

## `./events`

Type conditions: `types`

Declaration entry: `./dist/event-emitter/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { J as Container } from "<internal:types-http-hono.types.d.ts>";
import { Y as DiscoveryService } from "<internal:request-context.d.ts>";
import { n as SchemaOutput, r as ValidationSchema, t as SchemaInput } from "<internal:parse-schema.d.ts>";
import { m as OnApplicationBootstrap, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

type EventEmitterModuleOptions = Record<never, never>;
declare const ConfigurableModuleClass: ConfigurableModuleClassType<EventEmitterModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;
export declare class EventEmitterModule extends ConfigurableModuleClass {}

interface EventDefinition<Name extends string = string, Schema extends ValidationSchema = ValidationSchema> {
  readonly name: Name;
  readonly schema: Schema;
}
type EventInput<Event extends EventDefinition> = SchemaInput<Event['schema']>;
type EventPayload<Event extends EventDefinition> = SchemaOutput<Event['schema']>;
export declare function defineEvent<const Name extends string, Schema extends ValidationSchema>(name: Name, schema: Schema): EventDefinition<Name, Schema>;
type EventVocabulary<Schemas extends Record<string, ValidationSchema>> = { readonly [Name in keyof Schemas & string]: EventDefinition<Name, Schemas[Name]>; };

export declare function defineEventVocabulary<const Schemas extends Record<string, ValidationSchema>>(schemas: Schemas): EventVocabulary<Schemas>;

type EventHandler = (...args: unknown[]) => void | Promise<void>;
interface OnEventMetadata {
  event: string;
  methodName: string;
}
interface EventEmitOptions {

  settlement?: 'legacy' | 'complete';
}

export declare class EventEmitter {
  #private;
  on(event: string, handler: EventHandler): this;
  once(event: string, handler: EventHandler): this;
  off(event: string, handler: EventHandler): this;

  emit(event: string, ...args: unknown[]): Promise<void>;

  emitWithOptions(event: string, options: EventEmitOptions, ...args: unknown[]): Promise<void>;
  removeAllListeners(event?: string): this;
  listenerCount(event: string): number;
}

export declare class EventEmitterSubscriber implements OnApplicationBootstrap {
  #private;
  constructor(discovery: DiscoveryService, emitter: EventEmitter, root?: Container);
  onApplicationBootstrap(): void;
  dispose(): void;
}

type EventListenerDecorator<Payload> = <Handler extends (payload: Payload) => unknown>(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

export declare function OnEvent(event: string): MethodDecorator;
export declare function OnEvent<Event extends EventDefinition>(event: Event): EventListenerDecorator<EventPayload<Event>>;

export declare const ON_EVENT_METADATA = "vela:on-event";

export declare class EventDispatcher {
  #private;
  constructor(root: Container, discovery: DiscoveryService);

  emit<Event extends EventDefinition>(event: Event, input: EventInput<NoInfer<Event>>): Promise<void>;

  inScope(scope: Container): ScopedEventDispatcher;
}
interface ScopedEventDispatcher {

  emit<Event extends EventDefinition>(event: Event, input: EventInput<NoInfer<Event>>): Promise<void>;

  emitUnknown(event: EventDefinition, input: unknown): Promise<void>;

  defer<Event extends EventDefinition>(event: Event, input: EventInput<NoInfer<Event>>): void;
}

export type { EventDefinition, EventEmitOptions, EventEmitterModuleOptions, EventHandler, EventInput, EventListenerDecorator, EventPayload, EventVocabulary, OnEventMetadata, ScopedEventDispatcher };
```

## `./health`

Type conditions: `types`

Declaration entry: `./dist/health/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { U as ServiceUnavailableException, p as BeforeApplicationShutdown, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

type HealthModuleOptions = Record<never, never>;
declare const ConfigurableModuleClass: ConfigurableModuleClassType<HealthModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;
export declare class HealthModule extends ConfigurableModuleClass {}

type HealthCheckStatus = 'ok' | 'error' | 'shutting_down';
interface HealthIndicatorResult {
  [key: string]: {
    status: 'up' | 'down';
    [key: string]: unknown;
  };
}
interface HealthCheckResult {
  status: HealthCheckStatus;
  info: HealthIndicatorResult;
  error: HealthIndicatorResult;
  details: HealthIndicatorResult;
}
type HealthIndicatorFunction = () => Promise<HealthIndicatorResult>;
type ResponseCheckCallback = (response: Response) => boolean | Promise<boolean>;

export declare class HealthCheckException extends ServiceUnavailableException {
  readonly diagnostics: HealthCheckResult;
  constructor(status: 'error' | 'shutting_down', diagnostics: HealthCheckResult);
}
export declare class HealthCheckService implements BeforeApplicationShutdown {
  private isShuttingDown;
  beforeApplicationShutdown(): void;
  check(indicators: HealthIndicatorFunction[]): Promise<HealthCheckResult>;
}

interface HealthIndicatorBuilder {
  up(data?: Record<string, unknown>): HealthIndicatorResult;
  down(data?: Record<string, unknown>): HealthIndicatorResult;
}
export declare class HealthIndicatorService {
  check(key: string): HealthIndicatorBuilder;
}

interface HttpPingOptions {
  method?: string;
  headers?: Record<string, string>;
  timeout?: number;
  expectedStatus?: number;
}
export declare class HttpHealthIndicator {
  private indicator;
  constructor(indicator: HealthIndicatorService);
  pingCheck(key: string, url: string, options?: HttpPingOptions): Promise<HealthIndicatorResult>;
  responseCheck(key: string, url: string, callback: ResponseCheckCallback, options?: HttpPingOptions): Promise<HealthIndicatorResult>;
}

export type { HealthCheckResult, HealthCheckStatus, HealthIndicatorFunction, HealthIndicatorResult, HealthModuleOptions, HttpPingOptions, ResponseCheckCallback };
```

## `./http-client`

Type conditions: `types`

Declaration entry: `./dist/fetch/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { ot as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { n as SchemaOutput, r as ValidationSchema } from "<internal:parse-schema.d.ts>";
import { rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

type HttpFetch = (input: string, init?: RequestInit) => Promise<Response>;

type HttpTransport = HttpFetch | {
  fetch: HttpFetch;
};
interface HttpClientRequest {
  readonly method: string;
  readonly url: string;

  readonly headers: Headers;
}
interface HttpClientResponse {
  readonly status: number;
  readonly statusText: string;
  readonly headers: Headers;
}

interface HttpClientRequestObserver {
  onResponse?(response: HttpClientResponse): void;
  onError?(error: unknown): void;
  onEnd?(): void;
}

interface HttpClientObserver {
  onRequest(request: HttpClientRequest): HttpClientRequestObserver | void;
}
interface HttpModuleOptions {

  baseURL?: string;

  headers?: HeadersInit;

  timeout?: number;

  maxResponseBytes?: number;

  transport?: HttpTransport;
  observer?: HttpClientObserver;
}
interface RequestConfig<S extends ValidationSchema | undefined = undefined> {
  headers?: HeadersInit;
  params?: Record<string, string | number | boolean>;
  timeout?: number;
  signal?: AbortSignal;
  maxResponseBytes?: number;
  transport?: HttpTransport;

  schema?: S;
}
interface HttpRequestConfig<S extends ValidationSchema | undefined = undefined> extends RequestConfig<S> {
  method: string;
  url: string;

  body?: unknown;
}
interface HttpResponse<T = unknown> {
  data: T;
  status: number;
  statusText: string;
  headers: Headers;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<HttpModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;
export declare class HttpModule extends ConfigurableModuleClass {}

export declare const HTTP_MODULE_OPTIONS: InjectionToken<HttpModuleOptions>;
export declare class HttpRequestException extends Error {
  readonly status: number;
  readonly statusText: string;
  readonly response: Response;
  constructor(status: number, statusText: string, response: Response);
}
type SchemaConfig<S extends ValidationSchema> = RequestConfig<S> & {
  schema: S;
};
type SchemaHttpConfig<S extends ValidationSchema> = HttpRequestConfig<S> & {
  schema: S;
};
export declare class HttpService {
  private readonly baseURL;
  private readonly defaultHeaders;
  private readonly defaultTimeout;
  private readonly maxResponseBytes;
  private readonly transport;
  private readonly observer;
  constructor(options: HttpModuleOptions);
  get<S extends ValidationSchema>(url: string, config: SchemaConfig<S>): Promise<HttpResponse<SchemaOutput<S>>>;
  get<T = unknown>(url: string, config?: RequestConfig): Promise<HttpResponse<T>>;
  post<S extends ValidationSchema>(url: string, body: unknown, config: SchemaConfig<S>): Promise<HttpResponse<SchemaOutput<S>>>;
  post<T = unknown>(url: string, body?: unknown, config?: RequestConfig): Promise<HttpResponse<T>>;
  put<S extends ValidationSchema>(url: string, body: unknown, config: SchemaConfig<S>): Promise<HttpResponse<SchemaOutput<S>>>;
  put<T = unknown>(url: string, body?: unknown, config?: RequestConfig): Promise<HttpResponse<T>>;
  patch<S extends ValidationSchema>(url: string, body: unknown, config: SchemaConfig<S>): Promise<HttpResponse<SchemaOutput<S>>>;
  patch<T = unknown>(url: string, body?: unknown, config?: RequestConfig): Promise<HttpResponse<T>>;
  delete<S extends ValidationSchema>(url: string, config: SchemaConfig<S>): Promise<HttpResponse<SchemaOutput<S>>>;
  delete<T = unknown>(url: string, config?: RequestConfig): Promise<HttpResponse<T>>;
  head<S extends ValidationSchema>(url: string, config: SchemaConfig<S>): Promise<HttpResponse<SchemaOutput<S>>>;
  head(url: string, config?: RequestConfig): Promise<HttpResponse<void>>;
  request<S extends ValidationSchema>(config: SchemaHttpConfig<S>): Promise<HttpResponse<SchemaOutput<S>>>;
  request<T = unknown>(config: HttpRequestConfig): Promise<HttpResponse<T>>;
  request(config: HttpRequestConfig<ValidationSchema | undefined>): Promise<HttpResponse>;
  private execute;
  private buildUrl;
}

export declare class HttpResponseSizeException extends Error {
  readonly maxResponseBytes: number;
  readonly receivedBytes: number;
  constructor(maxResponseBytes: number, receivedBytes: number);
}

export type { HttpClientObserver, HttpClientRequest, HttpClientRequestObserver, HttpClientResponse, HttpFetch, HttpModuleOptions, HttpRequestConfig, HttpResponse, HttpTransport, RequestConfig };
```

## `./i18n`

Type conditions: `types`

Declaration entry: `./dist/i18n/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { ot as InjectionToken, u as NestMiddleware } from "<internal:types-http-hono.types.d.ts>";
import { n as RequestContext, r as RequestContextKey } from "<internal:request-context.d.ts>";
import { I as DynamicModule } from "<internal:types-http-version.d.ts>";
import { rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { Context, Next } from "hono";

type DetectionStrategy = 'header' | 'query' | 'cookie';
interface I18nModuleOptions {

  defaultLocale?: string;

  fallbackLocale?: string;

  locales?: string[];

  detection?: {

    enabled?: boolean;

    strategy?: DetectionStrategy;

    lookupKey?: string;
  };
}
interface ResolvedI18nOptions {
  defaultLocale: string;
  fallbackLocale: string;
  locales: string[];
  detection: {
    enabled: boolean;
    strategy: DetectionStrategy;
    lookupKey: string;
  };
}
export declare function resolveI18nOptions(options?: I18nModuleOptions): ResolvedI18nOptions;

declare const ConfigurableModuleClass: ConfigurableModuleClassType<I18nModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;
export declare class I18nModule extends ConfigurableModuleClass {

  static registerMessages(messages: Record<string, Record<string, unknown>>): DynamicModule;
}

interface II18nService {

  t(key: string, params?: Record<string, unknown>): string;

  getLocale(): string;
}

type Contribution = Record<string, Record<string, unknown>>;

export declare class MessageRegistry {

  static addMessages(messages: Contribution): void;

  getMergedMessages(): Record<string, Record<string, unknown>>;
  /** @internal */
  static reset(): void;
}

export declare class MessageLoaderService {
  private readonly cache;
  private readonly compiledCache;
  private readonly defaultLocale;
  private readonly fallbackLocale;
  constructor(registry: MessageRegistry, options: I18nModuleOptions);
  translate(locale: string, key: string, params?: Record<string, unknown>): string;
  getAvailableLocales(): string[];
  isLocaleSupported(locale: string): boolean;
  getDefaultLocale(): string;
  private getCompiledMessages;
  private flattenMessages;
}

export declare class I18nService implements II18nService {
  private readonly loader;
  private readonly ctx;
  private readonly options;
  constructor(loader: MessageLoaderService, ctx: RequestContext, options: I18nModuleOptions);
  t(key: string, params?: Record<string, unknown>): string;
  getLocale(): string;
}

export declare class I18nLocaleMiddleware implements NestMiddleware {
  private readonly resolved;
  constructor(options: I18nModuleOptions);
  use(c: Context, next: Next): Promise<Response | void>;
}

export declare const I18N_OPTIONS: InjectionToken<I18nModuleOptions>;

export declare const I18N_LOCALE_KEY: RequestContextKey<string>;

export declare function deepMerge(target: Record<string, unknown>, source: Record<string, unknown>): Record<string, unknown>;

export type { DetectionStrategy, I18nModuleOptions, II18nService, ResolvedI18nOptions };
```

## `./internal`

Type conditions: `types`

Declaration entry: `./dist/internal.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { $ as Diagnostics, J as Container, Z as ContainerOptions, _t as ROOT_MODULE_ID, gt as ProviderSnapshot, ot as InjectionToken, ut as ModuleScope } from "<internal:types-http-hono.types.d.ts>";
import { I as VelaApplicationContext, a as createRequestContext, c as applyRuntimeAdapters, d as BootstrapResult, f as bootstrap, h as VelaApplication, l as BootstrapInternals, m as ModuleOverrides, p as ModuleLoader, s as RuntimeAdapter, u as BootstrapOptions } from "<internal:request-context.d.ts>";
import { f as RouteManager, p as RouteManagerOptions } from "<internal:types-http-version.d.ts>";
import { a as getModuleMetadata, o as isModule, s as ConfigStore, t as ComponentManager } from "<internal:component.manager.d.ts>";
import { n as INVOCATION_TRANSPORT } from "<internal:tokens.d.ts>";
import { i as countRegisteredClasses, n as setRequestContainer } from "<internal:request-container.d.ts>";

export declare const CONFIG_OPTIONS: InjectionToken<Record<string, unknown>>;

export declare function finalizeApplicationContext({ container, loader }: BootstrapResult, context?: VelaApplicationContext): Promise<VelaApplicationContext>;

export declare function finalizeApplication(prepared: BootstrapResult, adapters?: readonly RuntimeAdapter[]): Promise<VelaApplication>;

export declare function bindAppProviders(routeManager: RouteManager, container: Container, loader: ModuleLoader): void;

export { type BootstrapInternals, type BootstrapOptions, type BootstrapResult, ComponentManager, ConfigStore, type ContainerOptions, type Diagnostics, INVOCATION_TRANSPORT, ModuleLoader, type ModuleOverrides, type ModuleScope, type ProviderSnapshot, ROOT_MODULE_ID, RouteManager, type RouteManagerOptions, applyRuntimeAdapters, bootstrap, countRegisteredClasses, createRequestContext, getModuleMetadata, isModule, setRequestContainer };
```

## `./live`

Type conditions: `types`

Declaration entry: `./dist/live/index.d.ts`

```ts
import { $ as LiveInspection, A as LiveInvalidatesTags, B as PresenceService, C as LIVE_PLATFORM, D as LiveInspector, E as InMemoryCursorLog, F as LiveEngine, G as LiveModule, H as LiveQuery, I as readPersistedLiveSubscriptions, J as InvalidationCommand, K as CommitStamp, L as PRESENCE_ROSTER_QUERY, M as localLive, N as stampCommitHeaders, O as LiveInvalidates, P as LIVE_SUBS_DATA_KEY, Q as LiveIdentity, R as PresenceMember, S as LIVE_MODULE_OPTIONS, T as encodeSubscriptionUpdate, U as LiveResolver, V as presenceTag, W as getLiveQueries, X as LiveDriver, Y as LiveDeliveryAuthorizationContext, Z as LiveEntrypointMeta, _ as isClientLiveFrame, a as LIVE_ERROR_CODES, at as LiveQueryMetadata, b as LIVE_CURSOR_LOG, c as LiveFrame, ct as PreparedLiveQuery, d as ServerLiveFrame, et as LiveInvalidationSink, f as applyListDelta, g as encodeLiveFrame, h as encodeLiveEnvelope, i as DEFAULT_KEY_FIELD, it as LiveQueryContext, j as LiveInvalidation, k as LiveInvalidatesOptions, l as LiveQueryDefinition, lt as ResumeVerdict, m as encodeListDelta, n as COMMIT_EPOCH_HEADER, nt as LivePlatform, o as LIVE_EVENT, ot as LiveQueryOptions, p as defineLiveQuery, q as CursorLog, r as ClientLiveFrame, rt as LivePresenceOptions, s as LIVE_PROTOCOL, st as LiveResolverMetadata, t as COMMIT_CURSOR_HEADER, tt as LiveModuleOptions, u as RowOp, ut as SubscriptionRecord, v as isServerLiveFrame, w as LIVE_RESOLVER_METADATA, x as LIVE_DRIVER, y as readLiveEnvelope, z as PresenceResolver } from "<internal:index-live-live.types.d.ts>";
export { COMMIT_CURSOR_HEADER, COMMIT_EPOCH_HEADER, type ClientLiveFrame, type CommitStamp, type CursorLog, DEFAULT_KEY_FIELD, InMemoryCursorLog, type InvalidationCommand, LIVE_CURSOR_LOG, LIVE_DRIVER, LIVE_ERROR_CODES, LIVE_EVENT, LIVE_MODULE_OPTIONS, LIVE_PLATFORM, LIVE_PROTOCOL, LIVE_RESOLVER_METADATA, LIVE_SUBS_DATA_KEY, type LiveDeliveryAuthorizationContext, type LiveDriver, LiveEngine, type LiveEntrypointMeta, type LiveFrame, type LiveIdentity, type LiveInspection, LiveInspector, LiveInvalidates, type LiveInvalidatesOptions, type LiveInvalidatesTags, LiveInvalidation, type LiveInvalidationSink, LiveModule, type LiveModuleOptions, type LivePlatform, type LivePresenceOptions, LiveQuery, type LiveQueryContext, type LiveQueryDefinition, type LiveQueryMetadata, type LiveQueryOptions, LiveResolver, type LiveResolverMetadata, PRESENCE_ROSTER_QUERY, type PreparedLiveQuery, type PresenceMember, PresenceResolver, PresenceService, type ResumeVerdict, type RowOp, type ServerLiveFrame, type SubscriptionRecord, applyListDelta, defineLiveQuery, encodeListDelta, encodeLiveEnvelope, encodeLiveFrame, encodeSubscriptionUpdate, getLiveQueries, isClientLiveFrame, isServerLiveFrame, localLive, presenceTag, readLiveEnvelope, readPersistedLiveSubscriptions, stampCommitHeaders };
```

## `./logging`

Type conditions: `types`

Declaration entry: `./dist/logging/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { J as Container, ot as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { dn as LogLevel, mn as LoggerService, pn as LoggerLevelName, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

type LogValue = null | boolean | number | string | readonly LogValue[] | {
  readonly [key: string]: LogValue;
};

interface LogRecord {
  readonly timestamp: number;
  readonly level: LoggerLevelName;
  readonly category: string;
  readonly message: string;
  readonly fields: Readonly<Record<string, LogValue>>;

  readonly arguments: readonly LogValue[];
}
type LogSink = (record: LogRecord) => void | Promise<void>;
type LogFields = Readonly<Record<string, unknown>>;
interface LogSerializationOptions {

  readonly maxDepth?: number;

  readonly maxEntries?: number;

  readonly maxNodes?: number;

  readonly maxStringLength?: number;

  readonly redactKeys?: readonly string[];
}
interface ApplicationLoggerOptions extends LogSerializationOptions {
  readonly level?: LogLevel;
  readonly categories?: Readonly<Record<string, LogLevel>>;

  readonly directive?: string;

  readonly sinks?: readonly LogSink[];

  readonly maxPending?: number;
}

interface LogDeliveryContext {
  readonly fields?: LogFields;
  readonly waitUntil?: (promise: Promise<void>) => void;

  readonly isActive?: () => boolean;
}

export declare const consoleLogSink: LogSink;
type Emit = (category: string, fields: LogFields, delivery: LogDeliveryContext, level: LoggerLevelName, message: unknown, rest: readonly unknown[]) => void;

export declare class StructuredLogger implements LoggerService {
  #private;
  /** @internal */
  constructor(category: string, fields: LogFields, delivery: LogDeliveryContext, emit: Emit, enabled: (level: LogLevel, category: string) => boolean, snapshot: (fields: LogFields) => LogFields);
  isEnabled(level: LogLevel): boolean;
  withFields(fields: LogFields): StructuredLogger;
  extend(namespace: string): StructuredLogger;
  log(message: unknown, ...rest: unknown[]): void;
  error(message: unknown, ...rest: unknown[]): void;
  warn(message: unknown, ...rest: unknown[]): void;
  debug(message: unknown, ...rest: unknown[]): void;
  verbose(message: unknown, ...rest: unknown[]): void;
}

export declare class ApplicationLogger {
  #private;
  constructor(options?: ApplicationLoggerOptions);
  get diagnostics(): Readonly<{
    pending: number;
    dropped: number;
    failed: number;
  }>;

  configure(directive: string): void;
  isEnabled(level: LogLevel, category?: string): boolean;
  createLogger(category?: string, fields?: LogFields, delivery?: LogDeliveryContext): StructuredLogger;

  subscribe(sink: LogSink): () => void;

  flush(): Promise<void>;
  dispose(): Promise<void>;
  onModuleDestroy(): Promise<void>;
}

export declare const APP_LOGGER: InjectionToken<ApplicationLogger>;

declare const ConfigurableModuleClass: ConfigurableModuleClassType<ApplicationLoggerOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;

export declare class LoggingModule extends ConfigurableModuleClass {}

export declare function serializeLogValue(value: unknown, options?: LogSerializationOptions): LogValue;

interface LogThresholds {
  readonly level: LogLevel;
  readonly categories: ReadonlyMap<string, LogLevel>;
}

export declare function parseLogDirective(directive: string, defaults?: {
  readonly level?: LogLevel;
  readonly categories?: Readonly<Record<string, LogLevel>>;
}): LogThresholds;

export declare function loggerForScope(container: Container, category?: string, fields?: LogFields): StructuredLogger;

export type { ApplicationLoggerOptions, LogDeliveryContext, LogFields, LogRecord, LogSerializationOptions, LogSink, LogThresholds, LogValue };
```

## `./module-kit`

Type conditions: `types`

Declaration entry: `./dist/module-kit.d.ts`

```ts
import { n as getMetadata, t as defineMetadata } from "<internal:metadata.d.ts>";
import { At as METADATA_KEYS, Ct as UnresolvedDependencyError, Dt as describeToken, J as Container, Pt as VelaHono, St as UnresolvedDependency, Tt as assertFactoryInject, X as Constructor, Y as CheckedProviders, a as ExecutionContext, c as HttpExecutionContext, ct as MissingInjectionMetadataReason, dt as ModuleVisibilityError, et as FactoryInject, ft as MultipleProvidersFoundError, jt as ParamType, kt as HttpMethod, lt as ModuleDescription, nt as InferToken, r as CanActivate, rt as InferTokens, st as MissingInjectionMetadataError, vt as Token, wt as UnresolvedDependencyReason, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { B as ContributesEntrypoints, D as OpenApiPathItem, G as DiscoveredMethodMeta, H as EntrypointKind, J as DiscoveryFilter, K as DiscoveredRegisteredMethodMeta, L as EntrypointRegistry, Q as createDiscoverableDecorator, R as getEntrypointKinds, U as contributesEntrypoints, V as Entrypoint, W as DiscoveredClass, X as CreateDiscoverableDecoratorOptions, Y as DiscoveryService, Z as DiscoverableDecorator, o as AdapterContext, q as DiscoveredRegistration, r as RequestContextKey, s as RuntimeAdapter, z as registerEntrypointKind } from "<internal:request-context.d.ts>";
import { B as InterceptorType, E as RoutePathOptions, L as FilterType, R as GuardType, V as MiddlewareType, _ as DEFAULT_QUERY_PARAMETER_LIMIT, d as RouteDescription, f as RouteManager, g as DEFAULT_QUERY_DEPTH_LIMIT, h as DEFAULT_QUERY_BYTES_LIMIT, m as trackResponseStream, q as PipeType, u as DEFAULT_BODY_LIMIT_BYTES } from "<internal:types-http-version.d.ts>";
import { An as orderGuardsByPhase, At as resolveScopedComponentsAsync, Bt as runInEntrypointScope, C as resolveErrorReporter, Cn as readJsonBody, Ct as getCatchTypes, Dt as getScopedComponents, Et as ResolvedComponentMap, Ft as ExecutionScope, Ht as lazyProvider, It as ExecutionScopeOptions, Lt as createExecutionScope, Mt as PipelineRunner, Ot as resolvePipelineComponents, Qt as ROOT_MODULE, Rt as finishExecutionScope, S as ErrorReporter, Sn as ReadJsonBodyOptions, Tt as PipelineComponentEntry, Ut as sideEffectModule, Vt as LazyProviderSpec, Xt as ModuleEntryList, Zt as UndefinedModuleError, _n as getCurrentContainer, ft as ReflectableDecorator, gn as enableAmbientContainer, jt as PipelineRunOptions, kn as SkippableGuardPhase, kt as resolveScopedComponents, vn as getCurrentRequestContext, wn as createLazyParamDecorator, wt as shouldFilterCatch, zt as getExecutionLifetime } from "<internal:index-factory.d.ts>";
import { a as defineBinding, i as EnvFactory, n as BindingKind, o as readEnv, r as BindingRef, s as resolveBinding, t as Binding } from "<internal:binding.d.ts>";
import { r as MetadataRegistry, t as getRequestContainer } from "<internal:request-container.d.ts>";
import { c as ScheduleInvocationSeed, i as IntervalMetadata, n as CronMetadata, s as ScheduleInvocation } from "<internal:schedule.types.d.ts>";
import { i as SCHEDULE_INVOCATION_SEED } from "<internal:schedule.tokens.d.ts>";
import { Context } from "hono";

export declare function stableHash(value: unknown): string;

export declare function referenceKey(...values: readonly unknown[]): string;

interface EntrypointExecutionContext<Kind extends string = string> extends ExecutionContext {
  getType(): Kind;
  getContext(): never;
  getRequest(): never;
  switchToHttp(): never;
  switchToWs(): never;

  getPayload(): unknown;
}

export declare function buildEntrypointExecutionContext<const Kind extends string>(kind: Kind, targetClass: Type, handlerName: string | symbol, payload: unknown, moduleId?: string, container?: Container): EntrypointExecutionContext<Kind>;

export declare function getEntrypointModuleId(container: Container, target: {
  readonly token: Token;
  readonly moduleId?: string;
}): string | undefined;

export declare function resolveEntrypoint<K extends Token>(container: Container, target: {
  readonly token: K;
  readonly moduleId?: string;
}): Promise<InferToken<K>>;

interface InvokeScheduledJobOptions {

  readonly seed?: (scope: Container) => void;
}

export declare function invokeScheduledJob(container: Container, entry: Entrypoint<CronMetadata | IntervalMetadata>, invocation: ScheduleInvocation, options?: InvokeScheduledJobOptions): Promise<void>;

export declare function parseCronMetadata(value: unknown): CronMetadata;

export declare function parseIntervalMetadata(value: unknown): IntervalMetadata;

export declare function cronDialectAmbiguity(meta: Pick<CronMetadata, 'expression' | 'dialect'>): string | undefined;

export declare function scheduledJobComponents(container: Container, entry: Entrypoint<{
  readonly methodName: string;
}>): string[];

interface RouteContributorContext {

  controller: Type;

  controllerPrefix: string;

  meta: unknown;

  globalPrefix: string;

  routePathOptions: RoutePathOptions;

  globalGuards: CanActivate[];

  getGlobalComponents: () => ReturnType<RouteManager['getGlobalComponents']>;

  container: Container;

  joinPaths: (...parts: string[]) => string;
}

interface RouteContributorOpenApiContext {
  controller: Type;
  meta: unknown;

  globalPrefix: string;

  controllerPrefix: string;
}

interface RouteContributor {

  id: string;

  claimsMetaKey: string;
  buildRoutes(app: VelaHono, ctx: RouteContributorContext): void | Promise<void>;
  buildOpenApiPaths?(ctx: RouteContributorOpenApiContext): Record<string, OpenApiPathItem>;
}

export declare function registerRouteContributor(contributor: RouteContributor): void;

export declare function getRouteContributors(): RouteContributor[];

declare function buildExecutionContext(c: Context, controller: Type, handlerName: string | symbol, moduleId?: string): HttpExecutionContext;

interface TrustedRequestPrincipal {
  readonly issuer: string;
  readonly subject: string;
  readonly principalType: 'user' | 'service';
}

interface TrustedRequestIdentity {
  readonly principal: TrustedRequestPrincipal;
  readonly tenantId?: string;

  readonly expiresAtMs?: number;

  readonly roles?: readonly string[];

  readonly claims?: Readonly<Record<string, unknown>>;
}

export declare function bindTrustedRequestContext(context: ExecutionContext, request: Request): void;

export declare function getTrustedContextRequest(context: ExecutionContext): Request | undefined;

export declare function setTrustedRequestIdentity(request: Request, identity: TrustedRequestIdentity): void;

export declare function clearTrustedRequestIdentity(request: Request): void;

export declare function getTrustedRequestIdentity(request: Request): TrustedRequestIdentity | undefined;

export declare const TRUSTED_REQUEST_IDENTITY: RequestContextKey<TrustedRequestIdentity>;

export declare function setTrustedRequestTenant(request: Request, expectedIdentity: TrustedRequestIdentity, tenantId: string): TrustedRequestIdentity;

interface TrustedRequestIdentityStore<T> {
  get(request: Request): T | undefined;

  set(request: Request, value: T): void;
}

export declare function createTrustedRequestIdentityStore<T>(): TrustedRequestIdentityStore<T>;

export declare const SkipGuardPhases: ReflectableDecorator<readonly SkippableGuardPhase[], readonly SkippableGuardPhase[]>;

export { type AdapterContext, type Binding, type BindingKind, type BindingRef, type CheckedProviders, type Constructor, Container, type ContributesEntrypoints, type CreateDiscoverableDecoratorOptions, DEFAULT_BODY_LIMIT_BYTES, DEFAULT_QUERY_BYTES_LIMIT, DEFAULT_QUERY_DEPTH_LIMIT, DEFAULT_QUERY_PARAMETER_LIMIT, type DiscoverableDecorator, type DiscoveredClass, type DiscoveredMethodMeta, type DiscoveredRegisteredMethodMeta, type DiscoveredRegistration, type DiscoveryFilter, DiscoveryService, type Entrypoint, type EntrypointExecutionContext, type EntrypointKind, EntrypointRegistry, type EnvFactory, type ErrorReporter, type ExecutionScope, type ExecutionScopeOptions, type FactoryInject, type FilterType, type GuardType, HttpMethod, type InferToken, type InferTokens, type InterceptorType, type InvokeScheduledJobOptions, type LazyProviderSpec, METADATA_KEYS, MetadataRegistry, type MiddlewareType, MissingInjectionMetadataError, type MissingInjectionMetadataReason, type ModuleDescription, type ModuleEntryList, ModuleVisibilityError, MultipleProvidersFoundError, ParamType, type PipeType, type PipelineComponentEntry, type PipelineRunOptions, PipelineRunner, ROOT_MODULE, type ReadJsonBodyOptions, type ResolvedComponentMap, type RouteContributor, type RouteContributorContext, type RouteContributorOpenApiContext, type RouteDescription, type RuntimeAdapter, SCHEDULE_INVOCATION_SEED, type ScheduleInvocationSeed, type SkippableGuardPhase, type TrustedRequestIdentity, type TrustedRequestIdentityStore, type TrustedRequestPrincipal, UndefinedModuleError, type UnresolvedDependency, UnresolvedDependencyError, type UnresolvedDependencyReason, assertFactoryInject, buildExecutionContext as buildHttpExecutionContext, contributesEntrypoints, createDiscoverableDecorator, createExecutionScope, createLazyParamDecorator, defineBinding, defineMetadata, describeToken, enableAmbientContainer, finishExecutionScope, getCatchTypes, getCurrentContainer, getCurrentRequestContext, getEntrypointKinds, getExecutionLifetime, getMetadata, getRequestContainer, getScopedComponents, lazyProvider, orderGuardsByPhase, readEnv, readJsonBody, registerEntrypointKind, resolveBinding, resolveErrorReporter, resolvePipelineComponents, resolveScopedComponents, resolveScopedComponentsAsync, runInEntrypointScope, shouldFilterCatch, sideEffectModule, trackResponseStream };
```

## `./observability`

Type conditions: `types`

Declaration entry: `./dist/observability/index.d.ts`

```ts
import { J as Container, ot as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { n as RequestContext, s as RuntimeAdapter } from "<internal:request-context.d.ts>";
import { C as HttpRequestObservation, S as HttpRequestCompletion, w as HttpRequestObserver } from "<internal:types-http-version.d.ts>";

interface TraceContext {
  readonly traceId: string;
  readonly spanId: string;
  readonly traceFlags: number;
  readonly traceState?: string;
  readonly isRemote?: boolean;
}
type TelemetryAttributes = Readonly<Record<string, string | number | boolean>>;
type TelemetryOutcome = 'success' | 'error' | 'cancelled';
type TelemetrySpanKind = 'internal' | 'server' | 'client' | 'producer' | 'consumer';
interface TelemetrySpanOptions {
  readonly kind?: TelemetrySpanKind;
  readonly parent?: TraceContext;
  readonly attributes?: TelemetryAttributes;
}
interface TelemetrySpan {
  readonly context: TraceContext | undefined;
  setAttributes(attributes: TelemetryAttributes): void;

  end(outcome?: TelemetryOutcome): void;
}
interface TelemetryCounter {
  add(value: number, attributes?: TelemetryAttributes): void;
}
interface TelemetryHistogram {
  record(value: number, attributes?: TelemetryAttributes): void;
}
interface TelemetryMetricOptions {
  readonly unit?: string;
  readonly description?: string;
}

interface Telemetry {
  startSpan(name: string, options?: TelemetrySpanOptions): TelemetrySpan;
  createCounter(name: string, options?: TelemetryMetricOptions): TelemetryCounter;
  createHistogram(name: string, options?: TelemetryMetricOptions): TelemetryHistogram;
}

interface ScopedTelemetry {
  readonly telemetry: Telemetry;
  readonly span: TelemetrySpan;
}

export declare function validateTraceContext(value: unknown): TraceContext | undefined;

export declare function extractTraceContext(headers: Headers): TraceContext | undefined;

export declare function injectTraceContext(headers: Headers, context: TraceContext | undefined): void;

export declare const noopSpan: TelemetrySpan;
export declare const noopTelemetry: Telemetry;
export declare const noopScopedTelemetry: ScopedTelemetry;

export declare function safeTelemetry(telemetry?: Telemetry): Telemetry;

export declare const REQUEST_TELEMETRY: InjectionToken<ScopedTelemetry>;

export declare function telemetryForScope(container: Container): ScopedTelemetry;
export declare function getRequestTelemetry(context: RequestContext): ScopedTelemetry;
interface ObservabilityOptions {
  readonly telemetry?: Telemetry;

  readonly trustIncomingTraceContext?: boolean;
}

export declare function observabilityAdapter(options?: ObservabilityOptions): RuntimeAdapter;

interface HttpClientTelemetryOptions {
  readonly telemetry?: Telemetry;

  readonly parent?: () => TraceContext | undefined;
}

interface HttpClientTelemetryObserver {
  onRequest(request: {
    readonly method: string;
    readonly headers: Headers;
  }): {
    onResponse(response: {
      readonly status: number;
    }): void;
    onError(error: unknown): void;
    onEnd(): void;
  };
}

export declare function createHttpClientTelemetryObserver(options?: HttpClientTelemetryOptions): HttpClientTelemetryObserver;

interface OpenTelemetrySpan {
  spanContext(): {
    readonly traceId: string;
    readonly spanId: string;
    readonly traceFlags: number;
    readonly traceState?: {
      serialize(): string;
    };
  };
  setAttributes(attributes: TelemetryAttributes): unknown;
  setStatus(status: {
    code: 0 | 1 | 2;
  }): unknown;
  end(): void;
}

interface OpenTelemetryOptions<Context> {
  readonly tracer: {
    startSpan(name: string, options: {
      kind: 0 | 1 | 2 | 3 | 4;
      attributes?: TelemetryAttributes;
    }, context: Context): OpenTelemetrySpan;
  };
  readonly meter?: {
    createCounter(name: string, options?: TelemetryMetricOptions): TelemetryCounter;
    createHistogram(name: string, options?: TelemetryMetricOptions): TelemetryHistogram;
  };

  readonly rootContext: Context;

  readonly parentContext: (parent: TraceContext) => Context;
}

export declare function createOpenTelemetry<Context>(options: OpenTelemetryOptions<Context>): Telemetry;

interface ExecutionTelemetryOptions {
  readonly telemetry?: Telemetry;

  readonly operation: string;
  readonly kind?: TelemetrySpanKind;
  readonly parent?: () => TraceContext | undefined;
}
interface ExecutionTelemetryObserver {
  onStart(): {
    onEnd(outcome: TelemetryOutcome): void;
  };
}

export declare function createExecutionTelemetryObserver(options: ExecutionTelemetryOptions): ExecutionTelemetryObserver;

export type { ExecutionTelemetryObserver, ExecutionTelemetryOptions, HttpClientTelemetryObserver, HttpClientTelemetryOptions, HttpRequestCompletion, HttpRequestObservation, HttpRequestObserver, ObservabilityOptions, OpenTelemetryOptions, OpenTelemetrySpan, ScopedTelemetry, Telemetry, TelemetryAttributes, TelemetryCounter, TelemetryHistogram, TelemetryMetricOptions, TelemetryOutcome, TelemetrySpan, TelemetrySpanKind, TelemetrySpanOptions, TraceContext };
```

## `./openapi`

Type conditions: `types`

Declaration entry: `./dist/openapi/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { A as OpenApiUi, C as OpenApiDocument, D as OpenApiPathItem, E as OpenApiParameter, O as OpenApiRequestBody, S as MountOpenApiOptions, T as OpenApiOperation, _ as ApiResponseEntry, b as HttpVerb, g as ApiDocMetadata, k as OpenApiResponse, v as ApiResponseOptions, w as OpenApiInfo, x as JsonSchema, y as CreateOpenApiDocumentOptions } from "<internal:request-context.d.ts>";
import { I as DynamicModule } from "<internal:types-http-version.d.ts>";
import { rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

export declare function createOpenApiDocument(rootModule: Type | DynamicModule, options?: CreateOpenApiDocumentOptions): OpenApiDocument;

interface OpenApiModuleOptions extends Omit<CreateOpenApiDocumentOptions, 'globalPrefix'> {

  path?: string;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<OpenApiModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;
type OpenApiModuleRegistration = Parameters<(typeof ConfigurableModuleClass)['forRoot']>[0];
export declare class OpenApiModule extends ConfigurableModuleClass {

  static forRoot(options?: OpenApiModuleRegistration): DynamicModule;
}

export declare function ApiExclude(): MethodDecorator & ClassDecorator;

export declare function isApiExcluded(target: object, propertyKey?: string | symbol): boolean;

export declare function ApiDoc(metadata: ApiDocMetadata): MethodDecorator & ClassDecorator;

export declare function ApiTags(...tags: string[]): MethodDecorator & ClassDecorator;

export declare function ApiResponse(options: ApiResponseOptions): MethodDecorator;

export declare function zodToJsonSchema(schema: unknown, direction?: 'input' | 'output', libraryOptions?: Record<string, unknown>): JsonSchema;

export type { ApiDocMetadata, ApiResponseEntry, ApiResponseOptions, CreateOpenApiDocumentOptions, HttpVerb, JsonSchema, MountOpenApiOptions, OpenApiDocument, OpenApiInfo, OpenApiModuleOptions, OpenApiOperation, OpenApiParameter, OpenApiPathItem, OpenApiRequestBody, OpenApiResponse, OpenApiUi };
```

## `./queue`

Type conditions: `types`

Declaration entry: `./dist/queue/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { J as Container, ot as InjectionToken, q as VelaEnv, vt as Token } from "<internal:types-http-hono.types.d.ts>";
import { L as EntrypointRegistry, Y as DiscoveryService } from "<internal:request-context.d.ts>";
import { I as DynamicModule, r as InvocationTarget } from "<internal:types-http-version.d.ts>";
import { g as StandardSchemaV1 } from "<internal:parse-schema.d.ts>";
import { rn as ConfigurableModuleClassType, tn as ConfigurableModuleAsyncOptions } from "<internal:index-factory.d.ts>";

export declare class QueueRegistry {
  #private;
  constructor(registrations: Iterable<QueueRegistration>);

  static discover(discovery: DiscoveryService): QueueRegistry;

  get(name: string): RegisteredQueue | undefined;
  has(name: string): boolean;

  all(): RegisteredQueue[];
}

interface QueueJob<T = unknown> {
  id: string;

  queue: string;

  name: string;
  data: T;

  attempt: number;
}
interface AddJobOptions {

  delayMs?: number;
}

interface QueueEnqueueRequest {
  readonly job: QueueJob;
  readonly options?: AddJobOptions;
}

type QueueDispatchFn = (job: QueueJob) => Promise<void>;
interface QueueDriverBindHooks {

  onError?: (error: unknown, job: QueueJob) => void;
}

interface QueueDriver {
  readonly kind: string;

  readonly entrypoints?: readonly QueueDriverEntrypoint[];

  consume?(payload: unknown, dispatch: QueueDispatchFn): Promise<void>;
  enqueue(job: QueueJob, options?: AddJobOptions): Promise<void>;

  enqueueBatch?(requests: readonly QueueEnqueueRequest[]): Promise<void>;
  bind?(dispatch: QueueDispatchFn, hooks?: QueueDriverBindHooks): void;

  unbind?(): void;
}

interface QueueDriverEntrypoint {
  readonly kind: string;
  readonly meta: Readonly<Record<string, unknown>>;
}

interface QueueDriverContext {

  readonly env: VelaEnv | undefined;

  readonly queues: QueueRegistry;
}

type QueueDriverFactory = (context: QueueDriverContext) => QueueDriver;

interface QueueRegistration {
  readonly name: string;

  readonly binding?: string;

  readonly consumer?: string;
}

interface RegisteredQueue {
  readonly name: string;

  readonly binding: string | undefined;

  readonly consumers: readonly string[];
}

type QueueDispatchMode = {
  readonly kind: 'direct';
} | {
  readonly kind: 'signed';

  readonly target: (job: QueueJob) => InvocationTarget;

  readonly method?: string;

  readonly ttlSeconds?: number;
};
interface QueueModuleOptions {

  driver?: QueueDriver | QueueDriverFactory;

  dispatch?: QueueDispatchMode;
}

interface ProcessorMetadata {
  queueName: string;
}

interface ProcessMetadata {
  jobName?: string;

  schema?: StandardSchemaV1;
  methodName: string | symbol;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<QueueModuleOptions, "forRoot", "create", Record<never, never>, never>, MODULE_OPTIONS_TOKEN: InjectionToken<QueueModuleOptions>;

export declare class QueueModule extends ConfigurableModuleClass {

  static forRoot(options?: QueueModuleOptions): DynamicModule;

  static forRootAsync<const Inject extends readonly Token[]>(options: ConfigurableModuleAsyncOptions<QueueModuleOptions, never, 'create', Inject>): DynamicModule;

  static registerQueue(registration: QueueRegistration, ...more: QueueRegistration[]): DynamicModule;
}

interface QueueJobDefinition<S extends StandardSchemaV1 = StandardSchemaV1> {
  readonly name: string;
  readonly schema: S;
}
type QueueJobInput<D extends QueueJobDefinition> = StandardSchemaV1.InferInput<D['schema']>;
type QueueJobOutput<D extends QueueJobDefinition> = StandardSchemaV1.InferOutput<D['schema']>;

export declare function defineQueueJob<const N extends string, S extends StandardSchemaV1>(name: N, schema: S): QueueJobDefinition<S> & {
  readonly name: N;
};

export declare function InjectQueue(name: string): ParameterDecorator;

export declare function Processor(queueName: string): ClassDecorator;

type QueueProcessDecorator<Data> = <Handler extends (job: QueueJob<Data>) => unknown>(target: object, key: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;
export declare function Process<S extends StandardSchemaV1>(definition: QueueJobDefinition<S>): QueueProcessDecorator<StandardSchemaV1.InferOutput<S>>;
export declare function Process(jobName?: string): MethodDecorator;

export declare function getProcessHandlers(processorClass: object): ProcessMetadata[];

interface QueueBulkJob<S extends StandardSchemaV1 = StandardSchemaV1> {
  readonly job: QueueJobDefinition<S>;

  readonly data: StandardSchemaV1.InferInput<S>;
  readonly options?: AddJobOptions;
}

interface QueueBulkNamedJob<T = unknown> {
  readonly job: string;
  readonly data: T;
  readonly options?: AddJobOptions;
}

type QueueBulkEntry = QueueBulkJob | QueueBulkNamedJob;

type NoExtraBulkKeys<E> = { readonly [K in Exclude<keyof E, keyof QueueBulkNamedJob>]: never; };

type CheckedBulkEntry<E> = (E extends {
  readonly job: QueueJobDefinition<infer S>;
} ? QueueBulkJob<S> : E) & NoExtraBulkKeys<E>;
type BulkEntryResult<E> = E extends {
  readonly job: QueueJobDefinition<infer S>;
} ? QueueJob<StandardSchemaV1.InferInput<S>> : E extends {
  readonly data: infer T;
} ? QueueJob<T> : never;

export declare class QueueClient {
  #private;
  constructor(queue: string, driver: QueueDriver);
  get name(): string;
  add<S extends StandardSchemaV1>(definition: QueueJobDefinition<S>, data: StandardSchemaV1.InferInput<S>, options?: AddJobOptions): Promise<QueueJob<StandardSchemaV1.InferInput<S>>>;
  add<T>(jobName: string, data: T, options?: AddJobOptions): Promise<QueueJob<T>>;

  addBulk<J extends readonly QueueBulkEntry[] | []>(jobs: J & { readonly [K in keyof J]: CheckedBulkEntry<J[K]>; }): Promise<{ -readonly [K in keyof J]: BulkEntryResult<J[K]>; }>;
}

export declare const PROCESSOR_METADATA = "vela:queue:processor";
export declare const PROCESS_METADATA = "vela:queue:process";

export declare const QUEUE_DRIVER: InjectionToken<QueueDriver>;

export declare function queueToken(name: string): InjectionToken<QueueClient>;

export declare class QueueBatchError extends Error {
  readonly name = "QueueBatchError";

  readonly accepted: readonly string[];

  readonly rejected: readonly string[];
  constructor(accepted: readonly string[], rejected: readonly string[], cause: unknown);
}

interface QueueDispatchOptions {

  unhandled?: 'ignore' | 'error';
}
interface QueueDispatchResult {

  handled: number;
}

interface QueueEntry {
  token: Token;
  moduleId?: string;
  meta: ProcessorMetadata;
}

export declare class QueueDispatchBinding {
  #private;
  constructor(container: Container, discovery: DiscoveryService, driver: QueueDriver, queues: QueueRegistry, dispatch?: QueueDispatchMode);
  dispose(): void;

  dispatch(job: QueueJob, options?: QueueDispatchOptions): Promise<QueueDispatchResult>;
}

export declare function dispatchQueueJob(container: Container, entrypoints: EntrypointRegistry, job: QueueJob, options?: QueueDispatchOptions): Promise<QueueDispatchResult>;

interface InlineQueueOptions {

  mode?: 'immediate' | 'manual';
}
interface InlineQueueDriver extends QueueDriver {

  flush(): Promise<number>;

  readonly size: number;
}

export declare function inline(options?: InlineQueueOptions): InlineQueueDriver;

interface QueueMessageLike<Body = unknown> {
  readonly id: string;
  readonly timestamp: Date;
  readonly body: Body;

  readonly attempts: number;

  ack(): void;

  retry(options?: {
    delaySeconds?: number;
  }): void;
}

type MessageOutcome = 'acked' | 'retried' | 'unsettled';

interface MessageDisposition {
  id: string;
  attempts: number;
  outcome: MessageOutcome;

  retryDelaySeconds?: number;

  retryExhausted?: boolean;

  deadLettered: boolean | undefined;
}
interface ObserveMessageOptions {

  maxRetries?: number;

  deadLetterQueue?: boolean;
}
interface ObservedMessage<Body = unknown> {

  message: QueueMessageLike<Body>;

  disposition(): MessageDisposition;
}

interface BatchDisposition {
  messages: MessageDisposition[];
  acked: number;
  retried: number;
  unsettled: number;

  deadLettered: number;
}
interface ObservedBatch<Body = unknown> {

  messages: QueueMessageLike<Body>[];

  report(): BatchDisposition;
}

export declare function observeMessage<Body = unknown>(message: QueueMessageLike<Body>, options?: ObserveMessageOptions): ObservedMessage<Body>;

export declare function observeBatch<Body = unknown>(messages: readonly QueueMessageLike<Body>[], options?: ObserveMessageOptions): ObservedBatch<Body>;

export declare function parseQueueJob(value: unknown, deliveryAttempt?: number): QueueJob;

export { type AddJobOptions, type BatchDisposition, type InlineQueueDriver, type InlineQueueOptions, type MessageDisposition, type MessageOutcome, type ObserveMessageOptions, type ObservedBatch, type ObservedMessage, type ProcessMetadata, type ProcessorMetadata, MODULE_OPTIONS_TOKEN as QUEUE_MODULE_OPTIONS, type QueueBulkEntry, type QueueBulkJob, type QueueBulkNamedJob, type QueueDispatchFn, type QueueDispatchMode, type QueueDispatchOptions, type QueueDispatchResult, type QueueDriver, type QueueDriverBindHooks, type QueueDriverContext, type QueueDriverEntrypoint, type QueueDriverFactory, type QueueEnqueueRequest, type QueueEntry, type QueueJob, type QueueJobDefinition, type QueueJobInput, type QueueJobOutput, type QueueMessageLike, type QueueModuleOptions, type QueueProcessDecorator, type QueueRegistration, type RegisteredQueue };
```

## `./schedule`

Type conditions: `types`

Declaration entry: `./dist/schedule/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { a as ScheduleDecorator, d as CronOptions, f as parseCron, i as IntervalMetadata, l as ScheduleJobRef, n as CronMetadata, o as ScheduleDispatchMode, r as IntervalInvocation, s as ScheduleInvocation, t as CronInvocation, u as CronMatcher } from "<internal:schedule.types.d.ts>";
import { n as INTERVAL_METADATA, r as SCHEDULE_DISPATCH, t as CRON_METADATA } from "<internal:schedule.tokens.d.ts>";
import { n as RegisteredIntervalJob, r as ScheduleRegistry, t as RegisteredCronJob } from "<internal:schedule.registry.d.ts>";

interface ScheduleModuleOptions {

  dispatch?: ScheduleDispatchMode;
}
declare const ConfigurableModuleClass: ConfigurableModuleClassType<ScheduleModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, "dispatch">;

export declare class ScheduleModule extends ConfigurableModuleClass {}

export declare function Cron(expression: string, options?: CronOptions): ScheduleDecorator<CronInvocation>;

export declare function Interval(ms: number): ScheduleDecorator<IntervalInvocation>;

export { CRON_METADATA, type CronInvocation, type CronMatcher, type CronMetadata, type CronOptions, INTERVAL_METADATA, type IntervalInvocation, type IntervalMetadata, type RegisteredCronJob, type RegisteredIntervalJob, SCHEDULE_DISPATCH, type ScheduleDecorator, type ScheduleDispatchMode, type ScheduleInvocation, type ScheduleJobRef, type ScheduleModuleOptions, ScheduleRegistry, parseCron };
```

## `./schedule-node`

Type conditions: `types`

Declaration entry: `./dist/schedule-node/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { J as Container } from "<internal:types-http-hono.types.d.ts>";
import { g as OnModuleDestroy, m as OnApplicationBootstrap, p as BeforeApplicationShutdown, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { t as InternalDispatcher } from "<internal:internal-dispatcher.d.ts>";
import { o as ScheduleDispatchMode } from "<internal:schedule.types.d.ts>";
import { r as ScheduleRegistry } from "<internal:schedule.registry.d.ts>";

type ScheduleNodeModuleOptions = Record<never, never>;
declare const ConfigurableModuleClass: ConfigurableModuleClassType<ScheduleNodeModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;

export declare class ScheduleNodeModule extends ConfigurableModuleClass {}

export declare class ScheduleExecutor implements OnApplicationBootstrap, BeforeApplicationShutdown, OnModuleDestroy {
  #private;
  constructor(registry: ScheduleRegistry, container: Container, dispatch?: ScheduleDispatchMode, dispatcher?: InternalDispatcher);
  onApplicationBootstrap(): void;

  beforeApplicationShutdown(): Promise<void>;
  onModuleDestroy(): Promise<void>;
}

export type { ScheduleNodeModuleOptions };
```

## `./security`

Type conditions: `types`

Declaration entry: `./dist/security/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { ot as InjectionToken, u as NestMiddleware } from "<internal:types-http-hono.types.d.ts>";
import { I as DynamicModule, a as NonceStore } from "<internal:types-http-version.d.ts>";
import { rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

interface SecurityCorsOptions {

  allowMethods?: string[];

  allowHeaders?: string[];
  exposeHeaders?: string[];

  credentials?: boolean;

  maxAge?: number;
}
interface OriginProtectionOptions {

  unsafeMethods?: string[];

  protectAuthorization?: boolean;

  allowMissingOrigin?: boolean;
}
interface SecurityHeadersOptions {
  noSniff?: boolean;
  referrerPolicy?: string | false;
  frameOptions?: 'DENY' | 'SAMEORIGIN' | false;

  hsts?: string | false;
  contentSecurityPolicy?: string | false;
}
interface SecurityModuleOptions {

  allowedOrigins?: string[];

  cors?: SecurityCorsOptions | false;

  originProtection?: OriginProtectionOptions | false;

  headers?: SecurityHeadersOptions;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<SecurityModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;
export declare class SecurityModule extends ConfigurableModuleClass {

  static forRoot(options?: SecurityModuleOptions & {
    isGlobal?: boolean;
    key?: string;
  }): DynamicModule;
}

export declare class Secret<T> {
  #private;
  constructor(value: T);
  reveal(): T;
  toJSON(): string;
  toString(): string;
}

export declare const SECURITY_OPTIONS: InjectionToken<SecurityModuleOptions>;

type PrioritizedMiddleware = NestMiddleware & {
  priority: number;
};
export declare function buildSecurityMiddleware(options: SecurityModuleOptions): PrioritizedMiddleware;

interface SignedUrlOptions {

  expiresIn: number;

  method: string;

  purpose: string;
}
type VerifySignedUrlOptions = Pick<SignedUrlOptions, 'method' | 'purpose'>;
export declare const HTTP_SIGNED_URL_PURPOSE = "vela:http-route";

export declare function signUrl(url: string, secret: string, options: SignedUrlOptions): Promise<string>;

export declare function verifySignedUrl(url: string, secret: string, options: VerifySignedUrlOptions): Promise<boolean>;

export declare const NONCE_STORE: InjectionToken<NonceStore>;

export declare class MemoryNonceStore implements NonceStore {
  private readonly seen;
  claim(nonce: string, expEpochSeconds: number): Promise<boolean>;

  private prune;
}

export type { NonceStore, OriginProtectionOptions, SecurityCorsOptions, SecurityHeadersOptions, SecurityModuleOptions, SignedUrlOptions, VerifySignedUrlOptions };
```

## `./seeder`

Type conditions: `types`

Declaration entry: `./dist/seeder/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { J as Container, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { h as VelaApplication } from "<internal:request-context.d.ts>";
import { m as OnApplicationBootstrap, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

interface Seeder {
  run(): void | Promise<void>;
}
interface SeederMetadata {

  name?: string;

  order?: number;
}
interface RegisteredSeeder {
  name: string;
  order: number;

  moduleId?: string;
  target: new (...args: any[]) => Seeder;
}
interface SeederResult {
  name: string;
  ok: boolean;
  error?: unknown;
}

declare function Seeder$1(options?: SeederMetadata): ClassDecorator;

export declare class SeederRegistry implements OnApplicationBootstrap {
  #private;
  constructor(container: Container);
  onApplicationBootstrap(): void;

  list(): RegisteredSeeder[];

  runAll(options?: {
    stopOnError?: boolean;
  }): Promise<SeederResult[]>;
}

interface SeederModuleOptions {
  seeders?: Type[];
}
declare const ConfigurableModuleClass: ConfigurableModuleClassType<SeederModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, "seeders">;
export declare class SeederModule extends ConfigurableModuleClass {}

export declare function runSeeders(app: VelaApplication, options?: {
  stopOnError?: boolean;
}): Promise<SeederResult[]>;

export declare const SEEDER_METADATA = "vela:seeder";

export { type Seeder as ISeeder, type RegisteredSeeder, Seeder$1 as Seeder, type SeederMetadata, type SeederResult };
```

## `./streaming`

Type conditions: `types`

Declaration entry: `./dist/streaming/index.d.ts`

```ts
import { SSEMessage, SSEStreamingApi, stream, streamSSE, streamText } from "hono/streaming";
export { type SSEMessage, type SSEStreamingApi, stream, streamSSE, streamText };
```

## `./throttler`

Type conditions: `types`

Declaration entry: `./dist/throttler/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { a as ExecutionContext, ot as InjectionToken, r as CanActivate } from "<internal:types-http-hono.types.d.ts>";
import { r as RequestContextKey } from "<internal:request-context.d.ts>";
import { f as RouteManager } from "<internal:types-http-version.d.ts>";
import { On as GuardPhase, pt as Reflector, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { i as EnvFactory } from "<internal:binding.d.ts>";

interface ThrottlerOptions {

  name?: string;

  ttl: number;

  limit: number;
}

interface ThrottleConfig {
  ttl?: number;
  limit?: number;
}
interface ThrottlerStorageRecord {
  count: number;
  ttlMs: number;

  allowed?: boolean;

  remaining?: number;
}
interface ThrottlerStore {

  increment(key: string, ttl: number, limit: number, throttlerName: string): ThrottlerStorageRecord | Promise<ThrottlerStorageRecord>;
  reset(key: string): void | Promise<void>;

  readonly fixedLimits?: boolean;

  validate?(throttlers: readonly Required<ThrottlerOptions>[]): void;
}
interface RateLimitInfo {
  limit: number;
  remaining?: number;
  reset: number;
}
interface ThrottlerModuleOptions {

  throttlers: ThrottlerOptions[];

  storage?: ThrottlerStore | EnvFactory<ThrottlerStore>;

  getTracker?: (request: Request, context: ExecutionContext) => string | null | undefined;

  generateKey?: (context: ExecutionContext, tracker: string, throttlerName: string) => string;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<ThrottlerModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, never>;
export declare class ThrottlerModule extends ConfigurableModuleClass {}

export declare class ThrottlerGuard implements CanActivate {
  #private;
  private options;
  private storage;
  private routeManager;
  private reflector;

  static readonly phase: GuardPhase;
  constructor(options: ThrottlerModuleOptions, storage: ThrottlerStore, routeManager: RouteManager, reflector: Reflector);
  canActivate(context: ExecutionContext): Promise<boolean>;
  private tracker;
}

interface ThrottlerStorageOptions {

  maxKeys?: number;
}

export declare class ThrottlerStorage implements ThrottlerStore {
  #private;
  constructor(options?: ThrottlerStorageOptions);
  increment(key: string, ttlMs: number, limit?: number): ThrottlerStorageRecord;
  reset(key: string): void;
}

export declare const Throttle: (overrides: Record<string, ThrottleConfig>) => (target: object, propertyKey?: string | symbol, descriptor?: PropertyDescriptor) => void;

export declare const SkipThrottle: (skip?: Record<string, boolean>) => (target: object, propertyKey?: string | symbol, descriptor?: PropertyDescriptor) => void;

export declare const THROTTLER_OPTIONS: InjectionToken<ThrottlerModuleOptions>;
export declare const THROTTLER_STORAGE: InjectionToken<ThrottlerStore>;

export declare const RATE_LIMIT: RequestContextKey<Readonly<Record<string, RateLimitInfo>>>;

export declare const THROTTLE_METADATA = "vela:throttle";

export declare const SKIP_THROTTLE_METADATA = "vela:skip-throttle";

export type { RateLimitInfo, ThrottleConfig, ThrottlerModuleOptions, ThrottlerOptions, ThrottlerStorageOptions, ThrottlerStorageRecord, ThrottlerStore };
```

## `./validation`

Type conditions: `types`

Declaration entry: `./dist/validation/index.d.ts`

```ts
import "<internal:metadata.d.ts>";
import { d as PipeTransform, t as ArgumentMetadata } from "<internal:types-http-hono.types.d.ts>";
import { _ as ValidationIssue, a as parseSchema, b as validateSchema, c as DtoOptions, d as SchemaParser, f as StandardDtoDefinition, g as StandardSchemaV1, h as StandardJSONSchemaV1, i as isValidationSchema, l as DtoSchema, m as SchemaValidationError, n as SchemaOutput, o as parseSchemaAsync, p as defineDto, r as ValidationSchema, s as DtoDefinition, t as SchemaInput, u as RuntimeParser, v as isStandardSchema, y as standardJsonSchema } from "<internal:parse-schema.d.ts>";

export declare class ValidationPipe implements PipeTransform {
  #private;
  readonly parser?: ValidationSchema | undefined;

  constructor(parser?: ValidationSchema | undefined);
  transformAsync(value: unknown, metadata: ArgumentMetadata): Promise<unknown>;
  transform(value: unknown, metadata: ArgumentMetadata): unknown;
}

export { type DtoDefinition, type DtoOptions, type DtoSchema, type RuntimeParser, type SchemaInput, type SchemaOutput, type SchemaParser, SchemaValidationError, type StandardDtoDefinition, type StandardJSONSchemaV1, type StandardSchemaV1, type ValidationIssue, type ValidationSchema, defineDto, isStandardSchema, isValidationSchema, parseSchema, parseSchemaAsync, standardJsonSchema, validateSchema };
```

## `./websocket`

Type conditions: `types`

Declaration entry: `./dist/websocket/index.d.ts`

```ts
import { A as WebSocketUpgradeAuthenticationContext, B as SyncDriver, C as ReservedWsEventHandler, D as WebSocketGatewayOptions, E as UpgradeAuthenticator, F as WsResponse, H as broadcastCommandFits, I as WsServer, L as InMemoryRoomRegistry, M as WsClient, N as WsExecutionContext, O as WebSocketPrincipal, P as WsMessage, R as MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES, S as OnGatewayInit, T as SubscribeMessageMetadata, U as local, V as assertBroadcastCommandFits, W as webSocketSyncEnvelopeFits, _ as GatewayDelivery, b as OnGatewayConnection, f as WsArgumentsHost, g as GatewayBroadcastOperator, h as ForwardedWebSocketUpgrade, j as WebSocketUpgradeIdentity, k as WebSocketTransport, m as BroadcastOperator, p as BroadcastCommand, v as GatewayEventArgs, w as ReservedWsEventMetadata, x as OnGatewayDisconnect, y as GatewayServer, z as RoomRegistry } from "<internal:types-http-hono.types.d.ts>";
import { $ as WebSocketModuleOptions, A as WebSocketTicketPrincipal, B as assertWebSocketRoomId, C as IssueWebSocketTicketOptions, D as WEBSOCKET_TICKET_PURPOSE, E as WEBSOCKET_TICKET_MAX_TTL_MS, F as DEFAULT_WS_MAX_FRAME_BYTES, G as resolveGatewayRoomParam, H as isWebSocketOriginAllowed, I as DEFAULT_WS_MAX_JOINED_ROOMS, J as WsDispatcher, K as resolveMaxFrameBytes, L as DEFAULT_WS_MAX_ROOM_ID_BYTES, M as issueWebSocketTicket, N as verifyAndConsumeWebSocketTicket, O as WebSocketTicketClaim, P as AuthenticatedWebSocketUpgrade, Q as WebSocketModule, R as WebSocketUpgradeGate, S as WsServerImpl, T as WEBSOCKET_TICKET_AUDIENCE, U as normalizeWebSocketUpgradeIdentity, V as createWebSocketUpgradeGate, W as resolveGatewayRoomId, X as readWsEntrypointMeta, Y as WsEntrypointMeta, Z as Gateways, _ as resolveWsArgs, a as WsMessageQueue, at as WebSocketServer, b as buildWsExecutionContext, c as WS_GATEWAY_METADATA, d as WS_ROOM_REGISTRY, et as ConnectedSocket, f as WS_SERVER, g as WsParamType, h as WS_TRANSPORT, i as readWebSocketEnvelope, it as WebSocketGateway, j as WebSocketTicketPrincipalType, k as WebSocketTicketNonceStore, l as WS_MODULE_OPTIONS, m as WS_SYNC_DRIVER, n as WebSocketSendPolicy, nt as ReservedWsEvent, o as trySendWebSocketFrame, p as WS_SUBSCRIBE_METADATA, q as webSocketFrameFits, r as WebSocketSendResult, rt as SubscribeMessage, s as RESERVED_WS_EVENT_PREFIX, t as WebSocketSendGate, tt as MessageBody, u as WS_RESERVED_METADATA, v as WsException, w as VerifyWebSocketTicketOptions, x as BroadcastOperatorImpl, y as toErrorFrame, z as WebSocketUpgradeGateway } from "<internal:index-websocket-websocket.decorators.d.ts>";
export { type AuthenticatedWebSocketUpgrade, type BroadcastCommand, type BroadcastOperator, BroadcastOperatorImpl, ConnectedSocket, DEFAULT_WS_MAX_FRAME_BYTES, DEFAULT_WS_MAX_JOINED_ROOMS, DEFAULT_WS_MAX_ROOM_ID_BYTES, type ForwardedWebSocketUpgrade, type GatewayBroadcastOperator, type GatewayDelivery, type GatewayEventArgs, type GatewayServer, Gateways, InMemoryRoomRegistry, type IssueWebSocketTicketOptions, MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES, MessageBody, type OnGatewayConnection, type OnGatewayDisconnect, type OnGatewayInit, RESERVED_WS_EVENT_PREFIX, ReservedWsEvent, type ReservedWsEventHandler, type ReservedWsEventMetadata, type RoomRegistry, SubscribeMessage, type SubscribeMessageMetadata, type SyncDriver, type UpgradeAuthenticator, type VerifyWebSocketTicketOptions, WEBSOCKET_TICKET_AUDIENCE, WEBSOCKET_TICKET_MAX_TTL_MS, WEBSOCKET_TICKET_PURPOSE, WS_GATEWAY_METADATA, WS_MODULE_OPTIONS, WS_RESERVED_METADATA, WS_ROOM_REGISTRY, WS_SERVER, WS_SUBSCRIBE_METADATA, WS_SYNC_DRIVER, WS_TRANSPORT, WebSocketGateway, type WebSocketGatewayOptions, WebSocketModule, type WebSocketModuleOptions, type WebSocketPrincipal, WebSocketSendGate, type WebSocketSendPolicy, type WebSocketSendResult, WebSocketServer, type WebSocketTicketClaim, type WebSocketTicketNonceStore, type WebSocketTicketPrincipal, type WebSocketTicketPrincipalType, type WebSocketTransport, type WebSocketUpgradeAuthenticationContext, type WebSocketUpgradeGate, type WebSocketUpgradeGateway, type WebSocketUpgradeIdentity, type WsArgumentsHost, type WsClient, WsDispatcher, type WsEntrypointMeta, WsException, type WsExecutionContext, type WsMessage, WsMessageQueue, WsParamType, type WsResponse, type WsServer, WsServerImpl, assertBroadcastCommandFits, assertWebSocketRoomId, broadcastCommandFits, buildWsExecutionContext, createWebSocketUpgradeGate, isWebSocketOriginAllowed, issueWebSocketTicket, local, normalizeWebSocketUpgradeIdentity, readWebSocketEnvelope, readWsEntrypointMeta, resolveGatewayRoomId, resolveGatewayRoomParam, resolveMaxFrameBytes, resolveWsArgs, toErrorFrame, trySendWebSocketFrame, verifyAndConsumeWebSocketTicket, webSocketFrameFits, webSocketSyncEnvelopeFits };
```

## `./websocket-node`

Type conditions: `types`

Declaration entry: `./dist/websocket-node/index.d.ts`

```ts
import { B as SyncDriver, M as WsClient, z as RoomRegistry } from "<internal:types-http-hono.types.d.ts>";
import { h as VelaApplication } from "<internal:request-context.d.ts>";
import { X as LiveDriver } from "<internal:index-live-live.types.d.ts>";
import "<internal:index-websocket-websocket.decorators.d.ts>";
import { WebSocketSendPolicy, WebSocketSendResult } from "@velajs/live-protocol";
import { UpgradeWebSocket, WSContext } from "hono/ws";

export declare class NodeWsClient<TData extends Record<string, unknown> = Record<string, unknown>> implements WsClient<TData> {
  #private;
  private readonly ws;
  private readonly registry;
  readonly path: string;
  readonly maxFrameBytes: number;
  readonly id: string;
  data: TData;
  constructor(ws: WSContext, registry: RoomRegistry, path: string, maxFrameBytes?: number, sendPolicy?: WebSocketSendPolicy);
  get rooms(): ReadonlySet<string>;
  get raw(): unknown;
  send(event: string, data?: unknown, id?: string): void;
  sendRaw(payload: string): void;
  trySendRaw(payload: string): WebSocketSendResult;
  join(room: string): void | Promise<void>;
  leave(room: string): void | Promise<void>;
  commit(): void;
  close(code?: number, reason?: string): void;
}

export declare function registerWebSocketGateways(app: VelaApplication, upgradeWebSocket: UpgradeWebSocket): void;

interface RedisPubSubClient {
  publish(channel: string, message: string): unknown;
  subscribe(channel: string): unknown;
  on(event: 'message', listener: (channel: string, message: string) => void): unknown;
  off?(event: 'message', listener: (channel: string, message: string) => void): unknown;
  removeListener?(event: 'message', listener: (channel: string, message: string) => void): unknown;
  unsubscribe?(channel: string): unknown;
}
interface RedisSyncOptions {

  pub: RedisPubSubClient;

  sub: RedisPubSubClient;

  prefix?: string;
}

export declare function redis(options: RedisSyncOptions): SyncDriver;

interface RedisLiveOptions {

  pub: RedisPubSubClient;

  sub: RedisPubSubClient;

  prefix?: string;
}

export declare function redisLive(options: RedisLiveOptions): LiveDriver;

export type { RedisLiveOptions, RedisPubSubClient, RedisSyncOptions };
```

## Referenced declaration chunks

### `<internal:binding.d.ts>`

```ts
import { J as Container, q as VelaEnv } from "<internal:types-http-hono.types.d.ts>";

interface BindingRef {
  readonly binding: string;
}

interface BindingKind<T> {

  readonly name: string;

  readonly configKey: string;

  readonly accepts: (value: unknown) => value is T;
}

type EnvFactory<T> = (env: VelaEnv) => T;

interface Binding<T> extends BindingRef {
  (env: VelaEnv): T;
  readonly kind: BindingKind<T>;
}

declare function resolveBinding<T>(env: VelaEnv | undefined, ref: BindingRef, kind: BindingKind<T>): T;

declare function defineBinding<T>(kind: BindingKind<T>): (ref: BindingRef) => Binding<T>;

declare function readEnv(container: Container): VelaEnv;

export { defineBinding as a, EnvFactory as i, BindingKind as n, readEnv as o, BindingRef as r, resolveBinding as s, Binding as t };
```

### `<internal:component.manager.d.ts>`

```ts
import { J as Container, X as Constructor, Y as CheckedProviders, a as ExecutionContext, d as PipeTransform, i as ExceptionFilter, l as NestInterceptor, mt as ProviderDefinition, ot as InjectionToken, pt as Provider, q as VelaEnv, r as CanActivate, t as ArgumentMetadata, u as NestMiddleware, vt as Token, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { B as InterceptorType, F as ComponentTypeMap, L as FilterType, P as ComponentType, R as GuardType, U as ModuleMetadata, V as MiddlewareType, W as ModuleOptions, q as PipeType } from "<internal:types-http-version.d.ts>";

interface ConfigNamespace<TKey extends string = string, TConfig extends object = object> {

  readonly KEY: InjectionToken<TConfig>;

  readonly namespace: TKey;

  readonly factory: (env: VelaEnv) => TConfig;

  asProvider(): ProviderDefinition<TConfig>;
}

interface AnyConfigNamespace {
  readonly KEY: Token;
  readonly namespace: string;
  asProvider(): ProviderDefinition<object>;
}

declare function registerAs<TKey extends string, TConfig extends object>(namespace: TKey, factory: (env: VelaEnv) => TConfig): ConfigNamespace<TKey, TConfig>;

type ConfigType<T extends AnyConfigNamespace> = T extends {
  readonly KEY: InjectionToken<infer C>;
} ? C : never;

type ConfigShape<L extends readonly AnyConfigNamespace[]> = { [N in L[number] as N['namespace']]: ConfigType<N>; };

type ConfigSchema = ((config: Record<string, unknown>) => unknown) | {
  parse: (config: unknown) => unknown;
};

interface ConfigModuleOptions {

  config?: Record<string, unknown>;

  load?: AnyConfigNamespace[];

  validate?: (config: Record<string, unknown>) => Record<string, unknown>;

  validateSchema?: ConfigSchema;
}

type ConfigPath<T, Depth extends number = 8> = [Depth] extends [never] ? never : { [K in keyof T & string]: [ConfigBranch<T[K]>] extends [never] ? K : K | `${K}.${ConfigPath<ConfigBranch<T[K]>, PreviousDepth[Depth]>}`; }[keyof T & string];

type ConfigPathValue<T, P extends string> = P extends keyof T ? T[P] : P extends `${infer K}.${infer Rest}` ? K extends keyof T ? unknown extends T[K] ? unknown : ConfigPathValue<NonNullable<T[K]>, Rest> | (undefined extends T[K] ? undefined : never) : never : never;

type PreviousDepth = [never, 0, 1, 2, 3, 4, 5, 6, 7];

type ConfigBranch<V> = V extends readonly unknown[] | ((...args: never[]) => unknown) ? never : V extends object ? V : never;

declare class ConfigStore {
  private readonly container;
  private readonly config;
  private readonly validateSchema?;
  private readonly namespaceKeys;
  private readonly cache;
  private validated;
  private validationError;
  constructor(container: Pick<Container, 'resolve'>, config?: Record<string, unknown>, namespaces?: ReadonlyArray<AnyConfigNamespace>, validateSchema?: ConfigSchema | undefined);

  addNamespace(namespace: AnyConfigNamespace): void;

  get(path: string): unknown;

  getOrThrow(path: string): unknown;

  has(path: string): boolean;

  all(): Record<string, unknown>;
  private read;

  private resolveNamespace;
  private merged;

  private ensureValidated;
}

declare function Global(): ClassDecorator;

interface ModuleDecoratorOptions<P extends readonly unknown[] = readonly Provider[]> extends Omit<ModuleOptions, 'providers'> {
  providers?: CheckedProviders<P>;
}
declare function Module(options?: ModuleDecoratorOptions<readonly (Type | ProviderDefinition)[]>): ClassDecorator;
declare function Module<const P extends readonly unknown[] = readonly Provider[]>(options?: ModuleDecoratorOptions<P>): ClassDecorator;

declare function isModule(target: Constructor): boolean;
declare function getModuleMetadata(target: Constructor): ModuleMetadata | undefined;

declare class ComponentManager {
  static registerController<T extends ComponentType>(type: T, controller: Constructor, ...components: ComponentTypeMap[T][]): void;
  static registerHandler<T extends ComponentType>(type: T, controller: Constructor, handlerName: string | symbol, ...components: ComponentTypeMap[T][]): void;
  private static resolveAll;
  static resolveMiddleware(items: MiddlewareType[], container: Container): NestMiddleware[];
  static resolveGuards(items: GuardType[], container: Container): CanActivate[];
  static resolvePipes(items: PipeType[], container: Container): PipeTransform[];
  static resolveInterceptors(items: InterceptorType[], container: Container): NestInterceptor[];
  static resolveFilters(items: FilterType[], container: Container): ExceptionFilter[];
  static executePipes(value: unknown, metadata: ArgumentMetadata, pipes: PipeTransform[]): Promise<unknown>;
  static runInterceptorChain(interceptors: NestInterceptor[], context: ExecutionContext, coreHandler: () => Promise<unknown>): Promise<unknown>;
}

export { getModuleMetadata as a, ConfigModuleOptions as c, ConfigSchema as d, AnyConfigNamespace as f, registerAs as g, ConfigType as h, ModuleDecoratorOptions as i, ConfigPath as l, ConfigShape as m, Global as n, isModule as o, ConfigNamespace as p, Module as r, ConfigStore as s, ComponentManager as t, ConfigPathValue as u };
```

### `<internal:index-factory.d.ts>`

```ts
import "<internal:metadata.d.ts>";
import { J as Container, X as Constructor, a as ExecutionContext, at as InjectableOptions, d as PipeTransform, et as FactoryInject, i as ExceptionFilter, l as NestInterceptor, mt as ProviderDefinition, o as HandlerFunction, ot as InjectionToken, pt as Provider, q as VelaEnv, r as CanActivate, rt as InferTokens, t as ArgumentMetadata, tt as ForwardRef, u as NestMiddleware, vt as Token, xt as TypedToken, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { I as VelaApplicationContext, M as ErrorReportContext, N as ExceptionHandler, h as VelaApplication, n as RequestContext, s as RuntimeAdapter, u as BootstrapOptions } from "<internal:request-context.d.ts>";
import { B as InterceptorType, F as ComponentTypeMap, H as ModuleImport, I as DynamicModule, L as FilterType, P as ComponentType, R as GuardType, V as MiddlewareType, q as PipeType } from "<internal:types-http-version.d.ts>";
import { g as StandardSchemaV1, h as StandardJSONSchemaV1, n as SchemaOutput, r as ValidationSchema, t as SchemaInput } from "<internal:parse-schema.d.ts>";
import "<internal:route-contract-http-route-contract.d.ts>";
import { M as applyDecorators, k as RouteOptions } from "<internal:url-generator.service.d.ts>";
import { c as ConfigModuleOptions, f as AnyConfigNamespace, l as ConfigPath, s as ConfigStore, u as ConfigPathValue } from "<internal:component.manager.d.ts>";
import { Context, MiddlewareHandler } from "hono";
import { CORE_CATALOG, Catalog, Catalog as Catalog$1, ErrorBodyResult, ErrorBodyResult as ErrorBodyResult$1, ErrorCatalogEntry, VelaError, VelaErrorOptions, composeCatalogs, defineErrorCatalog, isVelaError, toErrorBody } from "@velajs/errors";

interface VelaCreateOptions extends BootstrapOptions {

  adapters?: RuntimeAdapter[];
}

type VelaApplicationContextOptions = Pick<BootstrapOptions, 'diagnostics' | 'env' | 'configureContainer'>;
declare const VelaFactory: {

  create(rootModule: Type | DynamicModule, options?: VelaCreateOptions): Promise<VelaApplication>;

  createApplicationContext(rootModule: Type | DynamicModule, options?: VelaApplicationContextOptions): Promise<VelaApplicationContext>;
};

declare function Injectable(options?: InjectableOptions): ClassDecorator;
declare function Optional(): ParameterDecorator;
declare function Inject(token: Token | ForwardRef): ParameterDecorator;

declare function mixin<T>(mixinClass: Type<T>): Type<T>;

type GuardPhase = 'authenticate' | 'tenant' | 'authorize' | 'feature';

type SkippableGuardPhase = Extract<GuardPhase, 'tenant' | 'authorize'>;

declare function orderGuardsByPhase<T>(guards: readonly T[], skip?: ReadonlySet<GuardPhase>): T[];

interface MessageEvent {

  data: string | object;
  id?: string;

  type?: string;

  retry?: number;
}

type SseResult = AsyncIterable<MessageEvent> | Iterable<MessageEvent> | Response;

declare function Sse(path?: string, options?: Pick<RouteOptions, 'name'>): <Handler extends (...args: never[]) => SseResult | Promise<SseResult>>(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

declare function createLazyParamDecorator<TData = unknown>(factory: (data: TData, ctx: ExecutionContext) => unknown): (...args: undefined extends TData ? [data?: TData] : [data: TData]) => ParameterDecorator;

interface ReadJsonBodyOptions {

  maxBytes?: number;
}

declare function readJsonBody(c: Context, options?: ReadJsonBodyOptions): Promise<unknown>;

declare class SignedUrlGuard implements CanActivate {
  #private;
  constructor(secretToken?: string, env?: VelaEnv);
  canActivate(context: ExecutionContext): Promise<boolean>;
}

declare function SignedUrl(): ReturnType<typeof applyDecorators>;

declare const URL_SIGNING_SECRET: InjectionToken<string>;

declare function enableAmbientContainer(): MiddlewareHandler;

declare function getCurrentContainer(): Container;

declare function getCurrentRequestContext(): RequestContext;

declare const LogLevel: {
  readonly VERBOSE: 0;
  readonly DEBUG: 1;
  readonly LOG: 2;
  readonly WARN: 3;
  readonly ERROR: 4;
  readonly SILENT: 5;
};
type LogLevel = (typeof LogLevel)[keyof typeof LogLevel];
interface LoggerService {
  log(message: unknown, ...optionalParams: unknown[]): void;
  error(message: unknown, ...optionalParams: unknown[]): void;
  warn(message: unknown, ...optionalParams: unknown[]): void;
  debug?(message: unknown, ...optionalParams: unknown[]): void;
  verbose?(message: unknown, ...optionalParams: unknown[]): void;
}
type ContextProvider = () => Record<string, unknown>;
type LoggerLevelName = 'LOG' | 'ERROR' | 'WARN' | 'DEBUG' | 'VERBOSE';
type Writer = (level: LoggerLevelName, line: string, ...rest: unknown[]) => void;
declare class Logger implements LoggerService {
  static level: LogLevel;
  private static globalLogger;
  private static globalContextProviders;
  private static globalWriter;
  private context?;
  private instanceContextProviders;
  private instanceWriter?;
  static setLogLevel(level: LogLevel): void;
  static overrideLogger(logger: LoggerService | false): void;

  static addContextProvider(provider: ContextProvider): void;

  static clearContextProviders(): void;

  static setWriter(writer: Writer): void;

  static resetWriter(): void;
  constructor(context?: string);
  setContext(context: string): void;

  addContextProvider(provider: ContextProvider): this;

  setWriter(writer: Writer): this;

  extend(namespace: string): Logger;
  log(message: unknown, ...optionalParams: unknown[]): void;
  error(message: unknown, ...optionalParams: unknown[]): void;
  warn(message: unknown, ...optionalParams: unknown[]): void;
  debug(message: unknown, ...optionalParams: unknown[]): void;
  verbose(message: unknown, ...optionalParams: unknown[]): void;
  private emit;
  private formatMessage;
  private collectContext;
  private formatContextObj;
}

type ConfigurableModuleExtras = Record<string, unknown>;

interface ModuleRegistrationOptions {

  key?: string;

  lazy?: boolean;
}

type ModuleFactoryOptions<Opts, S extends keyof Opts = never> = [S] extends [never] ? Opts : Opts extends unknown ? Omit<Opts, S> & Partial<Record<S, never>> : never;

type ConfigurableModuleExtrasTransform<E extends ConfigurableModuleExtras> = (definition: DynamicModule, extras: E) => DynamicModule;

type ConfigurableModuleOptionsFactory<Opts, MethodName extends string> = { [K in MethodName]: () => Opts | Promise<Opts>; };

type ConfigurableModuleAsyncOptions<Opts, S extends keyof Opts = never, MethodName extends string = 'create', Inject extends readonly Token[] = readonly Token[]> = {
  imports?: ModuleImport[];
} & ModuleRegistrationOptions & Pick<Opts, S> & ConfigurableModuleAsyncFactory<ModuleFactoryOptions<Opts, S>, MethodName, Inject>;

type ConfigurableModuleAsyncFactory<Opts, MethodName extends string, Inject extends readonly Token[] = readonly Token[]> = ({
  useFactory: (...args: InferTokens<Inject>) => Opts | Promise<Opts>;
  useClass?: never;
  useExisting?: never;
} & FactoryInject<Inject>) | {
  useClass: Type<ConfigurableModuleOptionsFactory<Opts, MethodName>>;
  useFactory?: never;
  useExisting?: never;
  inject?: never;
} | {
  useExisting: InjectionToken<ConfigurableModuleOptionsFactory<Opts, MethodName>> | Type<ConfigurableModuleOptionsFactory<Opts, MethodName>>;
  useFactory?: never;
  useClass?: never;
  inject?: never;
};
interface ConfigurableModuleBuilderOptions<Opts = unknown> {

  moduleName?: string;

  optionsInjectionToken?: InjectionToken<Opts>;
}

type SyncOptionsArgs<Opts, Extras extends ConfigurableModuleExtras> = {} extends Opts ? [options?: Opts & Partial<Extras> & ModuleRegistrationOptions] : [options: Opts & Partial<Extras> & ModuleRegistrationOptions];

type ConfigurableModuleClassType<Opts, MethodKey extends string, FactoryMethodKey extends string, Extras extends ConfigurableModuleExtras, S extends keyof Opts = never> = (new () => object) & Record<MethodKey, (...options: SyncOptionsArgs<Opts, Extras>) => DynamicModule> & Record<`${MethodKey}Async`, <const Inject extends readonly Token[]>(options: ConfigurableModuleAsyncOptions<Opts, S, FactoryMethodKey, Inject> & Partial<Extras>) => DynamicModule>;
interface ConfigurableModuleHost<Opts, MethodKey extends string = 'forRoot', FactoryMethodKey extends string = 'create', Extras extends ConfigurableModuleExtras = {
  isGlobal?: boolean;
}, S extends keyof Opts = never> {

  ConfigurableModuleClass: ConfigurableModuleClassType<Opts, MethodKey, FactoryMethodKey, Extras, S>;

  MODULE_OPTIONS_TOKEN: InjectionToken<Opts>;

  OPTIONS_TYPE: Opts & Partial<Extras> & ModuleRegistrationOptions;

  ASYNC_OPTIONS_TYPE: ConfigurableModuleAsyncOptions<Opts, S, FactoryMethodKey> & Partial<Extras>;
}

declare const ConfigurableModuleClass$1: ConfigurableModuleClassType<ConfigModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, "load">;
declare class ConfigModule extends ConfigurableModuleClass$1 {

  static forRoot(options?: ConfigModuleOptions & {
    isGlobal?: boolean;
  } & ModuleRegistrationOptions): DynamicModule;

  static forFeature(namespace: AnyConfigNamespace): DynamicModule;
}

type ServicePath<T> = [keyof T] extends [never] ? string : ConfigPath<T>;
type ServiceValue<T, P extends string> = [keyof T] extends [never] ? unknown : ConfigPathValue<T, P>;

type PresentValue<T, P extends string> = Exclude<ServiceValue<T, P>, undefined>;

declare class ConfigService<T extends object = Record<string, unknown>> {
  private readonly store;
  constructor(store: ConfigStore);
  get<P extends ServicePath<T>>(path: P): ServiceValue<T, P> | undefined;
  get<P extends ServicePath<T>>(path: P, defaultValue: NoInfer<PresentValue<T, P>>): PresentValue<T, P>;
  getOrThrow<P extends ServicePath<T>>(path: P): PresentValue<T, P>;

  parse<R>(path: string, schema: {
    parse(value: unknown): R;
  }): R;
  has(path: string): boolean;
  getAll(): T;
}

declare const ROOT_MODULE: InjectionToken<DynamicModule | Type>;

type ModuleEntryList = 'imports' | 'providers' | 'controllers' | 'exports';

declare class UndefinedModuleError extends Error {
  readonly moduleName: string;
  readonly property: ModuleEntryList;
  readonly index: number;
  constructor(moduleName: string, property: ModuleEntryList, index: number, value: null | undefined);
}

interface GlobalComponentSlot {
  guards?: GuardType[];
  pipes?: PipeType[];
  interceptors?: InterceptorType[];
  filters?: FilterType[];
  middleware?: MiddlewareType[];
}

interface ModuleContributions {

  providers?: Provider[];
  controllers?: Type[];
  imports?: ModuleImport[];
  exports?: Token[];

  global?: GlobalComponentSlot;
}
interface ModuleSetupContext<Opts, S extends keyof Opts = never> {

  readonly OPTIONS: InjectionToken<Opts>;

  readonly options: Pick<Opts, S>;

  readonly key: string;
}
interface DefineModuleSpec<Opts, S extends keyof Opts = never, Extras extends ConfigurableModuleExtras = {
  isGlobal?: boolean;
}, MethodKey extends string = 'forRoot', FactoryMethodKey extends string = 'create'> {

  name: string;

  optionsToken?: InjectionToken<Opts>;

  structural?: readonly S[];

  defaults?: Partial<Pick<Opts, S>>;

  setup?: (ctx: ModuleSetupContext<Opts, S>) => ModuleContributions;

  key?: (options: Pick<Opts, S>) => string;

  extras?: Extras;

  transform?: ConfigurableModuleExtrasTransform<Extras>;

  methodName?: MethodKey;

  factoryMethodName?: FactoryMethodKey;

  lazy?: boolean;
}

declare function defineModule<Opts, S extends keyof Opts = never, Extras extends ConfigurableModuleExtras = {
  isGlobal?: boolean;
}, MethodKey extends string = 'forRoot', FactoryMethodKey extends string = 'create'>(spec: DefineModuleSpec<Opts, S, Extras, MethodKey, FactoryMethodKey>): ConfigurableModuleHost<Opts, MethodKey, FactoryMethodKey, Extras, S>;

declare class ConfiguredModuleBuilder<Opts, MethodKey extends string, FactoryMethodKey extends string, Extras extends ConfigurableModuleExtras> {
  private readonly spec;
  constructor(spec: DefineModuleSpec<Opts, never, Extras, MethodKey, FactoryMethodKey>);
  setExtras<NewExtras extends ConfigurableModuleExtras>(defaults: NewExtras, transform: ConfigurableModuleExtrasTransform<NewExtras>): ConfiguredModuleBuilder<Opts, MethodKey, FactoryMethodKey, NewExtras>;
  setClassMethodName<NewMethodKey extends string>(name: NewMethodKey): ConfiguredModuleBuilder<Opts, NewMethodKey, FactoryMethodKey, Extras>;
  setFactoryMethodName<NewFactoryMethodKey extends string>(name: NewFactoryMethodKey): ConfiguredModuleBuilder<Opts, MethodKey, NewFactoryMethodKey, Extras>;
  build(): ConfigurableModuleHost<Opts, MethodKey, FactoryMethodKey, Extras>;
}

declare class ConfigurableModuleBuilder<Opts> extends ConfiguredModuleBuilder<Opts, 'register', 'create', {
  isGlobal?: boolean;
}> {
  constructor(options?: ConfigurableModuleBuilderOptions<Opts>);
}

type LazyProviderSpec<T, Inject extends readonly Token[]> = {

  provide: InjectionToken<() => T>;
  useFactory: (...deps: InferTokens<Inject>) => NoInfer<T>;

  memoize?: boolean;
} & FactoryInject<Inject>;

declare function lazyProvider<T, const Inject extends readonly Token[] = readonly Token[]>(spec: LazyProviderSpec<T, Inject>): ProviderDefinition;

declare function sideEffectModule(owner: string | Type, contributions?: Omit<ModuleContributions, 'global'> & {
  key?: string;
}): DynamicModule;

interface ExecutionLifetime {

  readonly id: string;

  readonly startedAt: number;

  readonly signal: AbortSignal | undefined;
  readonly active: boolean;

  defer(work: () => unknown | Promise<unknown>): void;

  waitUntil(work: Promise<unknown>): void;
}
declare const EXECUTION_LIFETIME: InjectionToken<ExecutionLifetime>;
interface ExecutionScopeOptions {
  readonly signal?: AbortSignal;
}
interface ExecutionScope {
  readonly container: Container;
  readonly lifetime: ExecutionLifetime;

  finish(waitFor?: Promise<unknown>): Promise<void>;
}

declare function createExecutionScope(container: Container, options?: ExecutionScopeOptions): ExecutionScope;

declare function getExecutionLifetime(container: Container): ExecutionLifetime | undefined;

declare function finishExecutionScope(container: Container, waitFor?: Promise<unknown>): Promise<void>;

declare function runInEntrypointScope<T>(container: Container, fn: (scope: Container, lifetime: ExecutionLifetime) => T | Promise<T>, options?: ExecutionScopeOptions): Promise<T>;

interface PipelineRunOptions {
  context: ExecutionContext;

  guards: CanActivate[];
  interceptors: NestInterceptor[];

  resolveArgs: () => Promise<unknown[]>;

  invoke: (args: unknown[]) => Promise<unknown>;

  onGuardReject?: () => Error;
}

declare class PipelineRunner {
  static run(options: PipelineRunOptions): Promise<unknown>;

  static chainInterceptors(interceptors: NestInterceptor[], context: ExecutionContext, coreHandler: () => Promise<unknown>): Promise<unknown>;
}

interface ResolvedComponentMap {
  guard: CanActivate;
  pipe: PipeTransform;
  interceptor: NestInterceptor;
  filter: ExceptionFilter;
  middleware: NestMiddleware;
}

type PipelineComponentEntry<T extends ComponentType> = ResolvedComponentMap[T] | TypedToken<ResolvedComponentMap[T]>;

declare function resolvePipelineComponents<T extends ComponentType>(_type: T, entries: readonly PipelineComponentEntry<NoInfer<T>>[], container: Container, moduleId?: string): Promise<ResolvedComponentMap[T][]>;

declare function getScopedComponents<T extends ComponentType>(type: T, targetClass: Constructor, methodName: string | symbol, container: Container, moduleId?: string): ComponentTypeMap[T][];

declare function resolveScopedComponents<T extends ComponentType>(type: T, targetClass: Constructor, methodName: string | symbol, container: Container, moduleId?: string): ResolvedComponentMap[T][];

declare function resolveScopedComponentsAsync<T extends ComponentType>(type: T, targetClass: Constructor, methodName: string | symbol, container: Container, moduleId?: string): Promise<ResolvedComponentMap[T][]>;

declare function UseMiddleware(...middleware: ComponentTypeMap['middleware'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UseGuards(...guards: ComponentTypeMap['guard'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UsePipes(...pipes: ComponentTypeMap['pipe'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UseInterceptors(...interceptors: ComponentTypeMap['interceptor'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UseFilters(...filters: ComponentTypeMap['filter'][]): (target: object, propertyKey?: string | symbol) => void;
declare function Catch(...exceptions: Type<Error>[]): ClassDecorator;
declare function getCatchTypes(filter: unknown): Type<Error>[];
declare function shouldFilterCatch(filter: unknown, exception: unknown): boolean;

interface CreateDecoratorOptions<TParam = unknown, TTransformed = TParam> {

  key?: string;

  transform?: (value: TParam) => TTransformed;
}
interface ReflectableDecorator<TParam, TTransformed = TParam> {
  (value: TParam): ClassDecorator & MethodDecorator;
  readonly KEY: string;

  readonly '~value'?: TTransformed;
}

type ReflectorTarget = Type | HandlerFunction;

type ReflectorContext = Pick<ExecutionContext, 'getClass' | 'getHandlerName'>;
type MetadataKey<T> = string | ReflectableDecorator<never, T>;

declare function SetMetadata<V = unknown>(key: string, value: V): (target: object, propertyKey?: string | symbol, descriptor?: PropertyDescriptor) => void;

declare class Reflector {

  static createDecorator<TParam = unknown, TTransformed = TParam>(options?: CreateDecoratorOptions<TParam, TTransformed>): ReflectableDecorator<TParam, TTransformed>;

  get<T = unknown>(key: MetadataKey<T>, target: ReflectorContext | ReflectorTarget): T | undefined;

  getHandler<T = unknown>(key: MetadataKey<T>, context: ReflectorContext): T | undefined;

  getClass<T = unknown>(key: MetadataKey<T>, context: Pick<ExecutionContext, 'getClass'>): T | undefined;

  getAll<T = unknown>(key: MetadataKey<T>, targets: ReflectorContext | readonly ReflectorTarget[]): Array<T | undefined>;

  getAllAndOverride<T = unknown>(key: MetadataKey<T>, targets: ReflectorContext | readonly ReflectorTarget[]): T | undefined;

  getAllAndMerge<T = unknown>(key: MetadataKey<T>, targets: ReflectorContext | readonly ReflectorTarget[]): T | T[];
}

declare const APP_GUARD: InjectionToken<CanActivate>;

declare const APP_PIPE: InjectionToken<PipeTransform<unknown, unknown>>;

declare const APP_INTERCEPTOR: InjectionToken<NestInterceptor>;

declare const APP_FILTER: InjectionToken<ExceptionFilter<unknown>>;

declare const APP_MIDDLEWARE: InjectionToken<NestMiddleware>;

declare const APP_EXCEPTION_HANDLER: InjectionToken<ExceptionHandler>;

declare const ERROR_CATALOG: InjectionToken<Catalog<string>>;

declare class ParseIntPipe implements PipeTransform<string, number> {
  transform(value: string, metadata: ArgumentMetadata): number;
}
declare class ParseFloatPipe implements PipeTransform<string, number> {
  transform(value: string, metadata: ArgumentMetadata): number;
}
declare class ParseBoolPipe implements PipeTransform<string, boolean> {
  transform(value: string, metadata: ArgumentMetadata): boolean;
}
declare class DefaultValuePipe<T = unknown> implements PipeTransform<T | undefined, T> {
  private readonly defaultValue;
  constructor(defaultValue: T);
  transform(value: T | undefined, _metadata: ArgumentMetadata): T;
}
declare class RequiredPipe implements PipeTransform {
  transform(value: unknown, metadata: ArgumentMetadata): unknown;
}
interface ParseUUIDPipeOptions {
  version?: '3' | '4' | '5';
}
declare class ParseUUIDPipe implements PipeTransform<string, string> {
  private readonly options?;
  constructor(options?: ParseUUIDPipeOptions | undefined);
  transform(value: string, metadata: ArgumentMetadata): string;
}
declare class ParseEnumPipe<T extends Record<string, string | number>> implements PipeTransform<string, T[keyof T]> {
  private readonly allowedValues;
  private readonly valuesLabel;
  constructor(enumType: T);
  transform(value: string, metadata: ArgumentMetadata): T[keyof T];
}
interface ParseArrayPipeOptions {
  separator?: string;
  optional?: boolean;
}
declare class ParseArrayPipe implements PipeTransform {
  private readonly options?;
  constructor(options?: ParseArrayPipeOptions | undefined);
  transform(value: unknown, metadata: ArgumentMetadata): unknown[];
}

type ExceptionResponse = string | Record<string, unknown>;

interface HttpExceptionOptions {

  details?: unknown;

  cause?: unknown;
}

interface HttpErrorResponse {
  readonly status: number;
  readonly body: unknown;
}

declare class HttpException extends Error {
  #private;
  readonly statusCode: number;
  constructor(response: ExceptionResponse, statusCode: number, options?: HttpExceptionOptions);
  getStatus(): number;
  getResponse(): Record<string, unknown>;

  getDetails(): unknown;

  toResponse(): HttpErrorResponse | undefined;
}
declare class BadRequestException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class UnauthorizedException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class ForbiddenException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class NotFoundException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class MethodNotAllowedException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class NotAcceptableException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class RequestTimeoutException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class ConflictException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class GoneException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class PayloadTooLargeException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class UnsupportedMediaTypeException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class UnprocessableEntityException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class TooManyRequestsException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class InternalServerErrorException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class NotImplementedException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class BadGatewayException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class ServiceUnavailableException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}
declare class GatewayTimeoutException extends HttpException {
  constructor(message?: ExceptionResponse, options?: HttpExceptionOptions);
}

interface ErrorsModuleOptions {

  catalogs?: Catalog<string>[];

  handler?: Type<ExceptionHandler> | ExceptionHandler;
}
declare const ConfigurableModuleClass: ConfigurableModuleClassType<ErrorsModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, "catalogs" | "handler">;

declare class ErrorsModule extends ConfigurableModuleClass {}

interface ErrorReporter {
  catalog: Catalog<string>;
  report(error: unknown, ctx: ErrorReportContext): void;
  render(error: unknown, executionCtx: unknown): Response | ErrorBodyResult | undefined;
}

declare const resolveErrorReporter: (container: Container) => ErrorReporter;

interface RenderedHttpError {
  readonly status: number;
  readonly body: unknown;

  readonly redacted: boolean;
}
interface RenderHttpErrorOptions {

  catalog?: Catalog<string>;

  redactServerBodies?: boolean;
}

declare function getErrorStatus(error: unknown): number;

declare function renderHttpError(error: unknown, options?: RenderHttpErrorOptions): RenderedHttpError;

interface OnModuleInit {
  onModuleInit(): void | Promise<void>;
}
interface OnApplicationBootstrap {
  onApplicationBootstrap(): void | Promise<void>;
}
interface OnModuleDestroy {
  onModuleDestroy(): void | Promise<void>;
}
interface OnApplicationShutdown {
  onApplicationShutdown(signal?: string): void | Promise<void>;
}
interface BeforeApplicationShutdown {
  beforeApplicationShutdown(signal?: string): void | Promise<void>;
}

interface SerializerDefinition<Input, Wire, Output extends ValidationSchema> extends StandardSchemaV1<Input, Wire> {
  readonly '~standard': StandardSchemaV1.Props<Input, Wire> & StandardJSONSchemaV1.Props<Input, Wire>;

  readonly output: Output;
  parse(value: unknown): Promise<Wire>;
  serialize(value: Input): Promise<Wire>;
}

declare function defineSerializer<Input extends ValidationSchema, Output extends ValidationSchema>(options: {
  input: Input;
  output: Output;
  project: (value: NoInfer<SchemaOutput<Input>>) => NoInfer<SchemaInput<Output>> | PromiseLike<NoInfer<SchemaInput<Output>>>;
}): SerializerDefinition<SchemaInput<Input>, SchemaOutput<Output>, Output>;

export { ParseFloatPipe as $, ConfigService as $t, ForbiddenException as A, orderGuardsByPhase as An, resolveScopedComponentsAsync as At, NotImplementedException as B, runInEntrypointScope as Bt, resolveErrorReporter as C, readJsonBody as Cn, getCatchTypes as Ct, BadRequestException as D, SseResult as Dn, getScopedComponents as Dt, BadGatewayException as E, Sse as En, ResolvedComponentMap as Et, HttpExceptionOptions as F, VelaApplicationContextOptions as Fn, ExecutionScope as Ft, UnauthorizedException as G, DefineModuleSpec as Gt, RequestTimeoutException as H, lazyProvider as Ht, InternalServerErrorException as I, VelaCreateOptions as In, ExecutionScopeOptions as It, DefaultValuePipe as J, ModuleSetupContext as Jt, UnprocessableEntityException as K, GlobalComponentSlot as Kt, MethodNotAllowedException as L, VelaFactory as Ln, createExecutionScope as Lt, GoneException as M, Inject as Mn, PipelineRunner as Mt, HttpErrorResponse as N, Injectable as Nn, EXECUTION_LIFETIME as Nt, ConflictException as O, GuardPhase as On, resolvePipelineComponents as Ot, HttpException as P, Optional as Pn, ExecutionLifetime as Pt, ParseEnumPipe as Q, ROOT_MODULE as Qt, NotAcceptableException as R, finishExecutionScope as Rt, ErrorReporter as S, ReadJsonBodyOptions as Sn, UsePipes as St, ErrorsModuleOptions as T, MessageEvent as Tn, PipelineComponentEntry as Tt, ServiceUnavailableException as U, sideEffectModule as Ut, PayloadTooLargeException as V, LazyProviderSpec as Vt, TooManyRequestsException as W, ConfigurableModuleBuilder as Wt, ParseArrayPipeOptions as X, ModuleEntryList as Xt, ParseArrayPipe as Y, defineModule as Yt, ParseBoolPipe as Z, UndefinedModuleError as Zt, OnModuleInit as _, getCurrentContainer as _n, Catch as _t, VelaError as a, ConfigurableModuleExtrasTransform as an, APP_FILTER as at, getErrorStatus as b, SignedUrl as bn, UseInterceptors as bt, defineErrorCatalog as c, ModuleFactoryOptions as cn, APP_MIDDLEWARE as ct, SerializerDefinition as d, LogLevel as dn, CreateDecoratorOptions as dt, ConfigModule as en, ParseIntPipe as et, defineSerializer as f, Logger as fn, ReflectableDecorator as ft, OnModuleDestroy as g, enableAmbientContainer as gn, SetMetadata as gt, OnApplicationShutdown as h, Writer as hn, ReflectorTarget as ht, ErrorCatalogEntry as i, ConfigurableModuleExtras as in, APP_EXCEPTION_HANDLER as it, GatewayTimeoutException as j, mixin as jn, PipelineRunOptions as jt, ExceptionResponse as k, SkippableGuardPhase as kn, resolveScopedComponents as kt, isVelaError as l, ModuleRegistrationOptions as ln, APP_PIPE as lt, OnApplicationBootstrap as m, LoggerService as mn, ReflectorContext as mt, Catalog$1 as n, ConfigurableModuleBuilderOptions as nn, ParseUUIDPipeOptions as nt, VelaErrorOptions as o, ConfigurableModuleHost as on, APP_GUARD as ot, BeforeApplicationShutdown as p, LoggerLevelName as pn, Reflector as pt, UnsupportedMediaTypeException as q, ModuleContributions as qt, ErrorBodyResult$1 as r, ConfigurableModuleClassType as rn, RequiredPipe as rt, composeCatalogs as s, ConfigurableModuleOptionsFactory as sn, APP_INTERCEPTOR as st, CORE_CATALOG as t, ConfigurableModuleAsyncOptions as tn, ParseUUIDPipe as tt, toErrorBody as u, ContextProvider as un, ERROR_CATALOG as ut, RenderHttpErrorOptions as v, getCurrentRequestContext as vn, UseFilters as vt, ErrorsModule as w, createLazyParamDecorator as wn, shouldFilterCatch as wt, renderHttpError as x, SignedUrlGuard as xn, UseMiddleware as xt, RenderedHttpError as y, URL_SIGNING_SECRET as yn, UseGuards as yt, NotFoundException as z, getExecutionLifetime as zt };
```

### `<internal:index-live-live.types.d.ts>`

```ts
import "<internal:metadata.d.ts>";
import { C as ReservedWsEventHandler, J as Container, M as WsClient, N as WsExecutionContext, P as WsMessage, a as ExecutionContext, ot as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { B as ContributesEntrypoints, V as Entrypoint, Y as DiscoveryService } from "<internal:request-context.d.ts>";
import { I as DynamicModule } from "<internal:types-http-version.d.ts>";
import { m as OnApplicationBootstrap, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { COMMIT_CURSOR_HEADER, COMMIT_EPOCH_HEADER, ClientLiveFrame, DEFAULT_KEY_FIELD, LIVE_ERROR_CODES, LIVE_EVENT, LIVE_PROTOCOL, LiveFrame, LiveQueryDefinition, LiveQueryDefinition as LiveQueryDefinition$1, RowOp, ServerLiveFrame, ServerLiveFrame as ServerLiveFrame$1, applyListDelta, defineLiveQuery, encodeListDelta, encodeLiveEnvelope, encodeLiveFrame, isClientLiveFrame, isServerLiveFrame, readLiveEnvelope } from "@velajs/live-protocol";

interface LiveIdentity {
  [key: string]: unknown;
  expiresAtMs?: number;
}

interface LiveQueryContext {
  identity?: LiveIdentity;

  clientId: string;

  rooms: string[];

  path: string;
}
interface LiveQueryOptions<A = unknown> {

  tags: string[] | ((args: A, context: LiveQueryContext) => string[]);

  key?: string;

  coalesceBy?: (args: A, context: LiveQueryContext) => string | undefined;
}

interface LiveQueryMetadata {

  name: string;
  methodName: string | symbol;
  definition: LiveQueryDefinition;
  key?: string;
  prepare(input: unknown): PreparedLiveQuery;
}

interface PreparedLiveQuery {
  readonly input: unknown;
  readonly args: unknown;
  tags(context: LiveQueryContext): string[];
  coalesceBy?: (context: LiveQueryContext) => string | undefined;
  invoke(instance: unknown, context: LiveQueryContext): unknown | Promise<unknown>;
}

interface LiveResolverMetadata {}

interface CommitStamp {
  cursor: number;
  epoch: string;
}

type ResumeVerdict = 'resume' | 'rerun' | 'snapshot';

interface CursorLog {
  append(tags: string[]): CommitStamp | Promise<CommitStamp>;
  current(): CommitStamp | Promise<CommitStamp>;
  evaluateResume(sinceCursor: number, sinceEpoch: string, subscriptionTags: string[]): ResumeVerdict | Promise<ResumeVerdict>;
}

interface InvalidationCommand {
  room?: string;
  tags: string[];

  origin?: string;
}

interface LiveInvalidationSink {
  applyInvalidation(cmd: InvalidationCommand): CommitStamp | Promise<CommitStamp>;
}

interface LiveDriver {
  readonly kind: string;
  bind(sink: LiveInvalidationSink): void;
  dispatch(cmd: InvalidationCommand): CommitStamp | undefined | Promise<CommitStamp | undefined>;
  start?(): void | Promise<void>;
  stop?(): void | Promise<void>;
}

interface LiveInspection {
  subscriptions: Array<{
    id: string;
    query: string;
    room: string;
    clientId: string;
    tags: string[];

    connectedAt: number;
  }>;
  rooms: Array<{
    room: string;
    count: number;
    members: string[];
  }>;
}

interface LivePlatform {

  liveDriver(): LiveDriver;

  cursorLog?(): CursorLog | undefined;

  bindDriver?(driver: LiveDriver): void;

  inspect?(room: string): Promise<LiveInspection>;
}

interface SubscriptionRecord {
  sub: string;
  query: string;
  args: unknown;

  tags: string[];

  key?: string;
  identity?: LiveIdentity;

  lastJson?: string;
  lastCursor?: number;
}
interface LivePresenceOptions {

  ttlMs?: number;
}
interface LiveModuleOptions {

  driver?: () => LiveDriver | Promise<LiveDriver>;

  log?: () => CursorLog | Promise<CursorLog>;

  identity?: (client: WsClient) => LiveIdentity | undefined;

  authorizeDelivery?: (context: LiveDeliveryAuthorizationContext) => boolean | Promise<boolean>;

  maxSubscriptionsPerSocket?: number;

  maxRefreshFanout?: number;

  maxTags?: number;

  presence?: LivePresenceOptions | false;
}
interface LiveDeliveryAuthorizationContext {
  identity?: LiveIdentity;
  query: string;
  args: unknown;
  client: WsClient;
}

interface LiveEntrypointMeta {

  engine: unknown;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<LiveModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, "presence">;
type LiveModuleRegistration = Parameters<(typeof ConfigurableModuleClass)['forRoot']>[0];
declare class LiveModule extends ConfigurableModuleClass {

  static forRoot(options?: LiveModuleRegistration): DynamicModule;
}

declare function LiveResolver(): ClassDecorator;

declare function LiveQuery<Name extends string, Args, Result>(definition: LiveQueryDefinition<Name, Args, Result>, options: LiveQueryOptions<NoInfer<Args>>): <Handler extends (args: Args, context: LiveQueryContext) => Result | Promise<Result>>(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

declare function getLiveQueries(resolverClass: object): LiveQueryMetadata[];

declare const presenceTag: (gatewayPath: string, room: string) => string;

declare const PRESENCE_ROSTER_QUERY = "$presence.roster";

interface PresenceMember {

  id: string;
  meta?: unknown;
  lastSeen: number;
}

declare class PresenceService {
  private readonly ttlMs;
  readonly enabled: boolean;
  private readonly rooms;
  private readonly roomsByClient;
  private invalidator?;
  constructor(ttlMs?: number, enabled?: boolean);

  bindInvalidator(invalidator: (tags: string[]) => void): void;
  beat(gatewayPath: string, room: string, clientId: string, meta?: unknown): void;

  reap(clientId: string): void;

  roster(gatewayPath: string, room: string): PresenceMember[];

  inspectRooms(): Array<{
    room: string;
    count: number;
    members: string[];
  }>;
  private alive;
}

declare class PresenceResolver {
  private readonly presence;
  constructor(presence: PresenceService);
  roster(args: {
    room: string;
  }, context: LiveQueryContext): PresenceMember[];
}

declare const LIVE_SUBS_DATA_KEY = "__velaLiveSubs";

declare function readPersistedLiveSubscriptions(client: WsClient): SubscriptionRecord[];

declare class LiveEngine implements OnApplicationBootstrap, ContributesEntrypoints, ReservedWsEventHandler, LiveInvalidationSink {
  private readonly container;
  private readonly discovery;
  private readonly log;
  private readonly options;
  private readonly presence?;
  private readonly queries;
  private readonly preparedQueries;
  private readonly connections;
  private readonly pendingTags;
  private drainChain;
  private draining;
  private readonly maxSubscriptionsPerSocket;
  private readonly maxRefreshFanout;
  private readonly maxTags;
  constructor(container: Container, discovery: DiscoveryService, log: CursorLog, driver: LiveDriver, options: LiveModuleOptions, presence?: PresenceService | undefined);

  private dispatchPresenceInvalidation;
  onApplicationBootstrap(): Promise<void>;

  collectEntrypoints(): Entrypoint<LiveEntrypointMeta>[];

  inspect(): LiveInspection;
  handleReservedEvent(path: string, client: WsClient, message: WsMessage, context?: WsExecutionContext): Promise<void>;
  handleSocketClose(_path: string, client: WsClient): Promise<void>;
  applyInvalidation(cmd: InvalidationCommand): Promise<CommitStamp>;

  whenIdle(): Promise<void>;

  restoreSubscription(path: string, client: WsClient, record: SubscriptionRecord): void;
  private onSubscribe;
  private runSubscribeGuards;
  private scheduleDrain;
  private drainLoop;

  private static liveFrameCode;
  private push;
  private liveQueryContext;
  private resolveQueryExecution;
  private executeQuery;
  private runQuery;

  private persistSubscriptions;
  private preparedQuery;
  private ensureConnection;
  private authorizeRecord;
  private revokeConnection;
  private assertTags;
  private isRestorableRecord;
  private boundedOption;
  private isUnsupportedSubscribeFrame;
  private sendFrame;
}

declare function localLive(): LiveDriver;

declare class LiveInvalidation {
  private readonly driver;
  constructor(driver: LiveDriver);
  invalidate(cmd: InvalidationCommand): Promise<CommitStamp | undefined>;
}

declare function stampCommitHeaders(c: {
  header: (name: string, value: string) => unknown;
}, stamp: CommitStamp): void;

type LiveInvalidatesTags<Result> = readonly string[] | ((result: Result, context: ExecutionContext) => readonly string[]);
interface LiveInvalidatesOptions<Result = unknown> {

  room?: string | ((result: Result, context: ExecutionContext) => string | undefined);
}

declare function LiveInvalidates<Result = unknown>(tags: LiveInvalidatesTags<Result>, options?: LiveInvalidatesOptions<Result>): <Handler extends (...args: never[]) => Result | Promise<Result>>(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

declare class LiveInspector {
  private readonly engine;
  private readonly platform?;
  constructor(engine: LiveEngine, platform?: LivePlatform | undefined);
  inspect(rooms: readonly string[]): Promise<LiveInspection>;
}

declare class InMemoryCursorLog implements CursorLog {
  private readonly maxEntries;
  private readonly epoch;
  private readonly entries;
  private seq;
  constructor(maxEntries?: number);
  append(tags: string[]): CommitStamp;
  current(): CommitStamp;
  evaluateResume(sinceCursor: number, sinceEpoch: string, subscriptionTags: string[]): ResumeVerdict;
}

declare function encodeSubscriptionUpdate(record: SubscriptionRecord, json: string, result: unknown, stamp: CommitStamp, initial: boolean): ServerLiveFrame;

declare const LIVE_RESOLVER_METADATA = "vela:live:resolver";

declare const LIVE_DRIVER: InjectionToken<LiveDriver>;

declare const LIVE_CURSOR_LOG: InjectionToken<CursorLog>;

declare const LIVE_PLATFORM: InjectionToken<LivePlatform>;
declare const LIVE_MODULE_OPTIONS: InjectionToken<LiveModuleOptions>;

export { LiveInspection as $, LiveInvalidatesTags as A, PresenceService as B, LIVE_PLATFORM as C, LiveInspector as D, InMemoryCursorLog as E, LiveEngine as F, LiveModule as G, LiveQuery as H, readPersistedLiveSubscriptions as I, InvalidationCommand as J, CommitStamp as K, PRESENCE_ROSTER_QUERY as L, localLive as M, stampCommitHeaders as N, LiveInvalidates as O, LIVE_SUBS_DATA_KEY as P, LiveIdentity as Q, PresenceMember as R, LIVE_MODULE_OPTIONS as S, encodeSubscriptionUpdate as T, LiveResolver as U, presenceTag as V, getLiveQueries as W, LiveDriver as X, LiveDeliveryAuthorizationContext as Y, LiveEntrypointMeta as Z, isClientLiveFrame as _, LIVE_ERROR_CODES as a, LiveQueryMetadata as at, LIVE_CURSOR_LOG as b, LiveFrame as c, PreparedLiveQuery as ct, ServerLiveFrame$1 as d, LiveInvalidationSink as et, applyListDelta as f, encodeLiveFrame as g, encodeLiveEnvelope as h, DEFAULT_KEY_FIELD as i, LiveQueryContext as it, LiveInvalidation as j, LiveInvalidatesOptions as k, LiveQueryDefinition$1 as l, ResumeVerdict as lt, encodeListDelta as m, COMMIT_EPOCH_HEADER as n, LivePlatform as nt, LIVE_EVENT as o, LiveQueryOptions as ot, defineLiveQuery as p, CursorLog as q, ClientLiveFrame as r, LivePresenceOptions as rt, LIVE_PROTOCOL as s, LiveResolverMetadata as st, COMMIT_CURSOR_HEADER as t, LiveModuleOptions as tt, RowOp as u, SubscriptionRecord as ut, isServerLiveFrame as v, LIVE_RESOLVER_METADATA as w, LIVE_DRIVER as x, readLiveEnvelope as y, PresenceResolver as z };
```

### `<internal:index-websocket-websocket.decorators.d.ts>`

```ts
import "<internal:metadata.d.ts>";
import { B as SyncDriver, D as WebSocketGatewayOptions, I as WsServer, J as Container, M as WsClient, N as WsExecutionContext, d as PipeTransform, j as WebSocketUpgradeIdentity, k as WebSocketTransport, m as BroadcastOperator, ot as InjectionToken, y as GatewayServer, yt as Type, z as RoomRegistry } from "<internal:types-http-hono.types.d.ts>";
import { B as ContributesEntrypoints, V as Entrypoint, Y as DiscoveryService } from "<internal:request-context.d.ts>";
import { I as DynamicModule, K as ParameterMetadata, a as NonceStore, f as RouteManager } from "<internal:types-http-version.d.ts>";
import { _ as OnModuleInit, m as OnApplicationBootstrap, rn as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { WebSocketSendGate, WebSocketSendPolicy as WebSocketSendPolicy$1, WebSocketSendResult, WebSocketSendResult as WebSocketSendResult$1, readWebSocketEnvelope } from "@velajs/live-protocol";
import { Catalog } from "@velajs/errors";

declare function WebSocketGateway(options?: WebSocketGatewayOptions): ClassDecorator;

declare function SubscribeMessage(event: string): MethodDecorator;

declare function ReservedWsEvent(event: string): ClassDecorator;

declare const MessageBody: () => ParameterDecorator;

declare const ConnectedSocket: () => ParameterDecorator;

declare function WebSocketServer(): ParameterDecorator;

interface WebSocketModuleOptions {

  sync?: SyncDriver;

  registry?: RoomRegistry;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<WebSocketModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}, "sync">;
type WebSocketModuleRegistration = Parameters<(typeof ConfigurableModuleClass)['forRoot']>[0];
declare class WebSocketModule extends ConfigurableModuleClass {

  static forRoot(options?: WebSocketModuleRegistration): DynamicModule;
}

declare class WebSocketPlatform {
  readonly transport?: WebSocketTransport | undefined;
  constructor(transport?: WebSocketTransport | undefined);
}

declare class Gateways {
  private readonly platform;
  private readonly driver;
  constructor(platform: WebSocketPlatform, driver: SyncDriver);

  of<Events extends object = Record<string, unknown>>(gateway: Type): GatewayServer<Events>;
  private push;
}

interface WsEntrypointMeta {

  path: string;

  dispatcher: WsDispatcher;

  options: WebSocketGatewayOptions;

  moduleId: string;
}

declare function readWsEntrypointMeta(value: unknown): WsEntrypointMeta;

declare class WsDispatcher implements OnModuleInit, OnApplicationBootstrap, ContributesEntrypoints {
  #private;
  constructor(container: Container, discovery: DiscoveryService, server?: WsServer, routeManager?: RouteManager);

  onModuleInit(): Promise<void>;
  private discoveredGateways;

  private gatewayServer;

  private serverModule;

  private moduleServer;
  private resolveModuleServer;

  get gatewayPaths(): string[];

  getGatewayMaxFrameBytes(path: string): number | undefined;

  getMaximumGatewayFrameBytes(): number;
  onApplicationBootstrap(): Promise<void>;

  collectEntrypoints(): Entrypoint<WsEntrypointMeta>[];
  private initializeGateway;
  private resolveGateway;
  handleOpen(path: string, client: WsClient): Promise<void>;
  handleClose(path: string, client: WsClient, _code: number, _reason: string): Promise<void>;
  private resolveReserved;
  handleError(path: string, _client: WsClient, err: unknown): Promise<void>;

  authorizeDelivery(path: string, client: WsClient, invocationScope?: Container): Promise<boolean>;
  dispatchMessage(path: string, client: WsClient, raw: string | ArrayBuffer): Promise<void>;

  private dispatchReserved;
  private reply;
  private runFilters;
  private trySend;
  private reportBootstrapError;
  private parse;
  private frameFits;
  private buildGatewayEntry;
}

declare const DEFAULT_WS_MAX_FRAME_BYTES: number;

declare const DEFAULT_WS_MAX_ROOM_ID_BYTES = 512;

declare const DEFAULT_WS_MAX_JOINED_ROOMS = 32;
interface AuthenticatedWebSocketUpgrade {

  request: Request;
  identity: WebSocketUpgradeIdentity;
}

declare function assertWebSocketRoomId(value: unknown): asserts value is string;

declare function resolveGatewayRoomParam(options: WebSocketGatewayOptions): string | undefined;

declare function resolveGatewayRoomId(options: WebSocketGatewayOptions, param: (name: string) => string | undefined): string;

declare function resolveMaxFrameBytes(options: WebSocketGatewayOptions): number;

declare function webSocketFrameFits(frame: string | ArrayBuffer, maxFrameBytes: number): boolean;

declare function isWebSocketOriginAllowed(request: Request, allowedOrigins?: '*' | readonly string[]): boolean;

interface WebSocketUpgradeGateway {
  options: WebSocketGatewayOptions;

  moduleId?: string;
}

type WebSocketUpgradeGate = (request: Request, room: string) => Promise<AuthenticatedWebSocketUpgrade | false>;

declare function createWebSocketUpgradeGate(container: Container, gateway: WebSocketUpgradeGateway): WebSocketUpgradeGate;

declare function normalizeWebSocketUpgradeIdentity(value: unknown): WebSocketUpgradeIdentity | false;

declare const WEBSOCKET_TICKET_AUDIENCE: 'vela:websocket';

declare const WEBSOCKET_TICKET_PURPOSE: 'socket-ticket';

declare const WEBSOCKET_TICKET_MAX_TTL_MS = 30000;

type WebSocketTicketPrincipalType = 'user' | 'service';
interface WebSocketTicketPrincipal {
  issuer: string;
  subject: string;
  principalType: WebSocketTicketPrincipalType;
}

interface WebSocketTicketClaim {
  aud: typeof WEBSOCKET_TICKET_AUDIENCE;
  purpose: typeof WEBSOCKET_TICKET_PURPOSE;
  gatewayPath: string;
  room: string;
  principal: WebSocketTicketPrincipal;
  tenantId: string;
  issuedAtMs: number;
  expiresAtMs: number;
  nonce: string;
}

type WebSocketTicketNonceStore = Pick<NonceStore, 'claim'>;
interface IssueWebSocketTicketOptions {
  secret: string;
  gatewayPath: string;
  room: string;
  principal: WebSocketTicketPrincipal;
  tenantId: string;

  ttlMs?: number;

  nowMs?: number;
}
interface VerifyWebSocketTicketOptions {
  secret: string;

  gatewayPath: string;

  room: string;

  nonceStore: WebSocketTicketNonceStore;

  nowMs?: number;
}

declare function issueWebSocketTicket(options: IssueWebSocketTicketOptions): Promise<string>;

declare function verifyAndConsumeWebSocketTicket(token: unknown, options: VerifyWebSocketTicketOptions): Promise<WebSocketTicketClaim | false>;

declare class BroadcastOperatorImpl implements BroadcastOperator {
  private readonly driver;
  private readonly maxFrameBytes;
  private readonly rooms;
  private readonly exceptRooms;
  private readonly exceptIds;
  private readonly gatewayPath?;
  constructor(driver: SyncDriver, maxFrameBytes?: () => number, rooms?: Set<string>, exceptRooms?: Set<string>, exceptIds?: Set<string>, gatewayPath?: string | undefined);
  to(room: string): this;
  in(room: string): this;
  except(room: string): this;
  emit(event: string, data?: unknown): void | Promise<void>;
}

declare class WsServerImpl implements WsServer {
  private readonly driver;
  private readonly gatewayPath?;
  private maxFrameBytes;
  private hasGatewayLimit;

  constructor(driver: SyncDriver, gatewayPath?: string | undefined);
  setOutboundFrameLimit(maxFrameBytes: number): void;
  forGateway(gatewayPath: string, maxFrameBytes: number): WsServer;
  emit(event: string, data?: unknown): void | Promise<void>;
  to(room: string): BroadcastOperator;
  in(room: string): BroadcastOperator;
  except(room: string): BroadcastOperator;
  private operator;
}

declare function buildWsExecutionContext(client: WsClient, data: unknown, controller: Type, handlerName: string | symbol, pattern: string, moduleId?: string, container?: Container): WsExecutionContext;

declare class WsException extends Error {
  private readonly err;
  constructor(err: string | Record<string, unknown>);
  getError(): string | Record<string, unknown>;
}

declare function toErrorFrame(error: unknown, catalog?: Catalog<string>): {
  event: 'exception';
  data: unknown;
};

declare function resolveWsArgs(paramMeta: ParameterMetadata[], client: WsClient, data: unknown, pipes: PipeTransform[], paramTypes?: unknown[]): Promise<unknown[]>;

declare const WS_GATEWAY_METADATA = "vela:ws-gateway";
declare const WS_SUBSCRIBE_METADATA = "vela:ws-subscribe";
declare const WS_RESERVED_METADATA = "vela:ws-reserved";

declare const RESERVED_WS_EVENT_PREFIX = "$";
declare const WsParamType: {
  readonly SOCKET: 'ws_socket';
  readonly BODY: 'ws_body';
  readonly SERVER: 'ws_server';
};
type WsParamType = (typeof WsParamType)[keyof typeof WsParamType];

declare const WS_SERVER: InjectionToken<WsServer>;

declare const WS_SYNC_DRIVER: InjectionToken<SyncDriver>;

declare const WS_ROOM_REGISTRY: InjectionToken<RoomRegistry>;

declare const WS_TRANSPORT: InjectionToken<WebSocketTransport>;
declare const WS_MODULE_OPTIONS: InjectionToken<WebSocketModuleOptions>;

declare function trySendWebSocketFrame(client: WsClient, payload: string): WebSocketSendResult;

declare class WsMessageQueue {
  #private;
  constructor(overflow: () => void, maxMessages?: number, maxBytes?: number);
  stop(): void;
  run(frame: string | ArrayBuffer, work: () => Promise<void>): Promise<void>;
}

export { WebSocketModuleOptions as $, WebSocketTicketPrincipal as A, assertWebSocketRoomId as B, IssueWebSocketTicketOptions as C, WEBSOCKET_TICKET_PURPOSE as D, WEBSOCKET_TICKET_MAX_TTL_MS as E, DEFAULT_WS_MAX_FRAME_BYTES as F, resolveGatewayRoomParam as G, isWebSocketOriginAllowed as H, DEFAULT_WS_MAX_JOINED_ROOMS as I, WsDispatcher as J, resolveMaxFrameBytes as K, DEFAULT_WS_MAX_ROOM_ID_BYTES as L, issueWebSocketTicket as M, verifyAndConsumeWebSocketTicket as N, WebSocketTicketClaim as O, AuthenticatedWebSocketUpgrade as P, WebSocketModule as Q, WebSocketUpgradeGate as R, WsServerImpl as S, WEBSOCKET_TICKET_AUDIENCE as T, normalizeWebSocketUpgradeIdentity as U, createWebSocketUpgradeGate as V, resolveGatewayRoomId as W, readWsEntrypointMeta as X, WsEntrypointMeta as Y, Gateways as Z, resolveWsArgs as _, WsMessageQueue as a, WebSocketServer as at, buildWsExecutionContext as b, WS_GATEWAY_METADATA as c, WS_ROOM_REGISTRY as d, ConnectedSocket as et, WS_SERVER as f, WsParamType as g, WS_TRANSPORT as h, readWebSocketEnvelope as i, WebSocketGateway as it, WebSocketTicketPrincipalType as j, WebSocketTicketNonceStore as k, WS_MODULE_OPTIONS as l, WS_SYNC_DRIVER as m, WebSocketSendPolicy$1 as n, ReservedWsEvent as nt, trySendWebSocketFrame as o, WS_SUBSCRIBE_METADATA as p, webSocketFrameFits as q, WebSocketSendResult$1 as r, SubscribeMessage as rt, RESERVED_WS_EVENT_PREFIX as s, WebSocketSendGate as t, MessageBody as tt, WS_RESERVED_METADATA as u, WsException as v, VerifyWebSocketTicketOptions as w, BroadcastOperatorImpl as x, toErrorFrame as y, WebSocketUpgradeGateway as z };
```

### `<internal:internal-dispatcher.d.ts>`

```ts
import { J as Container, q as VelaEnv } from "<internal:types-http-hono.types.d.ts>";
import { n as InvocationRouteTarget, o as RunInit, s as RouteName, t as InvocationPathTarget } from "<internal:types-http-version.d.ts>";
import { r as UrlGeneratorService } from "<internal:url-generator.service.d.ts>";

declare class InternalDispatcher {
  #private;
  private readonly urls;
  private readonly container;
  constructor(urls: UrlGeneratorService, container: Container, invocationSecret?: string, urlSecret?: string, env?: VelaEnv);

  run<T = unknown, N extends RouteName = RouteName>(target: InvocationRouteTarget<N> | InvocationPathTarget, init?: RunInit): Promise<T>;
  private resolveTransport;
  private parseResponse;
  private toInvocationError;
}

export { InternalDispatcher as t };
```

### `<internal:metadata.d.ts>`

```ts

declare global {
  namespace Reflect {
    function defineMetadata(key: string, value: unknown, target: object, propertyKey?: string | symbol): void;
    function getMetadata(key: string, target: object, propertyKey?: string | symbol): unknown;
    function getOwnMetadata(key: string, target: object, propertyKey?: string | symbol): unknown;
    function metadata(key: string, value: unknown): (target: object, propertyKey?: string | symbol) => void;
  }
}
declare function defineMetadata(key: string, value: unknown, target: object, propertyKey?: string | symbol): void;
declare function getMetadata<T = unknown>(key: string, target: object, propertyKey?: string | symbol): T | undefined;

export { getMetadata as n, defineMetadata as t };
```

### `<internal:parse-schema.d.ts>`

```ts
import { StandardJSONSchemaV1, StandardSchemaV1, StandardSchemaV1 as StandardSchemaV1$1 } from "@standard-schema/spec";

declare class SchemaValidationError extends Error {
  readonly issues: readonly StandardSchemaV1.Issue[];
  constructor(issues: readonly StandardSchemaV1.Issue[]);
}

interface ValidationIssue {
  readonly message: string;
  readonly path?: readonly (string | number)[];
  readonly code?: string;
}
declare function isStandardSchema(value: unknown): value is StandardSchemaV1;

declare function validateSchema<Input, Output>(schema: StandardSchemaV1<Input, Output>, value: unknown): Output | Promise<Output>;

declare function standardJsonSchema(schema: unknown, direction?: 'input' | 'output', target?: string, libraryOptions?: Record<string, unknown>): unknown;

interface SchemaParser<Value> {
  parse(value: unknown): Value;
  parseAsync?(value: unknown): Promise<Awaited<Value>>;
}

type RuntimeParser = SchemaParser<unknown>;

interface DtoSchema<Value> extends SchemaParser<Value> {
  toJSONSchema?(): unknown;
}
interface DtoOptions {

  name?: string;
  jsonSchema?: unknown;
  schemaConverter?: (direction: 'input' | 'output') => unknown;
}

interface DtoDefinition<Value, S extends DtoSchema<unknown> = DtoSchema<Value>> extends SchemaParser<Value> {
  readonly name: string;
  readonly schema: S;
  toJSONSchema(direction?: 'input' | 'output'): unknown;
}
interface StandardDtoDefinition<Input, Output, S extends StandardSchemaV1$1<Input, Output> = StandardSchemaV1$1<Input, Output>> {
  readonly name: string;
  readonly schema: S;
  parse(value: unknown): Output | Promise<Output>;
  parseAsync?(value: unknown): Promise<Output>;
  toJSONSchema(direction?: 'input' | 'output'): unknown;
}
type AsyncParser<Value> = {
  parseAsync(value: unknown): Promise<Awaited<Value>>;
};
declare function defineDto<S extends DtoSchema<unknown>>(schema: S, options?: DtoOptions): DtoDefinition<ReturnType<S['parse']>, S> & AsyncParser<ReturnType<S['parse']>>;
declare function defineDto<S extends StandardSchemaV1$1>(schema: S, options?: DtoOptions): StandardDtoDefinition<StandardSchemaV1$1.InferInput<S>, StandardSchemaV1$1.InferOutput<S>, S> & AsyncParser<StandardSchemaV1$1.InferOutput<S>>;
declare function defineDto<Value>(schema: DtoSchema<Value>, options?: DtoOptions): DtoDefinition<Value> & AsyncParser<Value>;
declare function defineDto<Input, Output>(schema: StandardSchemaV1$1<Input, Output>, options?: DtoOptions): StandardDtoDefinition<Input, Output> & AsyncParser<Output>;

type ValidationSchema = RuntimeParser | StandardSchemaV1$1 | {
  readonly schema: RuntimeParser | StandardSchemaV1$1;
};
type SchemaInput<S extends ValidationSchema> = S extends {
  readonly schema: infer Inner extends ValidationSchema;
} ? SchemaInput<Inner> : S extends StandardSchemaV1$1 ? StandardSchemaV1$1.InferInput<S> : unknown;
type SchemaOutput<S extends ValidationSchema> = S extends {
  readonly schema: infer Inner extends ValidationSchema;
} ? SchemaOutput<Inner> : S extends StandardSchemaV1$1 ? StandardSchemaV1$1.InferOutput<S> : S extends {
  parseAsync(value: unknown): infer Output;
} ? Awaited<Output> : S extends SchemaParser<infer Output> ? Awaited<Output> : never;

declare function isValidationSchema(value: unknown): value is ValidationSchema;

declare function parseSchema<S extends ValidationSchema>(schema: S, value: unknown): SchemaOutput<S> | Promise<SchemaOutput<S>>;

declare function parseSchemaAsync<S extends ValidationSchema>(schema: S, value: unknown): Promise<SchemaOutput<S>>;

export { ValidationIssue as _, parseSchema as a, validateSchema as b, DtoOptions as c, SchemaParser as d, StandardDtoDefinition as f, StandardSchemaV1$1 as g, StandardJSONSchemaV1 as h, isValidationSchema as i, DtoSchema as l, SchemaValidationError as m, SchemaOutput as n, parseSchemaAsync as o, defineDto as p, ValidationSchema as r, DtoDefinition as s, SchemaInput as t, RuntimeParser as u, isStandardSchema as v, standardJsonSchema as y };
```

### `<internal:request-container.d.ts>`

```ts
import { J as Container, Mt as Scope, X as Constructor, it as InjectMetadata, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { F as ComponentTypeMap, G as ModuleRecord, J as RouteDefinition, K as ParameterMetadata, P as ComponentType, Q as VersionValue, z as HttpHandlerMeta } from "<internal:types-http-version.d.ts>";
import { Context } from "hono";

interface ControllerOptions {
  version?: VersionValue;
}

type HandlerMethod = readonly [Constructor, string | symbol];

declare function countRegisteredClasses(): number;
declare class MetadataRegistry {
  private static get routes();
  private static get controllers();
  private static get controllerOptions();
  private static get modules();
  private static get parameters();
  private static get injectables();
  private static get scopes();
  private static get injectTokens();
  private static get handlerHttpMeta();
  private static get catchTypes();
  private static get routeVersions();
  private static get classMeta();
  private static get handlerMeta();
  private static get classMetaIndex();
  private static get handlerMetaIndex();
  private static get controllerComponents();
  private static get handlerComponents();
  static getRoutes(controller: Constructor): RouteDefinition[];
  static addRoute(controller: Constructor, route: RouteDefinition): void;
  static getControllerPath(controller: Constructor): string;
  static setControllerPath(controller: Constructor, path: string): void;
  static getControllerOptions(controller: Constructor): ControllerOptions;
  static setControllerOptions(controller: Constructor, options: ControllerOptions): void;
  static getModuleOptions(module: Constructor): ModuleRecord | undefined;
  static setModuleOptions(module: Constructor, options: ModuleRecord): void;
  static getParameters(controller: Constructor): Map<string | symbol, ParameterMetadata[]>;
  static addParameter(controller: Constructor, methodName: string | symbol, param: ParameterMetadata): void;
  static registerController<T extends ComponentType>(type: T, controller: Constructor, component: ComponentTypeMap[T]): void;
  static getController<T extends ComponentType>(type: T, controller: Constructor): ComponentTypeMap[T][];
  static registerHandler<T extends ComponentType>(type: T, controller: Constructor, methodName: string | symbol, component: ComponentTypeMap[T]): void;
  static getHandler<T extends ComponentType>(type: T, controller: Constructor, methodName: string | symbol): ComponentTypeMap[T][];

  static getDeclaredComponents<T extends ComponentType>(type: T, target: Constructor): ComponentTypeMap[T][];
  static markInjectable(target: object): void;
  static hasInjectable(target: object): boolean;
  static setScope(target: object, scope: Scope): void;
  static getScope(target: object): Scope | undefined;
  static setInjectTokens(target: object, tokens: InjectMetadata[]): void;
  static getInjectTokens(target: object): InjectMetadata[] | undefined;
  static setHandlerHttpMeta(controller: Constructor, method: string | symbol, meta: HttpHandlerMeta): void;
  static getHandlerHttpMeta(controller: Constructor, method: string | symbol): HttpHandlerMeta | undefined;
  static setCatchTypes(filter: Constructor, types: Type<Error>[]): void;
  static getCatchTypes(filter: Constructor): Type<Error>[] | undefined;
  static hasCatchTypes(filter: Constructor): boolean;
  static setRouteVersion(controller: Constructor, method: string | symbol, version: VersionValue): void;
  static getRouteVersion(controller: Constructor, method: string | symbol): VersionValue | undefined;
  static setCustomClassMeta(target: object, key: string, value: unknown): void;
  static getCustomClassMeta(target: object, key: string): unknown;
  static getCustomClassMetaAll(target: object): Map<string, unknown> | undefined;
  static setCustomHandlerMeta(target: object, handler: string | symbol, key: string, value: unknown): void;
  static getCustomHandlerMeta(target: object, handler: string | symbol, key: string): unknown;

  static addHandlerMethod(handler: object, type: Constructor, name: string | symbol): void;

  static getHandlerMethods(handler: object): readonly HandlerMethod[];
  static getCustomHandlerMetaAll(target: object, handler: string | symbol): Map<string, unknown> | undefined;
  static appendCustomClassMeta<T>(target: object, key: string, item: T): void;
  static appendCustomHandlerMeta<T>(target: object, handler: string | symbol, key: string, item: T): void;
  static getClassesWithClassMeta(key: string): ReadonlySet<object>;
  static getClassesWithHandlerMeta(key: string): ReadonlySet<object>;

  static getHandlersWithMeta(target: object, key: string): Array<{
    handler: string | symbol;
    value: unknown;
  }>;
  static setReflectMetadata(target: object, key: string, value: unknown, propertyKey?: string | symbol): void;
  static getReflectMetadata<T = unknown>(target: object, key: string, propertyKey?: string | symbol): T | undefined;
  static getParamTypes(target: object, propertyKey?: string | symbol): unknown[] | undefined;

  static reset(): void;
}

/** @internal */
declare function setRequestContainer(context: Context, container: Container): void;

declare function getRequestContainer(c: Context): Container;

export { countRegisteredClasses as i, setRequestContainer as n, MetadataRegistry as r, getRequestContainer as t };
```

### `<internal:request-context.d.ts>`

```ts
import { $ as Diagnostics, It as VelaMiddlewareHandler, J as Container, Mt as Scope, Nt as VelaContext, Pt as VelaHono, a as ExecutionContext, nt as InferToken, ot as InjectionToken, q as VelaEnv, vt as Token, xt as TypedToken, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { B as InterceptorType, E as RoutePathOptions, I as DynamicModule, L as FilterType, M as CorsOptions, R as GuardType, d as RouteDescription, f as RouteManager, i as InvocationTransport, k as MiddlewareRouteDefinition, p as RouteManagerOptions, q as PipeType } from "<internal:types-http-version.d.ts>";
import { r as ValidationSchema } from "<internal:parse-schema.d.ts>";
import { Context } from "hono";
import { ErrorBodyResult } from "@velajs/errors";

interface ModuleRefLookupOptions {

  readonly strict?: boolean;
}

type ModuleRefContext = ExecutionContext | Context | Container;

declare class ModuleRef {
  #private;
  constructor(container: Container, moduleId: string);

  get<K extends Token>(token: K, options?: ModuleRefLookupOptions): InferToken<K>;

  resolve<K extends Token>(token: K, context?: ModuleRefContext, options?: ModuleRefLookupOptions): Promise<InferToken<K>>;

  create<T>(type: Type<T>): Promise<T>;
}

interface DiscoverableDecorator<T> {
  (value: T): ClassDecorator & MethodDecorator;

  KEY: string;
}
interface CreateDiscoverableDecoratorOptions {

  append?: boolean;
}

declare function createDiscoverableDecorator<T>(key: string, options?: CreateDiscoverableDecoratorOptions): DiscoverableDecorator<T>;

interface DiscoveredClass<T = unknown> {
  token: Token;
  metatype: Type<T>;

  moduleIds: string[];
  scope: Scope;

  instance: T | undefined;
}

interface DiscoveredRegistration<T = unknown> extends DiscoveredClass<T> {
  readonly moduleId: string;
}
interface DiscoveredRegisteredMethodMeta<M = unknown> {
  class: DiscoveredRegistration;
  methodName: string | symbol;
  meta: M;
}
interface DiscoveredMethodMeta<M = unknown> {
  class: DiscoveredClass;
  methodName: string | symbol;
  meta: M;
}
interface DiscoveryFilter {

  metadataOnly?: boolean;

  moduleId?: string | string[];

  requestScope?: Container;

  deferLazy?: boolean;

  deferRequestScoped?: boolean;
}

declare class DiscoveryService {
  #private;
  constructor(container: Container);

  getProviders(filter?: DiscoveryFilter): DiscoveredClass[];

  getRegistrations(filter?: DiscoveryFilter): DiscoveredRegistration[];
  registrationsWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): Array<DiscoveredRegistration & {
    meta: M;
  }>;

  providersWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): Array<DiscoveredClass & {
    meta: M;
  }>;

  methodsWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): DiscoveredMethodMeta<M>[];
  registeredMethodsWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): DiscoveredRegisteredMethodMeta<M>[];
  private findMethods;

  private candidatesWithClassMeta;
  private matchingOwners;
  private buildEntries;
  private buildEntry;
  private buildRegistration;
}

interface EntrypointKind {
  kind: string;
  metaKey: string;

  level: 'class' | 'method';
}

interface Entrypoint<M = unknown> {
  kind: string;
  token: Token;

  moduleId?: string;

  instance: unknown;

  methodName?: string | symbol;
  meta: M;
}

interface ContributesEntrypoints {
  collectEntrypoints(discovery: DiscoveryService): Entrypoint[] | Promise<Entrypoint[]>;
}
declare function contributesEntrypoints(x: unknown): x is ContributesEntrypoints;

declare function registerEntrypointKind(k: EntrypointKind): void;

declare function getEntrypointKinds(): EntrypointKind[];

declare class EntrypointRegistry {
  #private;
  static build(discovery: DiscoveryService, eagerInstances: readonly unknown[], options?: {
    deferLazy?: boolean;
  }): Promise<EntrypointRegistry>;
  private add;
  ofKind(kind: string): Entrypoint[];
  ofKind<M>(kind: string, parseMeta: (meta: unknown) => M): Entrypoint<M>[];
  kinds(): string[];
  all(): Entrypoint[];
}

interface ApplicationContextLookupOptions {

  readonly strict?: boolean;
}

declare class VelaApplicationContext {
  #private;

  constructor(container: Container, selection?: {
    readonly root: VelaApplicationContext;
    readonly module: Type | DynamicModule;
  });
  getContainer(): Container;
  getInstances(): unknown[];
  setInstances(instances: unknown[]): void;

  get<K extends Token>(token: K, options?: ApplicationContextLookupOptions): InferToken<K>;

  resolve<K extends Token>(token: K, context?: ModuleRefContext, options?: ApplicationContextLookupOptions): Promise<InferToken<K>>;

  select(module: Type | DynamicModule): VelaApplicationContext;

  init(): Promise<this>;
  callOnModuleInit(): Promise<void>;
  callOnApplicationBootstrap(): Promise<void>;

  materializeLazyModules(): Promise<void>;

  get entrypoints(): EntrypointRegistry;

  close(signal?: string): Promise<void>;

  dispose(signal?: string): Promise<void>;
}

type ErrorMatcher = string | ((error: unknown) => boolean) | (new (...args: never[]) => Error);

interface ErrorReportContext {
  edge: 'http' | 'ws' | 'live' | 'queue' | 'schedule' | 'hono' | 'graphql' | 'rpc' | 'cache' | 'durable-object';

  source?: string;

  note?: string;
  [key: string]: unknown;
}

interface ExceptionHandler {

  report?(error: unknown, ctx: ErrorReportContext): void | Promise<void>;

  dontReport?: ErrorMatcher[];

  context?(error: unknown, ctx: ErrorReportContext): Record<string, unknown>;

  render?(error: unknown, ctx: unknown): Response | ErrorBodyResult | undefined;
}

declare const matchesAny: (matchers: ErrorMatcher[] | undefined, error: unknown) => boolean;

interface OpenApiInfo {
  title: string;
  version: string;
  description?: string;
}
interface JsonSchema {
  type?: string | string[];
  format?: string;
  enum?: Array<string | number | boolean | null>;
  const?: unknown;
  description?: string;
  nullable?: boolean;
  default?: unknown;
  items?: JsonSchema;
  properties?: Record<string, JsonSchema>;
  required?: string[];
  additionalProperties?: boolean | JsonSchema;
  oneOf?: JsonSchema[];
  anyOf?: JsonSchema[];
  allOf?: JsonSchema[];
  minimum?: number;
  maximum?: number;
  minLength?: number;
  maxLength?: number;
  pattern?: string;
  $ref?: string;
  [key: string]: unknown;
}
interface OpenApiParameter {
  name: string;
  in: 'path' | 'query' | 'header' | 'cookie';
  required?: boolean;
  description?: string;
  schema?: JsonSchema;

  style?: 'form';
  explode?: boolean;
}
interface OpenApiRequestBody {
  description?: string;
  required?: boolean;
  content?: Record<string, {
    schema: JsonSchema;
    encoding?: Record<string, {
      style?: string;
      explode?: boolean;
    }>;
  }>;

  'x-vela-body-limits'?: {
    maxBytes?: number;
    maxFields?: number;
    maxFieldBytes?: number;
    maxFiles?: number;
    maxFileBytes?: number;
  };
}
interface OpenApiResponse {
  description: string;

  'x-vela-response-format'?: 'binary' | 'stream' | 'response';
  content?: Record<string, {
    schema: JsonSchema;
  }>;
}

type OpenApiSecurityRequirement = Record<string, string[]>;
interface OpenApiOperation {
  summary?: string;
  description?: string;
  operationId?: string;
  deprecated?: boolean;
  tags?: string[];
  parameters?: OpenApiParameter[];
  requestBody?: OpenApiRequestBody;
  responses: Record<string, OpenApiResponse>;

  security?: OpenApiSecurityRequirement[];
}

interface OpenApiServer {
  url: string;
  description?: string;
  variables?: Record<string, {
    enum?: string[];
    default: string;
    description?: string;
  }>;
  [key: string]: unknown;
}

interface OpenApiSecurityScheme {
  type: 'apiKey' | 'http' | 'oauth2' | 'openIdConnect' | 'mutualTLS';
  description?: string;
  name?: string;
  in?: 'query' | 'header' | 'cookie';
  scheme?: string;
  bearerFormat?: string;
  flows?: Record<string, unknown>;
  openIdConnectUrl?: string;
  [key: string]: unknown;
}
type HttpVerb = 'get' | 'post' | 'put' | 'patch' | 'delete' | 'options' | 'head';
type OpenApiPathItem = { [verb in HttpVerb]?: OpenApiOperation; };
interface OpenApiDocument {
  openapi: '3.1.0';
  info: OpenApiInfo;
  servers?: OpenApiServer[];
  paths: Record<string, OpenApiPathItem>;
  components?: {
    schemas?: Record<string, JsonSchema>;
    securitySchemes?: Record<string, OpenApiSecurityScheme>;
  };
  security?: OpenApiSecurityRequirement[];
  tags?: Array<{
    name: string;
    description?: string;
  }>;
}
interface ApiDocMetadata {
  summary?: string;
  description?: string;
  operationId?: string;
  deprecated?: boolean;
  tags?: string[];
}

interface ApiResponseOptions {

  status: number | `${1 | 2 | 3 | 4 | 5}XX` | 'default';
  description: string;

  contentType?: string;

  format?: 'binary' | 'stream' | 'response';

  schema?: ValidationSchema;
}

type ApiResponseEntry = ApiResponseOptions;
interface CreateOpenApiDocumentOptions extends RoutePathOptions {
  info?: Partial<OpenApiInfo>;

  tags?: Array<{
    name: string;
    description?: string;
  }>;

  servers?: OpenApiServer[];

  securitySchemes?: Record<string, OpenApiSecurityScheme>;

  security?: OpenApiSecurityRequirement[];
}
type OpenApiUi = 'swagger' | 'scalar' | 'redoc';
interface MountOpenApiOptions {

  document: OpenApiDocument;

  specPath?: string;

  ui?: OpenApiUi | OpenApiUi[] | 'all';

  swaggerPath?: string;

  scalarPath?: string;

  redocPath?: string;

  title?: string;
}

declare class VelaApplication extends VelaApplicationContext {
  private readonly routeManager;
  private honoApp;
  constructor(container: Container, routeManager: RouteManager);

  initRoutes(): Promise<void>;
  private getApp;
  get fetch(): VelaHono['fetch'];
  getHonoApp(): VelaHono;

  describeRoutes(): RouteDescription[];

  getGlobalPrefix(): string;

  enableCors(options?: CorsOptions): this;

  getRoutePathOptions(): RoutePathOptions;
  useGlobalPipes(...pipes: PipeType[]): this;
  useGlobalGuards(...guards: GuardType[]): this;
  useGlobalInterceptors(...interceptors: InterceptorType[]): this;
  useGlobalFilters(...filters: FilterType[]): this;

  useGlobalExceptionHandler(handler: Type<ExceptionHandler> | ExceptionHandler): this;

  mountOpenApi(options: MountOpenApiOptions): this;
}

interface LazyModuleGroupSpec {
  moduleId: string;
  tokens: Token[];
  hasEntrypointContributor: boolean;
}

type ModuleOverrides = ReadonlyMap<Type | DynamicModule, Type | DynamicModule>;
declare class ModuleLoader {
  #private;
  private container;
  private router;
  private readonly moduleOverrides?;
  constructor(container: Container, router: RouteManager, moduleOverrides?: ModuleOverrides | undefined);

  private override;

  load(rootModule: Type | DynamicModule, prepare?: () => void): void;
  private getModuleId;
  private isProcessed;
  private markProcessed;
  private getCachedExports;
  private cacheExports;
  private processModule;
  private registerController;

  private registerEnhancers;
  private isVisible;
  private warnOnMixedDefaultAndKeyed;

  private reportIdentityCollision;

  private registerProvider;
  private isAppToken;
  private buildExportSet;
  getControllers(): Type[];
  getAppProviderTokens<T>(token: TypedToken<T>): TypedToken<T>[];
  getConsumerMiddlewareDefinitions(): MiddlewareRouteDefinition[];

  getLazyGroups(): LazyModuleGroupSpec[];
  resolveAllInstances(): Promise<unknown[]>;
  private routeError;
}

interface BootstrapOptions extends RouteManagerOptions {
  diagnostics?: Diagnostics;

  env?: VelaEnv;

  configureContainer?(container: Container): void | Promise<void>;
}

interface BootstrapInternals {

  moduleOverrides?: ModuleOverrides;

  prepareGraph?(container: Container): void;
}
interface BootstrapResult {
  container: Container;
  routeManager: RouteManager;
  loader: ModuleLoader;
}

declare function bootstrap(rootModule: Type | DynamicModule, options?: BootstrapOptions, internals?: BootstrapInternals): Promise<BootstrapResult>;

interface AdapterContext {
  app: VelaApplication;
  container: Container;
  routeManager: RouteManager;
  discovery: DiscoveryService;
}

interface RuntimeAdapter {
  name: string;
  configureContainer?(container: Container): void | Promise<void>;

  getClientIp?: (c: VelaContext) => string | null;
  requestMiddleware?: VelaMiddlewareHandler[];
  onBootstrap?(ctx: AdapterContext): void | Promise<void>;
  onRoutesBuilt?(ctx: AdapterContext): void | Promise<void>;
  invocationTransport?(ctx: AdapterContext): InvocationTransport | undefined;
}

declare function applyRuntimeAdapters(options: BootstrapOptions, adapters: readonly RuntimeAdapter[]): BootstrapOptions;

interface RequestContext {
  readonly id: string;
  readonly receivedAt: Date;
  readonly request: Request;
  readonly hono: VelaContext;
  set<Value>(key: RequestContextKey<Value>, value: NoInfer<Value>): void;
  set(key: string | symbol, value: unknown): void;
  get<Value>(key: RequestContextKey<Value>): Value | undefined;
  get(key: string | symbol): unknown;
  has<Value>(key: RequestContextKey<Value> | string | symbol): boolean;
}
interface RequestContextKeyOptions<Value> {

  derive?: (context: RequestContext) => Value | undefined;
}

declare class RequestContextKey<Value> {
  #private;
  readonly description: string;
  constructor(description: string, options?: RequestContextKeyOptions<Value>);
  /** @internal */
  readonly read: (context: RequestContext) => Value | undefined;
  /** @internal */
  readonly write: (context: RequestContext, value: Value) => void;
  /** @internal */
  readonly contains: (context: RequestContext) => boolean;
}
declare const REQUEST_CONTEXT: InjectionToken<RequestContext>;
declare function createRequestContext(c: VelaContext): RequestContext;

export { ModuleRef as $, OpenApiUi as A, ContributesEntrypoints as B, OpenApiDocument as C, OpenApiPathItem as D, OpenApiParameter as E, ApplicationContextLookupOptions as F, DiscoveredMethodMeta as G, EntrypointKind as H, VelaApplicationContext as I, DiscoveryFilter as J, DiscoveredRegisteredMethodMeta as K, EntrypointRegistry as L, ErrorReportContext as M, ExceptionHandler as N, OpenApiRequestBody as O, matchesAny as P, createDiscoverableDecorator as Q, getEntrypointKinds as R, MountOpenApiOptions as S, OpenApiOperation as T, contributesEntrypoints as U, Entrypoint as V, DiscoveredClass as W, CreateDiscoverableDecoratorOptions as X, DiscoveryService as Y, DiscoverableDecorator as Z, ApiResponseEntry as _, createRequestContext as a, HttpVerb as b, applyRuntimeAdapters as c, BootstrapResult as d, ModuleRefContext as et, bootstrap as f, ApiDocMetadata as g, VelaApplication as h, RequestContextKeyOptions as i, ErrorMatcher as j, OpenApiResponse as k, BootstrapInternals as l, ModuleOverrides as m, RequestContext as n, AdapterContext as o, ModuleLoader as p, DiscoveredRegistration as q, RequestContextKey as r, RuntimeAdapter as s, REQUEST_CONTEXT as t, ModuleRefLookupOptions as tt, BootstrapOptions as u, ApiResponseOptions as v, OpenApiInfo as w, JsonSchema as x, CreateOpenApiDocumentOptions as y, registerEntrypointKind as z };
```

### `<internal:route-contract-contract-route-contract.d.ts>`

```ts
import { r as ValidationSchema } from "<internal:parse-schema.d.ts>";
import { i as RouteFormBody, l as RouteResponseOptions, o as RouteJsonBody, s as RouteMultipartBody } from "<internal:route-contract-http-route-contract.d.ts>";

type RouteContractMethod = 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE' | 'OPTIONS' | 'HEAD';

interface RouteContract<Method extends string = RouteContractMethod> extends RouteResponseOptions {
  readonly method: Method;

  readonly path?: string;

  readonly name?: string;

  readonly params?: ValidationSchema;

  readonly query?: ValidationSchema;

  readonly body?: ValidationSchema;

  readonly json?: RouteJsonBody;

  readonly form?: RouteFormBody;

  readonly multipart?: RouteMultipartBody;
}

declare function defineRoute<const Contract extends RouteContract>(contract: Contract): Contract;

interface ContractFormEncoding {
  readonly path: string;
  readonly method: string;
  readonly contentType: 'multipart/form-data' | 'application/x-www-form-urlencoded';
}

declare function contractFormEncodings(routes: readonly RouteContract[] | Readonly<Record<string, RouteContract>>): ContractFormEncoding[];

export { defineRoute as a, contractFormEncodings as i, RouteContract as n, RouteContractMethod as r, ContractFormEncoding as t };
```

### `<internal:route-contract-http-route-contract.d.ts>`

```ts
import { g as StandardSchemaV1, n as SchemaOutput, r as ValidationSchema, t as SchemaInput } from "<internal:parse-schema.d.ts>";
import { SuccessStatusCode } from "hono/utils/http-status";

type RouteResponseFormat = 'json' | 'text' | 'binary' | 'stream' | 'response';

interface RouteJsonBody {
  maxBytes?: number;
}

interface RouteFormBody {
  maxBytes?: number;
  maxFields?: number;
  maxFieldBytes?: number;
}

interface RouteMultipartBody extends RouteFormBody {
  maxFiles?: number;
  maxFileBytes?: number;
}

type RouteBodyOptions = {
  readonly json: RouteJsonBody;
} | {
  readonly form: RouteFormBody;
} | {
  readonly multipart: RouteMultipartBody;
};

interface RouteResponseOptions {

  response?: ValidationSchema | null;

  status?: SuccessStatusCode;

  format?: RouteResponseFormat;

  contentType?: string;

  validate?: boolean;
}

type RouteBinaryBody = Blob | ArrayBuffer | Uint8Array<ArrayBuffer>;

type RouteSchemaResult<S extends ValidationSchema> = S extends {
  readonly schema: infer Inner extends ValidationSchema;
} ? RouteSchemaResult<Inner> : S extends StandardSchemaV1 ? SchemaInput<S> : SchemaOutput<S>;

type RouteHandlerResult<Options> = Options extends {
  readonly format: 'response';
} ? Response : Options extends {
  readonly format: 'stream';
} ? ReadableStream<Uint8Array> | Response : Options extends {
  readonly format: 'binary';
} ? RouteBinaryBody | Response : Options extends {
  readonly response: null;
} ? undefined | null | void | Response : Options extends {
  readonly response: infer S extends ValidationSchema;
} ? Options extends {
  readonly format: 'text';
} ? Extract<RouteSchemaResult<S>, string> | Response : RouteSchemaResult<S> | Response : Options extends {
  readonly format: 'text';
} ? string | Response : unknown;

interface ResolvedRouteBody {
  readonly kind: 'json' | 'form' | 'multipart';

  readonly maxBytes?: number;

  readonly explicitMaxBytes: boolean;
  readonly maxFields: number;
  readonly maxFieldBytes: number;
  readonly maxFiles: number;
  readonly maxFileBytes: number;
}

interface RouteContractMetadata {

  readonly response?: ValidationSchema | null;
  readonly status?: SuccessStatusCode;

  readonly format?: RouteResponseFormat;

  readonly contentType?: string;

  readonly validate: boolean;
  readonly body?: ResolvedRouteBody;

  readonly params?: ValidationSchema;
  readonly query?: ValidationSchema;
  readonly bodySchema?: ValidationSchema;

  readonly path?: string;

  readonly shared?: boolean;
}

export { RouteHandlerResult as a, RouteResponseFormat as c, RouteFormBody as i, RouteResponseOptions as l, RouteBodyOptions as n, RouteJsonBody as o, RouteContractMetadata as r, RouteMultipartBody as s, RouteBinaryBody as t, RouteSchemaResult as u };
```

### `<internal:schedule.registry.d.ts>`

```ts
import { V as Entrypoint, Y as DiscoveryService } from "<internal:request-context.d.ts>";
import { m as OnApplicationBootstrap } from "<internal:index-factory.d.ts>";
import { i as IntervalMetadata, n as CronMetadata } from "<internal:schedule.types.d.ts>";

interface RegisteredCronJob {
  expression: string;
  methodName: string;
  instance: unknown;
  target: Function;
}
interface RegisteredIntervalJob {
  ms: number;
  methodName: string;
  instance: unknown;
  target: Function;
}
declare class ScheduleRegistry implements OnApplicationBootstrap {
  #private;
  constructor(discovery: DiscoveryService);
  onApplicationBootstrap(): void;

  getCronEntrypoints(): Entrypoint<CronMetadata>[];
  getIntervalEntrypoints(): Entrypoint<IntervalMetadata>[];

  getCronJobs(): RegisteredCronJob[];
  getIntervalJobs(): RegisteredIntervalJob[];
}

export { RegisteredIntervalJob as n, ScheduleRegistry as r, RegisteredCronJob as t };
```

### `<internal:schedule.tokens.d.ts>`

```ts
import { ot as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import "<internal:index-factory.d.ts>";
import { c as ScheduleInvocationSeed, o as ScheduleDispatchMode } from "<internal:schedule.types.d.ts>";

declare const CRON_METADATA = "vela:cron";
declare const INTERVAL_METADATA = "vela:interval";

declare const SCHEDULE_DISPATCH: InjectionToken<ScheduleDispatchMode>;

declare const SCHEDULE_INVOCATION_SEED: InjectionToken<ScheduleInvocationSeed>;

export { SCHEDULE_INVOCATION_SEED as i, INTERVAL_METADATA as n, SCHEDULE_DISPATCH as r, CRON_METADATA as t };
```

### `<internal:schedule.types.d.ts>`

```ts
import { J as Container } from "<internal:types-http-hono.types.d.ts>";
import { r as InvocationTarget } from "<internal:types-http-version.d.ts>";

type CronMatcher = (date: Date) => boolean;
interface CronOptions {

  dialect?: 'unix' | 'cloudflare';

  timeZone?: 'local' | 'UTC';
}

declare function parseCron(expression: string, options?: CronOptions): CronMatcher | null;

interface CronMetadata extends CronOptions {
  expression: string;
  methodName: string;
}
interface IntervalMetadata {
  ms: number;
  methodName: string;
}

type ScheduleInvocation = {
  readonly kind: 'cron';
  readonly expression: string;
  readonly scheduledTime: number;
  readonly signal: AbortSignal;
} | {
  readonly kind: 'interval';
  readonly ms: number;
  readonly scheduledTime: number;
  readonly signal: AbortSignal;
};

type ScheduleInvocationSeed = (scope: Container, invocation: ScheduleInvocation) => void;

type CronInvocation = Extract<ScheduleInvocation, {
  kind: 'cron';
}>;

type IntervalInvocation = Extract<ScheduleInvocation, {
  kind: 'interval';
}>;

type ScheduleDecorator<I extends ScheduleInvocation> = <Handler extends (invocation: I) => unknown>(target: object, key: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

interface ScheduleJobRef {
  kind: 'cron' | 'interval';

  methodName: string;

  expression?: string;

  ms?: number;
}

type ScheduleDispatchMode = {
  kind: 'direct';
} | {
  kind: 'signed';

  target: (job: ScheduleJobRef) => InvocationTarget;

  method?: string;

  ttlSeconds?: number;
};

export { ScheduleDecorator as a, ScheduleInvocationSeed as c, CronOptions as d, parseCron as f, IntervalMetadata as i, ScheduleJobRef as l, CronMetadata as n, ScheduleDispatchMode as o, IntervalInvocation as r, ScheduleInvocation as s, CronInvocation as t, CronMatcher as u };
```

### `<internal:tokens.d.ts>`

```ts
import { ot as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { i as InvocationTransport } from "<internal:types-http-version.d.ts>";

declare const INVOCATION_TRANSPORT: InjectionToken<InvocationTransport>;

declare const INVOCATION_SIGNING_SECRET: InjectionToken<string>;

export { INVOCATION_TRANSPORT as n, INVOCATION_SIGNING_SECRET as t };
```

### `<internal:types-http-hono.types.d.ts>`

```ts
import { Context, Hono, MiddlewareHandler, Next } from "hono";
import { WebSocketSendPolicy, WebSocketSendResult } from "@velajs/live-protocol";

interface VelaHonoEnv {
  Bindings: object;
  Variables: Record<string, unknown>;
}
type VelaContext = Context<VelaHonoEnv>;
type VelaHono = Hono<VelaHonoEnv>;
type VelaMiddlewareHandler = MiddlewareHandler<VelaHonoEnv>;

declare const METADATA_KEYS: {
  readonly INJECTABLE: 'vela:injectable';
  readonly INJECT: 'vela:inject';
  readonly SCOPE: 'vela:scope';
  readonly MODULE: 'vela:module';
  readonly MODULE_OPTIONS: 'vela:module-options';
  readonly CONTROLLER: 'vela:controller';
  readonly ROUTES: 'vela:routes';
  readonly PARAMS: 'vela:params';
  readonly HTTP_CODE: 'vela:http-code';
  readonly RESPONSE_HEADERS: 'vela:response-headers';
  readonly REDIRECT: 'vela:redirect';
  readonly CATCH: 'vela:catch';
  readonly CRUD: 'vela:crud';
};
declare const HttpMethod: {
  readonly GET: 'GET';
  readonly POST: 'POST';
  readonly PUT: 'PUT';
  readonly PATCH: 'PATCH';
  readonly DELETE: 'DELETE';
  readonly OPTIONS: 'OPTIONS';
  readonly HEAD: 'HEAD';
  readonly ALL: 'ALL';
};
type HttpMethod = (typeof HttpMethod)[keyof typeof HttpMethod];
declare const ParamType: {
  readonly BODY: 'body';
  readonly QUERY: 'query';
  readonly PARAM: 'param';
  readonly HEADERS: 'headers';
  readonly REQUEST: 'request';
  readonly CONTEXT: 'context';
  readonly RESPONSE: 'response';
  readonly IP: 'ip';
  readonly COOKIE: 'cookie';
  readonly RAW_BODY: 'raw_body';
};
type ParamType = (typeof ParamType)[keyof typeof ParamType];
declare const Scope: {
  readonly DEFAULT: 'default';
  readonly TRANSIENT: 'transient';
  readonly REQUEST: 'request';
};
type Scope = (typeof Scope)[keyof typeof Scope];

type Type<T = unknown> = new (...args: any[]) => T;
type Constructor<T = unknown> = abstract new (...args: any[]) => T;
interface InjectionTokenOptions<T> {
  readonly factory?: () => T;

  readonly scope?: Scope;
}
declare class InjectionTokenIdentity {
  private readonly description;
  readonly options?: Readonly<InjectionTokenOptions<unknown>>;
  constructor(description: string, options?: InjectionTokenOptions<unknown>);
  toString(): string;
}
declare class InjectionToken<T = unknown> extends InjectionTokenIdentity {
  protected readonly valueType: (value: T) => T;
  readonly options?: InjectionTokenOptions<T>;
  constructor(description: string, options?: InjectionTokenOptions<T>);
}
declare class ForwardRef<K extends Token = Token> {
  readonly factory: () => K;
  constructor(factory: () => K);
}
declare function forwardRef<K extends Token>(factory: () => K): ForwardRef<K>;
type TypedToken<T> = Type<T> | InjectionToken<T>;
type Token = Type | InjectionTokenIdentity | string | symbol;
type DependencyToken = Token | ForwardRef;

type InferToken<T> = T extends InjectionToken<infer U> ? U : T extends ForwardRef<infer U> ? InferToken<U> : T extends (abstract new (...args: any[]) => infer U) ? U : unknown;

type InferTokens<T extends readonly unknown[]> = { [K in keyof T]: InferToken<T[K]>; };
interface InjectableOptions {
  scope?: Scope;
}
interface InjectMetadata {
  index: number;
  token?: Token | ForwardRef;
  optional?: boolean;
}
interface ProviderOptions<T = unknown> {
  provide?: Token;
  scope?: Scope;
  useValue?: NoInfer<T>;
  useFactory?: (...args: any[]) => NoInfer<T> | Promise<NoInfer<T>>;
  useClass?: Type<NoInfer<T>>;
  inject?: readonly DependencyToken[];
  useExisting?: Token;
}
declare class CheckedProvider<T> {
  #private;
  readonly provide: Token;
  constructor(provide: Token, options: ProviderOptions<T>);
  get scope(): Scope | undefined;
  get useValue(): T | undefined;
  get useClass(): Type<T> | undefined;
  get useExisting(): Token | undefined;
  get inject(): readonly DependencyToken[] | undefined;
  get useFactory(): unknown;
  static read<T>(provider: CheckedProvider<T>): Readonly<ProviderOptions<T>>;
  static is(value: unknown): value is CheckedProvider<unknown>;
}

type ProviderDefinition<T = unknown> = CheckedProvider<T>;

declare function assertFactoryInject(provide: Token, useFactory: unknown, inject: unknown): void;
type ProviderStrategy<T, Inject extends readonly DependencyToken[]> = {
  useValue: NoInfer<T>;
  useClass?: never;
  useFactory?: never;
  useExisting?: never;
  inject?: never;
} | {
  useClass: Type<NoInfer<T>>;
  useValue?: never;
  useFactory?: never;
  useExisting?: never;
  inject?: never;
} | {
  useExisting: Type<NoInfer<T>> | InjectionToken<NoInfer<T>>;
  useValue?: never;
  useClass?: never;
  useFactory?: never;
  inject?: never;
} | ({
  useFactory: (...dependencies: InferTokens<Inject>) => NoInfer<T> | Promise<NoInfer<T>>;
  useValue?: never;
  useClass?: never;
  useExisting?: never;
} & FactoryInject<Inject>);

type FactoryInject<Inject extends readonly unknown[]> = {
  inject: Inject;
} | ([] extends Inject ? {
  inject?: never;
} : never);

/** @internal */
type AuthoringToken<K extends Token> = K extends InjectionTokenIdentity ? InjectionToken<InferToken<K>> : K;
declare function defineProvider<const K extends Token, const Inject extends readonly DependencyToken[] = readonly []>(provide: K & AuthoringToken<K>, options: ProviderStrategy<InferToken<K>, Inject> & {
  scope?: Scope;
}): ProviderDefinition<InferToken<K>>;

type ProviderLiteral = {
  provide: Token;
  useValue: unknown;
  scope?: Scope;
} | {
  provide: Token;
  useClass: Type;
  scope?: Scope;
} | {
  provide: Token;
  useExisting: Token;
  scope?: Scope;
} | {
  provide: Token;
  useFactory: (...args: never[]) => unknown;
  inject?: readonly DependencyToken[];
  scope?: Scope;
};

type Provider = Type | ProviderDefinition | ProviderLiteral;

type TypedProviderLiteral<K extends Token> = {
  provide: K & AuthoringToken<K>;
  scope?: Scope;
} & ProviderStrategy<InferToken<K>, readonly []>;
type CheckedProviderEntry<P> = P extends Type | ProviderDefinition ? P : P extends {
  provide: infer K extends Token;
} ? [Extract<ProviderLiteral, P>] extends [never] ? P extends TypedProviderLiteral<K> ? P : TypedProviderLiteral<K> : P : Provider;

type CheckedProviders<P extends readonly unknown[]> = { [I in keyof P]: CheckedProviderEntry<P[I]>; };

interface ProviderSnapshot {
  readonly token: Token;
  readonly moduleId: string;

  readonly scope: Scope;
  readonly kind: 'value' | 'factory' | 'class' | 'existing';
  readonly useClass?: Type;
  readonly useExisting?: Token;

  readonly instance?: {
    readonly value: unknown;
  };
}
interface ModuleScope {
  moduleId: string;
  localProviders: Set<Token>;
  importedModules: Set<string>;
  exportedTokens: Set<Token>;

  global: boolean;

  lazy?: boolean;

  moduleClass?: Constructor;

  moduleKey?: string;

  controllers?: ReadonlySet<Constructor>;
}

interface LazyResolutionHook {

  isPending(moduleId: string): boolean;

  claim(moduleId: string): void;

  hasClaimed(): boolean;

  isDraining(): boolean;

  drainSync(): void;

  drainAsync(): Promise<void>;
}
type Diagnostics = 'silent' | 'log' | 'throw';
interface ContainerOptions {
  diagnostics?: Diagnostics;
}

declare function describeToken(token: Token): string;

interface ModuleDescription {
  moduleId: string;

  imports: string[];

  global: boolean;
  lazy: boolean;

  providers: string[];

  exports: string[];
}
declare class ModuleVisibilityError extends Error {
  readonly moduleId: string;
  readonly token: Token;
  constructor(moduleId: string, token: Token);
}
declare class MultipleProvidersFoundError extends Error {
  readonly moduleId: string;
  readonly token: Token;
  readonly candidates: string[];
  constructor(moduleId: string, token: Token, candidates: string[]);
}

type MissingInjectionMetadataReason = 'missing' | 'erased' | 'undefined-inject';

declare class MissingInjectionMetadataError extends Error {
  readonly className: string;
  readonly parameterIndex: number;
  readonly reason: MissingInjectionMetadataReason;
  constructor(className: string, parameterIndex: number, reason: MissingInjectionMetadataReason, declaredParameters: number);
}

type UnresolvedDependencyReason = {
  readonly kind: 'not-exported';
  readonly modules: readonly string[];
} | {
  readonly kind: 'not-imported';
  readonly modules: readonly string[];
} | {
  readonly kind: 'not-provided';
};

interface UnresolvedDependency {

  readonly className: string;

  readonly moduleId: string;

  readonly parameters: readonly string[];
  readonly parameterIndex: number;

  readonly token: Token;
  readonly reason: UnresolvedDependencyReason;
}

declare class UnresolvedDependencyError extends Error implements UnresolvedDependency {
  readonly className: string;
  readonly moduleId: string;
  readonly parameters: readonly string[];
  readonly parameterIndex: number;
  readonly token: Token;
  readonly reason: UnresolvedDependencyReason;
  constructor(dependency: UnresolvedDependency, options?: ErrorOptions);
}
declare const ROOT_MODULE_ID = "__root__";

declare class Container {
  #private;
  constructor(options?: ContainerOptions);

  setLazyHook(hook: LazyResolutionHook): void;
  private claimLazyModule;
  private maybeDrainSync;
  register<T>(provider: Type<T> | ProviderDefinition<T>, declaringModuleId?: string): this;
  private registerClass;
  private registerOptions;
  private writeRegistration;
  registerScope(scope: ModuleScope): void;

  getModuleScope(moduleId: string): Readonly<ModuleScope> | undefined;
  markGlobalToken(token: Token): void;

  markRootDefaults(): void;
  setRequestInstance<K extends Token>(token: K & AuthoringToken<K>, value: NoInfer<InferToken<K>>): void;
  getDiagnostics(): Diagnostics;
  resolve<K extends Token>(token: K, requestingModuleId?: string): InferToken<K>;
  resolveAll<K extends Token>(token: K, requestingModuleId?: string): InferToken<K>[];

  getVisibleProviderSnapshots(token: Token, requestingModuleId?: string): readonly ProviderSnapshot[];

  private findAllRegistrations;

  has(token: Token): boolean;

  hasInScope(token: Token, moduleId: string): boolean;

  getOwnerModuleIds(token: Token): string[];

  replaceProvider<T>(provider: Type<T> | ProviderDefinition<T>, options?: {
    buckets?: 'all-existing' | 'root' | string[];
  }): this;

  isLazyPending(token: Token, moduleId?: string): boolean;

  isInstantiated(token: Token, moduleId?: string): boolean;

  getProviderScope(token: Token, moduleId?: string): Scope | undefined;

  getResolvedScope(token: Token, requestingModuleId?: string): Scope | undefined;
  private registerTokenDefault;
  getTokens(): Token[];

  getUseValues(): unknown[];

  getModuleDescriptions(): ModuleDescription[];

  computeEffectiveScopes(): void;

  supplyMissingDependencies(supply: (token: Token) => unknown): void;

  private dependencyTokensOf;
  private tryFindRegistration;

  createChild(): Container;

  sharesRootWith(other: Container): boolean;
  clear(): void;

  hasDisposables(): boolean;
  private rememberCallerOwned;
  private trackDisposable;

  dispose(): Promise<void>;
  private disposeOwned;
  private resolveRegistration;
  private assertNoSyncCycle;
  private resolveClass;

  private unresolvedDependency;
  private isUnresolvable;

  private unresolvedReason;
  private describeParameter;
  private resolveFactory;
  resolveAsync<K extends Token>(token: K, requestingModuleId?: string): Promise<InferToken<K>>;

  construct<T>(type: Type<T>, requestingModuleId?: string): Promise<T>;
  private runAsyncResolution;
  private constructAsync;
  private moduleRefFor;

  private isOptionalMissing;
  private reportHiddenOptional;
  private createLazyProxy;
}

interface VelaEnv {}

declare const ENV: InjectionToken<VelaEnv>;

declare function InjectEnv(): ParameterDecorator;

declare const MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES: number;

declare function broadcastCommandFits(value: unknown, maxFrameBytes?: number): value is BroadcastCommand;
declare function assertBroadcastCommandFits(value: unknown, maxFrameBytes?: number): asserts value is BroadcastCommand;

declare function webSocketSyncEnvelopeFits(serialized: string, maxFrameBytes: number): boolean;

interface RoomRegistry {

  register(client: WsClient): void;
  join(client: WsClient, room: string): void | Promise<void>;
  leave(client: WsClient, room: string): void | Promise<void>;

  leaveAll(client: WsClient): void | Promise<void>;

  deliverLocal(cmd: BroadcastCommand): void | Promise<void>;

  setDeliveryAuthorizer?(authorizer: (client: WsClient) => boolean | Promise<boolean>): void;
  localIdsInRoom(room: string): string[];
}

interface SyncDriver {
  readonly kind: string;

  bind(registry: RoomRegistry): void;

  dispatch(cmd: BroadcastCommand): void | Promise<void>;
  /** @internal */
  setMaxFrameBytes?(maxFrameBytes: number): void;
  countRoom?(room: string): Promise<number>;
  start?(): void | Promise<void>;
  stop?(): void | Promise<void>;
}

declare function local(): SyncDriver;

declare class InMemoryRoomRegistry implements RoomRegistry {
  private readonly rooms;
  private readonly clientRooms;
  private readonly clients;
  private deliveryAuthorizer?;
  setDeliveryAuthorizer(authorizer: (client: WsClient) => boolean | Promise<boolean>): void;
  register(client: WsClient): void;
  join(client: WsClient, room: string): void;
  leave(client: WsClient, room: string): void;
  leaveAll(client: WsClient): void;
  localIdsInRoom(room: string): string[];
  deliverLocal(cmd: BroadcastCommand): void | Promise<void>;
}

interface WsMessage<T = unknown> {
  id?: string;
  event: string;
  data?: T;
}

interface WsResponse<T = unknown> {
  event: string;
  data: T;
}

interface WsClient<TData = Record<string, unknown>> {
  readonly id: string;

  readonly path?: string;
  readonly rooms: ReadonlySet<string>;

  readonly maxFrameBytes?: number;
  data: TData;

  send(event: string, data?: unknown, id?: string): void;

  sendRaw(payload: string): void;

  trySendRaw?(payload: string): WebSocketSendResult;
  join(room: string): void | Promise<void>;
  leave(room: string): void | Promise<void>;

  commit(): void | Promise<void>;
  close(code?: number, reason?: string): void;

  readonly raw: unknown;
}

interface BroadcastOperator {
  to(room: string): BroadcastOperator;
  in(room: string): BroadcastOperator;
  except(room: string): BroadcastOperator;
  emit(event: string, data?: unknown): void | Promise<void>;
}

interface WsServer {
  emit(event: string, data?: unknown): void | Promise<void>;
  to(room: string): BroadcastOperator;
  in(room: string): BroadcastOperator;
  except(room: string): BroadcastOperator;
  /** @internal */
  setOutboundFrameLimit?(maxFrameBytes: number): void;

  forGateway?(gatewayPath: string, maxFrameBytes: number): WsServer;
}

interface WsExecutionContext extends ExecutionContext {
  getType(): 'ws';
  getContext(): never;
  getRequest(): never;
  switchToHttp(): never;
  switchToWs(): WsArgumentsHost;
}
interface OnGatewayInit {
  afterInit(server: WsServer): void | Promise<void>;
}
interface OnGatewayConnection {
  handleConnection(client: WsClient): void | Promise<void>;
}
interface OnGatewayDisconnect {
  handleDisconnect(client: WsClient): void | Promise<void>;
}

interface WebSocketPrincipal {
  issuer: string;
  subject: string;
  principalType: 'user' | 'service';
}

interface WebSocketUpgradeIdentity {
  principal: WebSocketPrincipal;
  tenantId: string;

  expiresAtMs: number;
}
interface WebSocketUpgradeAuthenticationContext {
  gatewayPath: string;
  room: string;

  ticket?: string;
}

interface UpgradeAuthenticator {
  authenticate(request: Request, context: WebSocketUpgradeAuthenticationContext): WebSocketUpgradeIdentity | false | Promise<WebSocketUpgradeIdentity | false>;
}
interface WebSocketGatewayOptions {

  path?: string;

  binding?: string;

  roomParam?: string;

  allowedOrigins?: '*' | readonly string[] | ((env: VelaEnv) => readonly string[]);

  authorizeUpgrade?: (request: Request) => boolean | Promise<boolean>;

  authenticator?: Type<UpgradeAuthenticator>;

  authorizeDelivery?: (client: WsClient) => boolean | Promise<boolean>;

  maxFrameBytes?: number;

  sendPolicy?: WebSocketSendPolicy;

  maxPendingMessages?: number;

  maxPendingBytes?: number;
}

interface SubscribeMessageMetadata {
  event: string;
  methodName: string;
}

interface ReservedWsEventMetadata {

  event: string;
}

interface ReservedWsEventHandler {
  handleReservedEvent(path: string, client: WsClient, message: WsMessage, context?: WsExecutionContext): void | Promise<void>;
  handleSocketClose?(path: string, client: WsClient): void | Promise<void>;
}

interface ForwardedWebSocketUpgrade {

  request: Request;

  gatewayPath: string;

  room: string;

  binding: string;

  identity: WebSocketUpgradeIdentity;
}

interface GatewayDelivery {

  gatewayPath: string;

  binding?: string;

  room: string;

  command: BroadcastCommand;
}

interface WebSocketTransport {

  createServer?(driver: SyncDriver): WsServer;

  deliver?(delivery: GatewayDelivery): Promise<void>;

  forwardUpgrade?(upgrade: ForwardedWebSocketUpgrade): Promise<Response>;

  readonly forwardingHeaders?: readonly string[];
}

type GatewayEventArgs<Events, Event extends keyof Events> = undefined extends Events[Event] ? [data?: Events[Event]] : [data: Events[Event]];

interface GatewayBroadcastOperator<Events extends object = Record<string, unknown>> {

  to(room: string): GatewayBroadcastOperator<Events>;

  in(room: string): GatewayBroadcastOperator<Events>;

  except(room: string): never;

  emit<Event extends keyof Events & string>(event: Event, ...data: GatewayEventArgs<Events, Event>): Promise<void>;
}

interface GatewayServer<Events extends object = Record<string, unknown>> {

  readonly path: string;

  to(room: string): GatewayBroadcastOperator<Events>;

  in(room: string): GatewayBroadcastOperator<Events>;

  emit(...args: never[]): never;

  except(room: string): never;
}

interface BroadcastCommand {

  rooms: string[];
  exceptRooms?: string[];
  exceptIds?: string[];

  gatewayPath?: string;

  frame: string;

  origin?: string;
}

interface HttpArgumentsHost {
  getRequest(): Request;
  getResponse(): VelaContext;
}

interface WsArgumentsHost {
  getClient(): WsClient;
  getData(): unknown;
  getPattern(): string;
}

type ContextType = string;

type HandlerFunction = (...args: never[]) => unknown;
interface ExecutionContext {
  getType(): ContextType;
  getClass(): Type;

  getHandler(): HandlerFunction;

  getHandlerName(): string | symbol;

  getModuleId(): string | undefined;

  getContainer(): Container | undefined;

  getContext(): VelaContext;

  getRequest(): Request;

  switchToHttp(): HttpArgumentsHost;

  switchToWs(): WsArgumentsHost;
}

interface HttpExecutionContext extends ExecutionContext {
  getType(): 'http';
  switchToWs(): never;
}
interface CanActivate {
  canActivate(context: ExecutionContext): boolean | Promise<boolean>;
}
interface CallHandler {
  handle(): Promise<unknown>;
}
interface NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Promise<unknown>;
}
interface ArgumentMetadata {
  type: string;

  metatype?: unknown;
  data?: string;
}
interface PipeTransform<T = unknown, R = unknown> {
  transform(value: T, metadata: ArgumentMetadata): R | Promise<R>;

  transformAsync?(value: T, metadata: ArgumentMetadata): Promise<R>;
}
interface ExceptionFilter<T = unknown> {
  catch(exception: T, context: ExecutionContext): unknown | Promise<unknown>;
}
interface NestMiddleware {
  use(c: VelaContext, next: Next): Promise<Response | void>;
}

export { Diagnostics as $, WebSocketUpgradeAuthenticationContext as A, METADATA_KEYS as At, SyncDriver as B, ReservedWsEventHandler as C, UnresolvedDependencyError as Ct, WebSocketGatewayOptions as D, describeToken as Dt, UpgradeAuthenticator as E, defineProvider as Et, WsResponse as F, VelaHonoEnv as Ft, ENV as G, broadcastCommandFits as H, WsServer as I, VelaMiddlewareHandler as It, Container as J, InjectEnv as K, InMemoryRoomRegistry as L, WsClient as M, Scope as Mt, WsExecutionContext as N, VelaContext as Nt, WebSocketPrincipal as O, forwardRef as Ot, WsMessage as P, VelaHono as Pt, DependencyToken as Q, MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES as R, OnGatewayInit as S, UnresolvedDependency as St, SubscribeMessageMetadata as T, assertFactoryInject as Tt, local as U, assertBroadcastCommandFits as V, webSocketSyncEnvelopeFits as W, Constructor as X, CheckedProviders as Y, ContainerOptions as Z, GatewayDelivery as _, ROOT_MODULE_ID as _t, ExecutionContext as a, InjectableOptions as at, OnGatewayConnection as b, TypedProviderLiteral as bt, HttpExecutionContext as c, MissingInjectionMetadataReason as ct, PipeTransform as d, ModuleVisibilityError as dt, FactoryInject as et, WsArgumentsHost as f, MultipleProvidersFoundError as ft, GatewayBroadcastOperator as g, ProviderSnapshot as gt, ForwardedWebSocketUpgrade as h, ProviderLiteral as ht, ExceptionFilter as i, InjectMetadata as it, WebSocketUpgradeIdentity as j, ParamType as jt, WebSocketTransport as k, HttpMethod as kt, NestInterceptor as l, ModuleDescription as lt, BroadcastOperator as m, ProviderDefinition as mt, CallHandler as n, InferToken as nt, HandlerFunction as o, InjectionToken as ot, BroadcastCommand as p, Provider as pt, VelaEnv as q, CanActivate as r, InferTokens as rt, HttpArgumentsHost as s, MissingInjectionMetadataError as st, ArgumentMetadata as t, ForwardRef as tt, NestMiddleware as u, ModuleScope as ut, GatewayEventArgs as v, Token as vt, ReservedWsEventMetadata as w, UnresolvedDependencyReason as wt, OnGatewayDisconnect as x, TypedToken as xt, GatewayServer as y, Type as yt, RoomRegistry as z };
```

### `<internal:types-http-version.d.ts>`

```ts
import { It as VelaMiddlewareHandler, J as Container, Mt as Scope, Nt as VelaContext, Pt as VelaHono, X as Constructor, d as PipeTransform, et as FactoryInject, i as ExceptionFilter, kt as HttpMethod, l as NestInterceptor, pt as Provider, r as CanActivate, rt as InferTokens, tt as ForwardRef, u as NestMiddleware, vt as Token, xt as TypedToken, yt as Type } from "<internal:types-http-hono.types.d.ts>";
import { r as RouteContractMetadata } from "<internal:route-contract-http-route-contract.d.ts>";
import { Context, MiddlewareHandler } from "hono";
import { RedirectStatusCode, StatusCode } from "hono/utils/http-status";

declare const VERSION_NEUTRAL: unique symbol;

type RouteVersion = number | typeof VERSION_NEUTRAL;

type VersionValue = RouteVersion | readonly RouteVersion[];

interface RouteMetadata {
  method: HttpMethod;
  path: string;
  handlerName: string | symbol;
  version?: VersionValue;

  name?: string;

  contract?: RouteContractMetadata;
}

interface RouteInputValues {
  readonly params?: unknown;
  readonly query?: unknown;
  readonly body?: unknown;
}

interface ParamExtractionRoute {
  readonly method: string;
  readonly contract?: RouteContractMetadata;

  readonly source: string;

  readonly paths: readonly string[];

  readonly input?: (c: Context) => Promise<RouteInputValues>;
}

type ParamReader = ((c: Context, pipes: readonly PipeTransform[]) => unknown) & {
  readonly skips?: (pipe: PipeTransform) => boolean;
};

type ParamExtractorFactory = (route: ParamExtractionRoute, param: ParamMetadata, metatype: unknown) => ParamReader;
interface ControllerOptions {
  path?: string;
  version?: VersionValue;

  scope?: Scope;
}
interface ControllerMetadata {
  prefix: string;
  version?: VersionValue;
}
interface ParamMetadata {
  index: number;
  type: string;
  name?: string;
  pipes?: PipeType[];
  factory?: (data: unknown, ctx: Context) => unknown;

  extract?: ParamExtractorFactory;

  metatype?: unknown;
}
interface ControllerRegistration {
  controller: Type;

  moduleId: string;
  metadata: ControllerMetadata;
  routes: RouteMetadata[];
}

type ComponentType = 'middleware' | 'guard' | 'pipe' | 'interceptor' | 'filter';
type MiddlewareType = Type<NestMiddleware> | NestMiddleware;
type GuardType = Type<CanActivate> | CanActivate;
type PipeType = Type<PipeTransform> | PipeTransform;
type InterceptorType = Type<NestInterceptor> | NestInterceptor;
type FilterType = Type<ExceptionFilter> | ExceptionFilter;
interface ComponentTypeMap {
  middleware: MiddlewareType;
  guard: GuardType;
  pipe: PipeType;
  interceptor: InterceptorType;
  filter: FilterType;
}
interface RouteDefinition {
  method: string;
  path: string;
  handlerName: string | symbol;
  version?: VersionValue;

  name?: string;

  contract?: RouteContractMetadata;
}
interface ParameterMetadata {
  index: number;
  type: string;
  name?: string;
  pipes?: PipeType[];
  factory?: (data: unknown, ctx: Context) => unknown;

  extract?: ParamExtractorFactory;

  metatype?: unknown;
}
interface HttpHandlerMeta {
  httpCode?: StatusCode;
  responseHeaders?: Array<[string, string]>;
  redirect?: {
    url: string;
    statusCode: RedirectStatusCode;
  };

  respond?: (c: Context, result: unknown, onStreamError: (error: unknown) => void) => Response;
}
type ModuleImport = Type | DynamicModule | ForwardRef;

type AsyncModuleOptions<T = unknown, Inject extends readonly Token[] = readonly Token[]> = {
  imports?: ModuleImport[];
  useFactory: (...args: InferTokens<Inject>) => T | Promise<T>;
} & FactoryInject<Inject>;
interface DynamicModule {
  module: Type;

  key?: string;
  imports?: ModuleImport[];

  providers?: Provider[];
  controllers?: Type[];
  exports?: Token[];

  global?: boolean;

  lazy?: boolean;
}

interface ModuleOptions {
  providers?: readonly Provider[];
  controllers?: Type[];
  imports?: ModuleImport[];
  exports?: Token[];

  lazy?: boolean;
}

interface ModuleRecord extends ModuleOptions {
  global?: boolean;
  /** @internal */
  hostOnly?: true;
}
interface ModuleMetadata {
  providers: readonly Provider[];
  controllers: Type[];
  imports: ModuleImport[];
  exports: Token[];

  global: boolean;
  lazy: boolean;
}

interface CorsOptions {

  origin?: string | string[] | ((origin: string) => string | undefined | null);

  allowMethods?: string[];

  allowHeaders?: string[];

  exposeHeaders?: string[];

  credentials?: boolean;

  maxAge?: number;
}

interface RouteInfo {
  path: string;
  method?: HttpMethod;

  absolute?: boolean;
}
interface MiddlewareRouteDefinition {

  readonly moduleId?: string;
  middleware: Array<Type<NestMiddleware> | NestMiddleware>;

  routes: Array<RouteInfo | Constructor>;
  excludes: RouteInfo[];

  priority?: number;
}
interface MiddlewareConfigProxy {

  exclude(...routes: Array<string | RouteInfo>): MiddlewareConfigProxy;
  withPriority(priority: number): MiddlewareConfigProxy;

  forRoutes(...routes: Array<string | Constructor | RouteInfo>): MiddlewareConsumer;
}
interface MiddlewareConsumer {
  apply(...middleware: Array<Type<NestMiddleware> | NestMiddleware>): MiddlewareConfigProxy;
}
interface NestModule {
  configure(consumer: MiddlewareConsumer): void;
}

interface GlobalPrefixOptions {

  exclude?: Array<string | RouteInfo>;
}

interface VersioningOptions {

  prefix?: string | false;
}

interface RoutePathOptions {
  globalPrefix?: string;
  globalPrefixOptions?: GlobalPrefixOptions;
  versioning?: VersioningOptions;
}

interface HttpRequestCompletion {

  readonly status: number | undefined;

  readonly route: string | undefined;
  readonly outcome: 'success' | 'error' | 'cancelled';
  readonly durationMs: number;
}
interface HttpRequestObservation {

  complete(completion: HttpRequestCompletion): void;
}

type HttpRequestObserver = (context: VelaContext, container: Container) => HttpRequestObservation | undefined;

interface VelaBodyLimitOverride {

  path: string;

  methods?: string[];

  maxBytes: number | false;
}
interface VelaBodySecurityOptions {

  maxBytes?: number | false;

  streamingOverrides?: VelaBodyLimitOverride[];
}
interface VelaQuerySecurityOptions {

  maxParameters?: number | false;

  maxDepth?: number | false;

  maxBytes?: number | false;
}

interface VelaSecurityOptions {
  body?: VelaBodySecurityOptions;
  query?: VelaQuerySecurityOptions;
}
declare const DEFAULT_QUERY_PARAMETER_LIMIT = 100;
declare const DEFAULT_QUERY_DEPTH_LIMIT = 5;
declare const DEFAULT_QUERY_BYTES_LIMIT: number;

interface RouteDescription {

  method: string;

  path: string;
  controller: string;
  handler: string;

  moduleId: string;
  version?: number;

  name?: string;
}
interface RouteManagerOptions {

  getClientIp?: (c: VelaContext) => string | null;
  middleware?: VelaMiddlewareHandler[];

  globalPrefix?: string;

  globalPrefixOptions?: GlobalPrefixOptions;

  versioning?: VersioningOptions;

  ambientContainer?: boolean;

  bodyLimit?: number | false;

  security?: VelaSecurityOptions;

  cors?: CorsOptions | boolean;
}

declare const DEFAULT_BODY_LIMIT_BYTES: number;

declare function trackResponseStream(body: ReadableStream<Uint8Array>, onFinish?: (outcome: HttpRequestCompletion['outcome']) => void): {
  body: ReadableStream<Uint8Array>;
  done: Promise<void>;
};
declare class RouteManager {
  private container;
  private static readonly METHOD_REGISTRAR;
  private controllers;
  private globalMiddleware;
  private globalPipes;
  private globalGuards;
  private globalInterceptors;
  private globalFilters;
  private globalPrefix;
  private globalPrefixOptions;
  private readonly versioning;
  private consumerMiddlewareDefinitions;
  private readonly routeOwners;
  private routeDescriptions;
  private readonly requestObservers;
  private readonly handlerExecutor;
  private readonly ambientContainer;
  private readonly bodyLimit;
  private readonly bodyLimitCap;
  private readonly bodyLimitOverrides;
  private readonly queryMaxParameters;
  private readonly queryMaxDepth;
  private readonly queryMaxBytes;
  private readonly clientIpResolver;
  private corsHandler;
  private corsOwner;
  constructor(container: Container, options?: RouteManagerOptions);

  getGlobalComponents(): {
    guards: Array<GuardType | TypedToken<CanActivate>>;
    pipes: Array<PipeType | TypedToken<PipeTransform>>;
    interceptors: Array<InterceptorType | TypedToken<NestInterceptor>>;
    filters: Array<FilterType | TypedToken<ExceptionFilter>>;
  };

  resolveClientIp(c: VelaContext): string | null;
  private validateBodyLimitOverride;
  private resolveBodyLimit;
  registerConsumerMiddleware(definitions: MiddlewareRouteDefinition[]): this;

  enableCors(options?: CorsOptions): this;
  /** @internal */
  reserveCors(owner: string): void;

  setGlobalPrefix(prefix: string, options?: GlobalPrefixOptions): this;
  useGlobalMiddleware(...middleware: MiddlewareType[]): this;
  useGlobalMiddlewareTokens(...middlewareTokens: Array<TypedToken<NestMiddleware>>): this;
  useGlobalPipes(...pipes: PipeType[]): this;
  useGlobalPipeTokens(...pipeTokens: Array<TypedToken<PipeTransform>>): this;
  useGlobalGuards(...guards: GuardType[]): this;
  useGlobalGuardTokens(...guardTokens: Array<TypedToken<CanActivate>>): this;
  private addGlobalGuards;
  private registeredPhaseRank;
  useGlobalInterceptors(...interceptors: InterceptorType[]): this;
  useGlobalInterceptorTokens(...interceptorTokens: Array<TypedToken<NestInterceptor>>): this;
  useGlobalFilters(...filters: FilterType[]): this;
  useGlobalFilterTokens(...filterTokens: Array<TypedToken<ExceptionFilter>>): this;
  private getMiddlewarePriority;
  private componentTarget;
  private getRequestContainer;
  private wrapMiddlewareWithFilters;
  private mapMiddlewareError;
  private rejectRequest;
  private filterOrRender;
  registerController(controller: Type, moduleId?: string): this;

  getRouteDescriptions(): RouteDescription[];

  getGlobalPrefix(): string;

  getRoutePathOptions(): RoutePathOptions;

  observeRequests(observer: HttpRequestObserver): () => void;
  build(): Promise<VelaHono>;
  private checkMiddlewareTargets;
  private registerRoute;
  private compileTargets;
  private resolveTarget;
  getControllers(): ControllerRegistration[];
}

interface VelaRouteMap {}

type RouteName = keyof VelaRouteMap extends never ? string : Extract<keyof VelaRouteMap, string>;

type RouteParams<N extends RouteName> = N extends keyof VelaRouteMap ? VelaRouteMap[N] : Record<string, string> | undefined;

type InvocationTransport = (request: Request) => Promise<Response>;

interface InvocationRouteTarget<N extends RouteName = RouteName> {
  route: N;
  params?: RouteParams<N>;
}

interface InvocationPathTarget {
  path: string;
}

type InvocationTarget = InvocationRouteTarget | InvocationPathTarget;

interface RunInit {

  method?: string;

  body?: unknown;

  headers?: HeadersInit;

  ttlSeconds?: number;

  iss?: string;

  signal?: AbortSignal;

  timeoutMs?: number;
}

interface NonceStore {
  claim(nonce: string, expEpochSeconds: number): Promise<boolean>;
}

export { NestModule as A, InterceptorType as B, HttpRequestObservation as C, VersioningOptions as D, RoutePathOptions as E, ComponentTypeMap as F, ModuleRecord as G, ModuleImport as H, DynamicModule as I, RouteDefinition as J, ParameterMetadata as K, FilterType as L, CorsOptions as M, AsyncModuleOptions as N, MiddlewareConsumer as O, ComponentType as P, VersionValue as Q, GuardType as R, HttpRequestCompletion as S, GlobalPrefixOptions as T, ModuleMetadata as U, MiddlewareType as V, ModuleOptions as W, RouteVersion as X, ControllerOptions as Y, VERSION_NEUTRAL as Z, DEFAULT_QUERY_PARAMETER_LIMIT as _, NonceStore as a, VelaQuerySecurityOptions as b, RouteParams as c, RouteDescription as d, RouteManager as f, DEFAULT_QUERY_DEPTH_LIMIT as g, DEFAULT_QUERY_BYTES_LIMIT as h, InvocationTransport as i, RouteInfo as j, MiddlewareRouteDefinition as k, VelaRouteMap as l, trackResponseStream as m, InvocationRouteTarget as n, RunInit as o, RouteManagerOptions as p, PipeType as q, InvocationTarget as r, RouteName as s, InvocationPathTarget as t, DEFAULT_BODY_LIMIT_BYTES as u, VelaBodyLimitOverride as v, HttpRequestObserver as w, VelaSecurityOptions as x, VelaBodySecurityOptions as y, HttpHandlerMeta as z };
```

### `<internal:url-generator.service.d.ts>`

```ts
import { a as ExecutionContext, q as VelaEnv } from "<internal:types-http-hono.types.d.ts>";
import { Q as VersionValue, Y as ControllerOptions, c as RouteParams, f as RouteManager, q as PipeType, s as RouteName } from "<internal:types-http-version.d.ts>";
import { r as ValidationSchema } from "<internal:parse-schema.d.ts>";
import { a as RouteHandlerResult, l as RouteResponseOptions, n as RouteBodyOptions } from "<internal:route-contract-http-route-contract.d.ts>";
import { n as RouteContract } from "<internal:route-contract-contract-route-contract.d.ts>";
import { RedirectStatusCode, StatusCode } from "hono/utils/http-status";

declare function Controller(pathOrOptions?: string | ControllerOptions): ClassDecorator;

declare function Version(version: VersionValue): MethodDecorator;

interface RouteOptions extends RouteResponseOptions {

  name?: string;

  body?: RouteBodyOptions;
}

type RouteMethodDecorator<Result> = unknown extends Result ? (target: object, propertyKey: string | symbol, descriptor: PropertyDescriptor) => void : <Handler extends (...args: never[]) => Result | Promise<Result>>(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

interface HttpMethodDecorator<Method extends string> {
  <const Contract extends RouteContract<Method>>(contract: Contract): RouteMethodDecorator<RouteHandlerResult<Contract>>;
  <const Contract extends RouteContract<Method>>(path: string, contract: Contract): RouteMethodDecorator<RouteHandlerResult<Contract>>;
  <const Options extends RouteOptions & {
    readonly method?: never;
  }>(options: Options): RouteMethodDecorator<RouteHandlerResult<Options>>;
  <const Options extends RouteOptions & {
    readonly method?: never;
  } = {}>(path?: string, options?: Options): RouteMethodDecorator<RouteHandlerResult<Options>>;
}
declare const Get: HttpMethodDecorator<"GET">;
declare const Post: HttpMethodDecorator<"POST">;
declare const Put: HttpMethodDecorator<"PUT">;
declare const Patch: HttpMethodDecorator<"PATCH">;
declare const Delete: HttpMethodDecorator<"DELETE">;
declare const Options: HttpMethodDecorator<"OPTIONS">;
declare const Head: HttpMethodDecorator<"HEAD">;
declare const All: HttpMethodDecorator<"ALL">;

interface SchemaParamDecorator {
  (schema: ValidationSchema, ...pipes: PipeType[]): ParameterDecorator;
  (name: string, schema: ValidationSchema, ...pipes: PipeType[]): ParameterDecorator;
  (nameOrPipe?: string | PipeType, ...pipes: PipeType[]): ParameterDecorator;
}
type PipedParamDecorator = (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;
declare const Param: SchemaParamDecorator;
declare const Query: SchemaParamDecorator;

declare const Body: SchemaParamDecorator;
declare const Headers: SchemaParamDecorator;

declare const Req: PipedParamDecorator;

declare const Ctx: PipedParamDecorator;

declare const Res: PipedParamDecorator;
declare const Ip: PipedParamDecorator;
declare const Cookie: SchemaParamDecorator;

declare const Cookies: SchemaParamDecorator;

declare function RawBody(): ParameterDecorator;

type CustomParamDecorator<TData> = (...args: undefined extends TData ? [data?: TData, ...pipes: PipeType[]] : [data: TData, ...pipes: PipeType[]]) => ParameterDecorator;
declare function createParamDecorator<TData = unknown>(factory: (data: TData, ctx: ExecutionContext) => unknown): CustomParamDecorator<TData>;

declare function HttpCode(statusCode: StatusCode): MethodDecorator;

declare function Header(name: string, value: string): MethodDecorator;

declare function Redirect(url: string, statusCode?: RedirectStatusCode): MethodDecorator;

type ComposedDecorator = <T>(target: T, propertyKey?: string | symbol, descriptor?: PropertyDescriptor | number) => void;

type ComposableDecorator = ClassDecorator | MethodDecorator | PropertyDecorator | ParameterDecorator | ((target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<never>) => unknown);
declare function applyDecorators(...decorators: ComposableDecorator[]): ComposedDecorator;

type UrlParamValue = string | number | boolean;

interface UrlForOptions {

  query?: Record<string, UrlParamValue>;
}

interface SignedUrlGenerateOptions {

  expiresIn: number;

  method?: string;

  secret?: string;
}

declare class UrlGeneratorService {
  #private;
  private readonly routeManager;
  private routeMap;
  constructor(routeManager: RouteManager, secretToken?: string, env?: VelaEnv);

  urlFor<N extends RouteName>(name: N, params?: RouteParams<N>, opts?: UrlForOptions): string;

  signedUrl<N extends RouteName>(name: N, params: RouteParams<N> | undefined, options: SignedUrlGenerateOptions): Promise<string>;

  private routes;
}

export { SchemaParamDecorator as A, Query as C, Res as D, Req as E, applyDecorators as M, createParamDecorator as N, RouteMethodDecorator as O, Put as S, Redirect as T, Ip as _, Body as a, Patch as b, Cookies as c, Get as d, Head as f, HttpMethodDecorator as g, HttpCode as h, All as i, Version as j, RouteOptions as k, Ctx as l, Headers as m, UrlForOptions as n, Controller as o, Header as p, UrlGeneratorService as r, Cookie as s, SignedUrlGenerateOptions as t, Delete as u, Options as v, RawBody as w, Post as x, Param as y };
```
