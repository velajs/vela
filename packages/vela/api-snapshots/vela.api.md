# `@velajs/vela` public API

Generated by `pnpm api:update` from the built declaration graph. Verified by
`pnpm api:check`; do not edit by hand. Documentation comments, content-hashed
chunk names, and line endings are normalized. Private/protected declarations remain
because they can affect TypeScript assignability.

## Ordered package export map

Object condition order is resolution-significant, so this tree is kept verbatim.

```json
{
  ".": {
    "types": "./dist/index.d.ts",
    "import": "./dist/index.js"
  },
  "./internal": {
    "types": "./dist/internal.d.ts",
    "import": "./dist/internal.js"
  },
  "./streaming": {
    "types": "./dist/streaming/index.d.ts",
    "import": "./dist/streaming/index.js"
  },
  "./schedule-node": {
    "types": "./dist/schedule-node/index.d.ts",
    "import": "./dist/schedule-node/index.js"
  },
  "./websocket": {
    "types": "./dist/websocket/index.d.ts",
    "import": "./dist/websocket/index.js"
  },
  "./websocket-node": {
    "types": "./dist/websocket-node/index.d.ts",
    "import": "./dist/websocket-node/index.js"
  },
  "./i18n": {
    "types": "./dist/i18n/index.d.ts",
    "import": "./dist/i18n/index.js"
  },
  "./queue": {
    "types": "./dist/queue/index.d.ts",
    "import": "./dist/queue/index.js"
  },
  "./live": {
    "types": "./dist/live/index.d.ts",
    "import": "./dist/live/index.js"
  },
  "./storage": {
    "types": "./dist/storage/index.d.ts",
    "import": "./dist/storage/index.js"
  },
  "./seeder": {
    "types": "./dist/seeder/index.d.ts",
    "import": "./dist/seeder/index.js"
  },
  "./validation": {
    "types": "./dist/validation/index.d.ts",
    "import": "./dist/validation/index.js"
  }
}
```

## `.`

Type conditions: `types`

Declaration entry: `./dist/index.d.ts`

```ts
import { A as Head, B as Query, C as All, D as Cookies, E as Cookie, F as Options, G as RouteOptions, H as Redirect, I as Param, J as applyDecorators, K as Sse, L as Patch, M as Headers, N as HttpCode, O as Delete, P as Ip, R as Post, S as UrlGeneratorService, T as Controller, U as Req, V as RawBody, W as Res, X as defineMetadata, Y as createParamDecorator, Z as getMetadata, _ as SignedInvocation, a as IntervalMetadata, b as SignedUrlGenerateOptions, c as ScheduleJobRef, d as parseCron, f as BeforeApplicationShutdown, g as OnModuleInit, h as OnModuleDestroy, i as CronMetadata, j as Header, k as Get, l as CronMatcher, m as OnApplicationShutdown, n as RegisteredIntervalJob, o as ScheduleDispatchMode, p as OnApplicationBootstrap, q as Version, r as ScheduleRegistry, s as ScheduleInvocation, t as RegisteredCronJob, u as CronOptions, v as SignedInvocationGuard, w as Body, x as UrlForOptions, y as InternalDispatcher, z as Put } from "<internal:schedule.registry.d.ts>";
import { $ as HttpMethod, A as Constructor, B as ModuleDescription, D as WsResponse, E as WsMessage, F as InferToken, G as ProviderSnapshot, H as ModuleVisibilityError, I as InferTokens, J as Type, K as ROOT_MODULE_ID, M as DependencyToken, N as Diagnostics, O as WsServer, P as ForwardRef, Q as forwardRef, R as InjectableOptions, T as WsExecutionContext, U as MultipleProvidersFoundError, V as ModuleScope, W as ProviderDefinition, X as defineProvider, Y as TypedToken, Z as describeToken, _ as ReservedWsEventHandler, a as ExecutionContext, at as VelaHonoEnv, c as NestInterceptor, d as WsArgumentsHost, et as METADATA_KEYS, f as BroadcastCommand, g as OnGatewayInit, h as OnGatewayDisconnect, i as ExceptionFilter, it as VelaHono, j as ContainerOptions, k as Container, l as NestMiddleware, m as OnGatewayConnection, n as CallHandler, nt as Scope, o as HttpArgumentsHost, ot as VelaMiddlewareHandler, q as Token, r as CanActivate, rt as VelaContext, s as HttpExecutionContext, t as ArgumentMetadata, tt as ParamType, u as PipeTransform, v as ReservedWsEventMetadata, w as WsClient, z as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { $ as registerEntrypointKind, A as ModuleRef, B as ApiResponseOptions, C as INVOCATION_SIGNING_SECRET, D as RequestContext, E as REQUEST_CONTEXT, F as AdapterContext, G as OpenApiInfo, H as HttpVerb, I as RuntimeAdapter, J as OpenApiPathItem, K as OpenApiOperation, L as VelaApplication, M as BootstrapResult, N as bootstrap, O as RequestContextKey, Q as getEntrypointKinds, R as ApiDocMetadata, T as NONCE_STORE, U as JsonSchema, V as CreateOpenApiDocumentOptions, W as OpenApiDocument, X as OpenApiResponse, Y as OpenApiRequestBody, Z as EntrypointRegistry, _ as Module, a as APP_INTERCEPTOR, c as ERROR_CATALOG, d as Plugin, et as ErrorMatcher, f as PluginRegistry, g as Global, h as definePlugin, i as APP_GUARD, j as BootstrapOptions, m as composePlugins, n as APP_EXCEPTION_HANDLER, nt as ExceptionHandler, o as APP_MIDDLEWARE, p as PluginRootModule, q as OpenApiParameter, r as APP_FILTER, rt as matchesAny, s as APP_PIPE, t as MetadataRegistry, tt as ErrorReportContext, u as PLUGIN_REGISTRY_TOKEN, v as defineDynamicModule, x as getRequestContainer, z as ApiResponseEntry } from "<internal:metadata.registry.d.ts>";
import { A as DiscoveredClass, D as Entrypoint, E as ContributesEntrypoints, F as DiscoveryService, H as DynamicModule, I as CreateDiscoverableDecoratorOptions, J as ModuleImport, K as InterceptorType, L as DiscoverableDecorator, M as DiscoveredRegisteredMethodMeta, N as DiscoveredRegistration, O as EntrypointKind, P as DiscoveryFilter, Q as PipeType, R as createDiscoverableDecorator, S as MiddlewareConsumer, T as RouteInfo, U as FilterType, W as GuardType, X as ModuleOptions, _ as VelaBodyLimitOverride, a as NonceStore, b as VelaSecurityOptions, c as RouteParams, d as RouteDescription, g as DEFAULT_QUERY_PARAMETER_LIMIT, h as DEFAULT_QUERY_DEPTH_LIMIT, i as InvocationTransport, j as DiscoveredMethodMeta, k as contributesEntrypoints, l as VelaRouteMap, m as DEFAULT_QUERY_BYTES_LIMIT, n as InvocationRouteTarget, o as RunInit, q as MiddlewareType, r as InvocationTarget, s as RouteName, t as InvocationPathTarget, u as DEFAULT_BODY_LIMIT_BYTES, v as VelaBodySecurityOptions, w as NestModule, y as VelaQuerySecurityOptions, z as AsyncModuleOptions } from "<internal:types-registry-types.d.ts>";
import { $ as SetMetadata, $a as setTrustedRequestTenant, $i as Awaitable, $r as WebSocketServer, $t as ModuleRegistrationOptions, A as defineSerializer, Aa as ConfigPathValue, Ai as ResponseCacheModule, An as HttpException, Ao as VelaFactory, At as getExecutionLifetime, B as ParseArrayPipe, Ba as LoggerLevelName, Bi as CACHE_MANAGER, Bn as UnauthorizedException, Bt as DefineModuleSpec, C as LogDeliveryContext, Ca as CONFIG_ENV, Ci as defineEventVocabulary, Cn as ResponseCheckCallback, Co as EndpointDefinition, Ct as resolveEntrypoint, D as LogSink, Da as ConfigModule, Di as ResponseCacheInterceptor, Dn as ForbiddenException, Do as defineEndpoint, Dt as ExecutionScopeOptions, E as LogSerializationOptions, Ea as ConfigStore, Ei as CacheResponse, En as ConflictException, Eo as EndpointSchema, Er as assertWebSocketRoomId, Et as ExecutionScope, F as ErrorReporter, Fa as InferConfigType, Fi as ResponseCacheOptions, Fn as NotImplementedException, Fr as WsDispatcher, Ft as provideGlobal, G as ParseIntPipe, Ga as TrustedRequestIdentityStore, Gi as Cacheable, Gt as ConfigurableModuleAsyncOptions, H as ParseBoolPipe, Ha as Writer, Hi as CACHE_TTL_METADATA, Hn as UnsupportedMediaTypeException, Hr as RoomRegistry, Ht as ModuleContributions, I as resolveErrorReporter, Ia as registerAs, Ii as ResponseCacheScope, In as PayloadTooLargeException, It as sideEffectModule, J as RequiredPipe, Ja as clearTrustedRequestIdentity, Ji as CacheInterceptor, Jr as ConnectedSocket, Jt as ConfigurableModuleExtras, K as ParseUUIDPipe, Ka as TrustedRequestPrincipal, Ki as TieredCacheStore, Kt as ConfigurableModuleBuilderOptions, L as ErrorsModule, La as ContextProvider, Li as ScopedResponseCache, Ln as RequestTimeoutException, Lt as ConfigurableModuleBuilder, M as SERIALIZE_METADATA, Ma as AnyConfigNamespace, Mi as CacheInvalidationStore, Mn as MethodNotAllowedException, Mt as LazyProviderSpec, N as SerializationDescriptor, Na as ConfigNamespace, Ni as CacheResponseOptions, Nn as NotAcceptableException, Nt as lazyProvider, O as LogValue, Oa as ConfigModuleOptions, Oi as RESPONSE_CACHE_OPTIONS, On as GatewayTimeoutException, Oo as createOpenApiDocument, Ot as createExecutionScope, P as Serialize, Pa as ConfigType, Pi as ResponseCacheEntryOptions, Pn as NotFoundException, Pt as moduleToken, Q as Reflector, Qa as setTrustedRequestIdentity, Qi as AsyncCacheStore, Qr as WebSocketGateway, Qt as DefineConfigurableModuleSpec, R as ErrorsModuleOptions, Ra as LogLevel, Ri as CACHEABLE_METADATA, Rn as ServiceUnavailableException, Rr as WebSocketModule, Rt as defineConfigurableModule, S as ApplicationLoggerOptions, Sa as SecurityModuleOptions, Si as defineEvent, Sn as HealthIndicatorResult, So as Endpoint, St as getEntrypointModuleId, T as LogRecord, Ta as ConfigService, Ti as MemoryCacheInvalidationStore, Tn as BadRequestException, To as EndpointRequest, Tt as ExecutionLifetime, U as ParseEnumPipe, Ua as buildExecutionContext, Ui as CacheKey, Un as HealthModule, Ur as SyncDriver, Ut as ModuleSetupContext, V as ParseArrayPipeOptions, Va as LoggerService, Vi as CACHE_MODULE_OPTIONS, Vn as UnprocessableEntityException, Vt as GlobalComponentSlot, W as ParseFloatPipe, Wa as TrustedRequestIdentity, Wi as CacheTTL, Wt as defineModule, X as CreateDecoratorOptions, Xa as getTrustedContextRequest, Xi as CacheModule, Xn as RESERVED_WS_EVENT_PREFIX, Xr as ReservedWsEvent, Xt as ConfigurableModuleHost, Y as ZodValidationPipe, Ya as createTrustedRequestIdentityStore, Yi as CacheService, Yn as trySendWebSocketFrame, Yr as MessageBody, Yt as ConfigurableModuleExtrasTransform, Z as ReflectableDecorator, Za as getTrustedRequestIdentity, Zi as AnyCacheStore, Zr as SubscribeMessage, Zt as ConfigurableModuleOptionsFactory, _ as APP_LOGGER, _a as Secret, _i as OnEventMetadata, _n as HealthIndicatorService, _o as Optional, _t as RouteContributorOpenApiContext, a as VelaError, aa as CORS_OPTIONS, ai as Cron, an as SkipThrottle, ao as verifyInvocation, at as UsePipes, b as StructuredLogger, ba as SecurityCorsOptions, bi as EventPayload, bn as HealthCheckStatus, bo as ApiResponse, bt as EntrypointExecutionContext, c as defineErrorCatalog, ca as HTTP_MODULE_OPTIONS, ci as EventDispatcher, cn as ThrottlerGuard, co as getCurrentContainer, ct as PipelineComponentEntry, d as isVelaError, da as HttpModule, di as EventListenerDecorator, dn as ThrottleConfig, do as SignedUrl, dt as resolveScopedComponents, ea as CacheEntry, ei as parseCronMetadata, en as stableHash, eo as INVOCATION_AUDIENCE, et as Catch, f as toErrorBody, fa as HttpModuleOptions, fi as OnEvent, fn as ThrottlerModuleOptions, fo as SignedUrlGuard, ft as resolveScopedComponentsAsync, g as serializeLogValue, ga as SECURITY_OPTIONS, gi as EventHandler, gn as HttpPingOptions, go as Injectable, gt as RouteContributorContext, h as parseLogDirective, ha as buildSecurityMiddleware, hi as EventEmitOptions, hn as HttpHealthIndicator, ho as Inject, ht as RouteContributor, i as ErrorCatalogEntry, ia as CacheStore, ii as SCHEDULE_DISPATCH, in as THROTTLE_METADATA, io as signInvocation, it as UseMiddleware, j as SerializerInterceptor, ja as ConfigSchema, ji as CacheInvalidationResult, jn as InternalServerErrorException, jt as runInEntrypointScope, k as SerializerDefinition, ka as ConfigPath, ki as ResponseCacheService, kn as GoneException, ko as VelaCreateOptions, kt as finishExecutionScope, l as env, la as HttpRequestException, li as ScopedEventDispatcher, ln as ThrottlerModule, lo as getCurrentRequestContext, lt as ResolvedComponentMap, m as LogThresholds, ma as HttpResponse, mi as EventEmitter, mn as ThrottlerStore, mo as mixin, mt as PipelineRunner, n as Catalog, na as CacheEntryWriter, ni as CRON_METADATA, nn as THROTTLER_OPTIONS, no as InvocationClaim, nt as UseGuards, o as VelaErrorOptions, oa as CorsModule, oi as Interval, on as Throttle, oo as MemoryNonceStore, or as WsException, ot as getCatchTypes, p as loggerForScope, pa as HttpRequestConfig, pi as EventEmitterSubscriber, pn as ThrottlerStorageRecord, po as createLazyParamDecorator, pt as PipelineRunOptions, q as ParseUUIDPipeOptions, qa as bindTrustedRequestContext, qi as MemoryCacheStore, qt as ConfigurableModuleClassType, r as ErrorBodyResult, ra as CacheModuleOptions, ri as INTERVAL_METADATA, rn as THROTTLER_STORAGE, ro as VerifyInvocationOptions, rt as UseInterceptors, s as composeCatalogs, sa as CorsOptions, si as ScheduleModule, sn as ThrottlerStorage, so as enableAmbientContainer, st as shouldFilterCatch, t as CORE_CATALOG, ta as CacheEntryReader, ti as parseIntervalMetadata, tn as SKIP_THROTTLE_METADATA, to as INVOCATION_DEFAULT_TTL_SECONDS, tr as WS_SERVER, tt as UseFilters, u as getRuntimeKey, ua as HttpService, ui as ON_EVENT_METADATA, un as RateLimitInfo, uo as URL_SIGNING_SECRET, ut as resolvePipelineComponents, v as LoggingModule, va as SecurityModule, vi as EventDefinition, vn as HealthCheckService, vo as zodToJsonSchema, vt as getRouteContributors, w as LogFields, wa as CONFIG_OPTIONS, wi as EventEmitterModule, wn as BadGatewayException, wo as EndpointHandlerOutput, wt as EXECUTION_LIFETIME, x as consoleLogSink, xa as SecurityHeadersOptions, xi as EventVocabulary, xn as HealthIndicatorFunction, xo as ApiTags, xt as buildEntrypointExecutionContext, y as ApplicationLogger, ya as OriginProtectionOptions, yi as EventInput, yn as HealthCheckResult, yo as ApiDoc, yt as registerRouteContributor, z as DefaultValuePipe, za as Logger, zi as CACHE_KEY_METADATA, zn as TooManyRequestsException, zr as WebSocketModuleOptions, zt as moduleKey } from "<internal:index-factory.d.ts>";
import { _ as StandardSchemaV1, a as isValidationSchema, b as standardJsonSchema, c as DtoDefinition, d as RuntimeParser, f as SchemaParser, g as StandardJSONSchemaV1, h as SchemaValidationError, i as ValidationSchema, l as DtoOptions, m as defineDto, n as SchemaInput, o as parseSchema, p as StandardDtoDefinition, r as SchemaOutput, s as parseSchemaAsync, t as ValidationPipe, u as DtoSchema, v as ValidationIssue, x as validateSchema, y as isStandardSchema } from "<internal:index-validation-standard-schema.d.ts>";
import { a as signUrl, i as VerifySignedUrlOptions, n as STORAGE_SIGNED_URL_PURPOSE, o as verifySignedUrl, r as SignedUrlOptions, t as HTTP_SIGNED_URL_PURPOSE } from "<internal:signed-url.d.ts>";
export { APP_EXCEPTION_HANDLER, APP_FILTER, APP_GUARD, APP_INTERCEPTOR, APP_LOGGER, APP_MIDDLEWARE, APP_PIPE, type AdapterContext, All, type AnyCacheStore, type AnyConfigNamespace, ApiDoc, type ApiDocMetadata, ApiResponse, type ApiResponseEntry, type ApiResponseOptions, ApiTags, ApplicationLogger, type ApplicationLoggerOptions, type ArgumentMetadata, type AsyncCacheStore, type AsyncModuleOptions, type Awaitable, BadGatewayException, BadRequestException, type BeforeApplicationShutdown, Body, type BootstrapOptions, type BootstrapResult, type BroadcastCommand, CACHEABLE_METADATA, CACHE_KEY_METADATA, CACHE_MANAGER, CACHE_MODULE_OPTIONS, CACHE_TTL_METADATA, CONFIG_ENV, CONFIG_OPTIONS, CORE_CATALOG, CORS_OPTIONS, CRON_METADATA, type CacheEntry, type CacheEntryReader, type CacheEntryWriter, CacheInterceptor, type CacheInvalidationResult, type CacheInvalidationStore, CacheKey, CacheModule, type CacheModuleOptions, CacheResponse, type CacheResponseOptions, CacheService, type CacheStore, CacheTTL, Cacheable, type CallHandler, type CanActivate, type Catalog, Catch, ConfigModule, type ConfigModuleOptions, type ConfigNamespace, type ConfigPath, type ConfigPathValue, type ConfigSchema, ConfigService, ConfigStore, type ConfigType, type ConfigurableModuleAsyncOptions, ConfigurableModuleBuilder, type ConfigurableModuleBuilderOptions, type ConfigurableModuleClassType, type ConfigurableModuleExtras, type ConfigurableModuleExtrasTransform, type ConfigurableModuleHost, type ConfigurableModuleOptionsFactory, ConflictException, ConnectedSocket, type Constructor, Container, type ContainerOptions, type ContextProvider, type ContributesEntrypoints, Controller, Cookie, Cookies, CorsModule, type CorsOptions, type CreateDecoratorOptions, type CreateDiscoverableDecoratorOptions, type CreateOpenApiDocumentOptions, Cron, type CronMatcher, type CronMetadata, type CronOptions, DEFAULT_BODY_LIMIT_BYTES, DEFAULT_QUERY_BYTES_LIMIT, DEFAULT_QUERY_DEPTH_LIMIT, DEFAULT_QUERY_PARAMETER_LIMIT, DefaultValuePipe, type DefineConfigurableModuleSpec, type DefineModuleSpec, Delete, type DependencyToken, type Diagnostics, type DiscoverableDecorator, type DiscoveredClass, type DiscoveredMethodMeta, type DiscoveredRegisteredMethodMeta, type DiscoveredRegistration, type DiscoveryFilter, DiscoveryService, type DtoDefinition, type DtoOptions, type DtoSchema, type DynamicModule, ERROR_CATALOG, EXECUTION_LIFETIME, Endpoint, type EndpointDefinition, type EndpointHandlerOutput, type EndpointRequest, type EndpointSchema, type Entrypoint, type EntrypointExecutionContext, type EntrypointKind, EntrypointRegistry, type ErrorBodyResult, type ErrorCatalogEntry, type ErrorMatcher, type ErrorReportContext, type ErrorReporter, ErrorsModule, type ErrorsModuleOptions, type EventDefinition, EventDispatcher, type EventEmitOptions, EventEmitter, EventEmitterModule, EventEmitterSubscriber, type EventHandler, type EventInput, type EventListenerDecorator, type EventPayload, type EventVocabulary, type ExceptionFilter, type ExceptionHandler, type ExecutionContext, type ExecutionLifetime, type ExecutionScope, type ExecutionScopeOptions, type FilterType, ForbiddenException, ForwardRef, GatewayTimeoutException, Get, Global, type GlobalComponentSlot, GoneException, type GuardType, HTTP_MODULE_OPTIONS, HTTP_SIGNED_URL_PURPOSE, Head, Header, Headers, type HealthCheckResult, HealthCheckService, type HealthCheckStatus, type HealthIndicatorFunction, type HealthIndicatorResult, HealthIndicatorService, HealthModule, type HttpArgumentsHost, HttpCode, HttpException, type HttpExecutionContext, HttpHealthIndicator, HttpMethod, HttpModule, type HttpModuleOptions, type HttpPingOptions, type HttpRequestConfig, HttpRequestException, type HttpResponse, HttpService, type HttpVerb, INTERVAL_METADATA, INVOCATION_AUDIENCE, INVOCATION_DEFAULT_TTL_SECONDS, INVOCATION_SIGNING_SECRET, type InferConfigType, type InferToken, type InferTokens, Inject, Injectable, type InjectableOptions, InjectionToken, type InterceptorType, InternalDispatcher, InternalServerErrorException, Interval, type IntervalMetadata, type InvocationClaim, type InvocationPathTarget, type InvocationRouteTarget, type InvocationTarget, type InvocationTransport, Ip, type JsonSchema, type LazyProviderSpec, type LogDeliveryContext, type LogFields, LogLevel, type LogRecord, type LogSerializationOptions, type LogSink, type LogThresholds, type LogValue, Logger, type LoggerLevelName, type LoggerService, LoggingModule, METADATA_KEYS, MemoryCacheInvalidationStore, MemoryCacheStore, MemoryNonceStore, MessageBody, MetadataRegistry, MethodNotAllowedException, type MiddlewareConsumer, type MiddlewareType, Module, type ModuleContributions, type ModuleDescription, type ModuleImport, type ModuleOptions, ModuleRef, type ModuleRegistrationOptions, type ModuleScope, type ModuleSetupContext, ModuleVisibilityError, MultipleProvidersFoundError, NONCE_STORE, type NestInterceptor, type NestMiddleware, type NestModule, type NonceStore, NotAcceptableException, NotFoundException, NotImplementedException, ON_EVENT_METADATA, type OnApplicationBootstrap, type OnApplicationShutdown, OnEvent, type OnEventMetadata, type OnGatewayConnection, type OnGatewayDisconnect, type OnGatewayInit, type OnModuleDestroy, type OnModuleInit, type OpenApiDocument, type OpenApiInfo, type OpenApiOperation, type OpenApiParameter, type OpenApiPathItem, type OpenApiRequestBody, type OpenApiResponse, Optional, Options, type OriginProtectionOptions, PLUGIN_REGISTRY_TOKEN, Param, ParamType, ParseArrayPipe, type ParseArrayPipeOptions, ParseBoolPipe, ParseEnumPipe, ParseFloatPipe, ParseIntPipe, ParseUUIDPipe, type ParseUUIDPipeOptions, Patch, PayloadTooLargeException, type PipeTransform, type PipeType, type PipelineComponentEntry, type PipelineRunOptions, PipelineRunner, type Plugin, PluginRegistry, PluginRootModule, Post, type ProviderDefinition, type ProviderSnapshot, Put, Query, REQUEST_CONTEXT, RESERVED_WS_EVENT_PREFIX, RESPONSE_CACHE_OPTIONS, ROOT_MODULE_ID, type RateLimitInfo, RawBody, Redirect, type ReflectableDecorator, Reflector, type RegisteredCronJob, type RegisteredIntervalJob, Req, type RequestContext, RequestContextKey, RequestTimeoutException, RequiredPipe, Res, ReservedWsEvent, type ReservedWsEventHandler, type ReservedWsEventMetadata, type ResolvedComponentMap, type ResponseCacheEntryOptions, ResponseCacheInterceptor, ResponseCacheModule, type ResponseCacheOptions, type ResponseCacheScope, ResponseCacheService, type ResponseCheckCallback, type RoomRegistry, type RouteContributor, type RouteContributorContext, type RouteContributorOpenApiContext, type RouteDescription, type RouteInfo, type RouteName, type RouteOptions, type RouteParams, type RunInit, type RuntimeAdapter, type RuntimeParser, SCHEDULE_DISPATCH, SECURITY_OPTIONS, SERIALIZE_METADATA, SKIP_THROTTLE_METADATA, STORAGE_SIGNED_URL_PURPOSE, type ScheduleDispatchMode, type ScheduleInvocation, type ScheduleJobRef, ScheduleModule, ScheduleRegistry, type SchemaInput, type SchemaOutput, type SchemaParser, SchemaValidationError, Scope, type ScopedEventDispatcher, type ScopedResponseCache, Secret, type SecurityCorsOptions, type SecurityHeadersOptions, SecurityModule, type SecurityModuleOptions, type SerializationDescriptor, Serialize, type SerializerDefinition, SerializerInterceptor, ServiceUnavailableException, SetMetadata, SignedInvocation, SignedInvocationGuard, SignedUrl, type SignedUrlGenerateOptions, SignedUrlGuard, type SignedUrlOptions, SkipThrottle, Sse, type StandardDtoDefinition, type StandardJSONSchemaV1, type StandardSchemaV1, StructuredLogger, SubscribeMessage, type SyncDriver, THROTTLER_OPTIONS, THROTTLER_STORAGE, THROTTLE_METADATA, Throttle, type ThrottleConfig, ThrottlerGuard, ThrottlerModule, type ThrottlerModuleOptions, ThrottlerStorage, type ThrottlerStorageRecord, type ThrottlerStore, TieredCacheStore, type Token, TooManyRequestsException, type TrustedRequestIdentity, type TrustedRequestIdentityStore, type TrustedRequestPrincipal, type Type, type TypedToken, URL_SIGNING_SECRET, UnauthorizedException, UnprocessableEntityException, UnsupportedMediaTypeException, type UrlForOptions, UrlGeneratorService, UseFilters, UseGuards, UseInterceptors, UseMiddleware, UsePipes, type ValidationIssue, ValidationPipe, type ValidationSchema, VelaApplication, type VelaBodyLimitOverride, type VelaBodySecurityOptions, type VelaContext, type VelaCreateOptions, VelaError, type VelaErrorOptions, VelaFactory, type VelaHono, type VelaHonoEnv, type VelaMiddlewareHandler, type VelaQuerySecurityOptions, type VelaRouteMap, type VelaSecurityOptions, type VerifyInvocationOptions, type VerifySignedUrlOptions, Version, WS_SERVER, WebSocketGateway, WebSocketModule, type WebSocketModuleOptions, WebSocketServer, type Writer, type WsArgumentsHost, type WsClient, WsDispatcher, WsException, type WsExecutionContext, type WsMessage, type WsResponse, type WsServer, ZodValidationPipe, applyDecorators, assertWebSocketRoomId, bindTrustedRequestContext, bootstrap, buildEntrypointExecutionContext, buildExecutionContext as buildHttpExecutionContext, buildSecurityMiddleware, clearTrustedRequestIdentity, composeCatalogs, composePlugins, consoleLogSink, contributesEntrypoints, createDiscoverableDecorator, createExecutionScope, createLazyParamDecorator, createOpenApiDocument, createParamDecorator, createTrustedRequestIdentityStore, defineConfigurableModule, defineDto, defineDynamicModule, defineEndpoint, defineErrorCatalog, defineEvent, defineEventVocabulary, defineMetadata, defineModule, definePlugin, defineProvider, defineSerializer, describeToken, enableAmbientContainer, env, finishExecutionScope, forwardRef, getCatchTypes, getCurrentContainer, getCurrentRequestContext, getEntrypointKinds, getEntrypointModuleId, getExecutionLifetime, getMetadata, getRequestContainer, getRouteContributors, getRuntimeKey, getTrustedContextRequest, getTrustedRequestIdentity, isStandardSchema, isValidationSchema, isVelaError, lazyProvider, loggerForScope, matchesAny, mixin, moduleKey, moduleToken, parseCron, parseCronMetadata, parseIntervalMetadata, parseLogDirective, parseSchema, parseSchemaAsync, provideGlobal, registerAs, registerEntrypointKind, registerRouteContributor, resolveEntrypoint, resolveErrorReporter, resolvePipelineComponents, resolveScopedComponents, resolveScopedComponentsAsync, runInEntrypointScope, serializeLogValue, setTrustedRequestIdentity, setTrustedRequestTenant, shouldFilterCatch, sideEffectModule, signInvocation, signUrl, stableHash, standardJsonSchema, toErrorBody, trySendWebSocketFrame, validateSchema, verifyInvocation, verifySignedUrl, zodToJsonSchema };
```

## `./i18n`

Type conditions: `types`

Declaration entry: `./dist/i18n/index.d.ts`

```ts
import "<internal:schedule.registry.d.ts>";
import { l as NestMiddleware, z as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { D as RequestContext, O as RequestContextKey } from "<internal:metadata.registry.d.ts>";
import { H as DynamicModule } from "<internal:types-registry-types.d.ts>";
import { qt as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { Context, Next } from "hono";

type DetectionStrategy = 'header' | 'query' | 'cookie';
interface I18nModuleOptions {

  defaultLocale?: string;

  fallbackLocale?: string;

  locales?: string[];

  detection?: {

    enabled?: boolean;

    strategy?: DetectionStrategy;

    lookupKey?: string;
  };
}
interface ResolvedI18nOptions {
  defaultLocale: string;
  fallbackLocale: string;
  locales: string[];
  detection: {
    enabled: boolean;
    strategy: DetectionStrategy;
    lookupKey: string;
  };
}
export declare function resolveI18nOptions(options?: I18nModuleOptions): ResolvedI18nOptions;

declare const ConfigurableModuleClass: ConfigurableModuleClassType<I18nModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
export declare class I18nModule extends ConfigurableModuleClass {

  static registerMessages(messages: Record<string, Record<string, unknown>>): DynamicModule;
}

interface II18nService {

  t(key: string, params?: Record<string, unknown>): string;

  getLocale(): string;
}

type Contribution = Record<string, Record<string, unknown>>;

export declare class MessageRegistry {

  static addMessages(messages: Contribution): void;

  getMergedMessages(): Record<string, Record<string, unknown>>;
  /** @internal */
  static reset(): void;
}

export declare class MessageLoaderService {
  private readonly cache;
  private readonly compiledCache;
  private readonly defaultLocale;
  private readonly fallbackLocale;
  constructor(registry: MessageRegistry, options: I18nModuleOptions);
  translate(locale: string, key: string, params?: Record<string, unknown>): string;
  getAvailableLocales(): string[];
  isLocaleSupported(locale: string): boolean;
  getDefaultLocale(): string;
  private getCompiledMessages;
  private flattenMessages;
}

export declare class I18nService implements II18nService {
  private readonly loader;
  private readonly ctx;
  private readonly options;
  constructor(loader: MessageLoaderService, ctx: RequestContext, options: I18nModuleOptions);
  t(key: string, params?: Record<string, unknown>): string;
  getLocale(): string;
}

export declare class I18nLocaleMiddleware implements NestMiddleware {
  private readonly resolved;
  constructor(options: I18nModuleOptions);
  use(c: Context, next: Next): Promise<Response | void>;
}

export declare const I18N_OPTIONS: InjectionToken<I18nModuleOptions>;

export declare const I18N_LOCALE_KEY: RequestContextKey<string>;

export declare function deepMerge(target: Record<string, unknown>, source: Record<string, unknown>): Record<string, unknown>;

export type { DetectionStrategy, I18nModuleOptions, II18nService, ResolvedI18nOptions };
```

## `./internal`

Type conditions: `types`

Declaration entry: `./dist/internal.d.ts`

```ts
import { H as ModuleVisibilityError, N as Diagnostics, V as ModuleScope, j as ContainerOptions, k as Container } from "<internal:types-http-hono.types.d.ts>";
import { A as ModuleRef, I as RuntimeAdapter, L as VelaApplication, M as BootstrapResult, N as bootstrap, P as ModuleLoader, S as setRequestContainer, a as APP_INTERCEPTOR, b as isModule, d as Plugin, f as PluginRegistry, h as definePlugin, i as APP_GUARD, j as BootstrapOptions, k as createRequestContext, l as ComponentManager, m as composePlugins, o as APP_MIDDLEWARE, p as PluginRootModule, r as APP_FILTER, s as APP_PIPE, t as MetadataRegistry, u as PLUGIN_REGISTRY_TOKEN, w as INVOCATION_TRANSPORT, y as getModuleMetadata } from "<internal:metadata.registry.d.ts>";
import { b as VelaSecurityOptions, f as RouteManager, p as RouteManagerOptions } from "<internal:types-registry-types.d.ts>";

export declare function bindAppProviders(routeManager: RouteManager, container: Container, loader: ModuleLoader): void;

export declare function finalizeApplication({ container, routeManager, loader }: BootstrapResult, adapters?: readonly RuntimeAdapter[]): Promise<VelaApplication>;

export { APP_FILTER, APP_GUARD, APP_INTERCEPTOR, APP_MIDDLEWARE, APP_PIPE, type BootstrapOptions, type BootstrapResult, ComponentManager, Container, type ContainerOptions, type Diagnostics, INVOCATION_TRANSPORT, MetadataRegistry, ModuleLoader, ModuleRef, type ModuleScope, ModuleVisibilityError, PLUGIN_REGISTRY_TOKEN, type Plugin, PluginRegistry, PluginRootModule, RouteManager, type RouteManagerOptions, VelaApplication, type VelaSecurityOptions, bootstrap, composePlugins, createRequestContext, definePlugin, getModuleMetadata, isModule, setRequestContainer };
```

## `./live`

Type conditions: `types`

Declaration entry: `./dist/live/index.d.ts`

```ts
import { $ as LiveQueryMetadata, A as LiveEngine, B as getLiveQueries, C as LIVE_RESOLVER_METADATA, D as localLive, E as LiveInvalidation, F as PresenceResolver, G as LiveDeliveryAuthorizationContext, H as CommitStamp, I as PresenceService, J as LiveIdentity, K as LiveDriver, L as presenceTag, M as readPersistedLiveSubscriptions, N as PRESENCE_ROSTER_QUERY, O as stampCommitHeaders, P as PresenceMember, Q as LiveQueryContext, R as LiveQuery, S as LIVE_MODULE_OPTIONS, T as InMemoryCursorLog, U as CursorLog, V as LiveModule, W as InvalidationCommand, X as LiveModuleOptions, Y as LiveInvalidationSink, Z as LivePresenceOptions, _ as isClientLiveFrame, a as LIVE_ERROR_CODES, b as LIVE_CURSOR_LOG, c as LiveFrame, d as ServerLiveFrame, et as LiveQueryOptions, f as applyListDelta, g as encodeLiveFrame, h as encodeLiveEnvelope, i as DEFAULT_KEY_FIELD, it as SubscriptionRecord, j as LiveInspection, k as LIVE_SUBS_DATA_KEY, l as LiveQueryDefinition, m as encodeListDelta, n as COMMIT_EPOCH_HEADER, nt as PreparedLiveQuery, o as LIVE_EVENT, p as defineLiveQuery, q as LiveEntrypointMeta, r as ClientLiveFrame, rt as ResumeVerdict, s as LIVE_PROTOCOL, t as COMMIT_CURSOR_HEADER, tt as LiveResolverMetadata, u as RowOp, v as isServerLiveFrame, w as encodeSubscriptionUpdate, x as LIVE_DRIVER, y as readLiveEnvelope, z as LiveResolver } from "<internal:index-live-live.types.d.ts>";
export { COMMIT_CURSOR_HEADER, COMMIT_EPOCH_HEADER, type ClientLiveFrame, type CommitStamp, type CursorLog, DEFAULT_KEY_FIELD, InMemoryCursorLog, type InvalidationCommand, LIVE_CURSOR_LOG, LIVE_DRIVER, LIVE_ERROR_CODES, LIVE_EVENT, LIVE_MODULE_OPTIONS, LIVE_PROTOCOL, LIVE_RESOLVER_METADATA, LIVE_SUBS_DATA_KEY, type LiveDeliveryAuthorizationContext, type LiveDriver, LiveEngine, type LiveEntrypointMeta, type LiveFrame, type LiveIdentity, type LiveInspection, LiveInvalidation, type LiveInvalidationSink, LiveModule, type LiveModuleOptions, type LivePresenceOptions, LiveQuery, type LiveQueryContext, type LiveQueryDefinition, type LiveQueryMetadata, type LiveQueryOptions, LiveResolver, type LiveResolverMetadata, PRESENCE_ROSTER_QUERY, type PreparedLiveQuery, type PresenceMember, PresenceResolver, PresenceService, type ResumeVerdict, type RowOp, type ServerLiveFrame, type SubscriptionRecord, applyListDelta, defineLiveQuery, encodeListDelta, encodeLiveEnvelope, encodeLiveFrame, encodeSubscriptionUpdate, getLiveQueries, isClientLiveFrame, isServerLiveFrame, localLive, presenceTag, readLiveEnvelope, readPersistedLiveSubscriptions, stampCommitHeaders };
```

## `./queue`

Type conditions: `types`

Declaration entry: `./dist/queue/index.d.ts`

```ts
import { k as Container, q as Token, z as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { Z as EntrypointRegistry } from "<internal:metadata.registry.d.ts>";
import { F as DiscoveryService, r as InvocationTarget } from "<internal:types-registry-types.d.ts>";
import { qt as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { _ as StandardSchemaV1 } from "<internal:index-validation-standard-schema.d.ts>";

interface QueueJob<T = unknown> {
  id: string;

  queue: string;

  name: string;
  data: T;

  attempt: number;
}
interface AddJobOptions {

  delayMs?: number;
}

type QueueDispatchFn = (job: QueueJob) => Promise<void>;
interface QueueDriverBindHooks {

  onError?: (error: unknown, job: QueueJob) => void;
}

interface QueueDriver {
  readonly kind: string;
  enqueue(job: QueueJob, options?: AddJobOptions): Promise<void>;
  bind?(dispatch: QueueDispatchFn, hooks?: QueueDriverBindHooks): void;

  unbind?(): void;
}

type QueueDispatchMode = {
  readonly kind: 'direct';
} | {
  readonly kind: 'signed';

  readonly target: (job: QueueJob) => InvocationTarget;

  readonly method?: string;

  readonly ttlSeconds?: number;
};
interface QueueModuleOptions {

  queues?: string[];

  driver?: QueueDriver | (() => QueueDriver);

  dispatch?: QueueDispatchMode;
}

interface ProcessorMetadata {
  queueName: string;
}

interface ProcessMetadata {
  jobName?: string;

  schema?: StandardSchemaV1;
  methodName: string | symbol;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<QueueModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>, MODULE_OPTIONS_TOKEN: InjectionToken<QueueModuleOptions>;
export declare class QueueModule extends ConfigurableModuleClass {}

interface QueueJobDefinition<S extends StandardSchemaV1 = StandardSchemaV1> {
  readonly name: string;
  readonly schema: S;
}
type QueueJobInput<D extends QueueJobDefinition> = StandardSchemaV1.InferInput<D['schema']>;
type QueueJobOutput<D extends QueueJobDefinition> = StandardSchemaV1.InferOutput<D['schema']>;

export declare function defineQueueJob<const N extends string, S extends StandardSchemaV1>(name: N, schema: S): QueueJobDefinition<S> & {
  readonly name: N;
};

export declare function Processor(queueName: string): ClassDecorator;

type QueueProcessDecorator<Data> = <Handler extends (job: QueueJob<Data>) => unknown>(target: object, key: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;
export declare function Process<S extends StandardSchemaV1>(definition: QueueJobDefinition<S>): QueueProcessDecorator<StandardSchemaV1.InferOutput<S>>;
export declare function Process(jobName?: string): MethodDecorator;

export declare function getProcessHandlers(processorClass: object): ProcessMetadata[];

export declare class QueueClient {
  #private;
  constructor(queue: string, driver: QueueDriver);
  get name(): string;
  add<S extends StandardSchemaV1>(definition: QueueJobDefinition<S>, data: StandardSchemaV1.InferInput<S>, options?: AddJobOptions): Promise<QueueJob<StandardSchemaV1.InferInput<S>>>;
  add<T>(jobName: string, data: T, options?: AddJobOptions): Promise<QueueJob<T>>;
}

export declare const PROCESSOR_METADATA = "vela:queue:processor";
export declare const PROCESS_METADATA = "vela:queue:process";

export declare const QUEUE_DRIVER: InjectionToken<QueueDriver>;

export declare function queueToken(name: string): InjectionToken<QueueClient>;

export declare class QueueDispatchBinding {
  #private;
  constructor(container: Container, discovery: DiscoveryService, driver: QueueDriver, queues: string[], dispatch?: QueueDispatchMode);
  dispose(): void;
}

interface QueueDispatchOptions {

  unhandled?: 'ignore' | 'error';
}
interface QueueDispatchResult {

  handled: number;
}

interface QueueEntry {
  token: Token;
  moduleId?: string;
  meta: ProcessorMetadata;
}

export declare function dispatchQueueJob(container: Container, entrypoints: EntrypointRegistry, job: QueueJob, options?: QueueDispatchOptions): Promise<QueueDispatchResult>;

interface InlineQueueOptions {

  mode?: 'immediate' | 'manual';
}
interface InlineQueueDriver extends QueueDriver {

  flush(): Promise<number>;

  readonly size: number;
}

export declare function inline(options?: InlineQueueOptions): InlineQueueDriver;

interface QueueMessageLike<Body = unknown> {
  readonly id: string;
  readonly timestamp: Date;
  readonly body: Body;

  readonly attempts: number;

  ack(): void;

  retry(options?: {
    delaySeconds?: number;
  }): void;
}

type MessageOutcome = 'acked' | 'retried' | 'unsettled';

interface MessageDisposition {
  id: string;
  attempts: number;
  outcome: MessageOutcome;

  retryDelaySeconds?: number;

  retryExhausted?: boolean;

  deadLettered: boolean | undefined;
}
interface ObserveMessageOptions {

  maxRetries?: number;

  deadLetterQueue?: boolean;
}
interface ObservedMessage<Body = unknown> {

  message: QueueMessageLike<Body>;

  disposition(): MessageDisposition;
}

interface BatchDisposition {
  messages: MessageDisposition[];
  acked: number;
  retried: number;
  unsettled: number;

  deadLettered: number;
}
interface ObservedBatch<Body = unknown> {

  messages: QueueMessageLike<Body>[];

  report(): BatchDisposition;
}

export declare function observeMessage<Body = unknown>(message: QueueMessageLike<Body>, options?: ObserveMessageOptions): ObservedMessage<Body>;

export declare function observeBatch<Body = unknown>(messages: readonly QueueMessageLike<Body>[], options?: ObserveMessageOptions): ObservedBatch<Body>;

export declare function parseQueueJob(value: unknown, deliveryAttempt?: number): QueueJob;

export { type AddJobOptions, type BatchDisposition, type InlineQueueDriver, type InlineQueueOptions, type MessageDisposition, type MessageOutcome, type ObserveMessageOptions, type ObservedBatch, type ObservedMessage, type ProcessMetadata, type ProcessorMetadata, MODULE_OPTIONS_TOKEN as QUEUE_MODULE_OPTIONS, type QueueDispatchFn, type QueueDispatchMode, type QueueDispatchOptions, type QueueDispatchResult, type QueueDriver, type QueueDriverBindHooks, type QueueEntry, type QueueJob, type QueueJobDefinition, type QueueJobInput, type QueueJobOutput, type QueueMessageLike, type QueueModuleOptions, type QueueProcessDecorator };
```

## `./schedule-node`

Type conditions: `types`

Declaration entry: `./dist/schedule-node/index.d.ts`

```ts
import { f as BeforeApplicationShutdown, h as OnModuleDestroy, o as ScheduleDispatchMode, p as OnApplicationBootstrap, r as ScheduleRegistry, y as InternalDispatcher } from "<internal:schedule.registry.d.ts>";
import { k as Container } from "<internal:types-http-hono.types.d.ts>";
import { H as DynamicModule } from "<internal:types-registry-types.d.ts>";

export declare class ScheduleNodeModule {
  static forRoot(): DynamicModule;
}

export declare class ScheduleExecutor implements OnApplicationBootstrap, BeforeApplicationShutdown, OnModuleDestroy {
  #private;
  constructor(registry: ScheduleRegistry, container: Container, dispatch?: ScheduleDispatchMode, dispatcher?: InternalDispatcher);
  onApplicationBootstrap(): void;

  beforeApplicationShutdown(): Promise<void>;
  onModuleDestroy(): Promise<void>;
}
```

## `./seeder`

Type conditions: `types`

Declaration entry: `./dist/seeder/index.d.ts`

```ts
import { p as OnApplicationBootstrap } from "<internal:schedule.registry.d.ts>";
import { J as Type, k as Container } from "<internal:types-http-hono.types.d.ts>";
import { L as VelaApplication } from "<internal:metadata.registry.d.ts>";
import { qt as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";

interface Seeder {
  run(): void | Promise<void>;
}
interface SeederMetadata {

  name?: string;

  order?: number;
}
interface RegisteredSeeder {
  name: string;
  order: number;

  moduleId?: string;
  target: new (...args: any[]) => Seeder;
}
interface SeederResult {
  name: string;
  ok: boolean;
  error?: unknown;
}

declare function Seeder$1(options?: SeederMetadata): ClassDecorator;

export declare class SeederRegistry implements OnApplicationBootstrap {
  #private;
  constructor(container: Container);
  onApplicationBootstrap(): void;

  list(): RegisteredSeeder[];

  runAll(options?: {
    stopOnError?: boolean;
  }): Promise<SeederResult[]>;
}

interface SeederModuleOptions {
  seeders?: Type[];
}
declare const ConfigurableModuleClass: ConfigurableModuleClassType<SeederModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
export declare class SeederModule extends ConfigurableModuleClass {}

export declare function runSeeders(app: VelaApplication, options?: {
  stopOnError?: boolean;
}): Promise<SeederResult[]>;

export declare const SEEDER_METADATA = "vela:seeder";

export { type Seeder as ISeeder, type RegisteredSeeder, Seeder$1 as Seeder, type SeederMetadata, type SeederResult };
```

## `./storage`

Type conditions: `types`

Declaration entry: `./dist/storage/index.d.ts`

```ts
import { a as signUrl, i as VerifySignedUrlOptions, n as STORAGE_SIGNED_URL_PURPOSE, o as verifySignedUrl, r as SignedUrlOptions, t as HTTP_SIGNED_URL_PURPOSE } from "<internal:signed-url.d.ts>";

export declare function expandPathTemplate(template: string, now?: Date): string;

export declare function joinStoragePath(root: string | undefined, relativePath: string, now?: Date): string;

type StorageBody = ReadableStream | ArrayBuffer | ArrayBufferView | string | Blob | null;
type PresignMethod = 'GET' | 'PUT' | 'DELETE' | 'HEAD';
interface UploadOptions {
  size?: number;
  mimeType?: string;
  metadata?: Record<string, string>;
}
interface UploadResult {
  path: string;
  disk: string;
  size?: number;
  mimeType: string;
  uploadedAt: Date;
}
interface DownloadResult {
  toStream(): ReadableStream;
  toArrayBuffer(): Promise<ArrayBuffer>;
  toText(): Promise<string>;
  contentType: string;
  size: number;
  metadata?: Record<string, string>;
}
interface PresignedUrlResult {
  url: string;
  method: PresignMethod;
  expiresIn: number;
  expiresAt: Date;
}

interface StorageDriver {
  upload(body: StorageBody, path: string, options: UploadOptions): Promise<UploadResult>;
  download(path: string): Promise<DownloadResult>;
  delete(path: string): Promise<void>;
  exists(path: string): Promise<boolean>;
  getPresignedUrl(path: string, method: PresignMethod, expiresIn: number): Promise<PresignedUrlResult>;
}

export { type DownloadResult, HTTP_SIGNED_URL_PURPOSE, type PresignMethod, type PresignedUrlResult, STORAGE_SIGNED_URL_PURPOSE, type SignedUrlOptions, type StorageBody, type StorageDriver, type UploadOptions, type UploadResult, type VerifySignedUrlOptions, signUrl, verifySignedUrl };
```

## `./streaming`

Type conditions: `types`

Declaration entry: `./dist/streaming/index.d.ts`

```ts
import { SSEMessage, SSEStreamingApi, stream, streamSSE, streamText } from "hono/streaming";
export { type SSEMessage, type SSEStreamingApi, stream, streamSSE, streamText };
```

## `./validation`

Type conditions: `types`

Declaration entry: `./dist/validation/index.d.ts`

```ts
import { _ as StandardSchemaV1, a as isValidationSchema, b as standardJsonSchema, c as DtoDefinition, d as RuntimeParser, f as SchemaParser, g as StandardJSONSchemaV1, h as SchemaValidationError, i as ValidationSchema, l as DtoOptions, m as defineDto, n as SchemaInput, o as parseSchema, p as StandardDtoDefinition, r as SchemaOutput, s as parseSchemaAsync, t as ValidationPipe, u as DtoSchema, v as ValidationIssue, x as validateSchema, y as isStandardSchema } from "<internal:index-validation-standard-schema.d.ts>";
export { type DtoDefinition, type DtoOptions, type DtoSchema, type RuntimeParser, type SchemaInput, type SchemaOutput, type SchemaParser, SchemaValidationError, type StandardDtoDefinition, type StandardJSONSchemaV1, type StandardSchemaV1, type ValidationIssue, ValidationPipe, type ValidationSchema, defineDto, isStandardSchema, isValidationSchema, parseSchema, parseSchemaAsync, standardJsonSchema, validateSchema };
```

## `./websocket`

Type conditions: `types`

Declaration entry: `./dist/websocket/index.d.ts`

```ts
import { C as WebSocketUpgradeIdentity, D as WsResponse, E as WsMessage, O as WsServer, S as WebSocketUpgradeAuthenticationContext, T as WsExecutionContext, _ as ReservedWsEventHandler, b as WebSocketGatewayOptions, d as WsArgumentsHost, f as BroadcastCommand, g as OnGatewayInit, h as OnGatewayDisconnect, m as OnGatewayConnection, p as BroadcastOperator, v as ReservedWsEventMetadata, w as WsClient, x as WebSocketPrincipal, y as SubscribeMessageMetadata } from "<internal:types-http-hono.types.d.ts>";
import { $n as WS_RESERVED_METADATA, $r as WebSocketServer, Ar as normalizeWebSocketUpgradeIdentity, Br as InMemoryRoomRegistry, Cr as DEFAULT_WS_MAX_FRAME_BYTES, Dr as authenticateWebSocketUpgrade, Er as assertWebSocketRoomId, Fr as WsDispatcher, Gn as WebSocketSendPolicy, Gr as broadcastCommandFits, Hr as RoomRegistry, Ir as WsEntrypointMeta, Jn as WsMessageQueue, Jr as ConnectedSocket, Kn as WebSocketSendResult, Kr as local, Lr as readWsEntrypointMeta, Mr as resolveGatewayRoomParam, Nr as resolveMaxFrameBytes, Or as authorizeWebSocketUpgrade, Pr as webSocketFrameFits, Qn as WS_MODULE_OPTIONS, Qr as WebSocketGateway, Rr as WebSocketModule, Sr as AuthenticatedWebSocketUpgrade, Tr as DEFAULT_WS_MAX_ROOM_ID_BYTES, Ur as SyncDriver, Vr as MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES, Wn as WebSocketSendGate, Wr as assertBroadcastCommandFits, Xn as RESERVED_WS_EVENT_PREFIX, Xr as ReservedWsEvent, Yn as trySendWebSocketFrame, Yr as MessageBody, Zn as WS_GATEWAY_METADATA, Zr as SubscribeMessage, _r as WebSocketTicketNonceStore, ar as resolveWsArgs, br as issueWebSocketTicket, cr as buildWsExecutionContext, dr as IssueWebSocketTicketOptions, er as WS_ROOM_REGISTRY, fr as VerifyWebSocketTicketOptions, gr as WebSocketTicketClaim, hr as WEBSOCKET_TICKET_PURPOSE, ir as WsParamType, jr as resolveGatewayRoomId, kr as isWebSocketOriginAllowed, lr as BroadcastOperatorImpl, mr as WEBSOCKET_TICKET_MAX_TTL_MS, nr as WS_SUBSCRIBE_METADATA, or as WsException, pr as WEBSOCKET_TICKET_AUDIENCE, qn as readWebSocketEnvelope, qr as webSocketSyncEnvelopeFits, rr as WS_SYNC_DRIVER, sr as toErrorFrame, tr as WS_SERVER, ur as WsServerImpl, vr as WebSocketTicketPrincipal, wr as DEFAULT_WS_MAX_JOINED_ROOMS, xr as verifyAndConsumeWebSocketTicket, yr as WebSocketTicketPrincipalType, zr as WebSocketModuleOptions } from "<internal:index-factory.d.ts>";
export { type AuthenticatedWebSocketUpgrade, type BroadcastCommand, type BroadcastOperator, BroadcastOperatorImpl, ConnectedSocket, DEFAULT_WS_MAX_FRAME_BYTES, DEFAULT_WS_MAX_JOINED_ROOMS, DEFAULT_WS_MAX_ROOM_ID_BYTES, InMemoryRoomRegistry, type IssueWebSocketTicketOptions, MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES, MessageBody, type OnGatewayConnection, type OnGatewayDisconnect, type OnGatewayInit, RESERVED_WS_EVENT_PREFIX, ReservedWsEvent, type ReservedWsEventHandler, type ReservedWsEventMetadata, type RoomRegistry, SubscribeMessage, type SubscribeMessageMetadata, type SyncDriver, type VerifyWebSocketTicketOptions, WEBSOCKET_TICKET_AUDIENCE, WEBSOCKET_TICKET_MAX_TTL_MS, WEBSOCKET_TICKET_PURPOSE, WS_GATEWAY_METADATA, WS_MODULE_OPTIONS, WS_RESERVED_METADATA, WS_ROOM_REGISTRY, WS_SERVER, WS_SUBSCRIBE_METADATA, WS_SYNC_DRIVER, WebSocketGateway, type WebSocketGatewayOptions, WebSocketModule, type WebSocketModuleOptions, type WebSocketPrincipal, WebSocketSendGate, type WebSocketSendPolicy, type WebSocketSendResult, WebSocketServer, type WebSocketTicketClaim, type WebSocketTicketNonceStore, type WebSocketTicketPrincipal, type WebSocketTicketPrincipalType, type WebSocketUpgradeAuthenticationContext, type WebSocketUpgradeIdentity, type WsArgumentsHost, type WsClient, WsDispatcher, type WsEntrypointMeta, WsException, type WsExecutionContext, type WsMessage, WsMessageQueue, WsParamType, type WsResponse, type WsServer, WsServerImpl, assertBroadcastCommandFits, assertWebSocketRoomId, authenticateWebSocketUpgrade, authorizeWebSocketUpgrade, broadcastCommandFits, buildWsExecutionContext, isWebSocketOriginAllowed, issueWebSocketTicket, local, normalizeWebSocketUpgradeIdentity, readWebSocketEnvelope, readWsEntrypointMeta, resolveGatewayRoomId, resolveGatewayRoomParam, resolveMaxFrameBytes, resolveWsArgs, toErrorFrame, trySendWebSocketFrame, verifyAndConsumeWebSocketTicket, webSocketFrameFits, webSocketSyncEnvelopeFits };
```

## `./websocket-node`

Type conditions: `types`

Declaration entry: `./dist/websocket-node/index.d.ts`

```ts
import { w as WsClient } from "<internal:types-http-hono.types.d.ts>";
import { L as VelaApplication } from "<internal:metadata.registry.d.ts>";
import { Hr as RoomRegistry, Ur as SyncDriver } from "<internal:index-factory.d.ts>";
import { K as LiveDriver } from "<internal:index-live-live.types.d.ts>";
import { WebSocketSendPolicy, WebSocketSendResult } from "@velajs/live-protocol";
import { UpgradeWebSocket, WSContext } from "hono/ws";

export declare class NodeWsClient<TData extends Record<string, unknown> = Record<string, unknown>> implements WsClient<TData> {
  #private;
  private readonly ws;
  private readonly registry;
  readonly path: string;
  readonly maxFrameBytes: number;
  readonly id: string;
  data: TData;
  constructor(ws: WSContext, registry: RoomRegistry, path: string, maxFrameBytes?: number, sendPolicy?: WebSocketSendPolicy);
  get rooms(): ReadonlySet<string>;
  get raw(): unknown;
  send(event: string, data?: unknown, id?: string): void;
  sendRaw(payload: string): void;
  trySendRaw(payload: string): WebSocketSendResult;
  join(room: string): void | Promise<void>;
  leave(room: string): void | Promise<void>;
  commit(): void;
  close(code?: number, reason?: string): void;
}

export declare function registerWebSocketGateways(app: VelaApplication, upgradeWebSocket: UpgradeWebSocket): void;

interface RedisPubSubClient {
  publish(channel: string, message: string): unknown;
  subscribe(channel: string): unknown;
  on(event: 'message', listener: (channel: string, message: string) => void): unknown;
  off?(event: 'message', listener: (channel: string, message: string) => void): unknown;
  removeListener?(event: 'message', listener: (channel: string, message: string) => void): unknown;
  unsubscribe?(channel: string): unknown;
}
interface RedisSyncOptions {

  pub: RedisPubSubClient;

  sub: RedisPubSubClient;

  prefix?: string;
}

export declare function redis(options: RedisSyncOptions): SyncDriver;

interface RedisLiveOptions {

  pub: RedisPubSubClient;

  sub: RedisPubSubClient;

  prefix?: string;
}

export declare function redisLive(options: RedisLiveOptions): LiveDriver;

export type { RedisLiveOptions, RedisPubSubClient, RedisSyncOptions };
```

## Referenced declaration chunks

### `<internal:index-factory.d.ts>`

```ts
import { J as applyDecorators, a as IntervalMetadata, f as BeforeApplicationShutdown, i as CronMetadata, o as ScheduleDispatchMode, p as OnApplicationBootstrap, u as CronOptions } from "<internal:schedule.registry.d.ts>";
import { A as Constructor, C as WebSocketUpgradeIdentity, F as InferToken, I as InferTokens, J as Type, O as WsServer, P as ForwardRef, R as InjectableOptions, T as WsExecutionContext, W as ProviderDefinition, Y as TypedToken, a as ExecutionContext, b as WebSocketGatewayOptions, c as NestInterceptor, f as BroadcastCommand, i as ExceptionFilter, it as VelaHono, k as Container, l as NestMiddleware, n as CallHandler, p as BroadcastOperator, q as Token, r as CanActivate, s as HttpExecutionContext, t as ArgumentMetadata, u as PipeTransform, w as WsClient, z as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { B as ApiResponseOptions, D as RequestContext, I as RuntimeAdapter, J as OpenApiPathItem, L as VelaApplication, R as ApiDocMetadata, U as JsonSchema, V as CreateOpenApiDocumentOptions, W as OpenApiDocument, j as BootstrapOptions, nt as ExceptionHandler, tt as ErrorReportContext } from "<internal:metadata.registry.d.ts>";
import { B as ComponentType, D as Entrypoint, E as ContributesEntrypoints, F as DiscoveryService, H as DynamicModule, J as ModuleImport, K as InterceptorType, Q as PipeType, U as FilterType, V as ComponentTypeMap, W as GuardType, Z as ParameterMetadata, a as NonceStore, f as RouteManager, q as MiddlewareType } from "<internal:types-registry-types.d.ts>";
import { _ as StandardSchemaV1, f as SchemaParser, i as ValidationSchema, n as SchemaInput, r as SchemaOutput } from "<internal:index-validation-standard-schema.d.ts>";
import { CORE_CATALOG as CORE_CATALOG$1, Catalog, Catalog as Catalog$1, ErrorBodyResult, ErrorBodyResult as ErrorBodyResult$1, ErrorCatalogEntry, VelaError as VelaError$1, VelaErrorOptions, composeCatalogs as composeCatalogs$1, defineErrorCatalog, isVelaError as isVelaError$1, toErrorBody as toErrorBody$1 } from "@velajs/errors";
import { Context, MiddlewareHandler } from "hono";
import { WebSocketSendGate as WebSocketSendGate$1, WebSocketSendPolicy as WebSocketSendPolicy$1, WebSocketSendResult, WebSocketSendResult as WebSocketSendResult$1, readWebSocketEnvelope as readWebSocketEnvelope$1 } from "@velajs/live-protocol";
import { env, getRuntimeKey } from "hono/adapter";
import { ContentfulStatusCode } from "hono/utils/http-status";

interface VelaCreateOptions extends BootstrapOptions {

  adapters?: RuntimeAdapter[];
}
declare const VelaFactory: {
  create(rootModule: Type, options?: VelaCreateOptions): Promise<VelaApplication>;
};

declare function createOpenApiDocument(rootModule: Type, options?: CreateOpenApiDocumentOptions): OpenApiDocument;

interface EndpointSchema<Value> {
  parse(value: unknown): Value;
  toJSONSchema(): unknown;
}

interface EndpointRequest {
  param?: unknown;
  query?: unknown;
  header?: unknown;
  json?: unknown;
}

type EndpointHandlerOutput<S extends ValidationSchema> = S extends {
  readonly schema: infer Inner extends ValidationSchema;
} ? EndpointHandlerOutput<Inner> : S extends StandardSchemaV1 ? SchemaInput<S> : SchemaOutput<S>;
interface EndpointDefinition<Input extends EndpointRequest, Output, HandlerOutput = Output, InputSchema extends ValidationSchema = EndpointSchema<Input>, OutputSchema extends ValidationSchema = EndpointSchema<Output>> {
  readonly status: ContentfulStatusCode;
  readonly input: InputSchema;
  readonly output: OutputSchema;
  readonly inputSchema: JsonSchema;
  readonly outputSchema: JsonSchema;
  readonly format: 'json' | 'text';
  readonly hasJsonBody: boolean;
  readonly queryParameters: readonly {
    name: string;
    multiple: boolean;
  }[];

  bind<This>(handler: (this: This, input: NoInfer<Input>) => NoInfer<HandlerOutput> | Promise<NoInfer<HandlerOutput>>): (this: This, input: unknown) => Promise<Output>;
}
declare function defineEndpoint<Input extends ValidationSchema, Output extends ValidationSchema>(options: {
  input: Input & (SchemaOutput<Input> extends EndpointRequest ? unknown : never);
  output: Output;
  status?: ContentfulStatusCode;
  format?: SchemaOutput<Output> extends string ? 'json' | 'text' : 'json';
}): EndpointDefinition<Extract<SchemaOutput<Input>, EndpointRequest>, SchemaOutput<Output>, EndpointHandlerOutput<Output>, Input, Output>;

declare function defineEndpoint<Input extends EndpointRequest, Output>(options: {
  input: EndpointSchema<Input>;
  output: EndpointSchema<Output>;
  status?: ContentfulStatusCode;
  format?: Output extends string ? 'json' | 'text' : 'json';
}): EndpointDefinition<Input, Output>;

interface RuntimeEndpointDefinition {
  readonly status: ContentfulStatusCode;
  readonly input: ValidationSchema;
  readonly output: ValidationSchema;
  readonly inputSchema: JsonSchema;
  readonly outputSchema: JsonSchema;
  readonly format: 'json' | 'text';
  readonly hasJsonBody: boolean;
  readonly queryParameters: readonly {
    name: string;
    multiple: boolean;
  }[];
}

declare function Endpoint<Input extends ValidationSchema, Output extends ValidationSchema>(definition: RuntimeEndpointDefinition & {
  readonly input: Input;
  readonly output: Output;
}): <Handler extends (input: NoInfer<SchemaOutput<Input>>) => NoInfer<EndpointHandlerOutput<Output>> | Promise<NoInfer<EndpointHandlerOutput<Output>>>>(target: object, key: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

declare function ApiDoc(metadata: ApiDocMetadata): MethodDecorator & ClassDecorator;

declare function ApiTags(...tags: string[]): MethodDecorator & ClassDecorator;

declare function ApiResponse(status: number | string, options: ApiResponseOptions): MethodDecorator;

declare function zodToJsonSchema(schema: unknown, direction?: 'input' | 'output'): JsonSchema;

declare function Injectable(options?: InjectableOptions): ClassDecorator;
declare function Optional(): ParameterDecorator;
declare function Inject(token: Token | ForwardRef): ParameterDecorator;

declare function mixin<T>(mixinClass: Type<T>): Type<T>;

declare function createLazyParamDecorator<TData = unknown>(factory: (data: TData, ctx: ExecutionContext) => unknown): (...args: undefined extends TData ? [data?: TData] : [data: TData]) => ParameterDecorator;

declare class SignedUrlGuard implements CanActivate {
  #private;
  constructor(secretToken?: string, env?: Record<string, unknown>);
  canActivate(context: ExecutionContext): Promise<boolean>;
}

declare function SignedUrl(): ReturnType<typeof applyDecorators>;

declare const URL_SIGNING_SECRET: InjectionToken<string>;

declare function enableAmbientContainer(): MiddlewareHandler;

declare function getCurrentContainer(): Container;

declare function getCurrentRequestContext(): RequestContext;

declare class MemoryNonceStore implements NonceStore {
  private readonly seen;
  claim(nonce: string, expEpochSeconds: number): Promise<boolean>;

  private prune;
}

declare const INVOCATION_AUDIENCE: 'vela:invoke';

declare const INVOCATION_DEFAULT_TTL_SECONDS = 60;

interface InvocationClaim {

  aud: typeof INVOCATION_AUDIENCE;

  method: string;

  path: string;

  bodyHash: string;

  exp: number;

  nonce: string;

  iss?: string;
}
interface VerifyInvocationOptions {

  now?: number;
}

declare function signInvocation(claim: InvocationClaim, secret: string): Promise<string>;

declare function verifyInvocation(token: string, secret: string, opts?: VerifyInvocationOptions): Promise<InvocationClaim | null>;

interface TrustedRequestPrincipal {
  readonly issuer: string;
  readonly subject: string;
  readonly principalType: 'user' | 'service';
}

interface TrustedRequestIdentity {
  readonly principal: TrustedRequestPrincipal;
  readonly tenantId?: string;

  readonly expiresAtMs?: number;

  readonly roles?: readonly string[];

  readonly claims?: Readonly<Record<string, unknown>>;
}

declare function bindTrustedRequestContext(context: ExecutionContext, request: Request): void;

declare function getTrustedContextRequest(context: ExecutionContext): Request | undefined;

declare function setTrustedRequestIdentity(request: Request, identity: TrustedRequestIdentity): void;

declare function clearTrustedRequestIdentity(request: Request): void;

declare function getTrustedRequestIdentity(request: Request): TrustedRequestIdentity | undefined;

declare function setTrustedRequestTenant(request: Request, expectedIdentity: TrustedRequestIdentity, tenantId: string): TrustedRequestIdentity;

interface TrustedRequestIdentityStore<T> {
  get(request: Request): T | undefined;

  set(request: Request, value: T): void;
}

declare function createTrustedRequestIdentityStore<T>(): TrustedRequestIdentityStore<T>;

declare function buildExecutionContext(c: Context, controller: Type, handlerName: string | symbol, moduleId?: string): HttpExecutionContext;

declare const LogLevel: {
  readonly VERBOSE: 0;
  readonly DEBUG: 1;
  readonly LOG: 2;
  readonly WARN: 3;
  readonly ERROR: 4;
  readonly SILENT: 5;
};
type LogLevel = (typeof LogLevel)[keyof typeof LogLevel];
interface LoggerService {
  log(message: unknown, ...optionalParams: unknown[]): void;
  error(message: unknown, ...optionalParams: unknown[]): void;
  warn(message: unknown, ...optionalParams: unknown[]): void;
  debug?(message: unknown, ...optionalParams: unknown[]): void;
  verbose?(message: unknown, ...optionalParams: unknown[]): void;
}
type ContextProvider = () => Record<string, unknown>;
type LoggerLevelName = 'LOG' | 'ERROR' | 'WARN' | 'DEBUG' | 'VERBOSE';
type Writer = (level: LoggerLevelName, line: string, ...rest: unknown[]) => void;
declare class Logger implements LoggerService {
  static level: LogLevel;
  private static globalLogger;
  private static globalContextProviders;
  private static globalWriter;
  private context?;
  private instanceContextProviders;
  private instanceWriter?;
  static setLogLevel(level: LogLevel): void;
  static overrideLogger(logger: LoggerService | false): void;

  static addContextProvider(provider: ContextProvider): void;

  static clearContextProviders(): void;

  static setWriter(writer: Writer): void;

  static resetWriter(): void;
  constructor(context?: string);
  setContext(context: string): void;

  addContextProvider(provider: ContextProvider): this;

  setWriter(writer: Writer): this;

  extend(namespace: string): Logger;
  log(message: unknown, ...optionalParams: unknown[]): void;
  error(message: unknown, ...optionalParams: unknown[]): void;
  warn(message: unknown, ...optionalParams: unknown[]): void;
  debug(message: unknown, ...optionalParams: unknown[]): void;
  verbose(message: unknown, ...optionalParams: unknown[]): void;
  private emit;
  private formatMessage;
  private collectContext;
  private formatContextObj;
}

interface ConfigNamespace<TKey extends string = string, TEnv = Record<string, unknown>, TConfig extends object = object> {

  readonly KEY: InjectionToken<TConfig>;

  readonly namespace: TKey;

  readonly factory: (env: TEnv) => TConfig;

  asProvider(): ProviderDefinition<TConfig>;
}

interface AnyConfigNamespace {
  readonly KEY: Token;
  readonly namespace: string;
  asProvider(): ProviderDefinition<object>;
}

declare function registerAs<TKey extends string, TEnv, TConfig extends object>(namespace: TKey, envToken: InjectionToken<TEnv> | Type<TEnv>, factory: (env: NoInfer<TEnv>) => TConfig): ConfigNamespace<TKey, TEnv, TConfig>;

type InferConfigType<T> = T extends {
  readonly KEY: InjectionToken<infer C>;
} ? C : never;

type ConfigType<L extends readonly AnyConfigNamespace[]> = { [N in L[number] as N['namespace']]: InferConfigType<N>; };

type ConfigSchema = ((config: Record<string, unknown>) => unknown) | {
  parse: (config: unknown) => unknown;
};

interface ConfigModuleOptions {

  config?: Record<string, unknown>;

  load?: AnyConfigNamespace[];

  validate?: (config: Record<string, unknown>) => Record<string, unknown>;

  validateSchema?: ConfigSchema;
  isGlobal?: boolean;
}

type ConfigPath<T> = { [K in keyof T & string]: T[K] extends Record<string, unknown> ? K | `${K}.${ConfigPath<T[K]>}` : K; }[keyof T & string];

type ConfigPathValue<T, P extends string> = P extends `${infer K}.${infer Rest}` ? K extends keyof T ? T[K] extends Record<string, unknown> ? ConfigPathValue<T[K], Rest> : never : never : P extends keyof T ? T[P] : never;

declare const ConfigurableModuleClass$9: ConfigurableModuleClassType<ConfigModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
declare class ConfigModule extends ConfigurableModuleClass$9 {

  static forRoot(options?: ConfigModuleOptions & {
    isGlobal?: boolean;
    key?: string;
  }): DynamicModule;
}

declare class ConfigStore {
  private readonly container;
  private readonly config;
  private readonly validateSchema?;
  private readonly namespaceKeys;
  private readonly cache;
  private validated;
  private validationError;
  constructor(container: Pick<Container, 'resolve'>, config?: Record<string, unknown>, namespaces?: ReadonlyArray<AnyConfigNamespace>, validateSchema?: ConfigSchema | undefined);

  get(path: string): unknown;

  getOrThrow(path: string): unknown;

  has(path: string): boolean;

  all(): Record<string, unknown>;
  private read;

  private resolveNamespace;
  private merged;

  private ensureValidated;
}

declare class ConfigService {
  private readonly store;
  constructor(store: ConfigStore);
  get(path: string, defaultValue?: unknown): unknown;
  getOrThrow(path: string): unknown;

  parse<T>(path: string, schema: {
    parse(value: unknown): T;
  }): T;
  has(path: string): boolean;
  getAll(): Record<string, unknown>;
}

declare const CONFIG_OPTIONS: InjectionToken<Record<string, unknown>>;

declare const CONFIG_ENV: InjectionToken<Record<string, unknown>>;

interface SecurityCorsOptions {

  allowMethods?: string[];

  allowHeaders?: string[];
  exposeHeaders?: string[];

  credentials?: boolean;

  maxAge?: number;
}
interface OriginProtectionOptions {

  unsafeMethods?: string[];

  protectAuthorization?: boolean;

  allowMissingOrigin?: boolean;
}
interface SecurityHeadersOptions {
  noSniff?: boolean;
  referrerPolicy?: string | false;
  frameOptions?: 'DENY' | 'SAMEORIGIN' | false;

  hsts?: string | false;
  contentSecurityPolicy?: string | false;
}
interface SecurityModuleOptions {

  allowedOrigins?: string[];

  cors?: SecurityCorsOptions | false;

  originProtection?: OriginProtectionOptions | false;

  headers?: SecurityHeadersOptions;
}

declare const ConfigurableModuleClass$8: ConfigurableModuleClassType<SecurityModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
declare class SecurityModule extends ConfigurableModuleClass$8 {

  static forRoot(options?: SecurityModuleOptions & {
    isGlobal?: boolean;
    key?: string;
  }): DynamicModule;
}

declare class Secret<T> {
  #private;
  constructor(value: T);
  reveal(): T;
  toJSON(): string;
  toString(): string;
}

declare const SECURITY_OPTIONS: InjectionToken<SecurityModuleOptions>;

type PrioritizedMiddleware = NestMiddleware & {
  priority: number;
};
declare function buildSecurityMiddleware(options: SecurityModuleOptions): PrioritizedMiddleware;

interface HttpModuleOptions {

  baseURL?: string;

  headers?: Record<string, string>;

  timeout?: number;
}
interface RequestConfig {
  headers?: Record<string, string>;
  params?: Record<string, string | number | boolean>;
  timeout?: number;
}
interface HttpRequestConfig extends RequestConfig {
  method: string;
  url: string;
  body?: unknown;
}
interface HttpResponse<T = unknown> {
  data: T;
  status: number;
  statusText: string;
  headers: Headers;
}

declare const ConfigurableModuleClass$7: ConfigurableModuleClassType<HttpModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
declare class HttpModule extends ConfigurableModuleClass$7 {}

declare const HTTP_MODULE_OPTIONS: InjectionToken<HttpModuleOptions>;
declare class HttpRequestException extends Error {
  readonly status: number;
  readonly statusText: string;
  readonly response: Response;
  constructor(status: number, statusText: string, response: Response);
}
declare class HttpService {
  private readonly baseURL;
  private readonly defaultHeaders;
  private readonly defaultTimeout;
  constructor(options: HttpModuleOptions);
  get<T = unknown>(url: string, config?: RequestConfig): Promise<HttpResponse<T>>;
  post<T = unknown>(url: string, body?: unknown, config?: RequestConfig): Promise<HttpResponse<T>>;
  put<T = unknown>(url: string, body?: unknown, config?: RequestConfig): Promise<HttpResponse<T>>;
  patch<T = unknown>(url: string, body?: unknown, config?: RequestConfig): Promise<HttpResponse<T>>;
  delete<T = unknown>(url: string, config?: RequestConfig): Promise<HttpResponse<T>>;
  head(url: string, config?: RequestConfig): Promise<HttpResponse<void>>;
  request<T = unknown>(config: HttpRequestConfig): Promise<HttpResponse<T>>;
  private buildUrl;
  private parseBody;
}

interface CorsOptions {
  origin?: string | string[] | ((origin: string) => string | undefined | null);
  allowMethods?: string[];
  allowHeaders?: string[];
  exposeHeaders?: string[];
  credentials?: boolean;
  maxAge?: number;
}

declare const ConfigurableModuleClass$6: ConfigurableModuleClassType<CorsOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
declare class CorsModule extends ConfigurableModuleClass$6 {}

declare const CORS_OPTIONS: InjectionToken<CorsOptions>;

type Awaitable<T> = T | Promise<T>;
interface CacheModuleOptions {
  ttl?: number;
  max?: number;
  isGlobal?: boolean;

  varyBy?: (request: Request) => Awaitable<string | undefined>;

  store?: CacheStore;
}

interface CacheStore {
  get(key: string): unknown;
  set(key: string, value: unknown, ttl?: number): void;
  del(key: string): void;
  clear(): void;
}

interface AsyncCacheStore {
  get(key: string): Promise<unknown>;
  set(key: string, value: unknown, ttl?: number): Promise<void>;
  del(key: string): Promise<void>;
  clear(): Promise<void>;
}

type AnyCacheStore = CacheStore | AsyncCacheStore;
interface CacheEntry<T = unknown> {
  value: T;
  expiresAt: number;
}

interface CacheEntryReader {
  getEntry(key: string): Awaitable<{
    value: unknown;
    expiresAt?: number;
  } | undefined>;
}

interface CacheEntryWriter {
  setEntry(key: string, entry: CacheEntry): Awaitable<void>;
}

declare const ConfigurableModuleClass$5: ConfigurableModuleClassType<CacheModuleOptions, "forRoot", "create", {
  isGlobal: boolean;
}>;
declare class CacheModule extends ConfigurableModuleClass$5 {}

declare class CacheService {
  private store;
  constructor(store: CacheStore);
  get(key: string): unknown;

  getParsed<T>(key: string, parse: (value: unknown) => T): T | undefined;
  set(key: string, value: unknown, ttl?: number): void;
  del(key: string): void;
  clear(): void;
}

declare class CacheInterceptor implements NestInterceptor {
  private cacheStore;
  private options;
  private reflector;
  constructor(cacheStore: CacheStore, options: CacheModuleOptions);
  intercept(context: ExecutionContext, next: CallHandler): Promise<unknown>;
}

declare class MemoryCacheStore implements CacheStore {
  private store;
  private defaultTtl;
  private max;
  constructor(ttl?: number, max?: number);
  get(key: string): unknown;
  getEntry(key: string): CacheEntry | undefined;
  set(key: string, value: unknown, ttl?: number): void;
  setEntry(key: string, entry: CacheEntry): void;
  del(key: string): void;
  clear(): void;
  private evict;
}

declare class TieredCacheStore implements AsyncCacheStore, CacheEntryReader, CacheEntryWriter {
  private readonly tiers;
  private revision;
  private pending;
  constructor(tiers: AnyCacheStore[]);
  get(key: string): Promise<unknown>;
  getEntry(key: string): Promise<{
    value: unknown;
    expiresAt?: number;
  } | undefined>;
  set(key: string, value: unknown, ttl?: number): Promise<void>;
  setEntry(key: string, entry: CacheEntry): Promise<void>;
  del(key: string): Promise<void>;
  clear(): Promise<void>;
  private mutate;
  private enqueue;
}

declare const Cacheable: () => (target: object, propertyKey?: string | symbol) => void;
declare const CacheKey: (key: string) => (target: object, propertyKey?: string | symbol) => void;
declare const CacheTTL: (seconds: number) => (target: object, propertyKey?: string | symbol) => void;

declare const CACHE_MANAGER: InjectionToken<CacheStore>;
declare const CACHE_MODULE_OPTIONS: InjectionToken<CacheModuleOptions>;
declare const CACHEABLE_METADATA = "vela:cacheable";
declare const CACHE_KEY_METADATA = "vela:cache-key";
declare const CACHE_TTL_METADATA = "vela:cache-ttl";

type ResponseCacheScope = {
  visibility: 'public';
  partition: string;
} | {
  visibility: 'private';
  partition: string;
};

interface CacheInvalidationStore {
  getVersion(key: string): Awaitable<string>;
  invalidate(key: string): Awaitable<void>;
}
interface ResponseCacheEntryOptions {

  ttl?: number;

  tags?: readonly string[];
}
interface CacheResponseOptions extends ResponseCacheEntryOptions {

  key?: string;
}
interface ResponseCacheOptions {

  namespace: string;
  store: AnyCacheStore;

  scope: (context: ExecutionContext) => Awaitable<ResponseCacheScope | undefined>;
  invalidation?: CacheInvalidationStore;
  ttl?: number;

  maxBytes?: number;

  shouldCache?: (value: unknown) => boolean;

  onError?: (operation: 'read' | 'write' | 'invalidate' | 'scope', error: unknown) => void;
}

type CacheInvalidationResult = {
  ok: true;
} | {
  ok: false;
  reason: 'unsupported' | 'invalid-input' | 'store-error';
};
interface ScopedResponseCache {
  get(key: string): Promise<unknown>;
  getParsed<T>(key: string, parse: (value: unknown) => T): Promise<T | undefined>;
  set(key: string, value: unknown, options?: ResponseCacheEntryOptions): Promise<boolean>;

  remember<T>(key: string, load: () => Promise<T>, options?: ResponseCacheEntryOptions): Promise<unknown | T>;
  invalidateKey(key: string): Promise<CacheInvalidationResult>;
  invalidateTags(tags: readonly string[]): Promise<CacheInvalidationResult>;
  invalidateAll(): Promise<CacheInvalidationResult>;
}

declare const ConfigurableModuleClass$4: ConfigurableModuleClassType<ResponseCacheOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;

declare class ResponseCacheModule extends ConfigurableModuleClass$4 {}

declare const RESPONSE_CACHE_OPTIONS: InjectionToken<ResponseCacheOptions>;
declare class ResponseCacheService {
  readonly options: Readonly<ResponseCacheOptions>;
  constructor(options: ResponseCacheOptions);

  scope(scope: ResponseCacheScope): ScopedResponseCache;
  /** @internal */
  scoped(scope: ResponseCacheScope, domain: 'service' | 'http'): ScopedResponseCache;
  /** @internal */
  report(operation: 'read' | 'write' | 'invalidate' | 'scope', error: unknown): void;
  private accept;
}

declare function CacheResponse(options?: CacheResponseOptions): (target: object, propertyKey?: string | symbol) => void;
declare class ResponseCacheInterceptor implements NestInterceptor {
  private readonly cache;
  private readonly reflector;
  constructor(cache: ResponseCacheService);
  intercept(context: ExecutionContext, next: CallHandler): Promise<unknown>;
}

declare class MemoryCacheInvalidationStore implements CacheInvalidationStore {
  private readonly max;
  private readonly versions;
  constructor(max?: number);
  getVersion(key: string): string;
  invalidate(key: string): void;
}

declare class EventEmitterModule {}

interface EventDefinition<Name extends string = string, Schema extends ValidationSchema = ValidationSchema> {
  readonly name: Name;
  readonly schema: Schema;
}
type EventInput<Event extends EventDefinition> = SchemaInput<Event['schema']>;
type EventPayload<Event extends EventDefinition> = SchemaOutput<Event['schema']>;
declare function defineEvent<const Name extends string, Schema extends ValidationSchema>(name: Name, schema: Schema): EventDefinition<Name, Schema>;
type EventVocabulary<Schemas extends Record<string, ValidationSchema>> = { readonly [Name in keyof Schemas & string]: EventDefinition<Name, Schemas[Name]>; };

declare function defineEventVocabulary<const Schemas extends Record<string, ValidationSchema>>(schemas: Schemas): EventVocabulary<Schemas>;

type EventHandler = (...args: unknown[]) => void | Promise<void>;
interface OnEventMetadata {
  event: string;
  methodName: string;
}
interface EventEmitOptions {

  settlement?: 'legacy' | 'complete';
}

declare class EventEmitter {
  #private;
  on(event: string, handler: EventHandler): this;
  once(event: string, handler: EventHandler): this;
  off(event: string, handler: EventHandler): this;

  emit(event: string, ...args: unknown[]): Promise<void>;

  emitWithOptions(event: string, options: EventEmitOptions, ...args: unknown[]): Promise<void>;
  removeAllListeners(event?: string): this;
  listenerCount(event: string): number;
}

declare class EventEmitterSubscriber implements OnApplicationBootstrap {
  #private;
  constructor(discovery: DiscoveryService, emitter: EventEmitter, root?: Container);
  onApplicationBootstrap(): void;
  dispose(): void;
}

type EventListenerDecorator<Payload> = <Handler extends (payload: Payload) => unknown>(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

declare function OnEvent(event: string): MethodDecorator;
declare function OnEvent<Event extends EventDefinition>(event: Event): EventListenerDecorator<EventPayload<Event>>;

declare const ON_EVENT_METADATA = "vela:on-event";

declare class EventDispatcher {
  #private;
  constructor(root: Container, discovery: DiscoveryService);

  emit<Event extends EventDefinition>(event: Event, input: EventInput<NoInfer<Event>>): Promise<void>;

  inScope(scope: Container): ScopedEventDispatcher;
}
interface ScopedEventDispatcher {

  emit<Event extends EventDefinition>(event: Event, input: EventInput<NoInfer<Event>>): Promise<void>;

  emitUnknown(event: EventDefinition, input: unknown): Promise<void>;

  defer<Event extends EventDefinition>(event: Event, input: EventInput<NoInfer<Event>>): void;
}

declare class ScheduleModule {
  static forRoot(options?: {
    dispatch?: ScheduleDispatchMode;
  }): DynamicModule;
}

declare function Cron(expression: string, options?: CronOptions): MethodDecorator;
declare function Interval(ms: number): MethodDecorator;

declare const CRON_METADATA = "vela:cron";
declare const INTERVAL_METADATA = "vela:interval";

declare const SCHEDULE_DISPATCH: InjectionToken<ScheduleDispatchMode>;

declare function parseCronMetadata(value: unknown): CronMetadata;

declare function parseIntervalMetadata(value: unknown): IntervalMetadata;

declare function WebSocketGateway(options?: WebSocketGatewayOptions): ClassDecorator;

declare function SubscribeMessage(event: string): MethodDecorator;

declare function ReservedWsEvent(event: string): ClassDecorator;

declare const MessageBody: () => ParameterDecorator;

declare const ConnectedSocket: () => ParameterDecorator;

declare function WebSocketServer(): ParameterDecorator;

declare const MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES: number;

declare function broadcastCommandFits(value: unknown, maxFrameBytes?: number): value is BroadcastCommand;
declare function assertBroadcastCommandFits(value: unknown, maxFrameBytes?: number): asserts value is BroadcastCommand;

declare function webSocketSyncEnvelopeFits(serialized: string, maxFrameBytes: number): boolean;

interface RoomRegistry {

  register(client: WsClient): void;
  join(client: WsClient, room: string): void | Promise<void>;
  leave(client: WsClient, room: string): void | Promise<void>;

  leaveAll(client: WsClient): void | Promise<void>;

  deliverLocal(cmd: BroadcastCommand): void | Promise<void>;

  setDeliveryAuthorizer?(authorizer: (client: WsClient) => boolean | Promise<boolean>): void;
  localIdsInRoom(room: string): string[];
}

interface SyncDriver {
  readonly kind: string;

  bind(registry: RoomRegistry): void;

  dispatch(cmd: BroadcastCommand): void | Promise<void>;
  /** @internal */
  setMaxFrameBytes?(maxFrameBytes: number): void;
  countRoom?(room: string): Promise<number>;
  start?(): void | Promise<void>;
  stop?(): void | Promise<void>;
}

declare function local(): SyncDriver;

declare class InMemoryRoomRegistry implements RoomRegistry {
  private readonly rooms;
  private readonly clientRooms;
  private readonly clients;
  private deliveryAuthorizer?;
  setDeliveryAuthorizer(authorizer: (client: WsClient) => boolean | Promise<boolean>): void;
  register(client: WsClient): void;
  join(client: WsClient, room: string): void;
  leave(client: WsClient, room: string): void;
  leaveAll(client: WsClient): void;
  localIdsInRoom(room: string): string[];
  deliverLocal(cmd: BroadcastCommand): void | Promise<void>;
}

interface WebSocketModuleOptions {

  sync?: SyncDriver;

  registry?: RoomRegistry;
}

declare const ConfigurableModuleClass$3: ConfigurableModuleClassType<WebSocketModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
declare class WebSocketModule extends ConfigurableModuleClass$3 {}

interface WsEntrypointMeta {

  path: string;

  dispatcher: WsDispatcher;

  options: WebSocketGatewayOptions;

  moduleId: string;
}

declare function readWsEntrypointMeta(value: unknown): WsEntrypointMeta;

declare class WsDispatcher implements OnApplicationBootstrap, ContributesEntrypoints {
  #private;
  constructor(container: Container, discovery: DiscoveryService, server?: WsServer, routeManager?: RouteManager);

  get gatewayPaths(): string[];

  getGatewayMaxFrameBytes(path: string): number | undefined;

  getMaximumGatewayFrameBytes(): number;
  onApplicationBootstrap(): Promise<void>;

  collectEntrypoints(): Entrypoint<WsEntrypointMeta>[];
  private initializeGateway;
  private resolveGateway;
  handleOpen(path: string, client: WsClient): Promise<void>;
  handleClose(path: string, client: WsClient, _code: number, _reason: string): Promise<void>;
  private resolveReserved;
  handleError(path: string, _client: WsClient, err: unknown): Promise<void>;

  authorizeDelivery(path: string, client: WsClient, invocationScope?: Container): Promise<boolean>;
  dispatchMessage(path: string, client: WsClient, raw: string | ArrayBuffer): Promise<void>;

  private dispatchReserved;
  private reply;
  private runFilters;
  private trySend;
  private reportBootstrapError;
  private parse;
  private frameFits;
  private buildGatewayEntry;
}

declare const DEFAULT_WS_MAX_FRAME_BYTES: number;

declare const DEFAULT_WS_MAX_ROOM_ID_BYTES = 512;

declare const DEFAULT_WS_MAX_JOINED_ROOMS = 32;
interface AuthenticatedWebSocketUpgrade {

  request: Request;
  identity: WebSocketUpgradeIdentity;
}

declare function assertWebSocketRoomId(value: unknown): asserts value is string;

declare function resolveGatewayRoomParam(options: WebSocketGatewayOptions): string | undefined;

declare function resolveGatewayRoomId(options: WebSocketGatewayOptions, param: (name: string) => string | undefined): string;

declare function resolveMaxFrameBytes(options: WebSocketGatewayOptions): number;

declare function webSocketFrameFits(frame: string | ArrayBuffer, maxFrameBytes: number): boolean;

declare function isWebSocketOriginAllowed(request: Request, allowedOrigins?: '*' | readonly string[]): boolean;

declare function authorizeWebSocketUpgrade(options: WebSocketGatewayOptions, request: Request): Promise<boolean>;

declare function authenticateWebSocketUpgrade(options: WebSocketGatewayOptions, request: Request, room: string): Promise<AuthenticatedWebSocketUpgrade | false>;

declare function normalizeWebSocketUpgradeIdentity(value: unknown): WebSocketUpgradeIdentity | false;

declare const WEBSOCKET_TICKET_AUDIENCE: 'vela:websocket';

declare const WEBSOCKET_TICKET_PURPOSE: 'socket-ticket';

declare const WEBSOCKET_TICKET_MAX_TTL_MS = 30000;

type WebSocketTicketPrincipalType = 'user' | 'service';
interface WebSocketTicketPrincipal {
  issuer: string;
  subject: string;
  principalType: WebSocketTicketPrincipalType;
}

interface WebSocketTicketClaim {
  aud: typeof WEBSOCKET_TICKET_AUDIENCE;
  purpose: typeof WEBSOCKET_TICKET_PURPOSE;
  gatewayPath: string;
  room: string;
  principal: WebSocketTicketPrincipal;
  tenantId: string;
  issuedAtMs: number;
  expiresAtMs: number;
  nonce: string;
}

type WebSocketTicketNonceStore = Pick<NonceStore, 'claim'>;
interface IssueWebSocketTicketOptions {
  secret: string;
  gatewayPath: string;
  room: string;
  principal: WebSocketTicketPrincipal;
  tenantId: string;

  ttlMs?: number;

  nowMs?: number;
}
interface VerifyWebSocketTicketOptions {
  secret: string;

  gatewayPath: string;

  room: string;

  nonceStore: WebSocketTicketNonceStore;

  nowMs?: number;
}

declare function issueWebSocketTicket(options: IssueWebSocketTicketOptions): Promise<string>;

declare function verifyAndConsumeWebSocketTicket(token: unknown, options: VerifyWebSocketTicketOptions): Promise<WebSocketTicketClaim | false>;

declare class BroadcastOperatorImpl implements BroadcastOperator {
  private readonly driver;
  private readonly maxFrameBytes;
  private readonly rooms;
  private readonly exceptRooms;
  private readonly exceptIds;
  constructor(driver: SyncDriver, maxFrameBytes?: () => number, rooms?: Set<string>, exceptRooms?: Set<string>, exceptIds?: Set<string>);
  to(room: string): this;
  in(room: string): this;
  except(room: string): this;
  emit(event: string, data?: unknown): void | Promise<void>;
}

declare class WsServerImpl implements WsServer {
  private readonly driver;
  private maxFrameBytes;
  private hasGatewayLimit;
  constructor(driver: SyncDriver);
  setOutboundFrameLimit(maxFrameBytes: number): void;
  emit(event: string, data?: unknown): void | Promise<void>;
  to(room: string): BroadcastOperator;
  in(room: string): BroadcastOperator;
  except(room: string): BroadcastOperator;
}

declare function buildWsExecutionContext(client: WsClient, data: unknown, controller: Type, handlerName: string | symbol, pattern: string, moduleId?: string, container?: Container): WsExecutionContext;

declare class WsException extends Error {
  private readonly err;
  constructor(err: string | Record<string, unknown>);
  getError(): string | Record<string, unknown>;
}

declare function toErrorFrame(error: unknown, catalog?: Catalog<string>): {
  event: 'exception';
  data: unknown;
};

declare function resolveWsArgs(paramMeta: ParameterMetadata[], client: WsClient, data: unknown, pipes: PipeTransform[], paramTypes?: unknown[]): Promise<unknown[]>;

declare const WS_GATEWAY_METADATA = "vela:ws-gateway";
declare const WS_SUBSCRIBE_METADATA = "vela:ws-subscribe";
declare const WS_RESERVED_METADATA = "vela:ws-reserved";

declare const RESERVED_WS_EVENT_PREFIX = "$";
declare const WsParamType: {
  readonly SOCKET: 'ws_socket';
  readonly BODY: 'ws_body';
  readonly SERVER: 'ws_server';
};
type WsParamType = (typeof WsParamType)[keyof typeof WsParamType];

declare const WS_SERVER: InjectionToken<WsServer>;

declare const WS_SYNC_DRIVER: InjectionToken<SyncDriver>;

declare const WS_ROOM_REGISTRY: InjectionToken<RoomRegistry>;
declare const WS_MODULE_OPTIONS: InjectionToken<WebSocketModuleOptions>;

declare function trySendWebSocketFrame(client: WsClient, payload: string): WebSocketSendResult;

declare class WsMessageQueue {
  #private;
  constructor(overflow: () => void, maxMessages?: number, maxBytes?: number);
  stop(): void;
  run(frame: string | ArrayBuffer, work: () => Promise<void>): Promise<void>;
}

declare class HealthModule {}

type ExceptionResponse = string | Record<string, unknown>;
declare class HttpException extends Error {
  readonly statusCode: number;
  private readonly _response;
  constructor(response: ExceptionResponse, statusCode: number);
  getStatus(): number;
  getResponse(): Record<string, unknown>;
  getRawResponse(): ExceptionResponse;
}
declare class BadRequestException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class UnauthorizedException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class ForbiddenException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class NotFoundException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class MethodNotAllowedException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class NotAcceptableException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class RequestTimeoutException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class ConflictException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class GoneException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class PayloadTooLargeException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class UnsupportedMediaTypeException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class UnprocessableEntityException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class TooManyRequestsException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class InternalServerErrorException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class NotImplementedException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class BadGatewayException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class ServiceUnavailableException extends HttpException {
  constructor(message?: ExceptionResponse);
}
declare class GatewayTimeoutException extends HttpException {
  constructor(message?: ExceptionResponse);
}

type HealthCheckStatus = 'ok' | 'error' | 'shutting_down';
interface HealthIndicatorResult {
  [key: string]: {
    status: 'up' | 'down';
    [key: string]: unknown;
  };
}
interface HealthCheckResult {
  status: HealthCheckStatus;
  info: HealthIndicatorResult;
  error: HealthIndicatorResult;
  details: HealthIndicatorResult;
}
type HealthIndicatorFunction = () => Promise<HealthIndicatorResult>;
type ResponseCheckCallback = (response: Response) => boolean | Promise<boolean>;

declare class HealthCheckService implements BeforeApplicationShutdown {
  private isShuttingDown;
  beforeApplicationShutdown(): void;
  check(indicators: HealthIndicatorFunction[]): Promise<HealthCheckResult>;
}

interface HealthIndicatorBuilder {
  up(data?: Record<string, unknown>): HealthIndicatorResult;
  down(data?: Record<string, unknown>): HealthIndicatorResult;
}
declare class HealthIndicatorService {
  check(key: string): HealthIndicatorBuilder;
}

interface HttpPingOptions {
  method?: string;
  headers?: Record<string, string>;
  timeout?: number;
  expectedStatus?: number;
}
declare class HttpHealthIndicator {
  private indicator;
  constructor(indicator: HealthIndicatorService);
  pingCheck(key: string, url: string, options?: HttpPingOptions): Promise<HealthIndicatorResult>;
  responseCheck(key: string, url: string, callback: ResponseCheckCallback, options?: HttpPingOptions): Promise<HealthIndicatorResult>;
}

interface ThrottleConfig {
  limit: number;
  ttl: number;
}
interface ThrottlerStorageRecord {
  count: number;
  ttlMs: number;

  allowed?: boolean;

  remaining?: number;

  enforcedLimit?: number;
}
interface ThrottlerStore {
  increment(key: string, ttlMs: number): ThrottlerStorageRecord | Promise<ThrottlerStorageRecord>;
  reset(key: string): void | Promise<void>;
}
interface RateLimitInfo {
  limit: number;
  remaining?: number;
  reset: number;
}
interface ThrottlerModuleOptions extends ThrottleConfig {
  storage?: ThrottlerStore;

  getTracker?: (request: Request, context: ExecutionContext) => string | null | undefined;
  generateKey?: (tracker: string, context: {
    className: string;
    handlerName: string;
  }) => string;
}

declare const ConfigurableModuleClass$2: ConfigurableModuleClassType<ThrottlerModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
declare class ThrottlerModule extends ConfigurableModuleClass$2 {}

declare class ThrottlerGuard implements CanActivate {
  private options;
  private storage;
  private routeManager;
  private reflector;
  constructor(options: ThrottlerModuleOptions, storage: ThrottlerStore, routeManager: RouteManager);
  canActivate(context: ExecutionContext): Promise<boolean>;
}

declare class ThrottlerStorage implements ThrottlerStore {
  private current;
  private previous;
  private lastSwap;
  private readonly swapIntervalMs;
  increment(key: string, ttlMs: number): ThrottlerStorageRecord;
  reset(key: string): void;
  private maybeSwap;
}

declare const Throttle: (config: ThrottleConfig) => (target: object, propertyKey?: string | symbol) => void;
declare const SkipThrottle: () => (target: object, propertyKey?: string | symbol) => void;

declare const THROTTLER_OPTIONS: InjectionToken<ThrottlerModuleOptions>;
declare const THROTTLER_STORAGE: InjectionToken<ThrottlerStore>;
declare const THROTTLE_METADATA = "vela:throttle";
declare const SKIP_THROTTLE_METADATA = "vela:skip-throttle";

declare function stableHash(value: unknown): string;

type ConfigurableModuleExtras = Record<string, unknown>;

interface ModuleRegistrationOptions {
  key?: string;
  lazy?: boolean;
}

type StructuralModuleOptions<Opts> = { [K in Exclude<keyof Opts, 'imports' | 'key' | 'lazy' | 'inject' | 'useFactory' | 'useClass' | 'useExisting'>]?: Opts[K] | undefined; };

type ConfigurableModuleExtrasTransform<E extends ConfigurableModuleExtras> = (definition: DynamicModule, extras: E) => DynamicModule;

type ConfigurableModuleOptionsFactory<Opts, MethodName extends string> = { [K in MethodName]: () => Opts | Promise<Opts>; };

type ConfigurableModuleAsyncOptions<Opts, MethodName extends string = 'create', Inject extends readonly Token[] = readonly Token[]> = {
  imports?: ModuleImport[];
} & ModuleRegistrationOptions & StructuralModuleOptions<Opts> & ConfigurableModuleAsyncFactory<Opts, MethodName, Inject>;

type ConfigurableModuleAsyncFactory<Opts, MethodName extends string, Inject extends readonly Token[] = readonly Token[]> = {
  inject: Inject;
  useFactory: (...args: InferTokens<Inject>) => Opts | Promise<Opts>;
  useClass?: never;
  useExisting?: never;
} | {
  useClass: Type<ConfigurableModuleOptionsFactory<Opts, MethodName>>;
  useFactory?: never;
  useExisting?: never;
  inject?: never;
} | {
  useExisting: InjectionToken<ConfigurableModuleOptionsFactory<Opts, MethodName>> | Type<ConfigurableModuleOptionsFactory<Opts, MethodName>>;
  useFactory?: never;
  useClass?: never;
  inject?: never;
};
interface ConfigurableModuleBuilderOptions<Opts = unknown> {

  moduleName?: string;

  optionsInjectionToken?: InjectionToken<Opts>;
}

type ConfigurableModuleClassType<Opts, MethodKey extends string, FactoryMethodKey extends string, Extras extends ConfigurableModuleExtras> = (new () => object) & Record<MethodKey, (options: Opts & Partial<Extras> & ModuleRegistrationOptions) => DynamicModule> & Record<`${MethodKey}Async`, <const Inject extends readonly Token[]>(options: ConfigurableModuleAsyncOptions<Opts, FactoryMethodKey, Inject> & Partial<Extras> & ModuleRegistrationOptions) => DynamicModule>;
interface ConfigurableModuleHost<Opts, MethodKey extends string = 'forRoot', FactoryMethodKey extends string = 'create', Extras extends ConfigurableModuleExtras = {
  isGlobal?: boolean;
}> {

  ConfigurableModuleClass: ConfigurableModuleClassType<Opts, MethodKey, FactoryMethodKey, Extras>;

  MODULE_OPTIONS_TOKEN: InjectionToken<Opts>;

  OPTIONS_TYPE: Opts & Partial<Extras> & ModuleRegistrationOptions;

  ASYNC_OPTIONS_TYPE: ConfigurableModuleAsyncOptions<Opts, FactoryMethodKey> & Partial<Extras> & ModuleRegistrationOptions;
}

interface DefineConfigurableModuleSpec<Args> {

  module: Type;

  methodName?: string;

  keyFrom: (args: Args) => string;

  providers: (args: Args) => DynamicModule['providers'];

  exports?: DynamicModule['exports'];

  imports?: (args: Args) => DynamicModule['imports'];

  global?: boolean;
}

interface GlobalComponentSlot {
  guards?: GuardType[];
  pipes?: PipeType[];
  interceptors?: InterceptorType[];
  filters?: FilterType[];
  middleware?: MiddlewareType[];
}

interface ModuleContributions {
  providers?: Array<Type | ProviderDefinition>;
  controllers?: Type[];
  imports?: ModuleImport[];
  exports?: Token[];

  global?: GlobalComponentSlot;
}
interface ModuleSetupContext<Opts> {

  readonly OPTIONS: InjectionToken<Opts>;

  readonly options: Partial<Opts>;

  readonly key: string;
}
interface DefineModuleSpec<Opts, Extras extends ConfigurableModuleExtras = {
  isGlobal?: boolean;
}, MethodKey extends string = 'forRoot', FactoryMethodKey extends string = 'create'> {

  name: string;

  optionsToken?: InjectionToken<Opts>;

  setup?: (ctx: ModuleSetupContext<Opts>) => ModuleContributions;

  key?: (options: Partial<Opts>) => string;

  extras?: Extras;

  transform?: ConfigurableModuleExtrasTransform<Extras>;

  methodName?: MethodKey;

  factoryMethodName?: FactoryMethodKey;

  lazy?: boolean;
}

declare function defineModule<Opts, Extras extends ConfigurableModuleExtras = {
  isGlobal?: boolean;
}, MethodKey extends string = 'forRoot', FactoryMethodKey extends string = 'create'>(spec: DefineModuleSpec<Opts, Extras, MethodKey, FactoryMethodKey>): ConfigurableModuleHost<Opts, MethodKey, FactoryMethodKey, Extras>;

declare const moduleKey: (options: unknown) => string;

declare class ConfiguredModuleBuilder<Opts, MethodKey extends string, FactoryMethodKey extends string, Extras extends ConfigurableModuleExtras> {
  private readonly spec;
  constructor(spec: DefineModuleSpec<Opts, Extras, MethodKey, FactoryMethodKey>);
  setExtras<NewExtras extends ConfigurableModuleExtras>(defaults: NewExtras, transform: ConfigurableModuleExtrasTransform<NewExtras>): ConfiguredModuleBuilder<Opts, MethodKey, FactoryMethodKey, NewExtras>;
  setClassMethodName<NewMethodKey extends string>(name: NewMethodKey): ConfiguredModuleBuilder<Opts, NewMethodKey, FactoryMethodKey, Extras>;
  setFactoryMethodName<NewFactoryMethodKey extends string>(name: NewFactoryMethodKey): ConfiguredModuleBuilder<Opts, MethodKey, NewFactoryMethodKey, Extras>;
  build(): ConfigurableModuleHost<Opts, MethodKey, FactoryMethodKey, Extras>;
}

declare class ConfigurableModuleBuilder<Opts> extends ConfiguredModuleBuilder<Opts, 'forRoot', 'create', {
  isGlobal?: boolean;
}> {
  constructor(options?: ConfigurableModuleBuilderOptions<Opts>);
}

declare function defineConfigurableModule<Args>(spec: DefineConfigurableModuleSpec<Args>): Record<string, (args: Args) => DynamicModule>;

interface LazyProviderSpec<T, Inject extends readonly Token[]> {

  provide: InjectionToken<() => T>;
  inject: Inject;
  useFactory: (...deps: InferTokens<Inject>) => NoInfer<T>;

  memoize?: boolean;
}

declare function lazyProvider<T, const Inject extends readonly Token[] = readonly Token[]>(spec: LazyProviderSpec<T, Inject>): ProviderDefinition;
declare function provideGlobal(...[kind, component]: { [K in ComponentType]: [kind: K, component: ComponentTypeMap[K]]; }[ComponentType]): Array<Type | ProviderDefinition>;

declare function sideEffectModule(owner: string | Type, contributions?: Omit<ModuleContributions, 'global'> & {
  key?: string;
}): DynamicModule;

declare function moduleToken<T>(name: string): InjectionToken<T>;

interface ExecutionLifetime {

  readonly id: string;

  readonly startedAt: number;

  readonly signal: AbortSignal | undefined;
  readonly active: boolean;

  defer(work: () => unknown | Promise<unknown>): void;

  waitUntil(work: Promise<unknown>): void;
}
declare const EXECUTION_LIFETIME: InjectionToken<ExecutionLifetime>;
interface ExecutionScopeOptions {
  readonly signal?: AbortSignal;
}
interface ExecutionScope {
  readonly container: Container;
  readonly lifetime: ExecutionLifetime;

  finish(waitFor?: Promise<unknown>): Promise<void>;
}

declare function createExecutionScope(container: Container, options?: ExecutionScopeOptions): ExecutionScope;

declare function getExecutionLifetime(container: Container): ExecutionLifetime | undefined;

declare function finishExecutionScope(container: Container, waitFor?: Promise<unknown>): Promise<void>;

declare function runInEntrypointScope<T>(container: Container, fn: (scope: Container, lifetime: ExecutionLifetime) => T | Promise<T>, options?: ExecutionScopeOptions): Promise<T>;

interface EntrypointExecutionContext<Kind extends string = string> extends ExecutionContext {
  getType(): Kind;
  getContext(): never;
  getRequest(): never;
  switchToHttp(): never;
  switchToWs(): never;

  getPayload(): unknown;
}

declare function buildEntrypointExecutionContext<const Kind extends string>(kind: Kind, targetClass: Type, handlerName: string | symbol, payload: unknown, moduleId?: string, container?: Container): EntrypointExecutionContext<Kind>;

declare function getEntrypointModuleId(container: Container, target: {
  readonly token: Token;
  readonly moduleId?: string;
}): string | undefined;

declare function resolveEntrypoint<K extends Token>(container: Container, target: {
  readonly token: K;
  readonly moduleId?: string;
}): Promise<InferToken<K>>;

interface RouteContributorContext {

  controller: Type;

  controllerPrefix: string;

  meta: unknown;

  globalPrefix: string;

  globalGuards: CanActivate[];

  container: Container;

  joinPaths: (...parts: string[]) => string;
}

interface RouteContributorOpenApiContext {
  controller: Type;
  meta: unknown;

  globalPrefix: string;

  controllerPrefix: string;
}

interface RouteContributor {

  id: string;

  claimsMetaKey: string;
  buildRoutes(app: VelaHono, ctx: RouteContributorContext): void | Promise<void>;
  buildOpenApiPaths?(ctx: RouteContributorOpenApiContext): Record<string, OpenApiPathItem>;
}

declare function registerRouteContributor(contributor: RouteContributor): void;

declare function getRouteContributors(): RouteContributor[];

interface PipelineRunOptions {
  context: ExecutionContext;

  guards: CanActivate[];
  interceptors: NestInterceptor[];

  resolveArgs: () => Promise<unknown[]>;

  invoke: (args: unknown[]) => Promise<unknown>;

  onGuardReject?: () => Error;
}

declare class PipelineRunner {
  static run(options: PipelineRunOptions): Promise<unknown>;

  static chainInterceptors(interceptors: NestInterceptor[], context: ExecutionContext, coreHandler: () => Promise<unknown>): Promise<unknown>;
}

interface ResolvedComponentMap {
  guard: CanActivate;
  pipe: PipeTransform;
  interceptor: NestInterceptor;
  filter: ExceptionFilter;
  middleware: NestMiddleware;
}

type PipelineComponentEntry<T extends ComponentType> = ResolvedComponentMap[T] | TypedToken<ResolvedComponentMap[T]>;

declare function resolvePipelineComponents<T extends ComponentType>(_type: T, entries: readonly PipelineComponentEntry<NoInfer<T>>[], container: Container, moduleId?: string): Promise<ResolvedComponentMap[T][]>;

declare function resolveScopedComponents<T extends ComponentType>(type: T, targetClass: Constructor, methodName: string | symbol, container: Container, moduleId?: string): ResolvedComponentMap[T][];

declare function resolveScopedComponentsAsync<T extends ComponentType>(type: T, targetClass: Constructor, methodName: string | symbol, container: Container, moduleId?: string): Promise<ResolvedComponentMap[T][]>;

declare function UseMiddleware(...middleware: ComponentTypeMap['middleware'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UseGuards(...guards: ComponentTypeMap['guard'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UsePipes(...pipes: ComponentTypeMap['pipe'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UseInterceptors(...interceptors: ComponentTypeMap['interceptor'][]): (target: object, propertyKey?: string | symbol) => void;
declare function UseFilters(...filters: ComponentTypeMap['filter'][]): (target: object, propertyKey?: string | symbol) => void;
declare function Catch(...exceptions: Type<Error>[]): ClassDecorator;
declare function getCatchTypes(filter: unknown): Type<Error>[];
declare function shouldFilterCatch(filter: unknown, exception: unknown): boolean;

interface CreateDecoratorOptions {
  key?: string;
}
interface ReflectableDecorator<TParam> {
  (value: TParam): ClassDecorator & MethodDecorator;
  KEY: string;
}

declare function SetMetadata<V = unknown>(key: string, value: V): (target: object, propertyKey?: string | symbol) => void;

declare class Reflector {

  static createDecorator<TParam = unknown>(options?: CreateDecoratorOptions): ReflectableDecorator<TParam>;
  private resolveKey;

  get<T = unknown>(key: string | ReflectableDecorator<T>, context: ExecutionContext): T | undefined;

  getHandler<T = unknown>(key: string | ReflectableDecorator<T>, context: ExecutionContext): T | undefined;

  getClass<T = unknown>(key: string | ReflectableDecorator<T>, context: Pick<ExecutionContext, 'getClass'>): T | undefined;

  getAll<T = unknown>(key: string | ReflectableDecorator<T>, context: ExecutionContext): [T | undefined, T | undefined];

  getAllAndOverride<T = unknown>(key: string | ReflectableDecorator<T>, context: ExecutionContext): T | undefined;

  getAllAndMerge<T = unknown>(key: string | ReflectableDecorator<T>, context: ExecutionContext): T | T[];
}

declare class ParseIntPipe implements PipeTransform<string, number> {
  transform(value: string, metadata: ArgumentMetadata): number;
}
declare class ParseFloatPipe implements PipeTransform<string, number> {
  transform(value: string, metadata: ArgumentMetadata): number;
}
declare class ParseBoolPipe implements PipeTransform<string, boolean> {
  transform(value: string, metadata: ArgumentMetadata): boolean;
}
declare class DefaultValuePipe<T = unknown> implements PipeTransform<T | undefined, T> {
  private readonly defaultValue;
  constructor(defaultValue: T);
  transform(value: T | undefined, _metadata: ArgumentMetadata): T;
}
declare class RequiredPipe implements PipeTransform {
  transform(value: unknown, metadata: ArgumentMetadata): unknown;
}
declare class ZodValidationPipe implements PipeTransform {
  private readonly schema;
  constructor(schema: {
    parse(data: unknown): unknown;
  });
  transform(value: unknown, _metadata: ArgumentMetadata): unknown;
}
interface ParseUUIDPipeOptions {
  version?: '3' | '4' | '5';
}
declare class ParseUUIDPipe implements PipeTransform<string, string> {
  private readonly options?;
  constructor(options?: ParseUUIDPipeOptions | undefined);
  transform(value: string, metadata: ArgumentMetadata): string;
}
declare class ParseEnumPipe<T extends Record<string, string | number>> implements PipeTransform<string, T[keyof T]> {
  private readonly allowedValues;
  private readonly valuesLabel;
  constructor(enumType: T);
  transform(value: string, metadata: ArgumentMetadata): T[keyof T];
}
interface ParseArrayPipeOptions {
  separator?: string;
  optional?: boolean;
}
declare class ParseArrayPipe implements PipeTransform {
  private readonly options?;
  constructor(options?: ParseArrayPipeOptions | undefined);
  transform(value: unknown, metadata: ArgumentMetadata): unknown[];
}

interface ErrorsModuleOptions {

  catalogs?: Catalog<string>[];

  handler?: Type<ExceptionHandler> | ExceptionHandler;
}
declare const ConfigurableModuleClass$1: ConfigurableModuleClassType<ErrorsModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;

declare class ErrorsModule extends ConfigurableModuleClass$1 {}

interface ErrorReporter {
  catalog: Catalog<string>;
  report(error: unknown, ctx: ErrorReportContext): void;
  render(error: unknown, executionCtx: unknown): Response | ErrorBodyResult | undefined;
}

declare const resolveErrorReporter: (container: Container) => ErrorReporter;

declare const SERIALIZE_METADATA = "vela:serialize";
interface SerializationDescriptor {
  readonly schema: ValidationSchema;
}

declare function Serialize(dto: SerializationDescriptor): MethodDecorator;

declare class SerializerInterceptor implements NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Promise<unknown>;
}

interface SerializerDefinition<Input, Wire, Output extends ValidationSchema> {

  readonly schema: SchemaParser<Promise<Wire>>;

  readonly output: Output;
  parse(value: unknown): Promise<Wire>;
  serialize(value: Input): Promise<Wire>;
}

declare function defineSerializer<Input extends ValidationSchema, Output extends ValidationSchema>(options: {
  input: Input;
  output: Output;
  project: (value: NoInfer<SchemaOutput<Input>>) => NoInfer<SchemaInput<Output>> | PromiseLike<NoInfer<SchemaInput<Output>>>;
}): SerializerDefinition<SchemaInput<Input>, SchemaOutput<Output>, Output>;

type LogValue = null | boolean | number | string | readonly LogValue[] | {
  readonly [key: string]: LogValue;
};

interface LogRecord {
  readonly timestamp: number;
  readonly level: LoggerLevelName;
  readonly category: string;
  readonly message: string;
  readonly fields: Readonly<Record<string, LogValue>>;

  readonly arguments: readonly LogValue[];
}
type LogSink = (record: LogRecord) => void | Promise<void>;
type LogFields = Readonly<Record<string, unknown>>;
interface LogSerializationOptions {

  readonly maxDepth?: number;

  readonly maxEntries?: number;

  readonly maxNodes?: number;

  readonly maxStringLength?: number;

  readonly redactKeys?: readonly string[];
}
interface ApplicationLoggerOptions extends LogSerializationOptions {
  readonly level?: LogLevel;
  readonly categories?: Readonly<Record<string, LogLevel>>;

  readonly directive?: string;

  readonly sinks?: readonly LogSink[];

  readonly maxPending?: number;
}

interface LogDeliveryContext {
  readonly fields?: LogFields;
  readonly waitUntil?: (promise: Promise<void>) => void;

  readonly isActive?: () => boolean;
}

declare const consoleLogSink: LogSink;
type Emit = (category: string, fields: LogFields, delivery: LogDeliveryContext, level: LoggerLevelName, message: unknown, rest: readonly unknown[]) => void;

declare class StructuredLogger implements LoggerService {
  #private;
  /** @internal */
  constructor(category: string, fields: LogFields, delivery: LogDeliveryContext, emit: Emit, enabled: (level: LogLevel, category: string) => boolean, snapshot: (fields: LogFields) => LogFields);
  isEnabled(level: LogLevel): boolean;
  withFields(fields: LogFields): StructuredLogger;
  extend(namespace: string): StructuredLogger;
  log(message: unknown, ...rest: unknown[]): void;
  error(message: unknown, ...rest: unknown[]): void;
  warn(message: unknown, ...rest: unknown[]): void;
  debug(message: unknown, ...rest: unknown[]): void;
  verbose(message: unknown, ...rest: unknown[]): void;
}

declare class ApplicationLogger {
  #private;
  constructor(options?: ApplicationLoggerOptions);
  get diagnostics(): Readonly<{
    pending: number;
    dropped: number;
    failed: number;
  }>;

  configure(directive: string): void;
  isEnabled(level: LogLevel, category?: string): boolean;
  createLogger(category?: string, fields?: LogFields, delivery?: LogDeliveryContext): StructuredLogger;

  subscribe(sink: LogSink): () => void;

  flush(): Promise<void>;
  dispose(): Promise<void>;
  onModuleDestroy(): Promise<void>;
}

declare const APP_LOGGER: InjectionToken<ApplicationLogger>;
declare const ConfigurableModuleClass: ConfigurableModuleClassType<ApplicationLoggerOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;

declare class LoggingModule extends ConfigurableModuleClass {}

declare function serializeLogValue(value: unknown, options?: LogSerializationOptions): LogValue;

interface LogThresholds {
  readonly level: LogLevel;
  readonly categories: ReadonlyMap<string, LogLevel>;
}

declare function parseLogDirective(directive: string, defaults?: {
  readonly level?: LogLevel;
  readonly categories?: Readonly<Record<string, LogLevel>>;
}): LogThresholds;

declare function loggerForScope(container: Container, category?: string, fields?: LogFields): StructuredLogger;

export { SetMetadata as $, setTrustedRequestTenant as $a, Awaitable as $i, WS_RESERVED_METADATA as $n, WebSocketServer as $r, ModuleRegistrationOptions as $t, defineSerializer as A, ConfigPathValue as Aa, ResponseCacheModule as Ai, HttpException as An, VelaFactory as Ao, normalizeWebSocketUpgradeIdentity as Ar, getExecutionLifetime as At, ParseArrayPipe as B, LoggerLevelName as Ba, CACHE_MANAGER as Bi, UnauthorizedException as Bn, InMemoryRoomRegistry as Br, DefineModuleSpec as Bt, LogDeliveryContext as C, CONFIG_ENV as Ca, defineEventVocabulary as Ci, ResponseCheckCallback as Cn, EndpointDefinition as Co, DEFAULT_WS_MAX_FRAME_BYTES as Cr, resolveEntrypoint as Ct, LogSink as D, ConfigModule as Da, ResponseCacheInterceptor as Di, ForbiddenException as Dn, defineEndpoint as Do, authenticateWebSocketUpgrade as Dr, ExecutionScopeOptions as Dt, LogSerializationOptions as E, ConfigStore as Ea, CacheResponse as Ei, ConflictException as En, EndpointSchema as Eo, assertWebSocketRoomId as Er, ExecutionScope as Et, ErrorReporter as F, InferConfigType as Fa, ResponseCacheOptions as Fi, NotImplementedException as Fn, WsDispatcher as Fr, provideGlobal as Ft, ParseIntPipe as G, TrustedRequestIdentityStore as Ga, Cacheable as Gi, WebSocketSendPolicy$1 as Gn, broadcastCommandFits as Gr, ConfigurableModuleAsyncOptions as Gt, ParseBoolPipe as H, Writer as Ha, CACHE_TTL_METADATA as Hi, UnsupportedMediaTypeException as Hn, RoomRegistry as Hr, ModuleContributions as Ht, resolveErrorReporter as I, registerAs as Ia, ResponseCacheScope as Ii, PayloadTooLargeException as In, WsEntrypointMeta as Ir, sideEffectModule as It, RequiredPipe as J, clearTrustedRequestIdentity as Ja, CacheInterceptor as Ji, WsMessageQueue as Jn, ConnectedSocket as Jr, ConfigurableModuleExtras as Jt, ParseUUIDPipe as K, TrustedRequestPrincipal as Ka, TieredCacheStore as Ki, WebSocketSendResult$1 as Kn, local as Kr, ConfigurableModuleBuilderOptions as Kt, ErrorsModule as L, ContextProvider as La, ScopedResponseCache as Li, RequestTimeoutException as Ln, readWsEntrypointMeta as Lr, ConfigurableModuleBuilder as Lt, SERIALIZE_METADATA as M, AnyConfigNamespace as Ma, CacheInvalidationStore as Mi, MethodNotAllowedException as Mn, resolveGatewayRoomParam as Mr, LazyProviderSpec as Mt, SerializationDescriptor as N, ConfigNamespace as Na, CacheResponseOptions as Ni, NotAcceptableException as Nn, resolveMaxFrameBytes as Nr, lazyProvider as Nt, LogValue as O, ConfigModuleOptions as Oa, RESPONSE_CACHE_OPTIONS as Oi, GatewayTimeoutException as On, createOpenApiDocument as Oo, authorizeWebSocketUpgrade as Or, createExecutionScope as Ot, Serialize as P, ConfigType as Pa, ResponseCacheEntryOptions as Pi, NotFoundException as Pn, webSocketFrameFits as Pr, moduleToken as Pt, Reflector as Q, setTrustedRequestIdentity as Qa, AsyncCacheStore as Qi, WS_MODULE_OPTIONS as Qn, WebSocketGateway as Qr, DefineConfigurableModuleSpec as Qt, ErrorsModuleOptions as R, LogLevel as Ra, CACHEABLE_METADATA as Ri, ServiceUnavailableException as Rn, WebSocketModule as Rr, defineConfigurableModule as Rt, ApplicationLoggerOptions as S, SecurityModuleOptions as Sa, defineEvent as Si, HealthIndicatorResult as Sn, Endpoint as So, AuthenticatedWebSocketUpgrade as Sr, getEntrypointModuleId as St, LogRecord as T, ConfigService as Ta, MemoryCacheInvalidationStore as Ti, BadRequestException as Tn, EndpointRequest as To, DEFAULT_WS_MAX_ROOM_ID_BYTES as Tr, ExecutionLifetime as Tt, ParseEnumPipe as U, buildExecutionContext as Ua, CacheKey as Ui, HealthModule as Un, SyncDriver as Ur, ModuleSetupContext as Ut, ParseArrayPipeOptions as V, LoggerService as Va, CACHE_MODULE_OPTIONS as Vi, UnprocessableEntityException as Vn, MAX_WS_SYNC_ENVELOPE_OVERHEAD_BYTES as Vr, GlobalComponentSlot as Vt, ParseFloatPipe as W, TrustedRequestIdentity as Wa, CacheTTL as Wi, WebSocketSendGate$1 as Wn, assertBroadcastCommandFits as Wr, defineModule as Wt, CreateDecoratorOptions as X, getTrustedContextRequest as Xa, CacheModule as Xi, RESERVED_WS_EVENT_PREFIX as Xn, ReservedWsEvent as Xr, ConfigurableModuleHost as Xt, ZodValidationPipe as Y, createTrustedRequestIdentityStore as Ya, CacheService as Yi, trySendWebSocketFrame as Yn, MessageBody as Yr, ConfigurableModuleExtrasTransform as Yt, ReflectableDecorator as Z, getTrustedRequestIdentity as Za, AnyCacheStore as Zi, WS_GATEWAY_METADATA as Zn, SubscribeMessage as Zr, ConfigurableModuleOptionsFactory as Zt, APP_LOGGER as _, Secret as _a, OnEventMetadata as _i, HealthIndicatorService as _n, Optional as _o, WebSocketTicketNonceStore as _r, RouteContributorOpenApiContext as _t, VelaError$1 as a, CORS_OPTIONS as aa, Cron as ai, SkipThrottle as an, verifyInvocation as ao, resolveWsArgs as ar, UsePipes as at, StructuredLogger as b, SecurityCorsOptions as ba, EventPayload as bi, HealthCheckStatus as bn, ApiResponse as bo, issueWebSocketTicket as br, EntrypointExecutionContext as bt, defineErrorCatalog as c, HTTP_MODULE_OPTIONS as ca, EventDispatcher as ci, ThrottlerGuard as cn, getCurrentContainer as co, buildWsExecutionContext as cr, PipelineComponentEntry as ct, isVelaError$1 as d, HttpModule as da, EventListenerDecorator as di, ThrottleConfig as dn, SignedUrl as do, IssueWebSocketTicketOptions as dr, resolveScopedComponents as dt, CacheEntry as ea, parseCronMetadata as ei, stableHash as en, INVOCATION_AUDIENCE as eo, WS_ROOM_REGISTRY as er, Catch as et, toErrorBody$1 as f, HttpModuleOptions as fa, OnEvent as fi, ThrottlerModuleOptions as fn, SignedUrlGuard as fo, VerifyWebSocketTicketOptions as fr, resolveScopedComponentsAsync as ft, serializeLogValue as g, SECURITY_OPTIONS as ga, EventHandler as gi, HttpPingOptions as gn, Injectable as go, WebSocketTicketClaim as gr, RouteContributorContext as gt, parseLogDirective as h, buildSecurityMiddleware as ha, EventEmitOptions as hi, HttpHealthIndicator as hn, Inject as ho, WEBSOCKET_TICKET_PURPOSE as hr, RouteContributor as ht, ErrorCatalogEntry as i, CacheStore as ia, SCHEDULE_DISPATCH as ii, THROTTLE_METADATA as in, signInvocation as io, WsParamType as ir, UseMiddleware as it, SerializerInterceptor as j, ConfigSchema as ja, CacheInvalidationResult as ji, InternalServerErrorException as jn, resolveGatewayRoomId as jr, runInEntrypointScope as jt, SerializerDefinition as k, ConfigPath as ka, ResponseCacheService as ki, GoneException as kn, VelaCreateOptions as ko, isWebSocketOriginAllowed as kr, finishExecutionScope as kt, env as l, HttpRequestException as la, ScopedEventDispatcher as li, ThrottlerModule as ln, getCurrentRequestContext as lo, BroadcastOperatorImpl as lr, ResolvedComponentMap as lt, LogThresholds as m, HttpResponse as ma, EventEmitter as mi, ThrottlerStore as mn, mixin as mo, WEBSOCKET_TICKET_MAX_TTL_MS as mr, PipelineRunner as mt, Catalog$1 as n, CacheEntryWriter as na, CRON_METADATA as ni, THROTTLER_OPTIONS as nn, InvocationClaim as no, WS_SUBSCRIBE_METADATA as nr, UseGuards as nt, VelaErrorOptions as o, CorsModule as oa, Interval as oi, Throttle as on, MemoryNonceStore as oo, WsException as or, getCatchTypes as ot, loggerForScope as p, HttpRequestConfig as pa, EventEmitterSubscriber as pi, ThrottlerStorageRecord as pn, createLazyParamDecorator as po, WEBSOCKET_TICKET_AUDIENCE as pr, PipelineRunOptions as pt, ParseUUIDPipeOptions as q, bindTrustedRequestContext as qa, MemoryCacheStore as qi, readWebSocketEnvelope$1 as qn, webSocketSyncEnvelopeFits as qr, ConfigurableModuleClassType as qt, ErrorBodyResult$1 as r, CacheModuleOptions as ra, INTERVAL_METADATA as ri, THROTTLER_STORAGE as rn, VerifyInvocationOptions as ro, WS_SYNC_DRIVER as rr, UseInterceptors as rt, composeCatalogs$1 as s, CorsOptions as sa, ScheduleModule as si, ThrottlerStorage as sn, enableAmbientContainer as so, toErrorFrame as sr, shouldFilterCatch as st, CORE_CATALOG$1 as t, CacheEntryReader as ta, parseIntervalMetadata as ti, SKIP_THROTTLE_METADATA as tn, INVOCATION_DEFAULT_TTL_SECONDS as to, WS_SERVER as tr, UseFilters as tt, getRuntimeKey as u, HttpService as ua, ON_EVENT_METADATA as ui, RateLimitInfo as un, URL_SIGNING_SECRET as uo, WsServerImpl as ur, resolvePipelineComponents as ut, LoggingModule as v, SecurityModule as va, EventDefinition as vi, HealthCheckService as vn, zodToJsonSchema as vo, WebSocketTicketPrincipal as vr, getRouteContributors as vt, LogFields as w, CONFIG_OPTIONS as wa, EventEmitterModule as wi, BadGatewayException as wn, EndpointHandlerOutput as wo, DEFAULT_WS_MAX_JOINED_ROOMS as wr, EXECUTION_LIFETIME as wt, consoleLogSink as x, SecurityHeadersOptions as xa, EventVocabulary as xi, HealthIndicatorFunction as xn, ApiTags as xo, verifyAndConsumeWebSocketTicket as xr, buildEntrypointExecutionContext as xt, ApplicationLogger as y, OriginProtectionOptions as ya, EventInput as yi, HealthCheckResult as yn, ApiDoc as yo, WebSocketTicketPrincipalType as yr, registerRouteContributor as yt, DefaultValuePipe as z, Logger as za, CACHE_KEY_METADATA as zi, TooManyRequestsException as zn, WebSocketModuleOptions as zr, moduleKey as zt };
```

### `<internal:index-live-live.types.d.ts>`

```ts
import { p as OnApplicationBootstrap } from "<internal:schedule.registry.d.ts>";
import { E as WsMessage, T as WsExecutionContext, _ as ReservedWsEventHandler, k as Container, w as WsClient, z as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { D as Entrypoint, E as ContributesEntrypoints, F as DiscoveryService } from "<internal:types-registry-types.d.ts>";
import { qt as ConfigurableModuleClassType } from "<internal:index-factory.d.ts>";
import { COMMIT_CURSOR_HEADER as COMMIT_CURSOR_HEADER$1, COMMIT_EPOCH_HEADER as COMMIT_EPOCH_HEADER$1, ClientLiveFrame, DEFAULT_KEY_FIELD as DEFAULT_KEY_FIELD$1, LIVE_ERROR_CODES as LIVE_ERROR_CODES$1, LIVE_EVENT, LIVE_PROTOCOL as LIVE_PROTOCOL$1, LiveFrame, LiveQueryDefinition, LiveQueryDefinition as LiveQueryDefinition$1, RowOp, ServerLiveFrame, ServerLiveFrame as ServerLiveFrame$1, applyListDelta, defineLiveQuery as defineLiveQuery$1, encodeListDelta as encodeListDelta$1, encodeLiveEnvelope as encodeLiveEnvelope$1, encodeLiveFrame, isClientLiveFrame as isClientLiveFrame$1, isServerLiveFrame, readLiveEnvelope } from "@velajs/live-protocol";

interface LiveIdentity {
  [key: string]: unknown;
  expiresAtMs?: number;
}

interface LiveQueryContext {
  identity?: LiveIdentity;

  clientId: string;

  rooms: string[];
}
interface LiveQueryOptions<A = unknown> {

  tags: string[] | ((args: A) => string[]);

  key?: string;

  coalesceBy?: (args: A, context: LiveQueryContext) => string | undefined;
}

interface LiveQueryMetadata {
  name: string;
  methodName: string | symbol;
  definition: LiveQueryDefinition<unknown, unknown>;
  key?: string;
  prepare(input: unknown): PreparedLiveQuery;
}

interface PreparedLiveQuery {
  readonly input: unknown;
  readonly args: unknown;
  tags(): string[];
  coalesceBy?: (context: LiveQueryContext) => string | undefined;
  invoke(instance: unknown, context: LiveQueryContext): unknown | Promise<unknown>;
}

interface LiveResolverMetadata {}

interface CommitStamp {
  cursor: number;
  epoch: string;
}

type ResumeVerdict = 'resume' | 'rerun' | 'snapshot';

interface CursorLog {
  append(tags: string[]): CommitStamp | Promise<CommitStamp>;
  current(): CommitStamp | Promise<CommitStamp>;
  evaluateResume(sinceCursor: number, sinceEpoch: string, subscriptionTags: string[]): ResumeVerdict | Promise<ResumeVerdict>;
}

interface InvalidationCommand {
  room?: string;
  tags: string[];

  origin?: string;
}

interface LiveInvalidationSink {
  applyInvalidation(cmd: InvalidationCommand): CommitStamp | Promise<CommitStamp>;
}

interface LiveDriver {
  readonly kind: string;
  bind(sink: LiveInvalidationSink): void;
  dispatch(cmd: InvalidationCommand): CommitStamp | undefined | Promise<CommitStamp | undefined>;
  start?(): void | Promise<void>;
  stop?(): void | Promise<void>;
}

interface SubscriptionRecord {
  sub: string;
  query: string;
  args: unknown;

  tags: string[];

  key?: string;
  identity?: LiveIdentity;

  lastJson?: string;
  lastCursor?: number;
}
interface LivePresenceOptions {

  ttlMs?: number;
}
interface LiveModuleOptions {

  driver?: () => LiveDriver | Promise<LiveDriver>;

  log?: () => CursorLog | Promise<CursorLog>;

  identity?: (client: WsClient) => LiveIdentity | undefined;

  authorizeDelivery?: (context: LiveDeliveryAuthorizationContext) => boolean | Promise<boolean>;

  maxSubscriptionsPerSocket?: number;

  maxRefreshFanout?: number;

  maxTags?: number;

  presence?: LivePresenceOptions | false;
}
interface LiveDeliveryAuthorizationContext {
  identity?: LiveIdentity;
  query: string;
  args: unknown;
  client: WsClient;
}

interface LiveEntrypointMeta {

  engine: unknown;
}

declare const ConfigurableModuleClass: ConfigurableModuleClassType<LiveModuleOptions, "forRoot", "create", {
  isGlobal?: boolean;
}>;
declare class LiveModule extends ConfigurableModuleClass {}

declare function LiveResolver(): ClassDecorator;

declare function LiveQuery<Args, Result>(name: string, definition: LiveQueryDefinition<Args, Result>, options: LiveQueryOptions<NoInfer<Args>>): <Handler extends (args: Args, context: LiveQueryContext) => Result | Promise<Result>>(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<Handler>) => void;

declare function getLiveQueries(resolverClass: object): LiveQueryMetadata[];

declare const presenceTag: (room: string) => string;

declare const PRESENCE_ROSTER_QUERY = "$presence.roster";

interface PresenceMember {

  id: string;
  meta?: unknown;
  lastSeen: number;
}

declare class PresenceService {
  private readonly ttlMs;
  readonly enabled: boolean;
  private readonly rooms;
  private readonly roomsByClient;
  private invalidator?;
  constructor(ttlMs?: number, enabled?: boolean);

  bindInvalidator(invalidator: (tags: string[]) => void): void;
  beat(room: string, clientId: string, meta?: unknown): void;

  reap(clientId: string): void;
  roster(room: string): PresenceMember[];

  inspectRooms(): Array<{
    room: string;
    count: number;
    members: string[];
  }>;
}

declare class PresenceResolver {
  private readonly presence;
  constructor(presence: PresenceService);
  roster(args: {
    room: string;
  }, context: LiveQueryContext): PresenceMember[];
}

interface LiveInspection {
  subscriptions: Array<{
    id: string;
    query: string;
    room: string;
    clientId: string;
    tags: string[];

    connectedAt: number;
  }>;
  rooms: Array<{
    room: string;
    count: number;
    members: string[];
  }>;
}

declare const LIVE_SUBS_DATA_KEY = "__velaLiveSubs";

declare function readPersistedLiveSubscriptions(client: WsClient): SubscriptionRecord[];

declare class LiveEngine implements OnApplicationBootstrap, ContributesEntrypoints, ReservedWsEventHandler, LiveInvalidationSink {
  private readonly container;
  private readonly discovery;
  private readonly log;
  private readonly options;
  private readonly presence?;
  private readonly queries;
  private readonly preparedQueries;
  private readonly connections;
  private readonly pendingTags;
  private drainChain;
  private draining;
  private readonly maxSubscriptionsPerSocket;
  private readonly maxRefreshFanout;
  private readonly maxTags;
  constructor(container: Container, discovery: DiscoveryService, log: CursorLog, driver: LiveDriver, options: LiveModuleOptions, presence?: PresenceService | undefined);
  onApplicationBootstrap(): Promise<void>;

  collectEntrypoints(): Entrypoint<LiveEntrypointMeta>[];

  inspect(): LiveInspection;
  handleReservedEvent(path: string, client: WsClient, message: WsMessage, context?: WsExecutionContext): Promise<void>;
  handleSocketClose(_path: string, client: WsClient): Promise<void>;
  applyInvalidation(cmd: InvalidationCommand): Promise<CommitStamp>;

  whenIdle(): Promise<void>;

  restoreSubscription(path: string, client: WsClient, record: SubscriptionRecord): void;
  private onSubscribe;
  private runSubscribeGuards;
  private scheduleDrain;
  private drainLoop;

  private static liveFrameCode;
  private push;
  private liveQueryContext;
  private resolveQueryExecution;
  private executeQuery;
  private runQuery;

  private persistSubscriptions;
  private preparedQuery;
  private ensureConnection;
  private authorizeRecord;
  private revokeConnection;
  private assertTags;
  private isRestorableRecord;
  private boundedOption;
  private isUnsupportedSubscribeFrame;
  private sendFrame;
}

declare function localLive(): LiveDriver;

declare class LiveInvalidation {
  private readonly driver;
  constructor(driver: LiveDriver);
  invalidate(cmd: InvalidationCommand): Promise<CommitStamp | undefined>;
}

declare function stampCommitHeaders(c: {
  header: (name: string, value: string) => unknown;
}, stamp: CommitStamp): void;

declare class InMemoryCursorLog implements CursorLog {
  private readonly maxEntries;
  private readonly epoch;
  private readonly entries;
  private seq;
  constructor(maxEntries?: number);
  append(tags: string[]): CommitStamp;
  current(): CommitStamp;
  evaluateResume(sinceCursor: number, sinceEpoch: string, subscriptionTags: string[]): ResumeVerdict;
}

declare function encodeSubscriptionUpdate(record: SubscriptionRecord, json: string, result: unknown, stamp: CommitStamp, initial: boolean): ServerLiveFrame;

declare const LIVE_RESOLVER_METADATA = "vela:live:resolver";

declare const LIVE_DRIVER: InjectionToken<LiveDriver>;

declare const LIVE_CURSOR_LOG: InjectionToken<CursorLog>;
declare const LIVE_MODULE_OPTIONS: InjectionToken<LiveModuleOptions>;

export { LiveQueryMetadata as $, LiveEngine as A, getLiveQueries as B, LIVE_RESOLVER_METADATA as C, localLive as D, LiveInvalidation as E, PresenceResolver as F, LiveDeliveryAuthorizationContext as G, CommitStamp as H, PresenceService as I, LiveIdentity as J, LiveDriver as K, presenceTag as L, readPersistedLiveSubscriptions as M, PRESENCE_ROSTER_QUERY as N, stampCommitHeaders as O, PresenceMember as P, LiveQueryContext as Q, LiveQuery as R, LIVE_MODULE_OPTIONS as S, InMemoryCursorLog as T, CursorLog as U, LiveModule as V, InvalidationCommand as W, LiveModuleOptions as X, LiveInvalidationSink as Y, LivePresenceOptions as Z, isClientLiveFrame$1 as _, LIVE_ERROR_CODES$1 as a, LIVE_CURSOR_LOG as b, LiveFrame as c, ServerLiveFrame$1 as d, LiveQueryOptions as et, applyListDelta as f, encodeLiveFrame as g, encodeLiveEnvelope$1 as h, DEFAULT_KEY_FIELD$1 as i, SubscriptionRecord as it, LiveInspection as j, LIVE_SUBS_DATA_KEY as k, LiveQueryDefinition$1 as l, encodeListDelta$1 as m, COMMIT_EPOCH_HEADER$1 as n, PreparedLiveQuery as nt, LIVE_EVENT as o, defineLiveQuery$1 as p, LiveEntrypointMeta as q, ClientLiveFrame as r, ResumeVerdict as rt, LIVE_PROTOCOL$1 as s, COMMIT_CURSOR_HEADER$1 as t, LiveResolverMetadata as tt, RowOp as u, isServerLiveFrame as v, encodeSubscriptionUpdate as w, LIVE_DRIVER as x, readLiveEnvelope as y, LiveResolver as z };
```

### `<internal:index-validation-standard-schema.d.ts>`

```ts
import { t as ArgumentMetadata, u as PipeTransform } from "<internal:types-http-hono.types.d.ts>";
import { StandardJSONSchemaV1, StandardSchemaV1, StandardSchemaV1 as StandardSchemaV1$1 } from "@standard-schema/spec";

declare class SchemaValidationError extends Error {
  readonly issues: readonly StandardSchemaV1.Issue[];
  constructor(issues: readonly StandardSchemaV1.Issue[]);
}

interface ValidationIssue {
  readonly message: string;
  readonly path?: readonly (string | number)[];
  readonly code?: string;
}
declare function isStandardSchema(value: unknown): value is StandardSchemaV1;

declare function validateSchema<Input, Output>(schema: StandardSchemaV1<Input, Output>, value: unknown): Output | Promise<Output>;

declare function standardJsonSchema(schema: unknown, direction?: 'input' | 'output', target?: string): unknown;

interface SchemaParser<Value> {
  parse(value: unknown): Value;
  parseAsync?(value: unknown): Promise<Awaited<Value>>;
}

type RuntimeParser = SchemaParser<unknown>;

interface DtoSchema<Value> extends SchemaParser<Value> {
  toJSONSchema?(): unknown;
}
interface DtoOptions {

  name?: string;
  jsonSchema?: unknown;
  schemaConverter?: (direction: 'input' | 'output') => unknown;
}

interface DtoDefinition<Value, S extends DtoSchema<unknown> = DtoSchema<Value>> extends SchemaParser<Value> {
  readonly name: string;
  readonly schema: S;
  toJSONSchema(direction?: 'input' | 'output'): unknown;
}
interface StandardDtoDefinition<Input, Output, S extends StandardSchemaV1$1<Input, Output> = StandardSchemaV1$1<Input, Output>> {
  readonly name: string;
  readonly schema: S;
  parse(value: unknown): Output | Promise<Output>;
  parseAsync?(value: unknown): Promise<Output>;
  toJSONSchema(direction?: 'input' | 'output'): unknown;
}
type AsyncParser<Value> = {
  parseAsync(value: unknown): Promise<Awaited<Value>>;
};
declare function defineDto<S extends DtoSchema<unknown>>(schema: S, options?: DtoOptions): DtoDefinition<ReturnType<S['parse']>, S> & AsyncParser<ReturnType<S['parse']>>;
declare function defineDto<S extends StandardSchemaV1$1>(schema: S, options?: DtoOptions): StandardDtoDefinition<StandardSchemaV1$1.InferInput<S>, StandardSchemaV1$1.InferOutput<S>, S> & AsyncParser<StandardSchemaV1$1.InferOutput<S>>;
declare function defineDto<Value>(schema: DtoSchema<Value>, options?: DtoOptions): DtoDefinition<Value> & AsyncParser<Value>;
declare function defineDto<Input, Output>(schema: StandardSchemaV1$1<Input, Output>, options?: DtoOptions): StandardDtoDefinition<Input, Output> & AsyncParser<Output>;

type ValidationSchema = RuntimeParser | StandardSchemaV1$1 | {
  readonly schema: RuntimeParser | StandardSchemaV1$1;
};
type SchemaInput<S extends ValidationSchema> = S extends {
  readonly schema: infer Inner extends ValidationSchema;
} ? SchemaInput<Inner> : S extends StandardSchemaV1$1 ? StandardSchemaV1$1.InferInput<S> : unknown;
type SchemaOutput<S extends ValidationSchema> = S extends {
  readonly schema: infer Inner extends ValidationSchema;
} ? SchemaOutput<Inner> : S extends StandardSchemaV1$1 ? StandardSchemaV1$1.InferOutput<S> : S extends {
  parseAsync(value: unknown): infer Output;
} ? Awaited<Output> : S extends SchemaParser<infer Output> ? Awaited<Output> : never;

declare function isValidationSchema(value: unknown): value is ValidationSchema;

declare function parseSchema<S extends ValidationSchema>(schema: S, value: unknown): SchemaOutput<S> | Promise<SchemaOutput<S>>;

declare function parseSchemaAsync<S extends ValidationSchema>(schema: S, value: unknown): Promise<SchemaOutput<S>>;

declare class ValidationPipe implements PipeTransform {
  #private;
  readonly parser?: ValidationSchema | undefined;
  /** @deprecated */
  static consumeValidated(_value: unknown, _schema: ValidationSchema): boolean;

  constructor(parser?: ValidationSchema | undefined);
  transformAsync(value: unknown, metadata: ArgumentMetadata): Promise<unknown>;
  transform(value: unknown, metadata: ArgumentMetadata): unknown;
}

export { StandardSchemaV1$1 as _, isValidationSchema as a, standardJsonSchema as b, DtoDefinition as c, RuntimeParser as d, SchemaParser as f, StandardJSONSchemaV1 as g, SchemaValidationError as h, ValidationSchema as i, DtoOptions as l, defineDto as m, SchemaInput as n, parseSchema as o, StandardDtoDefinition as p, SchemaOutput as r, parseSchemaAsync as s, ValidationPipe as t, DtoSchema as u, ValidationIssue as v, validateSchema as x, isStandardSchema as y };
```

### `<internal:metadata.registry.d.ts>`

```ts
import { A as Constructor, F as InferToken, J as Type, L as InjectMetadata, N as Diagnostics, Y as TypedToken, a as ExecutionContext, c as NestInterceptor, i as ExceptionFilter, it as VelaHono, k as Container, l as NestMiddleware, nt as Scope, ot as VelaMiddlewareHandler, q as Token, r as CanActivate, rt as VelaContext, t as ArgumentMetadata, u as PipeTransform, z as InjectionToken } from "<internal:types-http-hono.types.d.ts>";
import { $ as RouteDefinition, B as ComponentType, C as MiddlewareRouteDefinition, D as Entrypoint, F as DiscoveryService, G as HttpHandlerMeta, H as DynamicModule, K as InterceptorType, O as EntrypointKind, Q as PipeType, U as FilterType, V as ComponentTypeMap, W as GuardType, X as ModuleOptions, Y as ModuleMetadata, Z as ParameterMetadata, a as NonceStore, d as RouteDescription, f as RouteManager, i as InvocationTransport, p as RouteManagerOptions, q as MiddlewareType } from "<internal:types-registry-types.d.ts>";
import { Catalog, ErrorBodyResult } from "@velajs/errors";
import { Context } from "hono";

type ErrorMatcher = string | ((error: unknown) => boolean) | (new (...args: never[]) => Error);

interface ErrorReportContext {
  edge: 'http' | 'ws' | 'live' | 'queue' | 'schedule' | 'hono' | 'graphql' | 'rpc';

  source?: string;

  note?: string;
  [key: string]: unknown;
}

interface ExceptionHandler {

  report?(error: unknown, ctx: ErrorReportContext): void | Promise<void>;

  dontReport?: ErrorMatcher[];

  context?(error: unknown, ctx: ErrorReportContext): Record<string, unknown>;

  render?(error: unknown, ctx: unknown): Response | ErrorBodyResult | undefined;
}

declare const matchesAny: (matchers: ErrorMatcher[] | undefined, error: unknown) => boolean;

declare function registerEntrypointKind(k: EntrypointKind): void;

declare function getEntrypointKinds(): EntrypointKind[];

declare class EntrypointRegistry {
  #private;
  static build(discovery: DiscoveryService, eagerInstances: readonly unknown[], options?: {
    deferLazy?: boolean;
  }): Promise<EntrypointRegistry>;
  private add;
  ofKind(kind: string): Entrypoint[];
  ofKind<M>(kind: string, parseMeta: (meta: unknown) => M): Entrypoint<M>[];
  kinds(): string[];
  all(): Entrypoint[];
}

interface OpenApiInfo {
  title: string;
  version: string;
  description?: string;
}
interface JsonSchema {
  type?: string | string[];
  format?: string;
  enum?: Array<string | number | boolean | null>;
  const?: unknown;
  description?: string;
  nullable?: boolean;
  default?: unknown;
  items?: JsonSchema;
  properties?: Record<string, JsonSchema>;
  required?: string[];
  additionalProperties?: boolean | JsonSchema;
  oneOf?: JsonSchema[];
  anyOf?: JsonSchema[];
  allOf?: JsonSchema[];
  minimum?: number;
  maximum?: number;
  minLength?: number;
  maxLength?: number;
  pattern?: string;
  $ref?: string;
  [key: string]: unknown;
}
interface OpenApiParameter {
  name: string;
  in: 'path' | 'query' | 'header' | 'cookie';
  required?: boolean;
  description?: string;
  schema?: JsonSchema;
}
interface OpenApiRequestBody {
  description?: string;
  required?: boolean;
  content?: Record<string, {
    schema: JsonSchema;
  }>;
}
interface OpenApiResponse {
  description: string;
  content?: Record<string, {
    schema: JsonSchema;
  }>;
}

type OpenApiSecurityRequirement = Record<string, string[]>;
interface OpenApiOperation {
  summary?: string;
  description?: string;
  operationId?: string;
  deprecated?: boolean;
  tags?: string[];
  parameters?: OpenApiParameter[];
  requestBody?: OpenApiRequestBody;
  responses: Record<string, OpenApiResponse>;

  security?: OpenApiSecurityRequirement[];
}

interface OpenApiServer {
  url: string;
  description?: string;
  variables?: Record<string, {
    enum?: string[];
    default: string;
    description?: string;
  }>;
  [key: string]: unknown;
}

interface OpenApiSecurityScheme {
  type: 'apiKey' | 'http' | 'oauth2' | 'openIdConnect' | 'mutualTLS';
  description?: string;
  name?: string;
  in?: 'query' | 'header' | 'cookie';
  scheme?: string;
  bearerFormat?: string;
  flows?: Record<string, unknown>;
  openIdConnectUrl?: string;
  [key: string]: unknown;
}
type HttpVerb = 'get' | 'post' | 'put' | 'patch' | 'delete' | 'options' | 'head';
type OpenApiPathItem = { [verb in HttpVerb]?: OpenApiOperation; };
interface OpenApiDocument {
  openapi: '3.1.0';
  info: OpenApiInfo;
  servers?: OpenApiServer[];
  paths: Record<string, OpenApiPathItem>;
  components?: {
    schemas?: Record<string, JsonSchema>;
    securitySchemes?: Record<string, OpenApiSecurityScheme>;
  };
  security?: OpenApiSecurityRequirement[];
  tags?: Array<{
    name: string;
    description?: string;
  }>;
}
interface ApiDocMetadata {
  summary?: string;
  description?: string;
  operationId?: string;
  deprecated?: boolean;
  tags?: string[];
}
interface ApiResponseOptions {
  description: string;

  schema?: unknown;
}
interface ApiResponseEntry extends ApiResponseOptions {
  status: number | string;
}
interface CreateOpenApiDocumentOptions {
  info?: Partial<OpenApiInfo>;
  globalPrefix?: string;

  tags?: Array<{
    name: string;
    description?: string;
  }>;

  servers?: OpenApiServer[];

  securitySchemes?: Record<string, OpenApiSecurityScheme>;

  security?: OpenApiSecurityRequirement[];
}
type OpenApiUi = 'swagger' | 'scalar' | 'redoc';
interface MountOpenApiOptions {

  document: OpenApiDocument;

  specPath?: string;

  ui?: OpenApiUi | OpenApiUi[] | 'all';

  swaggerPath?: string;

  scalarPath?: string;

  redocPath?: string;

  title?: string;
  /** @deprecated */
  path?: string;
  /** @deprecated */
  uiPath?: string;
}

declare class VelaApplication {
  #private;
  private readonly container;
  private readonly routeManager;
  private instances;
  private honoApp;
  private entrypointRegistry;
  private readonly lazyManager;
  private readonly knownInstances;
  constructor(container: Container, routeManager: RouteManager);
  private trackNew;

  initRoutes(): Promise<void>;
  private getApp;
  get fetch(): VelaHono['fetch'];
  getInstances(): unknown[];
  getContainer(): Container;
  setInstances(instances: unknown[]): void;
  get<K extends Token>(token: K): InferToken<K>;
  getHonoApp(): VelaHono;

  describeRoutes(): RouteDescription[];

  getGlobalPrefix(): string;
  useGlobalPipes(...pipes: PipeType[]): this;
  useGlobalGuards(...guards: GuardType[]): this;
  useGlobalInterceptors(...interceptors: InterceptorType[]): this;
  useGlobalFilters(...filters: FilterType[]): this;

  useGlobalExceptionHandler(handler: Type<ExceptionHandler> | ExceptionHandler): this;

  mountOpenApi(options: MountOpenApiOptions): this;

  private absorbLazyInstances;
  callOnModuleInit(): Promise<void>;
  callOnApplicationBootstrap(): Promise<void>;

  materializeLazyModules(): Promise<void>;

  get entrypoints(): EntrypointRegistry;
  close(signal?: string): Promise<void>;

  dispose(signal?: string): Promise<void>;
}

interface AdapterContext {
  app: VelaApplication;
  container: Container;
  routeManager: RouteManager;
  discovery: DiscoveryService;
}

interface RuntimeAdapter {
  name: string;
  configureContainer?(container: Container): void | Promise<void>;

  getClientIp?: (c: VelaContext) => string | null;
  requestMiddleware?: VelaMiddlewareHandler[];
  onBootstrap?(ctx: AdapterContext): void | Promise<void>;
  onRoutesBuilt?(ctx: AdapterContext): void | Promise<void>;
  invocationTransport?(ctx: AdapterContext): InvocationTransport | undefined;
}

interface LazyModuleGroupSpec {
  moduleId: string;
  tokens: Token[];
  hasEntrypointContributor: boolean;
}
declare class ModuleLoader {
  #private;
  private container;
  private router;
  constructor(container: Container, router: RouteManager);
  load(rootModule: Type): void;
  private getModuleId;
  private isProcessed;
  private markProcessed;
  private getCachedExports;
  private cacheExports;
  private processModule;
  private registerController;
  private warnOnMixedDefaultAndKeyed;

  private registerProvider;
  private isAppToken;
  private buildExportSet;
  getControllers(): Type[];
  getRegisteredProviders(): Token[];
  getAppProviderTokens<T>(token: TypedToken<T>): TypedToken<T>[];
  getConsumerMiddlewareDefinitions(): MiddlewareRouteDefinition[];

  getLazyGroups(): LazyModuleGroupSpec[];
  resolveAllInstances(): Promise<unknown[]>;
  private routeError;
}

interface BootstrapOptions extends RouteManagerOptions {
  diagnostics?: Diagnostics;

  configureContainer?(container: Container): void | Promise<void>;
}
interface BootstrapResult {
  container: Container;
  routeManager: RouteManager;
  loader: ModuleLoader;
}

declare function bootstrap(rootModule: Type, options?: BootstrapOptions): Promise<BootstrapResult>;

declare class ModuleRef {
  private readonly container;
  constructor(container: Container);

  get<K extends Token>(token: K): InferToken<K>;

  resolve<K extends Token>(token: K): InferToken<K>;

  create<T>(type: Type<T>): T;
}

interface RequestContext {
  readonly id: string;
  readonly receivedAt: Date;
  readonly request: Request;
  readonly hono: VelaContext;
  set<Value>(key: RequestContextKey<Value>, value: NoInfer<Value>): void;
  set(key: string | symbol, value: unknown): void;
  get<Value>(key: RequestContextKey<Value>): Value | undefined;
  get(key: string | symbol): unknown;
  has<Value>(key: RequestContextKey<Value> | string | symbol): boolean;
}

declare class RequestContextKey<Value> {
  #private;
  readonly description: string;
  constructor(description: string);
  /** @internal */
  readonly read: (context: RequestContext) => Value | undefined;
  /** @internal */
  readonly write: (context: RequestContext, value: Value) => void;
  /** @internal */
  readonly contains: (context: RequestContext) => boolean;
}
declare const REQUEST_CONTEXT: InjectionToken<RequestContext>;
declare function createRequestContext(c: VelaContext): RequestContext;

declare const INVOCATION_TRANSPORT: InjectionToken<InvocationTransport>;

declare const INVOCATION_SIGNING_SECRET: InjectionToken<string>;

declare const NONCE_STORE: InjectionToken<NonceStore>;

/** @internal */
declare function setRequestContainer(context: Context, container: Container): void;

declare function getRequestContainer(c: Context): Container;

declare function Global(): ClassDecorator;
declare function Module(options?: ModuleOptions): ClassDecorator;
declare function isModule(target: Constructor): boolean;

declare function defineDynamicModule(input: DynamicModule): DynamicModule;
declare function getModuleMetadata(target: Constructor): ModuleMetadata | undefined;

interface Plugin {
  id: string;
  version: string;
  module: Type | DynamicModule;
  dependsOn?: string[];
  metadata?: Record<string, unknown>;
}

declare function definePlugin(plugin: Plugin): Plugin;
declare class PluginRegistry {
  private byId;
  constructor(plugins: readonly Plugin[]);
  list(): Plugin[];
  get(id: string): Plugin | undefined;
  dependents(id: string): Plugin[];
}
declare const PLUGIN_REGISTRY_TOKEN: InjectionToken<PluginRegistry>;
declare class PluginRootModule {}

declare function composePlugins(plugins: readonly Plugin[]): DynamicModule;

declare class ComponentManager {
  static registerController<T extends ComponentType>(type: T, controller: Constructor, ...components: ComponentTypeMap[T][]): void;
  static registerHandler<T extends ComponentType>(type: T, controller: Constructor, handlerName: string | symbol, ...components: ComponentTypeMap[T][]): void;
  static getScopedComponents<T extends ComponentType>(type: T, controller: Constructor, handlerName: string | symbol): ComponentTypeMap[T][];
  private static resolveAll;
  static resolveMiddleware(items: MiddlewareType[], container: Container): NestMiddleware[];
  static resolveGuards(items: GuardType[], container: Container): CanActivate[];
  static resolvePipes(items: PipeType[], container: Container): PipeTransform[];
  static resolveInterceptors(items: InterceptorType[], container: Container): NestInterceptor[];
  static resolveFilters(items: FilterType[], container: Container): ExceptionFilter[];
  static executePipes(value: unknown, metadata: ArgumentMetadata, pipes: PipeTransform[]): Promise<unknown>;
  static runInterceptorChain(interceptors: NestInterceptor[], context: ExecutionContext, coreHandler: () => Promise<unknown>): Promise<unknown>;
}

declare const APP_GUARD: InjectionToken<CanActivate>;

declare const APP_PIPE: InjectionToken<PipeTransform<unknown, unknown>>;

declare const APP_INTERCEPTOR: InjectionToken<NestInterceptor>;

declare const APP_FILTER: InjectionToken<ExceptionFilter<unknown>>;

declare const APP_MIDDLEWARE: InjectionToken<NestMiddleware>;

declare const APP_EXCEPTION_HANDLER: InjectionToken<ExceptionHandler>;

declare const ERROR_CATALOG: InjectionToken<Catalog<string>>;

interface ControllerOptions {
  version?: number | number[];
}
declare class MetadataRegistry {
  private static get routes();
  private static get controllers();
  private static get controllerOptions();
  private static get modules();
  private static get parameters();
  private static get injectables();
  private static get scopes();
  private static get injectTokens();
  private static get handlerHttpMeta();
  private static get catchTypes();
  private static get routeVersions();
  private static get classMeta();
  private static get handlerMeta();
  private static get classMetaIndex();
  private static get handlerMetaIndex();
  private static get controllerComponents();
  private static get handlerComponents();
  static getRoutes(controller: Constructor): RouteDefinition[];
  static addRoute(controller: Constructor, route: RouteDefinition): void;
  static getControllerPath(controller: Constructor): string;
  static setControllerPath(controller: Constructor, path: string): void;
  static getControllerOptions(controller: Constructor): ControllerOptions;
  static setControllerOptions(controller: Constructor, options: ControllerOptions): void;
  static getModuleOptions(module: Constructor): ModuleOptions | undefined;
  static setModuleOptions(module: Constructor, options: ModuleOptions): void;
  static getParameters(controller: Constructor): Map<string | symbol, ParameterMetadata[]>;
  static addParameter(controller: Constructor, methodName: string | symbol, param: ParameterMetadata): void;
  static registerController<T extends ComponentType>(type: T, controller: Constructor, component: ComponentTypeMap[T]): void;
  static getController<T extends ComponentType>(type: T, controller: Constructor): ComponentTypeMap[T][];
  static registerHandler<T extends ComponentType>(type: T, controller: Constructor, methodName: string | symbol, component: ComponentTypeMap[T]): void;
  static getHandler<T extends ComponentType>(type: T, controller: Constructor, methodName: string | symbol): ComponentTypeMap[T][];
  static markInjectable(target: object): void;
  static hasInjectable(target: object): boolean;
  static setScope(target: object, scope: Scope): void;
  static getScope(target: object): Scope | undefined;
  static setInjectTokens(target: object, tokens: InjectMetadata[]): void;
  static getInjectTokens(target: object): InjectMetadata[] | undefined;
  static setHandlerHttpMeta(controller: Constructor, method: string | symbol, meta: HttpHandlerMeta): void;
  static getHandlerHttpMeta(controller: Constructor, method: string | symbol): HttpHandlerMeta | undefined;
  static setCatchTypes(filter: Constructor, types: Type<Error>[]): void;
  static getCatchTypes(filter: Constructor): Type<Error>[] | undefined;
  static hasCatchTypes(filter: Constructor): boolean;
  static setRouteVersion(controller: Constructor, method: string | symbol, version: number | number[]): void;
  static getRouteVersion(controller: Constructor, method: string | symbol): number | number[] | undefined;
  static setCustomClassMeta(target: object, key: string, value: unknown): void;
  static getCustomClassMeta(target: object, key: string): unknown;
  static getCustomClassMetaAll(target: object): Map<string, unknown> | undefined;
  static setCustomHandlerMeta(target: object, handler: string | symbol, key: string, value: unknown): void;
  static getCustomHandlerMeta(target: object, handler: string | symbol, key: string): unknown;
  static getCustomHandlerMetaAll(target: object, handler: string | symbol): Map<string, unknown> | undefined;
  static appendCustomClassMeta<T>(target: object, key: string, item: T): void;
  static appendCustomHandlerMeta<T>(target: object, handler: string | symbol, key: string, item: T): void;
  static getClassesWithClassMeta(key: string): ReadonlySet<object>;
  static getClassesWithHandlerMeta(key: string): ReadonlySet<object>;

  static getHandlersWithMeta(target: object, key: string): Array<{
    handler: string | symbol;
    value: unknown;
  }>;
  static setReflectMetadata(target: object, key: string, value: unknown, propertyKey?: string | symbol): void;
  static getReflectMetadata<T = unknown>(target: object, key: string, propertyKey?: string | symbol): T | undefined;
  static getParamTypes(target: object, propertyKey?: string | symbol): unknown[] | undefined;
  static propagateControllerComponents(from: Constructor, to: Constructor): void;
  static clear(): void;
  static reset(): void;
}

export { registerEntrypointKind as $, ModuleRef as A, ApiResponseOptions as B, INVOCATION_SIGNING_SECRET as C, RequestContext as D, REQUEST_CONTEXT as E, AdapterContext as F, OpenApiInfo as G, HttpVerb as H, RuntimeAdapter as I, OpenApiPathItem as J, OpenApiOperation as K, VelaApplication as L, BootstrapResult as M, bootstrap as N, RequestContextKey as O, ModuleLoader as P, getEntrypointKinds as Q, ApiDocMetadata as R, setRequestContainer as S, NONCE_STORE as T, JsonSchema as U, CreateOpenApiDocumentOptions as V, OpenApiDocument as W, OpenApiResponse as X, OpenApiRequestBody as Y, EntrypointRegistry as Z, Module as _, APP_INTERCEPTOR as a, isModule as b, ERROR_CATALOG as c, Plugin as d, ErrorMatcher as et, PluginRegistry as f, Global as g, definePlugin as h, APP_GUARD as i, BootstrapOptions as j, createRequestContext as k, ComponentManager as l, composePlugins as m, APP_EXCEPTION_HANDLER as n, ExceptionHandler as nt, APP_MIDDLEWARE as o, PluginRootModule as p, OpenApiParameter as q, APP_FILTER as r, matchesAny as rt, APP_PIPE as s, MetadataRegistry as t, ErrorReportContext as tt, PLUGIN_REGISTRY_TOKEN as u, defineDynamicModule as v, INVOCATION_TRANSPORT as w, getRequestContainer as x, getModuleMetadata as y, ApiResponseEntry as z };
```

### `<internal:schedule.registry.d.ts>`

```ts
import { a as ExecutionContext, k as Container, r as CanActivate } from "<internal:types-http-hono.types.d.ts>";
import { D as Entrypoint, F as DiscoveryService, Q as PipeType, a as NonceStore, c as RouteParams, f as RouteManager, n as InvocationRouteTarget, o as RunInit, r as InvocationTarget, s as RouteName, t as InvocationPathTarget, x as ControllerOptions } from "<internal:types-registry-types.d.ts>";
import { RedirectStatusCode, StatusCode } from "hono/utils/http-status";

declare global {
  namespace Reflect {
    function defineMetadata(key: string, value: unknown, target: object, propertyKey?: string | symbol): void;
    function getMetadata(key: string, target: object, propertyKey?: string | symbol): unknown;
    function getOwnMetadata(key: string, target: object, propertyKey?: string | symbol): unknown;
    function metadata(key: string, value: unknown): (target: object, propertyKey?: string | symbol) => void;
  }
}
declare function defineMetadata(key: string, value: unknown, target: object, propertyKey?: string | symbol): void;
declare function getMetadata<T = unknown>(key: string, target: object, propertyKey?: string | symbol): T | undefined;

declare function Controller(pathOrOptions?: string | ControllerOptions): ClassDecorator;

declare function Version(version: number | number[]): MethodDecorator;

interface RouteOptions {

  name?: string;
}
declare const Get: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Post: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Put: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Patch: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Delete: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Options: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Head: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const All: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Sse: (path?: string, options?: RouteOptions) => MethodDecorator;
declare const Param: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;
declare const Query: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;
declare const Body: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;
declare const Headers: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;
declare const Req: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;

declare const Res: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;
declare const Ip: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;
declare const Cookie: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;

declare const Cookies: (nameOrPipe?: string | PipeType, ...pipes: PipeType[]) => ParameterDecorator;

declare function RawBody(): ParameterDecorator;

type CustomParamDecorator<TData> = (...args: undefined extends TData ? [data?: TData, ...pipes: PipeType[]] : [data: TData, ...pipes: PipeType[]]) => ParameterDecorator;
declare function createParamDecorator<TData = unknown>(factory: (data: TData, ctx: ExecutionContext) => unknown): CustomParamDecorator<TData>;

declare function HttpCode(statusCode: StatusCode): MethodDecorator;

declare function Header(name: string, value: string): MethodDecorator;

declare function Redirect(url: string, statusCode?: RedirectStatusCode): MethodDecorator;

type ComposedDecorator = <T>(target: T, propertyKey?: string | symbol, descriptor?: PropertyDescriptor | number) => void;
declare function applyDecorators(...decorators: Array<ClassDecorator | MethodDecorator | PropertyDecorator | ParameterDecorator>): ComposedDecorator;

type UrlParamValue = string | number | boolean;

interface UrlForOptions {

  query?: Record<string, UrlParamValue>;
}

interface SignedUrlGenerateOptions {

  expiresIn: number;

  method?: string;

  secret?: string;
}

declare class UrlGeneratorService {
  #private;
  private readonly routeManager;
  private routeMap;
  constructor(routeManager: RouteManager, secretToken?: string, env?: Record<string, unknown>);

  urlFor<N extends RouteName>(name: N, params?: RouteParams<N>, opts?: UrlForOptions): string;

  signedUrl<N extends RouteName>(name: N, params: RouteParams<N> | undefined, options: SignedUrlGenerateOptions): Promise<string>;

  private routes;
}

declare class InternalDispatcher {
  #private;
  private readonly urls;
  private readonly container;
  constructor(urls: UrlGeneratorService, container: Container, invocationSecret?: string, urlSecret?: string, env?: Record<string, unknown>);

  run<T = unknown, N extends RouteName = RouteName>(target: InvocationRouteTarget<N> | InvocationPathTarget, init?: RunInit): Promise<T>;
  private resolveTransport;
  private parseResponse;
  private toInvocationError;
}

declare class SignedInvocationGuard implements CanActivate {
  #private;
  private readonly nonceStore?;
  constructor(invocationSecret?: string, urlSecret?: string, nonceStore?: NonceStore | undefined, env?: Record<string, unknown>);
  canActivate(context: ExecutionContext): Promise<boolean>;
}

declare function SignedInvocation(): ReturnType<typeof applyDecorators>;

interface OnModuleInit {
  onModuleInit(): void | Promise<void>;
}
interface OnApplicationBootstrap {
  onApplicationBootstrap(): void | Promise<void>;
}
interface OnModuleDestroy {
  onModuleDestroy(): void | Promise<void>;
}
interface OnApplicationShutdown {
  onApplicationShutdown(signal?: string): void | Promise<void>;
}
interface BeforeApplicationShutdown {
  beforeApplicationShutdown(signal?: string): void | Promise<void>;
}

type CronMatcher = (date: Date) => boolean;
interface CronOptions {

  dialect?: 'unix' | 'cloudflare';

  timeZone?: 'local' | 'UTC';
}

declare function parseCron(expression: string, options?: CronOptions): CronMatcher | null;

interface CronMetadata extends CronOptions {
  expression: string;
  methodName: string;
}
interface IntervalMetadata {
  ms: number;
  methodName: string;
}

type ScheduleInvocation = {
  readonly kind: 'cron';
  readonly expression: string;
  readonly scheduledTime: number;
  readonly signal: AbortSignal;
} | {
  readonly kind: 'interval';
  readonly ms: number;
  readonly scheduledTime: number;
  readonly signal: AbortSignal;
};

interface ScheduleJobRef {
  kind: 'cron' | 'interval';

  methodName: string;

  expression?: string;

  ms?: number;
}

type ScheduleDispatchMode = {
  kind: 'direct';
} | {
  kind: 'signed';

  target: (job: ScheduleJobRef) => InvocationTarget;

  method?: string;

  ttlSeconds?: number;
};

interface RegisteredCronJob {
  expression: string;
  methodName: string;
  instance: unknown;
  target: Function;
}
interface RegisteredIntervalJob {
  ms: number;
  methodName: string;
  instance: unknown;
  target: Function;
}
declare class ScheduleRegistry implements OnApplicationBootstrap {
  #private;
  constructor(discovery: DiscoveryService);
  onApplicationBootstrap(): void;

  getCronEntrypoints(): Entrypoint<CronMetadata>[];
  getIntervalEntrypoints(): Entrypoint<IntervalMetadata>[];

  getCronJobs(): RegisteredCronJob[];
  getIntervalJobs(): RegisteredIntervalJob[];
}

export { Head as A, Query as B, All as C, Cookies as D, Cookie as E, Options as F, RouteOptions as G, Redirect as H, Param as I, applyDecorators as J, Sse as K, Patch as L, Headers as M, HttpCode as N, Delete as O, Ip as P, Post as R, UrlGeneratorService as S, Controller as T, Req as U, RawBody as V, Res as W, defineMetadata as X, createParamDecorator as Y, getMetadata as Z, SignedInvocation as _, IntervalMetadata as a, SignedUrlGenerateOptions as b, ScheduleJobRef as c, parseCron as d, BeforeApplicationShutdown as f, OnModuleInit as g, OnModuleDestroy as h, CronMetadata as i, Header as j, Get as k, CronMatcher as l, OnApplicationShutdown as m, RegisteredIntervalJob as n, ScheduleDispatchMode as o, OnApplicationBootstrap as p, Version as q, ScheduleRegistry as r, ScheduleInvocation as s, RegisteredCronJob as t, CronOptions as u, SignedInvocationGuard as v, Body as w, UrlForOptions as x, InternalDispatcher as y, Put as z };
```

### `<internal:signed-url.d.ts>`

```ts

interface SignedUrlOptions {

  expiresIn: number;

  method: string;

  purpose: string;
}
type VerifySignedUrlOptions = Pick<SignedUrlOptions, 'method' | 'purpose'>;
declare const HTTP_SIGNED_URL_PURPOSE = "vela:http-route";
declare const STORAGE_SIGNED_URL_PURPOSE = "vela:storage";

declare function signUrl(url: string, secret: string, options: SignedUrlOptions): Promise<string>;

declare function verifySignedUrl(url: string, secret: string, options: VerifySignedUrlOptions): Promise<boolean>;

export { signUrl as a, VerifySignedUrlOptions as i, STORAGE_SIGNED_URL_PURPOSE as n, verifySignedUrl as o, SignedUrlOptions as r, HTTP_SIGNED_URL_PURPOSE as t };
```

### `<internal:types-http-hono.types.d.ts>`

```ts
import { Context, Hono, MiddlewareHandler, Next } from "hono";
import { WebSocketSendPolicy, WebSocketSendResult } from "@velajs/live-protocol";

interface VelaHonoEnv {
  Bindings: object;
  Variables: Record<string, unknown>;
}
type VelaContext = Context<VelaHonoEnv>;
type VelaHono = Hono<VelaHonoEnv>;
type VelaMiddlewareHandler = MiddlewareHandler<VelaHonoEnv>;

declare const METADATA_KEYS: {
  readonly INJECTABLE: 'vela:injectable';
  readonly INJECT: 'vela:inject';
  readonly SCOPE: 'vela:scope';
  readonly MODULE: 'vela:module';
  readonly MODULE_OPTIONS: 'vela:module-options';
  readonly CONTROLLER: 'vela:controller';
  readonly ROUTES: 'vela:routes';
  readonly PARAMS: 'vela:params';
  readonly HTTP_CODE: 'vela:http-code';
  readonly RESPONSE_HEADERS: 'vela:response-headers';
  readonly REDIRECT: 'vela:redirect';
  readonly CATCH: 'vela:catch';
  readonly CRUD: 'vela:crud';
};
declare const HttpMethod: {
  readonly GET: 'GET';
  readonly POST: 'POST';
  readonly PUT: 'PUT';
  readonly PATCH: 'PATCH';
  readonly DELETE: 'DELETE';
  readonly OPTIONS: 'OPTIONS';
  readonly HEAD: 'HEAD';
  readonly ALL: 'ALL';
};
type HttpMethod = (typeof HttpMethod)[keyof typeof HttpMethod];
declare const ParamType: {
  readonly BODY: 'body';
  readonly QUERY: 'query';
  readonly PARAM: 'param';
  readonly HEADERS: 'headers';
  readonly REQUEST: 'request';
  readonly RESPONSE: 'response';
  readonly IP: 'ip';
  readonly COOKIE: 'cookie';
  readonly RAW_BODY: 'raw_body';
};
type ParamType = (typeof ParamType)[keyof typeof ParamType];
declare const Scope: {
  readonly SINGLETON: 'singleton';
  readonly TRANSIENT: 'transient';
  readonly REQUEST: 'request';
};
type Scope = (typeof Scope)[keyof typeof Scope];

type Type<T = unknown> = new (...args: any[]) => T;
type Constructor<T = unknown> = abstract new (...args: any[]) => T;
interface InjectionTokenOptions<T> {
  readonly factory?: () => T;
}
declare class InjectionTokenIdentity {
  private readonly description;
  readonly options?: Readonly<InjectionTokenOptions<unknown>>;
  constructor(description: string, options?: InjectionTokenOptions<unknown>);
  toString(): string;
}
declare class InjectionToken<T = unknown> extends InjectionTokenIdentity {
  protected readonly valueType: (value: T) => T;
  readonly options?: InjectionTokenOptions<T>;
  constructor(description: string, options?: InjectionTokenOptions<T>);
}
declare class ForwardRef<K extends Token = Token> {
  readonly factory: () => K;
  constructor(factory: () => K);
}
declare function forwardRef<K extends Token>(factory: () => K): ForwardRef<K>;
type TypedToken<T> = Type<T> | InjectionToken<T>;
type Token = Type | InjectionTokenIdentity | string | symbol;
type DependencyToken = Token | ForwardRef;

type InferToken<T> = T extends InjectionToken<infer U> ? U : T extends ForwardRef<infer U> ? InferToken<U> : T extends (abstract new (...args: any[]) => infer U) ? U : unknown;

type InferTokens<T extends readonly unknown[]> = { [K in keyof T]: InferToken<T[K]>; };
interface InjectableOptions {
  scope?: Scope;
}
interface InjectMetadata {
  index: number;
  token?: Token | ForwardRef;
  optional?: boolean;
}
interface ProviderOptions<T = unknown> {
  provide?: Token;
  scope?: Scope;
  useValue?: NoInfer<T>;
  useFactory?: (...args: any[]) => NoInfer<T> | Promise<NoInfer<T>>;
  useClass?: Type<NoInfer<T>>;
  inject?: readonly DependencyToken[];
  useExisting?: Token;
}
declare class CheckedProvider<T> {
  #private;
  readonly provide: Token;
  constructor(provide: Token, options: ProviderOptions<T>);
  get scope(): Scope | undefined;
  get useValue(): T | undefined;
  get useClass(): Type<T> | undefined;
  get useExisting(): Token | undefined;
  get inject(): readonly DependencyToken[] | undefined;
  get useFactory(): unknown;
  static read<T>(provider: CheckedProvider<T>): Readonly<ProviderOptions<T>>;
}

type ProviderDefinition<T = unknown> = CheckedProvider<T>;
type ProviderStrategy<T, Inject extends readonly DependencyToken[]> = {
  useValue: NoInfer<T>;
  useClass?: never;
  useFactory?: never;
  useExisting?: never;
  inject?: never;
} | {
  useClass: Type<NoInfer<T>>;
  useValue?: never;
  useFactory?: never;
  useExisting?: never;
  inject?: never;
} | {
  useExisting: Type<NoInfer<T>> | InjectionToken<NoInfer<T>>;
  useValue?: never;
  useClass?: never;
  useFactory?: never;
  inject?: never;
} | {
  useFactory: (...dependencies: InferTokens<Inject>) => NoInfer<T> | Promise<NoInfer<T>>;
  inject: Inject;
  useValue?: never;
  useClass?: never;
  useExisting?: never;
};

/** @internal */
type AuthoringToken<K extends Token> = K extends InjectionTokenIdentity ? InjectionToken<InferToken<K>> : K;
declare function defineProvider<const K extends Token, const Inject extends readonly DependencyToken[] = readonly []>(provide: K & AuthoringToken<K>, options: ProviderStrategy<InferToken<K>, Inject> & {
  scope?: Scope;
}): ProviderDefinition<InferToken<K>>;

interface ProviderSnapshot {
  readonly token: Token;
  readonly moduleId: string;

  readonly scope: Scope;
  readonly kind: 'value' | 'factory' | 'class' | 'existing';
  readonly useClass?: Type;
  readonly useExisting?: Token;

  readonly instance?: {
    readonly value: unknown;
  };
}
interface ModuleScope {
  moduleId: string;
  localProviders: Set<Token>;
  importedModules: Set<string>;
  exportedTokens: Set<Token>;
  isGlobal: boolean;

  lazy?: boolean;
}

interface LazyResolutionHook {

  isPending(moduleId: string): boolean;

  claim(moduleId: string): void;

  hasClaimed(): boolean;

  isDraining(): boolean;

  drainSync(): void;

  drainAsync(): Promise<void>;
}
type Diagnostics = 'silent' | 'log' | 'throw';
interface ContainerOptions {
  diagnostics?: Diagnostics;
}

declare function describeToken(token: Token): string;

interface ModuleDescription {
  moduleId: string;

  imports: string[];
  isGlobal: boolean;
  lazy: boolean;

  providers: string[];

  exports: string[];
}
declare class ModuleVisibilityError extends Error {
  readonly moduleId: string;
  readonly token: Token;
  constructor(moduleId: string, token: Token);
}
declare class MultipleProvidersFoundError extends Error {
  readonly moduleId: string;
  readonly token: Token;
  readonly candidates: string[];
  constructor(moduleId: string, token: Token, candidates: string[]);
}
declare const ROOT_MODULE_ID = "__root__";

declare class Container {
  #private;
  constructor(options?: ContainerOptions);

  setLazyHook(hook: LazyResolutionHook): void;
  private claimLazyModule;
  private maybeDrainSync;
  register<T>(provider: Type<T> | ProviderDefinition<T>, declaringModuleId?: string): this;
  private registerClass;
  private registerOptions;
  private writeRegistration;
  registerScope(scope: ModuleScope): void;
  markGlobalToken(token: Token): void;
  setRequestInstance<K extends Token>(token: K & AuthoringToken<K>, value: NoInfer<InferToken<K>>): void;
  getDiagnostics(): Diagnostics;
  resolve<K extends Token>(token: K, requestingModuleId?: string): InferToken<K>;
  private resolveToken;
  resolveAll<K extends Token>(token: K, requestingModuleId?: string): InferToken<K>[];

  getVisibleProviderSnapshots(token: Token, requestingModuleId?: string): readonly ProviderSnapshot[];

  private findRegistration;

  private lookupInBucket;

  private collectFromImports;
  private findAllRegistrations;

  has(token: Token): boolean;

  hasInScope(token: Token, moduleId: string): boolean;

  getOwnerModuleIds(token: Token): string[];

  replaceProvider<T>(provider: Type<T> | ProviderDefinition<T>, options?: {
    buckets?: 'all-existing' | 'root' | string[];
  }): this;

  isLazyPending(token: Token, moduleId?: string): boolean;

  isInstantiated(token: Token, moduleId?: string): boolean;

  getProviderScope(token: Token, moduleId?: string): Scope | undefined;
  getTokens(): Token[];

  getUseValues(): unknown[];

  getModuleDescriptions(): ModuleDescription[];

  computeEffectiveScopes(): void;

  private dependencyTokensOf;
  private tryFindRegistration;

  createChild(): Container;
  createDetached(): Container;
  clear(): void;

  hasDisposables(): boolean;
  private assertNotDisposing;
  private rememberCallerOwned;
  private trackDisposable;

  dispose(): Promise<void>;
  private disposeOwned;
  private resolveRegistration;
  private assertNoSyncCycle;
  private resolveClass;
  private resolveFactory;
  resolveAsync<K extends Token>(token: K, requestingModuleId?: string): Promise<InferToken<K>>;
  private resolveAsyncInner;
  private constructAsync;
  private createLazyProxy;
  private tokenToString;
}

interface WsMessage<T = unknown> {
  id?: string;
  event: string;
  data?: T;
}

interface WsResponse<T = unknown> {
  event: string;
  data: T;
}

interface WsClient<TData = Record<string, unknown>> {
  readonly id: string;
  readonly rooms: ReadonlySet<string>;

  readonly maxFrameBytes?: number;
  data: TData;

  send(event: string, data?: unknown, id?: string): void;

  sendRaw(payload: string): void;

  trySendRaw?(payload: string): WebSocketSendResult;
  join(room: string): void | Promise<void>;
  leave(room: string): void | Promise<void>;

  commit(): void | Promise<void>;
  close(code?: number, reason?: string): void;

  readonly raw: unknown;
}

interface BroadcastOperator {
  to(room: string): BroadcastOperator;
  in(room: string): BroadcastOperator;
  except(room: string): BroadcastOperator;
  emit(event: string, data?: unknown): void | Promise<void>;
}

interface WsServer {
  emit(event: string, data?: unknown): void | Promise<void>;
  to(room: string): BroadcastOperator;
  in(room: string): BroadcastOperator;
  except(room: string): BroadcastOperator;
  /** @internal */
  setOutboundFrameLimit?(maxFrameBytes: number): void;
}

interface WsExecutionContext extends ExecutionContext {
  getType(): 'ws';
  getContext(): never;
  getRequest(): never;
  switchToHttp(): never;
  switchToWs(): WsArgumentsHost;
}
interface OnGatewayInit {
  afterInit(server: WsServer): void | Promise<void>;
}
interface OnGatewayConnection {
  handleConnection(client: WsClient): void | Promise<void>;
}
interface OnGatewayDisconnect {
  handleDisconnect(client: WsClient): void | Promise<void>;
}

interface WebSocketPrincipal {
  issuer: string;
  subject: string;
  principalType: 'user' | 'service';
}

interface WebSocketUpgradeIdentity {
  principal: WebSocketPrincipal;
  tenantId: string;

  expiresAtMs: number;
}
interface WebSocketUpgradeAuthenticationContext {
  gatewayPath: string;
  room: string;

  ticket?: string;
}
interface WebSocketGatewayOptions {

  path?: string;

  binding?: string;

  roomParam?: string;

  allowedOrigins?: '*' | readonly string[];

  authorizeUpgrade?: (request: Request) => boolean | Promise<boolean>;

  authenticateUpgrade?: (request: Request, context: WebSocketUpgradeAuthenticationContext) => WebSocketUpgradeIdentity | false | Promise<WebSocketUpgradeIdentity | false>;

  authorizeDelivery?: (client: WsClient) => boolean | Promise<boolean>;

  maxFrameBytes?: number;

  sendPolicy?: WebSocketSendPolicy;

  maxPendingMessages?: number;

  maxPendingBytes?: number;
}

interface SubscribeMessageMetadata {
  event: string;
  methodName: string;
}

interface ReservedWsEventMetadata {

  event: string;
}

interface ReservedWsEventHandler {
  handleReservedEvent(path: string, client: WsClient, message: WsMessage, context?: WsExecutionContext): void | Promise<void>;
  handleSocketClose?(path: string, client: WsClient): void | Promise<void>;
}

interface BroadcastCommand {

  rooms: string[];
  exceptRooms?: string[];
  exceptIds?: string[];

  frame: string;

  origin?: string;
}

interface HttpArgumentsHost {
  getRequest(): Request;
  getResponse(): VelaContext;
}

interface WsArgumentsHost {
  getClient(): WsClient;
  getData(): unknown;
  getPattern(): string;
}

type ContextType = string;
interface ExecutionContext {
  getType(): ContextType;
  getClass(): Type;
  getHandler(): string | symbol;

  getModuleId(): string | undefined;

  getContainer(): Container | undefined;

  getContext(): VelaContext;

  getRequest(): Request;

  switchToHttp(): HttpArgumentsHost;

  switchToWs(): WsArgumentsHost;
}

interface HttpExecutionContext extends ExecutionContext {
  getType(): 'http';
  switchToWs(): never;
}
interface CanActivate {
  canActivate(context: ExecutionContext): boolean | Promise<boolean>;
}
interface CallHandler {
  handle(): Promise<unknown>;
}
interface NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Promise<unknown>;
}
interface ArgumentMetadata {
  type: string;

  metatype?: unknown;
  data?: string;
}
interface PipeTransform<T = unknown, R = unknown> {
  transform(value: T, metadata: ArgumentMetadata): R | Promise<R>;

  transformAsync?(value: T, metadata: ArgumentMetadata): Promise<R>;
}
interface ExceptionFilter<T = unknown> {
  catch(exception: T, context: ExecutionContext): unknown | Promise<unknown>;
}
interface NestMiddleware {
  use(c: VelaContext, next: Next): Promise<Response | void>;
}

export { HttpMethod as $, Constructor as A, ModuleDescription as B, WebSocketUpgradeIdentity as C, WsResponse as D, WsMessage as E, InferToken as F, ProviderSnapshot as G, ModuleVisibilityError as H, InferTokens as I, Type as J, ROOT_MODULE_ID as K, InjectMetadata as L, DependencyToken as M, Diagnostics as N, WsServer as O, ForwardRef as P, forwardRef as Q, InjectableOptions as R, WebSocketUpgradeAuthenticationContext as S, WsExecutionContext as T, MultipleProvidersFoundError as U, ModuleScope as V, ProviderDefinition as W, defineProvider as X, TypedToken as Y, describeToken as Z, ReservedWsEventHandler as _, ExecutionContext as a, VelaHonoEnv as at, WebSocketGatewayOptions as b, NestInterceptor as c, WsArgumentsHost as d, METADATA_KEYS as et, BroadcastCommand as f, OnGatewayInit as g, OnGatewayDisconnect as h, ExceptionFilter as i, VelaHono as it, ContainerOptions as j, Container as k, NestMiddleware as l, OnGatewayConnection as m, CallHandler as n, Scope as nt, HttpArgumentsHost as o, VelaMiddlewareHandler as ot, BroadcastOperator as p, Token as q, CanActivate as r, VelaContext as rt, HttpExecutionContext as s, ArgumentMetadata as t, ParamType as tt, PipeTransform as u, ReservedWsEventMetadata as v, WsClient as w, WebSocketPrincipal as x, SubscribeMessageMetadata as y, InjectionToken as z };
```

### `<internal:types-registry-types.d.ts>`

```ts
import { $ as HttpMethod, A as Constructor, I as InferTokens, J as Type, P as ForwardRef, W as ProviderDefinition, Y as TypedToken, c as NestInterceptor, i as ExceptionFilter, it as VelaHono, k as Container, l as NestMiddleware, nt as Scope, ot as VelaMiddlewareHandler, q as Token, r as CanActivate, rt as VelaContext, u as PipeTransform } from "<internal:types-http-hono.types.d.ts>";
import { Context } from "hono";
import { RedirectStatusCode, StatusCode } from "hono/utils/http-status";

type ComponentType = 'middleware' | 'guard' | 'pipe' | 'interceptor' | 'filter';
type MiddlewareType = Type<NestMiddleware> | NestMiddleware;
type GuardType = Type<CanActivate> | CanActivate;
type PipeType = Type<PipeTransform> | PipeTransform;
type InterceptorType = Type<NestInterceptor> | NestInterceptor;
type FilterType = Type<ExceptionFilter> | ExceptionFilter;
interface ComponentTypeMap {
  middleware: MiddlewareType;
  guard: GuardType;
  pipe: PipeType;
  interceptor: InterceptorType;
  filter: FilterType;
}
interface RouteDefinition {
  method: string;
  path: string;
  handlerName: string | symbol;
  version?: number | number[];

  name?: string;
}
interface ParameterMetadata {
  index: number;
  type: string;
  name?: string;
  pipes?: PipeType[];
  factory?: (data: unknown, ctx: Context) => unknown;

  metatype?: unknown;
}
interface HttpHandlerMeta {
  httpCode?: StatusCode;
  responseHeaders?: Array<[string, string]>;
  redirect?: {
    url: string;
    statusCode: RedirectStatusCode;
  };
}
type ModuleImport = Type | DynamicModule | ForwardRef;

interface AsyncModuleOptions<T = unknown, Inject extends readonly Token[] = readonly Token[]> {
  imports?: ModuleImport[];
  inject: Inject;
  useFactory: (...args: InferTokens<Inject>) => T | Promise<T>;
}
interface DynamicModule {
  module: Type;

  key?: string;
  imports?: ModuleImport[];
  providers?: Array<Type | ProviderDefinition>;
  controllers?: Type[];
  exports?: Token[];
  global?: boolean;

  lazy?: boolean;
}
interface ModuleOptions {
  providers?: Array<Type | ProviderDefinition>;
  controllers?: Type[];
  imports?: ModuleImport[];
  exports?: Token[];
  isGlobal?: boolean;

  lazy?: boolean;
}
interface ModuleMetadata {
  providers: Array<Type | ProviderDefinition>;
  controllers: Type[];
  imports: ModuleImport[];
  exports: Token[];
  isGlobal: boolean;
  lazy: boolean;
}

interface DiscoverableDecorator<T> {
  (value: T): ClassDecorator & MethodDecorator;

  KEY: string;
}
interface CreateDiscoverableDecoratorOptions {

  append?: boolean;
}

declare function createDiscoverableDecorator<T>(key: string, options?: CreateDiscoverableDecoratorOptions): DiscoverableDecorator<T>;

interface DiscoveredClass<T = unknown> {
  token: Token;
  metatype: Type<T>;

  moduleIds: string[];
  scope: Scope;

  instance: T | undefined;
}

interface DiscoveredRegistration<T = unknown> extends DiscoveredClass<T> {
  readonly moduleId: string;
}
interface DiscoveredRegisteredMethodMeta<M = unknown> {
  class: DiscoveredRegistration;
  methodName: string | symbol;
  meta: M;
}
interface DiscoveredMethodMeta<M = unknown> {
  class: DiscoveredClass;
  methodName: string | symbol;
  meta: M;
}
interface DiscoveryFilter {

  metadataOnly?: boolean;

  moduleId?: string | string[];

  includeRequestScoped?: boolean;

  deferLazy?: boolean;
}

declare class DiscoveryService {
  #private;
  constructor(container: Container);

  getProviders(filter?: DiscoveryFilter): DiscoveredClass[];

  getRegistrations(filter?: DiscoveryFilter): DiscoveredRegistration[];
  registrationsWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): Array<DiscoveredRegistration & {
    meta: M;
  }>;

  providersWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): Array<DiscoveredClass & {
    meta: M;
  }>;

  methodsWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): DiscoveredMethodMeta<M>[];
  registeredMethodsWithMeta<M>(key: string | DiscoverableDecorator<M>, filter?: DiscoveryFilter): DiscoveredRegisteredMethodMeta<M>[];
  private findMethods;

  private candidatesWithClassMeta;
  private matchingOwners;
  private buildEntries;
  private buildEntry;
  private buildRegistration;
}

interface EntrypointKind {
  kind: string;
  metaKey: string;

  level: 'class' | 'method';
}

interface Entrypoint<M = unknown> {
  kind: string;
  token: Token;

  moduleId?: string;

  instance: unknown;

  methodName?: string | symbol;
  meta: M;
}

interface ContributesEntrypoints {
  collectEntrypoints(discovery: DiscoveryService): Entrypoint[] | Promise<Entrypoint[]>;
}
declare function contributesEntrypoints(x: unknown): x is ContributesEntrypoints;

interface RouteInfo {
  path: string;
  method?: HttpMethod;
}
interface MiddlewareRouteDefinition {

  readonly moduleId?: string;
  middleware: Array<Type<NestMiddleware> | NestMiddleware>;
  routes: RouteInfo[];
  excludes: RouteInfo[];

  priority?: number;
}
interface MiddlewareConfigProxy {
  exclude(...routes: Array<string | RouteInfo>): MiddlewareConfigProxy;
  withPriority(priority: number): MiddlewareConfigProxy;
  forRoutes(...routes: Array<string | Constructor | RouteInfo>): MiddlewareConsumer;
}
interface MiddlewareConsumer {
  apply(...middleware: Array<Type<NestMiddleware> | NestMiddleware>): MiddlewareConfigProxy;
}
interface NestModule {
  configure(consumer: MiddlewareConsumer): void;
}

interface RouteMetadata {
  method: HttpMethod;
  path: string;
  handlerName: string | symbol;
  version?: number | number[];

  name?: string;
}
interface ControllerOptions {
  path?: string;
  version?: number | number[];
}
interface ControllerMetadata {
  prefix: string;
  version?: number | number[];
}
interface ControllerRegistration {
  controller: Type;

  moduleId: string;
  metadata: ControllerMetadata;
  routes: RouteMetadata[];
}

interface VelaBodyLimitOverride {

  path: string;

  methods?: string[];

  maxBytes: number | false;
}
interface VelaBodySecurityOptions {

  maxBytes?: number | false;

  streamingOverrides?: VelaBodyLimitOverride[];
}
interface VelaQuerySecurityOptions {

  maxParameters?: number | false;

  maxDepth?: number | false;

  maxBytes?: number | false;
}

interface VelaSecurityOptions {
  body?: VelaBodySecurityOptions;
  query?: VelaQuerySecurityOptions;
}
declare const DEFAULT_QUERY_PARAMETER_LIMIT = 100;
declare const DEFAULT_QUERY_DEPTH_LIMIT = 5;
declare const DEFAULT_QUERY_BYTES_LIMIT: number;

interface RouteDescription {

  method: string;

  path: string;
  controller: string;
  handler: string;

  moduleId: string;
  version?: number;

  name?: string;
}
interface RouteManagerOptions {

  getClientIp?: (c: VelaContext) => string | null;
  middleware?: VelaMiddlewareHandler[];
  globalPrefix?: string;

  ambientContainer?: boolean;

  bodyLimit?: number | false;

  security?: VelaSecurityOptions;
}

declare const DEFAULT_BODY_LIMIT_BYTES: number;
declare class RouteManager {
  private container;
  private static readonly METHOD_REGISTRAR;
  private controllers;
  private globalMiddleware;
  private globalPipes;
  private globalGuards;
  private globalInterceptors;
  private globalFilters;
  private globalPrefix;
  private consumerMiddlewareDefinitions;
  private routeDescriptions;
  private readonly handlerExecutor;
  private readonly ambientContainer;
  private readonly bodyLimit;
  private readonly bodyLimitOverrides;
  private readonly queryMaxParameters;
  private readonly queryMaxDepth;
  private readonly queryMaxBytes;
  private readonly clientIpResolver;
  constructor(container: Container, options?: RouteManagerOptions);

  getGlobalComponents(): {
    guards: Array<GuardType | TypedToken<CanActivate>>;
    pipes: Array<PipeType | TypedToken<PipeTransform>>;
    interceptors: Array<InterceptorType | TypedToken<NestInterceptor>>;
    filters: Array<FilterType | TypedToken<ExceptionFilter>>;
  };

  resolveClientIp(c: VelaContext): string | null;
  private validateBodyLimitOverride;
  private resolveBodyLimit;
  registerConsumerMiddleware(definitions: MiddlewareRouteDefinition[]): this;
  setGlobalPrefix(prefix: string): this;
  useGlobalMiddleware(...middleware: MiddlewareType[]): this;
  useGlobalMiddlewareTokens(...middlewareTokens: Array<TypedToken<NestMiddleware>>): this;
  useGlobalPipes(...pipes: PipeType[]): this;
  useGlobalPipeTokens(...pipeTokens: Array<TypedToken<PipeTransform>>): this;
  useGlobalGuards(...guards: GuardType[]): this;
  useGlobalGuardTokens(...guardTokens: Array<TypedToken<CanActivate>>): this;
  useGlobalInterceptors(...interceptors: InterceptorType[]): this;
  useGlobalInterceptorTokens(...interceptorTokens: Array<TypedToken<NestInterceptor>>): this;
  useGlobalFilters(...filters: FilterType[]): this;
  useGlobalFilterTokens(...filterTokens: Array<TypedToken<ExceptionFilter>>): this;
  private getMiddlewarePriority;
  private getRequestContainer;
  private createRequestContainer;
  private wrapMiddlewareWithFilters;
  private mapMiddlewareError;
  registerController(controller: Type, moduleId?: string): this;

  getRouteDescriptions(): RouteDescription[];

  getGlobalPrefix(): string;
  build(): Promise<VelaHono>;
  private registerRoute;
  private buildVersionedPaths;
  private compilePathMatcher;
  private compileRouteMatcher;
  getControllers(): ControllerRegistration[];
}

interface VelaRouteMap {}

type RouteName = keyof VelaRouteMap extends never ? string : Extract<keyof VelaRouteMap, string>;

type RouteParams<N extends RouteName> = N extends keyof VelaRouteMap ? VelaRouteMap[N] : Record<string, string> | undefined;

type InvocationTransport = (request: Request) => Promise<Response>;

interface InvocationRouteTarget<N extends RouteName = RouteName> {
  route: N;
  params?: RouteParams<N>;
}

interface InvocationPathTarget {
  path: string;
}

type InvocationTarget = InvocationRouteTarget | InvocationPathTarget;

interface RunInit {

  method?: string;

  body?: unknown;

  headers?: HeadersInit;

  ttlSeconds?: number;

  iss?: string;

  signal?: AbortSignal;

  timeoutMs?: number;
}

interface NonceStore {
  claim(nonce: string, expEpochSeconds: number): Promise<boolean>;
}

export { RouteDefinition as $, DiscoveredClass as A, ComponentType as B, MiddlewareRouteDefinition as C, Entrypoint as D, ContributesEntrypoints as E, DiscoveryService as F, HttpHandlerMeta as G, DynamicModule as H, CreateDiscoverableDecoratorOptions as I, ModuleImport as J, InterceptorType as K, DiscoverableDecorator as L, DiscoveredRegisteredMethodMeta as M, DiscoveredRegistration as N, EntrypointKind as O, DiscoveryFilter as P, PipeType as Q, createDiscoverableDecorator as R, MiddlewareConsumer as S, RouteInfo as T, FilterType as U, ComponentTypeMap as V, GuardType as W, ModuleOptions as X, ModuleMetadata as Y, ParameterMetadata as Z, VelaBodyLimitOverride as _, NonceStore as a, VelaSecurityOptions as b, RouteParams as c, RouteDescription as d, RouteManager as f, DEFAULT_QUERY_PARAMETER_LIMIT as g, DEFAULT_QUERY_DEPTH_LIMIT as h, InvocationTransport as i, DiscoveredMethodMeta as j, contributesEntrypoints as k, VelaRouteMap as l, DEFAULT_QUERY_BYTES_LIMIT as m, InvocationRouteTarget as n, RunInit as o, RouteManagerOptions as p, MiddlewareType as q, InvocationTarget as r, RouteName as s, InvocationPathTarget as t, DEFAULT_BODY_LIMIT_BYTES as u, VelaBodySecurityOptions as v, NestModule as w, ControllerOptions as x, VelaQuerySecurityOptions as y, AsyncModuleOptions as z };
```
